# Runtime + dev dependencies. Mirrored from pyproject.toml so a cloner
# can set up a venv without parsing the project metadata.
#
# Install: .venv/bin/pip install -r requirements.txt
# (or just run ./setup.sh)
#
# NOTE: when changing pyproject.toml dependencies, mirror them here
# (and vice versa) — the two must not drift.

jsonschema>=4.17
jinja2>=3.0
anthropic>=0.40
openai>=1.40
google-genai>=1.0
python-dotenv>=1.0
pyyaml>=6.0
kuzu>=0.11
mcp>=1.12,<2
networkx>=3.0
python-louvain>=0.16

# dev
pytest>=7
pytest-cov>=4
