diff --git a/docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md b/docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md
index 0e8366e..9428135 100644
--- a/docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md
+++ b/docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md
@@ -1,7 +1,7 @@
-# #123 — Semantic Graph Search: Spec v0.6 (v0.4 RATIFIED + post-ratification amendments D1–D5 + normative-body sync)
+# #123 — Semantic Graph Search: Spec v0.7 (v0.4 RATIFIED + post-ratification amendments D1–D6)
 
-Date: 2026-07-25 · Task: **#123 Semantic graph search** · Status: **v0.6 — v0.4 RATIFIED (Joseph, 2026-07-25) + amendments D1–D5 (Joseph, 2026-07-26); D1–D4 folded through the normative body per codex confirmation #2; stage-2 bound made enforceable via excerpt policy v2 (codex confirmation #3 remedy 1 ≡ opus5 H1)**
-Basis: vision v1.2 RATIFIED (D1–D10) + v1.3 (SD-4 scale amendment) + v1.4 (R1 salvage/no-fallback amendment) · v0.1 reviews (codex F1–F8, opus5 §A–§F) · v0.2 re-reviews (codex REVISE F1–F6, opus5 CONCUR §1–§3) · syntheses (`…-spec-review-synthesis.md`, `…-spec-v0.2-review-synthesis.md` — all checkable claims verified against the repo) · **Joseph's rulings 2026-07-25: R1 per-entry salvage + retry-not-fallback; R2 whole-graph stage-1 option 1; R3 narrower gate** · **Joseph's rulings 2026-07-26: D1–D5 (below)** · blueprint v0.3 confirmation reviews (codex REVISE 1–4; opus5 H1–H9).
+Date: 2026-07-25 · Task: **#123 Semantic graph search** · Status: **v0.7 — v0.4 RATIFIED (Joseph, 2026-07-25) + amendments D1–D6 (Joseph, 2026-07-26); D6 amends R2: one budget calculation method for BOTH stages (codex K1 option 2 ≡ opus5 J1)**
+Basis: vision v1.2 RATIFIED (D1–D10) + v1.3 (SD-4 scale amendment) + v1.4 (R1 salvage/no-fallback amendment) · v0.1 reviews (codex F1–F8, opus5 §A–§F) · v0.2 re-reviews (codex REVISE F1–F6, opus5 CONCUR §1–§3) · syntheses (`…-spec-review-synthesis.md`, `…-spec-v0.2-review-synthesis.md` — all checkable claims verified against the repo) · **Joseph's rulings 2026-07-25: R1 per-entry salvage + retry-not-fallback; R2 whole-graph stage-1 option 1; R3 narrower gate** · **Joseph's rulings 2026-07-26: D1–D6 (below)** · blueprint v0.3 confirmation reviews (codex REVISE 1–4; opus5 H1–H9) · blueprint v0.4 confirmation reviews (codex REVISE K1–K3 + minors; opus5 CONCUR J1–J4).
 
 Scope of this document: the graph-search contract, the selector prompt contract, the pass-1.5 adapter, determinism/replay, evidence status + telemetry, vault-scale sizing, and the **D7 truth-set program** (defined before any tuning). Out of scope: package boundary + selector model choice (blueprint, P9); artifact-sink mechanics (blueprint); FTS infrastructure implementation (blueprint track).
 
@@ -14,6 +14,7 @@ Scope of this document: the graph-search contract, the selector prompt contract,
 - **D3 — R4 amended: no fat call on thin-empty with N>M (amends §7.2 R4).** Thin retains zero validated slugs over N>M ⇒ **skip the fat call**: `status: completed`, `execution: thin_attempted`, hits `[]`, all request expressions unresolved, concordance null, `evidence_status: not_applicable`, plus the **`thin_retained_zero`** watched telemetry class (a recall-oriented selector returning zero from a large in-domain space is more likely malfunctioning than correct — it must not read as honest-empty in the KPI series). Every fat call is a thin→fat call (Joseph). Codex's run-the-fat-call / distinct-terminal-state position is recorded as dissent. (Unreachable below 150-entity domains; largest today is 51.)
 - **D4 — selector screening cohort (blueprint scope, recorded here for completeness).** Truth-set selector screening runs three candidates: `gemini-3.6-flash`, `gpt-5.4-mini`, **`deepseek-v4-flash`** (codex: its #120 pass-2 collapse is a different prompt contract — measured directly or not at all). Production default decided by the D7 results.
 - **D5 — estimator calibration contract (amends D1 narrowly; blueprint §7/§11).** Exactly **one non-comparative calibration call per screening candidate** (hard 3-call ceiling): fixed prompt = the rendered fixture thin block, provider-reported usage read, no ranking or selection effect, **Joseph fires it at end of P2**. Persisted as a **sibling calibration artifact** (`task123_search_calibration_v1.json` — fixture version, input sha256, prompt version, model id, `counting_source: provider_reported_usage`, input_tokens, measured bytes/token); the checksummed fixture manifest is **never mutated** (codex confirmation #4). P1 budget tests are structural-only (synthetic windows); measurement assertions live at the calibration gate. D1 otherwise unchanged — live selector experiments, tuning, and vault ingestion still wait for labels + gates.
+- **D6 — R2 amended: one budget calculation method for BOTH stages (amends §7.2 R2; blueprint §2.2/§7/§8).** The pre-flight budget rule — calibrated `ceil(utf8_bytes ÷ 4)` estimate over the **fully rendered request** (system + template + query + evidence) plus the stage's reserved output, against 80% of the configured selector window — applies to the **fat call exactly as to the thin call** (Joseph's consistency ruling). Over budget ⇒ typed `budget_exceeded` with the stage recorded, zero spend, never retried. Supporting contract: the rendered **query block** is hard-capped at **4,096 UTF-8 B** in the projector (truncating `summary` at a char boundary — `summary`/`key_themes` are schema-unbounded, verified `pass1_schema.py:77-88`; `query_truncated` telemetry); **`OUTPUT_ALLOWANCE_FAT = 2,000`** declared and reserved via `max_tokens`, exactly as thin's. The static byte accounting (evidence ≤ 150 × 2,500 B + query ≤ 4,096 B + system/template ≈ ≤ ~381 kB) bounds the absolute worst case (≤ 1 token/byte); the calibrated estimate + 0.8 headroom is the **operative** guard — "never underestimates" / "provably fits any window ≥ 128k" stay **withdrawn**, the headroom explicitly carrying density variance (opus5 J3). D5 unchanged (3-call calibration). Codex's option 1 (authoritative per-provider tokenizer preflight) rejected: no local tokenizer exists, provider counters are gemini-only, and fail-config-without-counter would break the pool. (Codex K1 ≡ opus5 J1, convergent.)
 
 ---
 
@@ -151,11 +152,11 @@ Separately, **before any call**, the §7.2 R2 pre-flight budget check can fail t
 
 ### 3.1 QueryPayload assembly (Joseph's [9]; field list = **SD-1, resolved**)
 
-v1 field list from the pass-1 frontmatter: **`domain`, `summary`, `key_themes`, `entity_search_keys`, `author`**. `author` is retained per codex's SD-1 revision — authorship is a person signal for the motivating retrieval class (`warren-buffett`); the truth set may A/B its exclusion later with evidence. Excluded as noise: `source_type`, `confidence`, `uncertainty_reason`. (`expressions` = `entity_search_keys`; `text` = summary + themes + keys + author rendered into a fixed template.)
+v1 field list from the pass-1 frontmatter: **`domain`, `summary`, `key_themes`, `entity_search_keys`, `author`**. `author` is retained per codex's SD-1 revision — authorship is a person signal for the motivating retrieval class (`warren-buffett`); the truth set may A/B its exclusion later with evidence. Excluded as noise: `source_type`, `confidence`, `uncertainty_reason`. (`expressions` = `entity_search_keys`; `text` = summary + themes + keys + author rendered into a fixed template.) **Rendered-query byte ceiling (D6):** the rendered query block never exceeds **4,096 UTF-8 B** — the projector truncates `summary` at a char boundary to fit (the only realistically unbounded SD-1 field; `entity_search_keys` is schema-capped at 10); firings counted as `query_truncated` telemetry.
 
 ### 3.2 Integration point
 
-Orchestrator loop, per source, after pass-1 enrichment commits and before `compile_source`'s context build. The adapter: builds the domain subtree (§1.2) → calls graph search **once per source** (two selector calls per executed search — thin then fat, R4) → hands the **ordered hit slugs** to the context builder as the **T2 tier** (replacing `_t2_from_search_keys`' exact-match seeds). T1 (SUPPORTS) and T3 (structural BFS) are untouched; tier ranking inside T2 = selector order (P2); tier-then-PageRank retained for T1/T3; `page_cap=50` applies to the merged tiers as today. The ordered T2 is reflected in the EXISTING CONTEXT prompt in that order (D2).
+Orchestrator loop, per source, after pass-1 enrichment commits and before `compile_source`'s context build. The adapter: builds the domain subtree (§1.2) → calls graph search **once per source** (branch-specific selector call counts per executed search — R4 as amended by D3; blueprint §8's table) → hands the **ordered hit slugs** to the context builder as the **T2 tier** (replacing `_t2_from_search_keys`' exact-match seeds). T1 (SUPPORTS) and T3 (structural BFS) are untouched; tier ranking inside T2 = selector order (P2); tier-then-PageRank retained for T1/T3; `page_cap=50` applies to the merged tiers as today. The ordered T2 is reflected in the EXISTING CONTEXT prompt in that order (D2).
 
 **`max_results` for pass-1.5 = 50** (the merged page_cap; typical expression counts are 3–15, so cap-exhaustion is rare and, when it occurs, is annotated per §2.3).
 
@@ -183,7 +184,7 @@ Consequences:
 ## 4. Text projection (fat evidence — D10 mechanics)
 
 - **Source of text**: the entity's wiki page body via `common/wiki_io.get_body` (frontmatter-stripped, established authority). The graph is never read for content text.
-- **Excerpt rule (v2 — amended 2026-07-26, panel convergence codex confirmation #3 remedy 1 ≡ opus5 H1)**: leading excerpt, **250 words** (whitespace-tokenized), no mid-sentence cut (extend to sentence end within +10%); then a **hard byte ceiling — the rendered per-entity fat block (identity line + excerpt field + delimiters) never exceeds 2,500 UTF-8 bytes**, the projector truncating excerpt content at a character boundary to fit; deterministic — same body ⇒ same excerpt. Versioned as `excerpt_policy_version: "2"`, recorded in the artifact. The ceiling converts the §7.2 stage-2 payload bound from a corpus estimate into a property of the code (the word cap alone cannot bound bytes — one 200-character URL is one "word"; 102/163 fixture entities already exceed the byte density v0.3's derivation implied). Fixture v1's largest rendered block is 2,209 B — the ceiling binds on nothing in the frozen fixture: no regeneration, checksums unchanged.
+- **Excerpt rule (v2 — amended 2026-07-26, panel convergence codex confirmation #3 remedy 1 ≡ opus5 H1)**: leading excerpt, **250 words** (whitespace-tokenized), no mid-sentence cut (extend to sentence end within +10%); then a **hard byte ceiling — the rendered per-entity fat block (identity line + excerpt field + delimiters) never exceeds 2,500 UTF-8 bytes**, the projector truncating excerpt content at a character boundary to fit; deterministic — same body ⇒ same excerpt. Versioned as `excerpt_policy_version: "2"`, recorded in the artifact. The ceiling makes the §7.2 stage-2 **byte accounting** a projector property rather than a corpus estimate (the word cap alone cannot bound bytes — one 200-character URL is one "word"; 102/163 fixture entities already exceed the byte density v0.3's derivation implied; token fit = calibrated estimate + D6 preflight guard, §7.2). Fixture v1's largest rendered block is 2,209 B — the ceiling binds on nothing in the frozen fixture: no regeneration, checksums unchanged. When the ceiling binds, **byte truncation takes precedence over the no-mid-sentence-cut rule** — the sentence rule yields (codex confirmation minor).
 - **SD-3 (qualified per codex F3):** 250 words is a **safety bound, not a sizing lever** — inert *on the measured corpus* (binds 2/163 pages; body medians 62w concepts / 57w summaries / 135w articles) but a live bound at vault scale, where the body-length distribution can differ (§7.1 carries the measured rate + the policy-v2 byte ceiling as the bound).
 - **Missing body** (`ContentNotFoundError` = graph/disk drift): entity degrades to title-only; counted in `body_coverage`; see §6.1 status rules.
 - **Where projection runs**: inside graph search (§1.1 — callers pass identities only).
@@ -262,7 +263,7 @@ Mid-run, the space is a function of intra-run compile order (source N reads bodi
 
 ### 6.3 SearchTelemetry (controller-produced)
 
-Counts — eligible-space size, stage-1 retained, stage-2 hydrated, stage-2 title-only, returned entries, **valid_entry_yield** (`None` when returned = 0, §2.3), **per-class attempted violations** (§2.3), `all_entries_dropped` occurrences, `unattributed_hit_count`, retry attempts, `selector_failure` class — `status`, `execution`, per-stage model route + stamps, per-stage latency + cost, **pre-flight budget record (`budget_estimate_tokens`, `selector_window`, `headroom_factor`, routing outcome — §7.2 R2)**, **thin/fat concordance (fat top-10 ∩ thin top-20 — §8.3)**, search_snapshot_hash, artifact ref. Threads into the #122 record (§5.1) and the run's KPI emission; **selector-quality fields (yield, per-class violation rates, retry rate, failure rate) are watched KPIs per model — Joseph's leaderboard counter (R1 §3.2), promotion to scored is a later, separate decision.**
+Counts — eligible-space size, stage-1 retained, stage-2 hydrated, stage-2 title-only, returned entries, **valid_entry_yield** (`None` when returned = 0, §2.3), **per-class attempted violations** (§2.3), `all_entries_dropped` occurrences, `unattributed_hit_count`, `query_truncated` occurrences (D6), `search_artifact_write_failures` (warn-only envelope sink — codex minor), retry attempts, `selector_failure` class — `status`, `execution`, per-stage model route + stamps, per-stage latency + cost, **pre-flight budget record (`budget_estimate_tokens`, `selector_window`, `headroom_factor`, routing outcome — §7.2 R2)**, **thin/fat concordance (fat top-10 ∩ thin top-20 — §8.3)**, search_snapshot_hash, artifact ref. Threads into the #122 record (§5.1) and the run's KPI emission; **selector-quality fields (yield, per-class violation rates, retry rate, failure rate) are watched KPIs per model — Joseph's leaderboard counter (R1 §3.2), promotion to scored is a later, separate decision.**
 
 ## 7. Vault-scale sizing (pre-implementation gate — corrected per codex F1, dual projection per F3)
 
@@ -281,7 +282,7 @@ Measured from the SD-6 snapshot (`benchmark/runs/gemini-3.6-flash-2026-07-25T09-
 
 **Blueprint duty (codex F1's required recompute, items 4–5) — discharged (v0.6):** serialized stage-1/stage-2 counts are fixture-measured (§7.1) and the estimator is calibrated against provider-reported usage per D5 (blueprint §7); the R2 guardrail routes on the measured serialized estimate (runtime-authoritative), not `space_size × excerpt_tokens` alone. (The "two-stage switch" phrasing is stale — R4 dissolved the switch; every executed search runs thin.)
 
-**Cost on record (for Joseph's "any expense" ruling; two-stage is the uniform operating path — R4; figures updated v0.6 to fixture-measured rates):** today, per source in the largest domain — thin (51 × 86 B ≈ 1.1k tokens) + fat (51 × 755 B ≈ 9.6k) ≈ **~10.7k input tokens**. At vault scale, per source in the largest domain — thin (~3,000 × 88 B ≈ 66k) + fat (150 × 719 B ≈ 27k expected; ≤94k ceiling-bound) ≈ **~93k expected, ≤160k policy-bound input tokens**; a full 1,706-source re-ingest ≈ **~160M input tokens upper bound** (most domains are far smaller) ≈ **$25 deepseek / $115 gpt-5.4-mini / $230 gemini** (blueprint §7). Whole-graph human queries (~211k thin + ~27k fat each) are interactive/low-volume — cents per query.
+**Cost on record (for Joseph's "any expense" ruling; two-stage is the uniform operating path — R4; figures updated v0.6 to fixture-measured rates):** today, per source in the largest domain — thin (51 × 86 B ≈ 1.1k tokens) + fat (51 × 755 B ≈ 9.6k) ≈ **~10.7k input tokens**. At vault scale, per source in the largest domain — thin (~3,000 × 88 B ≈ 66k) + fat (150 × 719 B ≈ 27k expected; ≤94k ceiling-bound) ≈ **~93k expected, ≤160k policy-bound input tokens**; a full 1,706-source re-ingest ≈ **~160M input tokens expected-case** (an upper bound in the domain-size sense — most domains are far smaller) ≈ **$25 deepseek / $115 gpt-5.4-mini / $230 gemini**; the **policy-ceiling worst case** is 1,706 × ~160k ≈ **273M ≈ $38 / $205 / $410** — both figures on the expense record (codex minor + opus5 for-the-record; blueprint §7). Whole-graph human queries (~211k thin + ~27k fat each) are interactive/low-volume — cents per query.
 
 ### 7.2 The sizing decisions
 
@@ -296,10 +297,10 @@ Measured from the SD-6 snapshot (`benchmark/runs/gemini-3.6-flash-2026-07-25T09-
 
   **Corrected rationale (opus5 §C):** options 1 and 2 **both** cap recall; the operative distinction is **which cap is measured** — option 2's is measurable with the harness we are already building. **Predeclared measurement:** stage-1 recall@150 on the fixed truth set, run **before** vault ingestion; gate failure ⇒ options 1 or 3 revisited.
   **Vision impact:** P1 amended (v1.3, then v1.4 R4) — two-stage thin→fat is the uniform production path at every scale; no single-stage path, no activation threshold.
-- **R2 (oversized spaces) — RESOLVED (Joseph, 2026-07-25): one uniform pre-flight budget rule, no per-consumer distinction.** The contract treats every caller identically (Joseph's [2]: nothing special for pass-1.5, nothing special for human/CLI/MCP — the space is whatever the caller passed). Before the stage-1 call, the controller estimates the serialized thin input tokens (evidence + fixed prompt overhead + output allowance) and compares against **80% of the configured selector model's context window** (Joseph's headroom factor — covers system task, query, and provider margin). **The estimator must be conservative (codex concurrence #2):** the exact per-model tokenizer where available; otherwise a genuinely safe bound — UTF-8 bytes ÷ 4, calibrated against the measured thin block. `words × 1.3` is **not** conservative for slug-heavy text (2.2k vs 3.8k measured on the real 163-entity block — a ~1.7× underestimate), and an underestimated guardrail can authorize a request it was meant to block. Over budget → **fail the search without invoking the API** — `budget_exceeded`, zero spend, typed telemetry, no retry (a deterministic condition; retrying changes nothing). Period — no silent degradation, no partial search, no consumer-specific escape hatch. (Stage 2 needs no estimate — "always fits" is now a **property of the code**, not a corpus statistic (v0.6 — codex confirmation #3 remedy 1 ≡ opus5 H1): the policy-v2 byte ceiling caps every rendered fat entry at 2,500 B, so the stage-2 evidence block never exceeds 150 × 2,500 B = 375 kB ≈ **94k tokens < 102.4k** (80% × 128k) — holds for any selector window ≥ 128k; §9 asserts the bound against the ceiling the projector enforces. The v0.4 premises (150 × ~366 ≈ 55k < 64k) bounded words, not bytes, and are superseded.)
+- **R2 (oversized spaces) — RESOLVED (Joseph, 2026-07-25): one uniform pre-flight budget rule, no per-consumer distinction.** The contract treats every caller identically (Joseph's [2]: nothing special for pass-1.5, nothing special for human/CLI/MCP — the space is whatever the caller passed). Before **each selector call — thin and fat alike (D6: one calculation method for both stages)** — the controller estimates the serialized input tokens of the **fully rendered request** (evidence + system template + user wrapper + query + the stage's reserved output) and compares against **80% of the configured selector model's context window** (Joseph's headroom factor — covers provider margin and density variance). **The estimator is calibrated, not a proved bound (codex confirmation K1):** the exact per-model tokenizer where available; otherwise UTF-8 bytes ÷ 4, calibrated against provider-reported usage (D5) — the 0.8 headroom is the accepted margin for density variance (opus5 J3, explicit), and the "never underestimates" claim stays withdrawn. `words × 1.3` is **not** conservative for slug-heavy text (2.2k vs 3.8k measured on the real 163-entity block — a ~1.7× underestimate), and an underestimated guardrail can authorize a request it was meant to block. Over budget → **fail the search without invoking the API** — `budget_exceeded`, zero spend, typed telemetry, no retry (a deterministic condition; retrying changes nothing). Period — no silent degradation, no partial search, no consumer-specific escape hatch. (Stage 2 is guarded the same way — **D6**: the "stage-1 only" asymmetry is superseded. The static byte accounting bounds the worst case: evidence ≤ 150 × 2,500 B = 375 kB + query ≤ 4,096 B + system/template ≈ **≤ ~381 kB full request** ⇒ ≤ ~381k tokens at the absolute 1-token/byte worst case; at the calibrated ÷4 ratio the full fat request is ≈ 96–98k tokens — fits a 128k window's 102.4k budget with ~5–7k headroom, and the configured pool's budgets (320k/800k/800k) with 3–8× margin. If a real corpus ever breaks the estimate, the fat preflight fires the same typed, zero-spend `budget_exceeded` — a typed outcome, never a provider error. The v0.4 premises (150 × ~366 ≈ 55k < 64k) bounded words, not bytes, and are superseded; the v0.6 "property of the code" phrasing overclaimed a token bound from a byte bound — codex K1.)
 
-  What fits is then an **empirical fact about the configured model, not a rule about consumers**: at vault scale, pass-1.5's largest domain space thin-projects to ~40k–70k tokens and whole-graph human queries to ~127k–222k (§7.1) — both pass against a 1M-window selector (800k budget); against a 128k-window model the whole-graph query fails closed and *visibly*, which is exactly what the rule is for — the operator sees `budget_exceeded` and configures a bigger-window selector. (The earlier per-surface "window requirement for the whole-graph surface" framing is withdrawn: there is no whole-graph special case, only one budget rule.) Sharded thin selection stays recorded as contingency for a pool era with no long-window models; lexical candidate generation for oversized spaces stays rejected (an unmeasured recall cap cannot be load-bearing — codex's own v0.1 F4 premise).
-- **R4 (staging posture) — RESOLVED (Joseph, 2026-07-25): always two-stage — thin call then fat call, every source, every run; no single-stage path, no routing logic.** (As amended — **D3**: the thin-retained-zero N>M terminal **skips the fat call** (`thin_attempted`, honest empty); **opus5 F1**: the thin-failed/N≤M path proceeds to fat as `fat_after_thin_failure`. Branch-specific call counts — 1–2 thin + 0–2 fat per terminal path — are the executable contract, blueprint §8's table.) Rationale (Joseph's uniformity principle — the same one behind R2): the system never behaves conditionally when it can behave uniformly — no estimator-dependent path selection, no behavioral discontinuity as domains grow, and the stage-1 **code path** is exercised on 100% of production traffic from day one. (Stated precisely — opus5 §2.2: the *gate* becomes informative only once a domain exceeds M; below M, recall@M is 1.0 by controller-enforced retain-all construction, not by selector quality — §2.1's small-space enforcement, codex #3.) At today's scale the results equal a single fat call's **by construction** (largest domain 51 < M=150), for the price of one ~1k-token thin call per source. There is **no small-space skip** — a 200-token thin call is the price of one code path. Consequences: SD-5's routing threshold **dissolves** (no single/two-stage switch exists to tune — the estimator survives only as the R2 guardrail); `execution` records how far execution progressed (`thin_attempted | two_stage_attempted | fat_after_thin_failure`); the stage-1 prompt is recall-oriented (§2.1); the stage-1 recall@150 hard gate guards the only path (predeclared per SD-4; its measurable form is the §8.3 reduced-M protocol — opus5 §2.3).
+  What fits is then an **empirical fact about the configured model, not a rule about consumers**: at vault scale, pass-1.5's largest domain space thin-projects to ~66k tokens and whole-graph human queries to ~211k (§7.1, measured projections) — both pass against a 1M-window selector (800k budget); against a 128k-window model the whole-graph query fails closed and *visibly*, which is exactly what the rule is for — the operator sees `budget_exceeded` and configures a bigger-window selector. (The earlier per-surface "window requirement for the whole-graph surface" framing is withdrawn: there is no whole-graph special case, only one budget rule.) Sharded thin selection stays recorded as contingency for a pool era with no long-window models; lexical candidate generation for oversized spaces stays rejected (an unmeasured recall cap cannot be load-bearing — codex's own v0.1 F4 premise).
+- **R4 (staging posture) — RESOLVED (Joseph, 2026-07-25): always two-stage — thin call then fat call, every source, every run; no single-stage path, no routing logic.** (As amended — **D3**: the thin-retained-zero N>M terminal **skips the fat call** (`thin_attempted`, honest empty); **opus5 F1**: the thin-failed/N≤M path proceeds to fat as `fat_after_thin_failure`. Branch-specific call counts — 1–2 thin + 0–2 fat per terminal path — are the executable contract, blueprint §8's table.) Rationale (Joseph's uniformity principle — the same one behind R2): the system never behaves conditionally when it can behave uniformly — no estimator-dependent path selection, no behavioral discontinuity as domains grow, and the stage-1 **code path** is exercised on 100% of production traffic from day one. (Stated precisely — opus5 §2.2: the *gate* becomes informative only once a domain exceeds M; below M, recall@M is 1.0 by controller-enforced retain-all construction, not by selector quality — §2.1's small-space enforcement, codex #3.) At today's scale the results equal a single fat call's **by construction** (largest domain 51 < M=150), for the price of one ~1k-token thin call per source. There is **no small-space skip** — a 200-token thin call is the price of one code path. Consequences: SD-5's routing threshold **dissolves** (no single/two-stage switch exists to tune — the estimator survives only as the R2 guardrail); `execution` records how far execution progressed (`thin_attempted | two_stage_attempted | fat_after_thin_failure`); the stage-1 prompt is recall-oriented (§2.1); the stage-1 recall hard gate **at the predeclared reduced-M points** guards the only path (predeclared per SD-4; M=150 stays the production constant — naming corrected per codex K2: recall@150 is non-binding on this fixture, so the gate lives on the §8.3 reduced-M curve — opus5 §2.3).
 - **SD-5 (threshold) — DISSOLVED by R4 as a routing decision; survives as the R2 guardrail's budget.** There is no single/two-stage switch to tune — every executed search runs thin (fat per the branch contract — R4 as amended by D3). What remains: the pre-flight `budget_exceeded` check (80% of the configured window, runtime estimated serialized thin tokens) and **space entity count as the primary tracked series** so the guardrail's approach is visible before it fires (dual projection preserved per codex F3 — measured serialized tokens authoritative, entity count a trend series, not the sole variable; e.g. a 1M-window/800k-budget guardrail admits thin spaces up to ~35k–60k entities — far beyond any vault projection; a 128k-window model admits ~4k–8k).
 - **FTS-in-v1: NO** as a stage-1 candidate filter (recorded). FTS still ships early as infrastructure for the CLI/MCP human surface per D4 — independent track, never the relevance mechanism.
 
@@ -320,6 +321,8 @@ Measured from the SD-6 snapshot (`benchmark/runs/gemini-3.6-flash-2026-07-25T09-
 
 Joseph's relevance labeling — the most expensive artifact in the D7 program — is pinned to exactly this frozen state, so durability precedes labeling, not follows it.
 
+**Fixture v1 ↔ excerpt policy v2 (codex K3 option b ≈ opus5 J2):** the v1 manifest stays **immutable** (`excerpt_policy_version: "1"`; no in-place mutation). A checked sibling compatibility artifact (`task123_search_snapshot_v1_policy_v2_compat.json`) records the v1↔v2 equivalence with its proof — the policy-v2 ceiling binds on nothing in fixture v1 (largest rendered block 2,209 B < 2,500 B), so every v1 excerpt renders byte-identical under v2, checksums unchanged. The harness loads v1 + the compat artifact and records effective policy `"2"`.
+
 ### 8.2 The truth-set artifact (codex F8) + probe classes
 
 **Truth set = a checked-in, versioned JSON artifact** (under `benchmark/truth/`), not a doc section. Per probe:
@@ -368,12 +371,12 @@ One cohort, selector run twice per source (domain-scoped space vs whole graph),
 
 ## 9. Test plan outline (TDD-first; expands in blueprint)
 
-- **Contract tests**: request/result shapes; result-state coverage (`status` × `execution` × `evidence_status` combinations, incl. empty-space `not_executed`/`not_applicable`/`None`, `budget_exceeded`, attempted-but-failed `thin_attempted`/`two_stage_attempted` paths, the D3 thin-retained-zero terminal, and the `fat_after_thin_failure` path); the §2.3 four-way response classification (`unparseable` / `structurally_unusable` / `all_entries_dropped` retryable vs empty-`selections` honest-empty **not** a failure); `valid_entry_yield = None` on zero-returned.
+- **Contract tests**: request/result shapes; result-state coverage (`status` × `execution` × `evidence_status` combinations, incl. empty-space `not_executed`/`not_applicable`/`None`, `budget_exceeded`, attempted-but-failed `thin_attempted`/`two_stage_attempted` paths, the D3 thin-retained-zero terminal, the `fat_after_thin_failure` path, and the fat-stage `budget_exceeded` terminal (`execution: thin_attempted` — D6)); the §2.3 four-way response classification (`unparseable` / `structurally_unusable` / `all_entries_dropped` retryable vs empty-`selections` honest-empty **not** a failure); `valid_entry_yield = None` on zero-returned.
 - **Small-space retain-all enforcement (codex #3)**: with `eligible_space_size ≤ M`, stage-2 input = every eligible identity **regardless of the thin response** (thin returns 0, 3, or M slugs — stage-2 input unchanged); thin's ranked list still feeds concordance.
 - **Per-entry salvage tests (R1 — the named case is Joseph's 6-of-10)**: 10 returned slugs with 1 duplicate + 3 malformed/foreign ⇒ the 6 good slugs survive in returned order, per-class counts recorded; unknown-expression coercion (entry kept, stray attribution removed, unattributed hit stands); over-cap truncation keeps the selector's own top-`max_results`; **a parseable response is never wholesale-discarded**; `unparseable_response` is the only response-level failure.
 - **Zero-escape proof tests**: no matter the raw output (foreign slugs, fabrication attempts), the emitted hit set is always ⊆ search space.
 - **Retry-then-honest-empty tests (R1)**: transport error / timeout / `unparseable_response` / `structurally_unusable_response` / `all_entries_dropped` ⇒ retry once ⇒ success path records `attempt: 2`; exhausted budget ⇒ `status: selector_failure`, empty T2, typed failure class; **no deterministic substitution ever fires**; the deterministic resolver is never invoked anywhere in the search path (no fallback, no annotation, no comparator); retried calls archive **one stage entry per attempt** (rendered messages + raw response per attempt — opus5 §2.6b).
-- **Pre-flight budget tests (R2/R4)**: thin estimate stubbed over 80% of a fake window ⇒ `budget_exceeded` with **zero API invocations** (call mock asserts never called); under ⇒ thin then fat in branch order (call counts per terminal path — blueprint §8); `budget_exceeded` is never retried; identical routing regardless of caller (consumer-neutrality — no pass-1.5 vs human/CLI/MCP distinction); **no small-space skip** (a 5-entity space still runs both calls); estimator conservatism (the fallback bound never underestimates the measured fixture thin block — codex #2); **stage-2 payload bound asserted** (150 × the 2,500 B policy-v2 ceiling ≈ 94k < 102.4k — a property of the projector, not a corpus premise; v0.6).
+- **Pre-flight budget tests (R2/R4)**: thin estimate stubbed over 80% of a fake window ⇒ `budget_exceeded` with **zero API invocations** (call mock asserts never called); under ⇒ thin then fat in branch order (call counts per terminal path — blueprint §8); `budget_exceeded` is never retried **at either stage** (fat preflight ⇒ zero fat-call invocations — D6); identical routing regardless of caller (consumer-neutrality — no pass-1.5 vs human/CLI/MCP distinction); **no small-space skip** (a 5-entity space still runs both calls); estimator calibration (the ÷4 bound does not underestimate the measured fixture thin block — codex #2); **stage-2 full-request accounting asserted** (150 × 2,500 B evidence + query ≤ 4,096 B + system/template + `OUTPUT_ALLOWANCE_FAT` — the byte bound is a projector property; token fit is the calibrated estimate + headroom, runtime-guarded by the D6 fat preflight — codex K1).
 - **Prompt-contract golden tests**: fixed space + fixed query ⇒ pinned rendered message bytes (archived verbatim); SELECTOR_PROMPT_VERSION / SELECTOR_THIN_PROMPT_VERSION + SHA guard (the #115 pattern).
 - **P10 adversarial fixtures** (class H) — must-pass.
 - **Stage-aware artifact tests**: two-stage execution records both stages (evidence, rendered messages, raw response text, failure class, per-class validation counts, retained identities, per-stage model/cost); `graph_ref` present; `search_snapshot_hash` + `artifact_integrity_hash` well-formed.
@@ -395,13 +398,13 @@ One cohort, selector run twice per source (domain-scoped space vs whole graph),
 | **SD-1** | pass-1.5 prompt fields | **RESOLVED (Joseph, 2026-07-25):** domain, summary, key_themes, entity_search_keys, **author** (codex's revision adopted); truth-set A/B may revisit |
 | **SD-2** | T2 candidates/delivered | **RATIFIED ×2:** selector-validated hits pre/post merged cap; stage-1 pool recorded separately |
 | **SD-3** | excerpt bound | **RATIFIED:** 250 words fixed/versioned — safety bound (inert on this corpus, live at vault scale, codex F3); not a lever. **Amended v0.6 (policy v2):** + hard byte ceiling — rendered per-entity fat block ≤ 2,500 UTF-8 B, projector-enforced |
-| **SD-4** | scale path | **RESOLVED (Joseph, 2026-07-25): option 2** — two-stage all-LLM; comparison on record (§7.2); stage-1 recall@150 gate predeclared before vault ingestion |
+| **SD-4** | scale path | **RESOLVED (Joseph, 2026-07-25): option 2** — two-stage all-LLM; comparison on record (§7.2); stage-1 recall at the predeclared reduced-M points gated before vault ingestion (M=150 stays the production constant — codex K2) |
 | **SD-5** | two-stage threshold | **DISSOLVED by R4 as a routing decision** — no single/two-stage switch exists; survives as the R2 guardrail budget (80% of configured window) + entity count as tracked series (codex F3 preserved: measured tokens authoritative) |
 | **SD-6** | truth snapshot | **Choice RATIFIED; substrate GATED (R3):** fixture + smoke test → probe draft → Joseph's labels/gates → experiments (§8.1) |
 | **R1** | contract-violation posture | **RESOLVED (Joseph, 2026-07-25): per-entry salvage + retry, never wholesale-discard, no deterministic fallback** (§2.3/§3.4). Extends opus5's §1 (coerce 4/5) to all entry-level classes per Joseph's first principle ("take the content, not the semantics"; the 6-of-10 rule). **Supersedes** opus5's A1 fallback and v0.2's whole-response fail-closed; **codex's dissent recorded** (his v0.1 F6/v0.2 gate 6 held foreign slug, unknown expression, duplicate, over-cap, bad accounting as whole-response failures). Compensating controls: per-entry closed-world validation (D9 output invariant), per-class attempted-violation telemetry + valid_entry_yield → leaderboard, selector-failure-rate hard gate, escaped-foreign-identity = 0 hard gate |
-| **R2** | oversized spaces / context-window budget | **RESOLVED (Joseph, 2026-07-25): one uniform pre-flight budget rule** — estimate thin input tokens before the stage-1 call; >80% of the configured selector model's window ⇒ fail `budget_exceeded` **without invoking the API** (zero spend, no retry); identical for all consumers (Joseph's [2]: no pass-1.5 vs human/CLI/MCP distinction — the per-surface window-requirement framing is withdrawn). Sharded thin = recorded contingency; lexical candidate-gen rejected (§7.2) |
-| **R3** | ratification semantics | **RESOLVED (Joseph, 2026-07-25): codex's narrower gate adopted** — "spec design approved; evaluation substrate open"; D7 sequence recorded (§8.1); no implementation/tuning/vault ingestion crosses D7 until fixture + truth set + labels + gates land |
-| **R4** | staging posture | **RESOLVED (Joseph, 2026-07-25): always two-stage** — thin call then fat call, every source, every run (**as amended by D3**: thin-retained-zero N>M skips fat; the thin-failed/N≤M path runs fat as `fat_after_thin_failure`); no single-stage path, no routing logic, no small-space skip. Uniformity (Joseph's principle, same as R2): no estimator-driven behavior, no behavioral discontinuity at scale, stage-1 gate measured on 100% of traffic; result-identical to single-fat at today's scale (51 < M=150 ⇒ retention non-binding); stage-1 prompt recall-oriented; SD-5's routing threshold dissolved (§7.2); stage-1 recall@150 gate guards the only path |
+| **R2** | oversized spaces / context-window budget | **RESOLVED (Joseph, 2026-07-25): one uniform pre-flight budget rule** — estimate thin input tokens before the stage-1 call; >80% of the configured selector model's window ⇒ fail `budget_exceeded` **without invoking the API** (zero spend, no retry); identical for all consumers (Joseph's [2]: no pass-1.5 vs human/CLI/MCP distinction — the per-surface window-requirement framing is withdrawn). Sharded thin = recorded contingency; lexical candidate-gen rejected (§7.2). **Amended by D6 (2026-07-26): the same estimate-and-guard now covers the fat call — one calculation method for both stages, over the fully rendered request (system + template + query-capped-4,096 B + evidence + reserved output); the estimator is calibrated-with-headroom, not a proved bound; `OUTPUT_ALLOWANCE_FAT` declared** |
+| **R3** | ratification semantics | **RESOLVED (Joseph, 2026-07-25): codex's narrower gate adopted** — "spec design approved; evaluation substrate open"; D7 sequence recorded (§8.1). **Amended by D1/D5 (2026-07-26):** implementation (P1→P4-harness, canned/mocked, zero live selector spend) proceeds at blueprint ratification — it does not wait for labels; the sole pre-D7 live exception is the 3-call D5 calibration; experiments/tuning/vault ingestion still wait for labels + gates |
+| **R4** | staging posture | **RESOLVED (Joseph, 2026-07-25): always two-stage** — thin call then fat call, every source, every run (**as amended by D3**: thin-retained-zero N>M skips fat; the thin-failed/N≤M path runs fat as `fat_after_thin_failure`); no single-stage path, no routing logic, no small-space skip. Uniformity (Joseph's principle, same as R2): no estimator-driven behavior, no behavioral discontinuity at scale, stage-1 gate measured on 100% of traffic; result-identical to single-fat at today's scale (51 < M=150 ⇒ retention non-binding); stage-1 prompt recall-oriented; SD-5's routing threshold dissolved (§7.2); stage-1 recall gate at the predeclared reduced-M points guards the only path (M=150 production constant) |
 
 ### Concurrence round (v0.3 → v0.4, 2026-07-25)
 
@@ -411,6 +414,7 @@ One cohort, selector run twice per source (domain-scoped space vs whole graph),
 
 ## Changelog
 
+- **v0.7 (2026-07-26)** — **D6 (Joseph): R2 amended — one budget calculation method for BOTH stages** (codex K1 option 2 ≡ opus5 J1): the fat call gets the same calibrated pre-flight estimate + 80%-of-window guard as thin (typed `budget_exceeded`, stage recorded, zero spend, never retried); full-request accounting (evidence ≤ 375 kB + query ≤ 4,096 B + system/template ≈ ≤ ~381 kB worst-case bytes; `OUTPUT_ALLOWANCE_FAT = 2,000` reserved); rendered-query byte ceiling 4,096 B with `query_truncated` telemetry (`summary`/`key_themes` schema-unbounded — verified `pass1_schema.py:77-88`); "conservative"/"provably fits ≥128k" relabeled calibrated-with-headroom (the headroom explicitly carries density variance — opus5 J3; D5 unchanged at 3 calls); codex option 1 (tokenizer preflight) rejected (no local tokenizer; gemini-only provider counters). **K2:** R3 decision row synced with D1/D5; SD-4/R4 name the gate "stage-1 recall at the predeclared reduced-M points" (M=150 production constant). **K3 ≈ J2:** fixture v1 immutable; sibling policy-v2 compatibility artifact (proof: max rendered block 2,209 B < 2,500 B); harness records effective policy `"2"`. **Minors:** byte ceiling takes precedence over the no-mid-sentence-cut rule; ~160M relabeled expected-case + 273M policy-max expense record ($38/$205/$410); envelope-count completeness qualified as successful-write (+ `search_artifact_write_failures`); §7.2 figures synced to measured 66k/211k (J4); §3.2's "two selector calls" branch-synced. Basis for blueprint v0.5.
 - **v0.6 (2026-07-26)** — **D5 (Joseph):** estimator calibration contract (one non-comparative call per candidate, 3-call ceiling, provider-reported usage, Joseph fires at end of P2, sibling calibration artifact — the checksummed fixture manifest is never mutated; narrow D1 amendment). **Normative-body sync (codex confirmation #2 ≡ opus5 H2):** `execution` enum gains `fat_after_thin_failure` (§1.1, §5.1); R4 body + decision row carry the D3/F1 branch contract (1–2 thin + 0–2 fat per terminal path); §8.1 step 4 carries D1's re-scope inline; §9 test language de-"exactly two calls" / de-recall@150; §10's blueprint-routed items retired (resolved by blueprint v0.4). **Stage-2 bound made enforceable (codex confirmation #3 remedy 1 ≡ opus5 H1):** excerpt policy v2 — the rendered per-entity fat block is hard-capped at 2,500 UTF-8 B (projector-enforced, char-boundary truncation); `150 × 2,500 B ≈ 94k < 102.4k` becomes a property of the code (the 55k/64k word-based premises superseded); fixture v1 untouched (max rendered block 2,209 B < ceiling — no regeneration, checksums unchanged). **Calibration artifact separated (codex confirmation #4).** **Sizing figures refreshed to fixture-v1 measurements** (§7.1: thin 88 B / fat 691 B mean per entity; cost-on-record updated; the uncapped single-stage fat vault cell withdrawn). Basis for blueprint v0.4.
 - **v0.5 (2026-07-26)** — **post-ratification amendments D1–D4 (Joseph, panel-informed)** added as §0: D1 D7-gate re-scope (implementation proceeds at blueprint ratification; experiments/tuning/ingestion + destructive P3b stay gated); D2 State C runs the search (`query_kind: state_c`; D-90-8 retired); D3 R4 amended (no fat call on thin-empty N>M; `thin_retained_zero` watched class; codex dissent recorded); D4 three-candidate screening cohort (deepseek-v4-flash included). Published with blueprint v0.3 per codex c-5 (synchronize the owner rulings into the ratified basis); ledger + North Star updated in the same batch.
 - **v0.4 (2026-07-25)** — concurrence round absorbed (opus5 CONCUR-WITH-ITEMS §1–§4; codex CONCUR-WITH-ITEMS, 5 corrections). opus5: §1.1 R1 rationale record-correction (the trade stated honestly in §3.4); §2.1 `foregone_deterministic_hits` telemetry on `selector_failure` (keeps R1 reversible on evidence); §2.2 gate-informative-only-above-M precision in R4; §2.3 **reduced-M gate protocol** (the SD-4 gate's measurable form: recall at M=10/20 over value-investing, M=20/40 over whole graph; M=150 production constant); §2.4 `unattributed_possible` exclusion from abstention scoring; §2.5 vault-scale concordance inversion note; §2.6 stage-2-fit premises (150 × ~366 ≈ 55k < 64k window, test-asserted) + per-attempt stage entries. codex: #1 four-way response classification (`unparseable` / `structurally_unusable` / `all_entries_dropped` retryable; empty-`selections` = honest empty); #2 conservative estimator (exact tokenizer or bytes÷4 — `words × 1.3` underestimates ~1.7×); #3 controller-enforced retain-all for `N ≤ M` (R4's premise true by construction); #4 `status` + `budget_exceeded`, `execution` = `not_executed | thin_attempted | two_stage_attempted`; #5 stale language removed (vision v1.5). **Joseph's post-concurrence ruling (2026-07-25): the deterministic exact/alias method is not a valid search method — its output is never surfaced anywhere (no fallback, no annotations, no exact_matchable delta, no `foregone_deterministic_hits`); supersedes opus5's §2.1 and §D.2; class-B probes reframed as the LLM's own sanity baseline, not a string-match comparison (§1.1, §3.4, §8.2–§8.4).** Gate conditions now: 1, 7 closed; 2, 3 closed-by-R3-sequence; 4, 5 closed by the v0.4 corrections; 6 closed by R1 ruling (dissent-for-record preserved). **Post-concurrence Joseph ruling:** deterministic exact/alias is not a valid search method — never surfaced anywhere (fallback, annotations, delta, `foregone_deterministic_hits` all removed; opus5 §2.1/§D.2 superseded; R1 reversibility = selector-quality evidence).
diff --git a/docs/superpowers/specs/2026-07-26-task123-semantic-graph-search-blueprint.md b/docs/superpowers/specs/2026-07-26-task123-semantic-graph-search-blueprint.md
index 2995106..d37c921 100644
--- a/docs/superpowers/specs/2026-07-26-task123-semantic-graph-search-blueprint.md
+++ b/docs/superpowers/specs/2026-07-26-task123-semantic-graph-search-blueprint.md
@@ -1,9 +1,9 @@
-# #123 — Semantic Graph Search: Blueprint v0.4 (for Joseph's ratification)
+# #123 — Semantic Graph Search: Blueprint v0.5 (for Joseph's ratification)
 
-Date: 2026-07-26 · Task: **#123 Semantic graph search** · Status: **v0.4 — confirmation round absorbed (codex REVISE items 1–4; opus5 H1–H9 incl. the removal audit; all checkable claims verified against the repo); for Joseph's ratification**
-Basis: **spec v0.4 RATIFIED + v0.6 amendments (D1–D5, Joseph 2026-07-26)** · vision v1.5 · fixture v1 (`3d271e2`) · truth probes draft-v1 (39 probes, `a3c832a`) · v0.1 reviews + synthesis · v0.2 concurrence responses (`…-blueprint-v0.2-concurrence-codex.md`, `…-blueprint-v0.2-concurrence-opus5.md`) · v0.3 confirmation reviews (`…-blueprint-v0.3-review-codex.md`, `…-blueprint-v0.3-review-opus5.md`, `…-blueprint-v0.3-confirmation-diff-audit-opus5.md`).
+Date: 2026-07-26 · Task: **#123 Semantic graph search** · Status: **v0.5 — v0.4 confirmation round absorbed (codex REVISE K1–K3 + minors; opus5 CONCUR J1–J4; all checkable claims verified against the repo); for Joseph's ratification**
+Basis: **spec v0.4 RATIFIED + v0.7 amendments (D1–D6, Joseph 2026-07-26)** · vision v1.5 · fixture v1 (`3d271e2`) · truth probes draft-v1 (39 probes, `a3c832a`) · v0.1 reviews + synthesis · v0.2 concurrence responses (`…-blueprint-v0.2-concurrence-codex.md`, `…-blueprint-v0.2-concurrence-opus5.md`) · v0.3 confirmation reviews (`…-blueprint-v0.3-review-codex.md`, `…-blueprint-v0.3-review-opus5.md`, `…-blueprint-v0.3-confirmation-diff-audit-opus5.md`) · v0.4 confirmation reviews (`…-blueprint-v0.4-review-codex.md`, `…-blueprint-v0.4-review-opus5.md`).
 
-**Joseph's rulings (2026-07-26), binding:** **D1** — coding starts at blueprint sign-off; live experiments/tuning/ingestion and the destructive P3b wait for labels+gates/experiments. **D2** — State C runs the search (`expressions: []`, `query_kind: state_c`); D-90-8 retired. **D3** — no fat call on thin-empty with N>M; `completed` + `thin_attempted` + `thin_retained_zero` (R4 amendment; codex's dissent recorded). **D4** — screening cohort: gemini-3.6-flash (interim default) + gpt-5.4-mini + deepseek-v4-flash. **D5** — calibration contract: one non-comparative call per candidate (3-call ceiling), provider-reported usage, Joseph fires at end of P2, sibling calibration artifact (fixture manifest never mutated) — a narrow D1 amendment.
+**Joseph's rulings (2026-07-26), binding:** **D1** — coding starts at blueprint sign-off; live experiments/tuning/ingestion and the destructive P3b wait for labels+gates/experiments. **D2** — State C runs the search (`expressions: []`, `query_kind: state_c`); D-90-8 retired. **D3** — no fat call on thin-empty with N>M; `completed` + `thin_attempted` + `thin_retained_zero` (R4 amendment; codex's dissent recorded). **D4** — screening cohort: gemini-3.6-flash (interim default) + gpt-5.4-mini + deepseek-v4-flash. **D5** — calibration contract: one non-comparative call per candidate (3-call ceiling), provider-reported usage, Joseph fires at end of P2, sibling calibration artifact (fixture manifest never mutated) — a narrow D1 amendment. **D6** — R2 amended: one budget calculation method for BOTH stages (fat preflight + rendered-query ceiling 4,096 B + `OUTPUT_ALLOWANCE_FAT` = 2,000; calibrated-with-headroom labeling; codex K1 ≡ opus5 J1).
 
 ---
 
@@ -61,6 +61,9 @@ graph_search(request):
           evidence_status=not_applicable, body_coverage=None, fat-side yields null,
           telemetry thin_retained_zero (watched)
 
+    estimate(system + template + query + fat evidence over stage2_slugs) > floor(selector.ctx_window × 0.8)
+        → budget_exceeded, execution=thin_attempted, stage=fat recorded   # D6 — same rule as thin; zero spend, never retried
+
     fat = stage_call("fat", render fat over stage2_slugs with excerpts)  # up to 2 logical attempts (§8)
     if fat failed after its retry budget:
         → selector_failure, execution=two_stage_attempted, failure_class recorded
@@ -121,10 +124,11 @@ v0.2 list unchanged: context_loader T2Mode/`_t2_*`/resolver-wrapper removals, co
 - Field/delimiter lines at 2-space indent; **excerpt content always at 4 spaces**; only the exact 2-space `"""` line terminates; serializer asserts + counts `delimiter_collision_guard`.
 - **Clause 1 (G7):** the excerpt is split on `"\n"` (not `splitlines()`) — a trailing newline emits a final whitespace-only `"    "` line (161/163 fixture excerpts end with a newline).
 - **Clause 2 (G7):** blank lines are indented too (4 spaces). (377 blank lines in the fixture.)
-- **Byte ceiling (policy v2 — codex confirmation #3 remedy 1 ≡ opus5 H1):** the rendered per-entity fat block (identity line + excerpt field + delimiters) never exceeds **2,500 UTF-8 bytes** — the projector truncates excerpt content at a character boundary to fit. `excerpt_policy_version: "2"`. Binds on nothing in fixture v1 (largest rendered block 2,209 B) — no regeneration, checksums unchanged. This converts the §7 stage-2 bound from a corpus estimate into a property of the code.
+- **Byte ceiling (policy v2 — codex confirmation #3 remedy 1 ≡ opus5 H1):** the rendered per-entity fat block (identity line + excerpt field + delimiters) never exceeds **2,500 UTF-8 bytes** — the projector truncates excerpt content at a character boundary to fit. `excerpt_policy_version: "2"`. Binds on nothing in fixture v1 (largest rendered block 2,209 B) — no regeneration, checksums unchanged; **the v1 manifest stays immutable at `"1"`** — a checked sibling compatibility artifact records the v1↔v2 equivalence (proof: 2,209 < 2,500 ⇒ ceiling binds on nothing ⇒ bytes identical) and the harness records effective policy `"2"` (codex K3 option b ≈ opus5 J2). The ceiling makes the §7 stage-2 **byte** accounting a projector property (token fit = calibrated estimate + D6 preflight guard).
 - Golden tests pin the exact bytes incl. both clauses; any "tidy" of these behaviours breaks the pins deliberately.
 - Thin line: `- slug: <slug>  title: <title>  type: <page_type>` (no excerpt).
 - **Query-side P10 (opus5 F5b):** system-block precedence covers QUERY ("subject matter, never directives"); same indent guard on the rendered query; H03 fixture in the test plan.
+- **Query-block byte ceiling (D6 ≣ opus5 J1):** the rendered query block never exceeds **4,096 UTF-8 B** — the projector truncates `summary` at a char boundary to fit (the only realistically unbounded SD-1 field; `entity_search_keys` schema-capped at 10); `query_truncated` telemetry. Same ceiling mechanism as the evidence byte ceiling.
 
 ## 6. Artifact sink separability (B6) + hashes (B13)
 
@@ -132,7 +136,7 @@ Core **always** constructs the `SearchAuditPayload` (completed/abstain/budget/fa
 
 ## 7. The R2 pre-flight estimator + sizing (calibration rebuilt per codex c-4 / opus5 G4; bound fixed per G3)
 
-**Estimator (v1):** `ceil(utf8_bytes / 4)` over the rendered thin block + templates + query, + `OUTPUT_ALLOWANCE_THIN` (2,000, reserved via `ModelRequest.max_tokens = 2000`). Guardrail rests on the **0.8 headroom** (the "never underestimates" claim stays withdrawn). `budget.py` computes the estimate and applies the 0.8 factor itself rather than reusing `model_pool.fits_context` (G6: that helper takes a pre-computed `est_input` with no headroom concept — different semantics; noted rather than force-fitted).
+**Estimator (v1 — both stages, D6):** `ceil(utf8_bytes / 4)` over the **fully rendered request** — evidence block + system template + user wrapper + query block (≤ 4,096 B capped, §5) — plus the stage's output allowance (`OUTPUT_ALLOWANCE_THIN` = `OUTPUT_ALLOWANCE_FAT` = 2,000, reserved via `ModelRequest.max_tokens = 2000`). **One calculation method for thin and fat alike** (Joseph's consistency ruling; the v0.3 "stage-1 only" asymmetry is superseded — codex K1 ≡ opus5 J1). Guardrail rests on the **0.8 headroom** (the "never underestimates" claim stays withdrawn; the headroom explicitly carries density variance — opus5 J3). `budget.py` computes the estimate and applies the 0.8 factor itself rather than reusing `model_pool.fits_context` (G6: that helper takes a pre-computed `est_input` with no headroom concept — different semantics; noted rather than force-fitted).
 
 **Calibration gate (D5 — Joseph, 2026-07-26; mechanics per opus5 G4; timing per codex c-4; artifact separability per codex confirmation #4):** `count_tokens` endpoints exist only for gemini — so calibration reads **provider-reported usage** instead: **exactly one non-comparative real call per candidate (hard 3-call ceiling)** over the exact rendered fixture thin block, reading the returned input-token count `call_model` already surfaces — the billed number, authoritative for all three candidates. Fixed prompt; no ranking or selection effect. **Timing: end of P2** (after golden rendered bytes pass — the production renderer exists then; a pre-P1 measurement would calibrate a renderer that doesn't exist yet), **as a gate before D7 live experiments**. It is a paid run — **Joseph fires it**; D5 records this as a **narrow D1 amendment** (the only live selector spend before D7; experiments/tuning/ingestion still wait for labels + gates). Persisted per candidate as a **sibling artifact** `task123_search_calibration_v1.json` — `{counting_source: "provider_reported_usage", fixture_version, input sha256, prompt version, model id, input_tokens, measured bytes/token}`; **the checksummed fixture manifest is never mutated**. P1's budget tests are structural-only (H3); measurement assertions live at this gate.
 
@@ -146,10 +150,10 @@ Core **always** constructs the `SearchAuditPayload` (completed/abstain/budget/fa
 | fat, value-investing | 51 | 38,512 | ~9.6k | 755 B |
 | fat, largest-150 subset | 150 | 107,885 | ~27.0k | 719 B |
 
-- **Stage-2 bound (codex confirmation #3 remedy 1 ≡ opus5 H1 — the corpus estimate is replaced by a code property):** excerpt policy v2 caps every rendered fat entry at **2,500 UTF-8 bytes** (§5), so `150 × 2,500 B = 375 kB ≈ 94k tokens < 102.4k` (80% × 128k) holds **by construction** for any window ≥ 128k, independent of corpus byte density. (The v0.3 derivation — 275w at the corpus's ~8.8 B/word — was falsified as a bound: 102/163 fixture entities exceed the implied 9.09 B/word; the max excerpt density over all pages, 10.53 B/word, yields 3,026 B at 275w ⇒ ~113k over budget — and vault-scale tails (URLs, code, CJK) are unmeasured. The word cap cannot bound bytes.) The §9 test asserts the bound against the ceiling the projector enforces, cross-checked against the fixture maximum rendered block 2,209 B (`value-investing-as-owner-mindset…`, 251w — 2,209 < 2,500: binds on nothing in fixture v1). No architecture consequence (pool is 400k/1M/1M).
-- **Why only stage 1 is guarded (the named asymmetry, G3's optional item — declined for v1):** thin is unbounded in N (the whole point of the guard); fat is statically bounded by M=150 × the **enforced** 2,500 B ceiling ≈ 94k — under policy v2 a code property, which holds for any window ≥128k (the decline is genuinely safe, not conditionally safe — H1). A fat-stage estimator would be a spec amendment (R2 is a stage-1 rule); recorded, not adopted.
+- **Stage-2 accounting (codex K1 ≡ opus5 J1 — byte bound vs token fit, stated honestly):** excerpt policy v2 caps every rendered fat entry at **2,500 UTF-8 bytes** (§5), so the fat **evidence block** never exceeds `150 × 2,500 B = 375 kB` — a projector property. The **full request** adds query ≤ 4,096 B + system/template ≈ **≤ ~381 kB worst-case bytes** ⇒ ≤ ~381k tokens at the absolute 1-token/byte worst case. **Token fit is not a static claim** — it rests on the calibrated ÷4 estimate (full fat request ≈ 96–98k tokens: fits a 128k window's 102.4k budget with ~5–7k headroom; the configured pool's 320k/800k/800k budgets with 3–8× margin) **plus the D6 fat preflight as the operative guard**: if a real corpus ever breaks the estimate, the failure is a typed, zero-spend `budget_exceeded`, never a provider error. (The v0.3 derivation — 275w at ~8.8 B/word — was falsified as a bound: 102/163 fixture entities exceed the implied density; the v0.4 "holds by construction for any window ≥ 128k" phrasing overclaimed a token bound from a byte bound — codex K1.) §9 asserts the byte accounting against the ceiling the projector enforces, cross-checked against the fixture maximum rendered block 2,209 B (`value-investing-as-owner-mindset…`, 251w — binds on nothing in fixture v1). No architecture consequence (pool is 400k/1M/1M).
+- **Both stages guarded (D6 — this supersedes the v0.3/v0.4 "stage-1 only" asymmetry):** G3's optional fat-stage estimator is **adopted** as R2's amendment (codex K1 ≡ opus5 J1, convergent): the same estimate-and-guard covers the fat call. Thin remains the unbounded-in-N case the guard exists for; fat is byte-capped by construction (≤ ~381 kB full request) and now estimate-guarded at runtime as well.
 - Vault-scale projections (measured): thin whole-graph ~9,600 × 88 B ≈ 845 kB ≈ **~211k tokens**; largest-domain ~3,000 ≈ 264 kB ≈ **~66k**; fat ≤ 150 ≈ ≤27k expected / ~94k policy-bound. All pass the configured pool's 80% budgets.
-- Cost upper bound per source (largest domain): ~10.7k conservative input tokens → ~$0.01–0.02 today; 1,706-source re-ingest ≈ **$25 deepseek / $115 gpt-5.4-mini / $230 gemini**.
+- Cost per source (largest domain): ~10.7k input tokens today → ~$0.01–0.02; 1,706-source re-ingest ≈ **~160M expected-case ⇒ $25 deepseek / $115 gpt-5.4-mini / $230 gemini**; **policy-ceiling worst case 1,706 × ~160k ≈ 273M ⇒ $38 / $205 / $410** (both on the expense record — codex minor + opus5 for-the-record).
 
 ## 8. Selector route (B10) + retry composition (B11 — contract frozen per codex c-1; policy label per opus5 G5)
 
@@ -171,6 +175,7 @@ Core **always** constructs the `SearchAuditPayload` (completed/abstain/budget/fa
 | thin exhausted, N>M | 2 thin, 0 fat |
 | thin exhausted, N≤M → fat (F1) | 2 thin + 1–2 fat = 3–4 |
 | fat exhausted | 1–2 thin + 2 fat = 3–4 |
+| fat budget_exceeded (D6) | 1–2 thin, 0 fat |
 
 ## 9. Replay modes (spec §5.2 — unchanged)
 
@@ -178,7 +183,7 @@ Record replay (default; integrity hash validated; no calls/reads) and historical
 
 ## 10. Truth-set harness shape (P4)
 
-`tools/benchmark/` harness over fixture v1 (checksum-verified; body_reader serves frozen bytes), 39 probes (incl. H03), spec §8.3 metrics 1–7 + failure/retry rates + concordance + cost; reduced-M protocol parameterized (M=10/20 over the 51; M=20/40 over the 163). **Live runs wait for Joseph's labels + gates (D1).**
+`tools/benchmark/` harness over fixture v1 (checksum-verified; body_reader serves frozen bytes) **+ the policy-v2 compatibility artifact — effective policy `"2"` recorded (codex K3 option b ≈ opus5 J2; the v1 manifest stays immutable at `"1"`)**, 39 probes (incl. H03), spec §8.3 metrics 1–7 + failure/retry rates + concordance + cost; reduced-M protocol parameterized (M=10/20 over the 51; M=20/40 over the 163). **Live runs wait for Joseph's labels + gates (D1).**
 
 ## 11. Measurement/board contract (codex c-3 + opus5 G1 — decided) + phased plan
 
@@ -189,7 +194,7 @@ Record replay (default; integrity hash validated; no calls/reads) and historical
 - **Existing diagnostic series keep their population (H4):** `retry_load`, `token_overrun_rate`, `repair_rung_rate` (computed over the whole `calls` list — `processing.py:91-106`) stay pass1+pass2; pass-1.5 records do **not** join them (no silent diagnostic re-baseline). Selector retry/token/latency behavior surfaces in the new `pass1_5` columns instead.
 - **One measurement per search** (not per stage): avoids the pass-1 `duplicate_source_id` completeness collision; per-stage cost/prompt attribution survives as fields inside the search measurement — `prompt_versions: {thin, fat}` (G1.3; `PassCallMeasurement`'s single `prompt_version` doesn't fit two prompts).
 - **Header analog (G1.2):** `RunMeasurementHeader` gains `searches_attempted` so the D-117-5 completeness contract can mark the pass-1.5 column complete.
-- **Reconciliation invariant (codex c-3 — stated as what it is, H5): a mis-partitioning check** — total run cost and call count == pass1 + pass1.5 + pass2, catching a dropped or double-counted pass-1.5 record; both sides derive from the same measurement list, so the **independent side** is the envelopes on disk: `len(glob("search/*.json")) == searches_attempted` (integration test).
+- **Reconciliation invariant (codex c-3 — stated as what it is, H5): a mis-partitioning check** — total run cost and call count == pass1 + pass1.5 + pass2, catching a dropped or double-counted pass-1.5 record; both sides derive from the same measurement list, so the **independent side** is the envelopes on disk: `len(glob("search/*.json")) == searches_attempted` — a **successful-write** completeness condition: envelope persistence is warn-only, so a write failure surfaces as an incomplete measurement state plus a `search_artifact_write_failures` count, never as a silent invariant violation (codex minor; integration test).
 
 **Phases (calibration re-timed per codex c-4; plumbing per G8.2):**
 
@@ -207,7 +212,7 @@ Record replay (default; integrity hash validated; no calls/reads) and historical
 
 ## 12. Test plan (TDD-first; concurrence items folded)
 
-**P1 (`kdb_search/tests/`):** `test_contract.py` (status × execution × evidence_status matrix incl. the D3 terminal's complete contract: all expressions unresolved, concordance null, evidence_status `not_applicable`, body_coverage None; `fat_after_thin_failure` naming); `test_response.py` (four-way, 6-of-10, coercion, accounting); `test_zero_escape.py` (property); `test_budget.py` (zero-invocation `budget_exceeded`; never retried; **estimator structure only — threshold arithmetic against synthetic windows (H3: measurement assertions live at the calibration gate, not P1)**; `max_tokens=2000`; **`ctx_window=None` route ⇒ typed config error at resolution, before any work**); `test_projection.py` (policy v1 caps/extension + **policy-v2 byte ceiling — truncation at a character boundary, total rendered entry ≤ 2,500 B**; **§5 grammar incl. both G7 clauses — trailing-newline whitespace line, indented blank lines**; `delimiter_collision_guard` counts); `test_artifact.py` (payload on all paths; `logical_call_count == StageRecords`; hashes); packaging smoke tests.
+**P1 (`kdb_search/tests/`):** `test_contract.py` (status × execution × evidence_status matrix incl. the D3 terminal's complete contract: all expressions unresolved, concordance null, evidence_status `not_applicable`, body_coverage None; `fat_after_thin_failure` naming); `test_response.py` (four-way, 6-of-10, coercion, accounting); `test_zero_escape.py` (property); `test_budget.py` (zero-invocation `budget_exceeded` **at both stages** (D6 — fat preflight ⇒ zero fat-call invocations + typed `budget_exceeded`/`execution=thin_attempted`); never retried; **full-request accounting — system + template + query + evidence + output allowance** (codex K1); **estimator structure only — threshold arithmetic against synthetic windows (H3: measurement assertions live at the calibration gate, not P1)**; `max_tokens=2000` both stages; **`ctx_window=None` route ⇒ typed config error at resolution, before any work**); `test_projection.py` (policy v1 caps/extension + **policy-v2 byte ceiling — truncation at a character boundary, total rendered entry ≤ 2,500 B** + **query-block ceiling ≤ 4,096 B (D6) with `query_truncated` count**; **§5 grammar incl. both G7 clauses — trailing-newline whitespace line, indented blank lines**; `delimiter_collision_guard` counts); `test_artifact.py` (payload on all paths; `logical_call_count == StageRecords`; hashes); packaging smoke tests.
 
 **P2 (`kdb_search/tests/`):** `test_prompts_golden.py` (pinned bytes; version/SHA guard); `test_two_stage.py` (thin→fat order; retain-all variants; **branch-specific call counts per the §8 table**; F1 path (proceed + `thin_failed_nonbinding` + concordance null + `fat_after_thin_failure`); D3 terminal (no fat call + `thin_retained_zero`); concordance formula + null cases; retry classes ⇒ attempt 2 archived; exhausted ⇒ typed `selector_failure`; stage entry records the provider's actual sub-retry policy); `test_adversarial.py` (H01/H02 evidence-side, H03 query-side); `test_replay.py`.
 
@@ -222,9 +227,9 @@ Record replay (default; integrity hash validated; no calls/reads) and historical
 | # | Question | Disposition |
 |---|---|---|
 | **B1** | Package boundary | **Selected, pending blueprint ratification** (label corrected per codex c-5): `kdb_search/`, `{common}`; packaging per §1 |
-| **B3** | Estimator | `ceil(utf8_bytes/4)` + 2k reserved output vs 80% window; calibration = provider-reported usage, one non-comparative call per candidate at end of P2, Joseph fires (**D5**), sibling artifact (§7) |
+| **B3** | Estimator | `ceil(utf8_bytes/4)` over the full rendered request + 2k reserved output vs 80% window — **both stages (D6)**; calibration = provider-reported usage, one non-comparative call per candidate at end of P2, Joseph fires (**D5**), sibling artifact (§7) |
 | **B4** | Prompt storage | `kdb_search/prompts/*.txt`, version + SHA guard, package-data + wheel smoke test |
-| **B5** | Escaping | Frozen grammar (§5) incl. G7 clauses + **policy-v2 byte ceiling (2,500 B rendered entry)**; JSON-array amendment rejected |
+| **B5** | Escaping | Frozen grammar (§5) incl. G7 clauses + **policy-v2 byte ceiling (2,500 B rendered entry)** + **query-block ceiling (4,096 B — D6)**; JSON-array amendment rejected |
 | **B6** | Artifact sink | Core always constructs; caller persists; `artifact_path` null on failure; `logical_call_count == StageRecords` |
 | **B7** | Adapter placement | `compiler/search_adapter.py` in `compile_source` step 1; explicit `vault_root`/`intra_run_order`/`ModelSpec`; §3.1 plumbing named |
 | **B8** | Record evolution | V2 + sibling envelope; dispatching loader; `context_failed.search` preserved; **hit-level provenance + representative per-expression projection**; KPI resolver read removed (no scored axis touched) |
@@ -233,10 +238,11 @@ Record replay (default; integrity hash validated; no calls/reads) and historical
 | **B11** | Retry composition | Up-to-2 logical attempts/executed stage; per-attempt StageRecords; actual sub-retry policy recorded; pass-1 no-backoff posture adopted (§8) |
 | **B12** | FTS track | P5b, independent CLI/MCP-surface track |
 | **B13** | Refs + hashes | Caller-computed `GraphSnapshotRef`; canonical-JSON sha256s |
-| **B14** | Gate + edges | D1–D5 per Joseph (binding); plus F1 (`fat_after_thin_failure`), F4/c-3 (§11 measurement contract), F5b (query-side P10), codex #12 (concordance); confirmation round absorbed: codex 1–4, opus5 H1–H9 |
+| **B14** | Gate + edges | D1–D6 per Joseph (binding); plus F1 (`fat_after_thin_failure`), F4/c-3 (§11 measurement contract), F5b (query-side P10), codex #12 (concordance); confirmation rounds absorbed: codex 1–4 + K1–K3, opus5 H1–H9 + J1–J4 (D6) |
 
 ## Changelog
 
+- **v0.5 (2026-07-26)** — v0.4 confirmation round absorbed (codex REVISE K1–K3 + 3 minors; opus5 CONCUR J1–J4; every checkable claim verified against the repo first: `summary`/`key_themes` schema-unbounded at `pass1_schema.py:77-88`; fixture manifest at policy `"1"`). codex K1 ≡ opus5 J1 → **D6 (Joseph): one budget calculation method for BOTH stages** — fat gains the same calibrated pre-flight estimate + 80% guard (typed `budget_exceeded`, stage recorded, zero spend, never retried; branch table gains the fat-budget row); full-request accounting (evidence ≤ 375 kB + query ≤ 4,096 B + system/template ≈ ≤ ~381 kB worst-case bytes; `OUTPUT_ALLOWANCE_FAT = 2,000`); rendered-query byte ceiling 4,096 B with `query_truncated` telemetry; "conservative"/"provably fits ≥128k" relabeled calibrated-with-headroom (J3 — D5 unchanged at 3 calls); codex's tokenizer option rejected (no local tokenizer; gemini-only counters). codex K2: R3 row synced with D1/D5; SD-4/R4 gate named "recall at the predeclared reduced-M points". codex K3 ≈ opus5 J2: fixture v1 immutable; sibling policy-v2 compatibility artifact (2,209 < 2,500 proof); harness records effective policy `"2"`. Minors: byte-ceiling precedence over the sentence rule; 160M expected-case + 273M/$38/$205/$410 policy-max expense record; envelope-count = successful-write completeness (+ `search_artifact_write_failures`); §7.2 measured-figure sync (J4); §3.2 branch-language sync.
 - **v0.4 (2026-07-26)** — confirmation round absorbed (codex REVISE items 1–4; opus5 H1–H9 incl. the v0.2→v0.3 removal audit; every checkable claim verified against the repo first). codex #1 ≡ opus5 H3: calibration reconciled with D1 via **D5** (Joseph) — one non-comparative call per candidate, 3-call ceiling, Joseph fires at end of P2; P1 budget tests structural-only (estimator structure against synthetic windows; measurement assertions live at the calibration gate). codex #2 ≡ opus5 H2: spec v0.6 folds D1–D4 through the normative body (`execution` enum gains `fat_after_thin_failure` at both sites; branch call contract replaces "exactly two calls"; 55k/64k superseded; stale gating retired; resolved open items closed). codex #3 ≡ opus5 H1: stage-2 bound made enforceable — excerpt policy v2 hard-caps the rendered per-entity fat block at 2,500 UTF-8 B (projector-enforced, char-boundary truncation; binds on nothing in fixture v1, max 2,209 B — no regeneration); `150 × 2,500 B ≈ 94k < 102.4k` is now a code property, and the fat-estimator decline becomes genuinely safe. codex #4: calibration persists as a sibling artifact (`task123_search_calibration_v1.json`); the checksummed fixture manifest is never mutated. opus5 H7–H9 + minors restored (V2 kept-field list; #119 byte-pinning note + caller-supplied-snapshot qualifier; the five-way never-surfaced enumeration; SD-5 survivor series; fail-hard propagation posture; T3's T1∪T2 seed clause; delimiter-guard test hook). opus5 H4 (existing diagnostic series keep pass1+pass2 population), H5 (reconciliation = mis-partitioning check + envelope-count independent side), H6 (§3.1 flow gains the batched provenance read, adapter-side).
 - **v0.3 (2026-07-26)** — concurrence pass absorbed (codex items 1–5, opus5 G1–G8; every checkable claim verified against the repo first: fixture max fat block 2,209 B; gemini has no `max_retries` (`call_model.py:212`); `ModelSpec.ctx_window` is `int|None`; retiring KPI fields absent from `GRAPH_WEIGHTS`). codex c-1: stage/attempt contract frozen ("up to two logical attempts per executed stage"), branch-specific call-count table, `logical_call_count == StageRecords`, D3 terminal's complete result contract. c-2 ≡ opus5 G2: provenance moved to hit level (batched direct read, no resolver) with the per-expression representative projection; State C/unattributed hits covered. c-3 ≡ G1: measurement contract decided — pass-1.5 out of the scored union (non-movement test), cost-centre diagnostics, per-search measurement with `prompt_versions {thin,fat}`, `searches_attempted` header, reconciliation invariant, explicit weights case. c-4 ≡ G4: calibration = provider-reported usage at end of P2, Joseph fires; no `count_tokens` dependency. c-5: spec v0.5 amendments published (D1–D4), ledger + North Star synced, B1 label corrected. opus5 G3: stage-2 bound re-derived from policy-max (2,500 B/entity ⇒ 94k < 102.4k, 8% margin) verified against fixture max; fat-stage estimator declined with the unbounded-vs-static asymmetry recorded. G5: actual sub-retry policy recorded per provider (gemini none); pass-1 no-backoff posture adopted. G6: `ctx_window` asserted at resolution; `fits_context` non-reuse noted. G7: two serializer clauses (trailing-newline whitespace line; indented blank lines). G8: `fat_after_thin_failure` execution value; P3a plumbing named.
 - **v0.2 (2026-07-26)** — v0.1 panel review absorbed (codex REVISE 9+2; opus5 F1–F8) + **Joseph's rulings D1–D4** (coding at sign-off; State C runs; no fat call on thin-empty N>M + `thin_retained_zero`; deepseek joins the cohort). 12 convergent absorptions: packaging; serializer grammar frozen + sizing recomputed; emit_kpis dispatch; KPI resolver removal; 2-attempt loop; adapter wiring; single empty-space audit path; estimator honesty + calibration gate; concordance denominator; thin-failure-nonbinding; P3a/P3b split; measurement reader + query-side P10 (H03).
