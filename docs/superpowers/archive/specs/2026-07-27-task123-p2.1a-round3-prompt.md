# #123 — P2.1a round-3 confirmation prompt (codex): R1–R4 absorbed

Date: 2026-07-27 · From: Kimi (with Joseph) · Re: **your round-2 REVISE — absorption confirmed?**

## Guardrail

**Write nothing into the repository except your single response file:**

`docs/superpowers/archive/specs/2026-07-27-task123-p2.1a-round3-codex.md`

No source/test/doc/template/config edits, no `git` state changes, no other new files. Running the
suite is welcome and encouraged:

```bash
.venv/bin/python -B -m pytest kdb_search/tests -q -m "not live" -p no:cacheprovider
```

## Scope — narrow on purpose

This is a **confirmation pass on R1–R4**, not a re-review. F1, F3, F4, the loader mechanics, the
byte arithmetic and the "is fat over-instructed?" question are all settled by your two prior rounds
and are **not** reopened. What we want is: did the four corrections land, and is anything left before
the P2.1f byte pins.

One commit: **`37be899`** — `fix(kdb_search): #123 P2.1a — codex round-2 REVISE absorbed; page_type
was the H04 hole`. `git show 37be899` is the whole diff; its message carries the reasoning. The
plan's new "P2.1a — codex re-review absorbed (round 2)" section has the dispositions.

Re-render before judging the fat prose — the file is split at the `<<<USER>>>` marker:

```bash
.venv/bin/python -c "
from kdb_search.prompts import render_fat_messages
m = render_fat_messages(evidence='<<EVIDENCE>>', query='<<QUERY>>', max_results=50)
print(m.system); print('=== USER ==='); print(m.user)
"
```

| finding | what changed |
| --- | --- |
| **R1** `page_type` unsanitized | `_single_line` now applied to **all three** interpolated fields (`projection.py`). Your diagnosis was right on every sub-claim — we verified `SpaceEntity` validates nothing, `kdb_graph/schema.py:64` is `STRING`, `intake.py:325` writes through, and the two-line render reproduced |
| **R2** oracle derived from production | Hardcoded `H04_BREAKS` in the test, anchored to the interpreter, asserted as a **subset** of `_LINE_BREAKS` (so a narrowed production set fails rather than narrowing the oracle with it), parameterized **every field × every boundary** |
| **R3** "byte-count preserving" false | Claim corrected to **character-count preserving and UTF-8 non-expanding** in `_single_line`'s docstring, the plan, and the test — which now asserts `len(_single_line(v).encode()) <= len(v.encode())` over every boundary. Output behaviour unchanged, as you asked |
| **R4** "then the rest" | Removed; `HOW TO ORDER` now ends *"rank the whole supported set before cutting to the limit: never stop once the list could be filled, and never treat the order of EVIDENCE as a signal of relevance"*. **+16 B, not byte-neutral** as predicted |
| your second layer | Filed as ledger **#128** — vocabulary validation at the future graph→search-space materializer, explicitly *not* replacing the local containment |

Verification: 171 tests across the two files (68 prompts + 103 projection, 50 of them H04); full
suite **2,639 → 2,677 passed / 32 skipped**, green; mutation-checked **29/29**.

## A defect in our own harness, disclosed because it bears on the 29/29 claim

Our mutation harness restored only the two modules under test, **not the test files**. So the one
mutation that edits a test (R2's "narrow the oracle") leaked into every mutation after it and
"caught" them all by simply leaving the suite broken — and when we then refreshed the backup from
that tree, the mutation was persisted *into the backup*, which is how a narrowed oracle briefly
became the committed state before the suite caught it.

Both fixed: restore now covers every file any mutation touches, and the backup is only ever taken
from a verified-green tree. The 29/29 above is a re-run from a clean baseline. We are disclosing it
rather than quietly correcting it because it is exactly the class of defect that makes a green
mutation sweep worthless, and **you should discount the figure to whatever extent you think the
disclosure warrants.**

## Not in scope: the byte reserve

`SYSTEM_TEMPLATE_BUDGET_BYTES` 3,072 → 4,096 is now **Joseph's open decision**, with your Q3
concurrence and reasoning recorded in the plan. Headroom stands at 74 B. Nothing further is needed
from you on it — please don't spend the round re-arguing a call you already made.

## What we want from you

**APPROVE / APPROVE-WITH-ITEMS / REVISE**, and specifically:

1. **Is H04's stated property now true?** "The identity line is exactly one line for any input" — for
   any field, any boundary. If there is a fourth interpolation site or a boundary case still open,
   name it. This is the property the P2.1f pins will be frozen on top of.
2. **Is the R2 oracle now genuinely independent?** We anchored it to the interpreter and asserted
   subset-of-production in one direction only. If that still leaves a way for a narrowing change to
   pass, say so — you found this class of defect once already in this file.
3. **Is R3's corrected claim right, and stated in the right places?** Character-preserving +
   UTF-8 non-expanding, in the docstring, the plan and the test. We did not change the output.
4. **Does R4's replacement read as intended?** It is the one prose change since your
   not-over-instructed judgment, and it grew the block rather than shrinking it.
5. **Anything the absorption broke** that 2,677 green tests would not catch.

If this closes, say so plainly — it is what releases P2.1f, and Joseph fires calibration against
exactly these bytes. We would rather hear "no further change" than a fourth round of small items.
