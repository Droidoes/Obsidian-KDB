# #123 — P2.1a re-review prompt (codex): F1–F5 absorbed, one figure left open

Date: 2026-07-27 · From: Kimi (with Joseph) · Re: **your REVISE on P2.1a — absorption pass**

## Guardrail

**Write nothing into the repository except your single response file:**

`docs/superpowers/archive/specs/2026-07-27-task123-p2.1a-rereview-codex.md`

No edits to source, tests, docs, templates or config; no `git` state changes; no other new files.

**Loosened from last time, deliberately:** you declined to run pytest because of cache writes.
Running the suite is welcome — use `-p no:cacheprovider` (and `PYTHONDONTWRITEBYTECODE=1` if you
want), which keeps it to temp dirs:

```bash
.venv/bin/python -m pytest kdb_search/tests -q -m "not live" -p no:cacheprovider
```

## What changed

One commit: **`568313c`** — `fix(kdb_search): #123 P2.1a — codex REVISE absorbed; H04 fixed at the
serializer`. Its message carries the full reasoning; `git show 568313c` is the whole diff. The
plan's P2.1a section has the absorption table and the open item.

Your review is on record verbatim at `docs/superpowers/archive/specs/2026-07-27-task123-p2.1a-review-codex.md`.

Re-render before judging the prose — the file is split at the `<<<USER>>>` marker, so the bytes the
model receives are not the file's bytes:

```bash
.venv/bin/python -c "
from kdb_search.prompts import render_fat_messages, render_thin_messages
for name, m in (('FAT', render_fat_messages(evidence='<<EVIDENCE>>', query='<<QUERY>>', max_results=50)),
                ('THIN', render_thin_messages(evidence='<<EVIDENCE>>', query='<<QUERY>>'))):
    print(f'===== {name} SYSTEM ====='); print(m.system)
    print(f'===== {name} USER =====');   print(m.user)
"
```

| finding | disposition |
| --- | --- |
| **F1** thin contradictory at a binding cap | **absorbed** — replaced with two explicit steps (eligibility, then the limit), cap-aware economics, plus two prohibitions in both templates: never stop reading once the list could be filled; never treat EVIDENCE order as a relevance signal |
| **F3** fat ranking + one-directional excerpt clause | **absorbed** — a `HOW TO ORDER` section (directness/strength of positive support → centrally-about → supporting coverage; breadth across keys/themes as tiebreaker), and "text you cannot see is not evidence for it either" |
| **F2 / H04** serializer framing | **absorbed and fixed** — `projection._single_line`, applied to slug *and* title, over the full `str.splitlines()` boundary set. Joseph authorized the P1 byte change. 12 regression tests |
| **F4** version parser unreachable at import | **absorbed** — one `_version_of()` after `PromptTemplateError`, used by the constants *and* `load_template`; parser tested directly over five malformed names |
| **F5** wheel gap | **filed, not closed** — ledger **#127**, on your own disposition (blocks neither the pin nor calibration) |

Verification: 80 tests across the two files, full suite **2,621 → 2,639 passed / 32 skipped**, green.
Mutation-checked **27/27** — the original 20 plus 7 for this round (title escaping removed; slug
escaping removed; the `splitlines()` set narrowed to `\n`; the escape dropping the character rather
than collapsing it; the escape expanded to a 2-char literal, which breaks byte-count preservation;
`_version_of` made total again; `load_template` hardcoding the version).

## Two corrections we are putting to you

**1. Your F2 rationale, on the trust boundary.** You wrote that "graph titles ultimately contain
model-authored/source-derived content." Model-authored titles were already closed:
`compiler.page_writer.emit_frontmatter` **raises** `PagePatchError` on a newline in any frontmatter
string (pinned, `compiler/tests/test_page_writer.py:116`). The channel that is actually open is
**hand-authored ingested notes** — `yaml.safe_load` (`common/source_io.py:39`) →
`kdb_graph/intake.py:325`, no single-line check — which is exactly the 1,586-note vault queued next.
Same conclusion, different path. We fixed it. Does the narrower path change your view of the
remedy's shape, or of what else in the ingest path deserves the same treatment?

**2. Ours, not yours: precision@5.** You were right that our prompt overstated it. Its *formula* is
normative (spec §8.3 metric 3); the gate table has one binding gate — escaped foreign-identity rate
— and precision@5 is not in it. Recorded and corrected.

## The one thing still open — and where we most want your judgment

Your F1 and F3 prose cost fat **541 B**. Overhead moved **2,381 → 2,922 B**, so headroom against
`SYSTEM_TEMPLATE_BUDGET_BYTES = 3,072` fell **691 → 150 B**. The constant still holds and the suite
is green. But **P2.1f pins these bytes next**, and freezing a prompt 150 B under a ceiling means the
next sentence anyone writes breaks a ratified figure.

You answered question 5 last round with *"is 691 B enough? Yes for these corrections … no blueprint
bump appears necessary."* After your own findings landed, that is no longer where the numbers are —
so we are asking again with the real figures.

**Our recommendation: raise it to 4,096 B.** The argument is that 3,072 was never a measured bound —
it is the "~3 kB system/template" line written into the M=100 static guarantee before any prompt
existed. What the guarantee needs is slack against `SMALLEST_POOL_BUDGET_TOKENS`, and it has
**36,832 tokens** of it (283,168 against 320,000). Raising the reserve 1,024 B puts the worst case at
258,192 B and the guarantee at 284,192 — still ~35.8k under the pool — and 4,096 matches
`QUERY_BLOCK_CEILING_BYTES`, so the two per-request reserves read as one scheme. Cost: a **blueprint
v0.15** amendment to §7.0/§7.0a plus the `constants.py` docstring. No allowance, ceiling or wire
figure moves.

Current measurements, for checking:

| | overhead | headroom vs 3,072 |
| --- | --- | --- |
| fat | 2,922 B (2,862 system + 60 wrapper) | **150 B** |
| thin | 2,507 B (2,446 + 61) | 565 B |

Stage-2 bound re-measured: `100 × 2,500 + 4,096 + 2,922 = 257,018 B` ≤ ~257 kB; `+ 26,000` reserved
output = `283,018 < 320,000`.

## What we want from you

**APPROVE / APPROVE-WITH-ITEMS / REVISE**, plus:

1. **Do F1's two steps actually resolve the contradiction, or introduce a new one?** Specifically:
   is "eligibility, then rank the whole eligible set and cut" a rule a model can follow, and does
   anything in the new text still imply that a weak retention is free?
2. **Is the fat block now over-instructed?** This is the question we cannot answer ourselves and the
   one we are most wary of. A longer prompt is not automatically a better one — `WHAT TO SELECT`
   plus `HOW TO ORDER` is now four paragraphs of judgment guidance, and dilution or conflicting
   emphasis would be invisible to us until P5a. If something should be cut to make the rest land
   harder, name it.
3. **The byte figure.** Raise to 4,096, hold at 3,072 and trim, or something else — with the reason.
   Joseph decides, but you are the reviewer who created the pressure, so your view carries weight.
4. **H04's remedy, three specific choices:** collapsing a break to a space (byte-count preserving)
   versus escaping visibly to `\n`; defining the escaped set as everything `str.splitlines()` breaks
   on rather than `\n\r`; and **not counting** the anomaly, on the ground that no consumer needs the
   count and — unlike `delimiter_collision_guard` — the anomaly is removed rather than left in place.
5. **F4 and the tests:** is the parser now the single checked path on both routes, and do the 12 H04
   regression tests pin the property rather than the example?
6. **Anything the absorption broke** that the suite would not catch.

If the prose is now right, say so plainly — we will freeze it at P2.1f and Joseph fires calibration
against exactly these bytes, so "no further change" is the answer that unblocks the phase.
