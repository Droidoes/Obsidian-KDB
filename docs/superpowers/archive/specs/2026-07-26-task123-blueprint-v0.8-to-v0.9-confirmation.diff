diff --git a/docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md b/docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md
index 40b695e..af0d28b 100644
--- a/docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md
+++ b/docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md
@@ -1,7 +1,7 @@
-# #123 — Semantic Graph Search: Spec v0.10 (v0.4 RATIFIED + post-ratification amendments D1–D8)
+# #123 — Semantic Graph Search: Spec v0.11 (v0.4 RATIFIED + post-ratification amendments D1–D8 + D9 drafted)
 
 Date: 2026-07-25 · Task: **#123 Semantic graph search** · Status: **v0.10 — v0.4 RATIFIED (Joseph, 2026-07-25) + amendments D1–D8 (Joseph, 2026-07-26); D8 mechanics completed per codex N1–N4 ≡ opus5 N1–N3 (fully-bounded wire, allowances from exact max serialization under `tokens_lte_bytes` — no density step; slug bound = `MAX_SLUG_LEN` imported)**
-Basis: vision v1.2 RATIFIED (D1–D10) + v1.3 (SD-4 scale amendment) + v1.4 (R1 salvage/no-fallback amendment) · v0.1 reviews (codex F1–F8, opus5 §A–§F) · v0.2 re-reviews (codex REVISE F1–F6, opus5 CONCUR §1–§3) · syntheses (`…-spec-review-synthesis.md`, `…-spec-v0.2-review-synthesis.md` — all checkable claims verified against the repo) · **Joseph's rulings 2026-07-25: R1 per-entry salvage + retry-not-fallback; R2 whole-graph stage-1 option 1; R3 narrower gate** · **Joseph's rulings 2026-07-26: D1–D8 (below)** · blueprint v0.3 confirmation reviews (codex REVISE 1–4; opus5 H1–H9) · blueprint v0.4 confirmation reviews (codex REVISE K1–K3 + minors; opus5 CONCUR J1–J4) · blueprint v0.5 confirmation reviews (codex REVISE L1–L3; opus5 CONCUR L1–L3) · blueprint v0.6 confirmation reviews (codex REVISE M1 + sweep; opus5 CONCUR M1–M4).
+Basis: vision v1.2 RATIFIED (D1–D10) + v1.3 (SD-4 scale amendment) + v1.4 (R1 salvage/no-fallback amendment) · v0.1 reviews (codex F1–F8, opus5 §A–§F) · v0.2 re-reviews (codex REVISE F1–F6, opus5 CONCUR §1–§3) · syntheses (`…-spec-review-synthesis.md`, `…-spec-v0.2-review-synthesis.md` — all checkable claims verified against the repo) · **Joseph's rulings 2026-07-25: R1 per-entry salvage + retry-not-fallback; R2 whole-graph stage-1 option 1; R3 narrower gate** · **Joseph's rulings 2026-07-26: D1–D8 (below)** · blueprint v0.3 confirmation reviews (codex REVISE 1–4; opus5 H1–H9) · blueprint v0.4 confirmation reviews (codex REVISE K1–K3 + minors; opus5 CONCUR J1–J4) · blueprint v0.7 confirmation reviews (codex REVISE N1–N4; opus5 CONCUR N1–N3) · blueprint v0.8 confirmation reviews (codex REVISE O1–O3; opus5 CONCUR O1–O3) · blueprint v0.5 confirmation reviews (codex REVISE L1–L3; opus5 CONCUR L1–L3) · blueprint v0.6 confirmation reviews (codex REVISE M1 + sweep; opus5 CONCUR M1–M4).
 
 Scope of this document: the graph-search contract, the selector prompt contract, the pass-1.5 adapter, determinism/replay, evidence status + telemetry, vault-scale sizing, and the **D7 truth-set program** (defined before any tuning). Out of scope: package boundary + selector model choice (blueprint, P9); artifact-sink mechanics (blueprint); FTS infrastructure implementation (blueprint track).
 
@@ -15,8 +15,13 @@ Scope of this document: the graph-search contract, the selector prompt contract,
 - **D4 — selector screening cohort (blueprint scope, recorded here for completeness).** Truth-set selector screening runs three candidates: `gemini-3.6-flash`, `gpt-5.4-mini`, **`deepseek-v4-flash`** (codex: its #120 pass-2 collapse is a different prompt contract — measured directly or not at all). Production default decided by the D7 results.
 - **D5 — estimator calibration contract (amends D1 narrowly; blueprint §7/§11).** Exactly **one non-comparative calibration call per screening candidate** (hard 3-call ceiling): fixed prompt = the rendered fixture thin block, provider-reported usage read, no ranking or selection effect, **Joseph fires it at end of P2**. Persisted as a **sibling calibration artifact** (`task123_search_calibration_v1.json` — fixture version, input sha256, prompt version, model id, `counting_source: provider_reported_usage`, input_tokens, measured bytes/token); the checksummed fixture manifest is **never mutated** (codex confirmation #4). P1 budget tests are structural-only (synthetic windows); measurement assertions live at the calibration gate. D1 otherwise unchanged — live selector experiments, tuning, and vault ingestion still wait for labels + gates.
 - **D6 — R2 amended: one budget calculation method for BOTH stages (amends §7.2 R2; blueprint §2.2/§7/§8).** The pre-flight budget rule — calibrated `ceil(utf8_bytes ÷ 4)` estimate over the **fully rendered request** (system + template + query + evidence) plus the stage's reserved output, against 80% of the configured selector window — applies to the **fat call exactly as to the thin call** (Joseph's consistency ruling). Over budget ⇒ typed `budget_exceeded` with the stage recorded, zero spend, never retried. Supporting contract: the rendered **query block** is hard-capped at **4,096 UTF-8 B** in the projector (truncating `summary` at a char boundary — `summary`/`key_themes` are schema-unbounded, verified `pass1_schema.py:77-88`; `query_truncated` telemetry); **`OUTPUT_ALLOWANCE_FAT = 2,000`** declared and reserved via `max_tokens`, exactly as thin's. The static byte accounting (evidence ≤ 150 × 2,500 B + query ≤ 4,096 B + system/template ≈ ≤ ~381 kB) bounds the absolute worst case (≤ 1 token/byte); the calibrated estimate + 0.8 headroom is the **operative** guard — "never underestimates" / "provably fits any window ≥ 128k" stay **withdrawn**, the headroom explicitly carrying density variance (opus5 J3). D5 unchanged (3-call calibration). Codex's option 1 (authoritative per-provider tokenizer preflight) rejected: no local tokenizer exists, provider counters are gemini-only, and fail-config-without-counter would break the pool. (Codex K1 ≡ opus5 J1, convergent.)
-- **D7 — M=100 + the honest budget settlement (supersedes D6's uniform-2,000 allowance and the 150-constant everywhere; blueprint §2.2/§7/§8).** (i) **M=100** (thin retention cap, fat hydration cap): the fat request's absolute worst case — evidence ≤ 100 × 2,500 B = 250 kB + query ≤ 4,096 B + system/template ≈ **≤ ~257 kB bytes ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + 4k reserved output** — **fits the configured pool's smallest budget (320k) by construction**, for any window ≥ ~330k: **no estimation in fat's safety path** (codex K1/L1 closed by sizing, not measurement; his 381 kB counterexample dies with the constant). Zero behavioral change at current scale (largest domain 51 < 100, retain-all intact); the reduced-M gate protocol is unchanged; **M=100 is the production constant**. (ii) **Thin stays estimate-guarded** — its input is the whole space, unbounded in N by Joseph's ratified searched-whole principle: the calibrated preflight is the operative guard; a density-driven underestimate is caught from the provider's context-length rejection and typed **`budget_estimation_miss`** (call attempted, spend possible, never retried — deterministic; watched per model — codex L1 option 2; re-typed as `budget_exceeded`/`detected: post_call` and excluded from the selector-failure gate by opus5 M1 — see §3.4). "Never a provider error" is withdrawn for thin and unnecessary for fat at pool windows. (iii) **Output allowances sized from response schemas** (opus5 L1): THIN/FAT per-schema — D6's uniform 2,000 sat fat's response at 95% of its own ceiling, a mid-JSON truncation ⇒ `unparseable` ⇒ retry ⇒ identical overflow ⇒ hard-gate `selector_failure` chain (values finalized by D8: THIN 4,000 / FAT 4,000 from the bounded compact schema). (iv) **Query ceiling per-field** (codex L2 fix 1): deterministic byte allocations per SD-1 field — `author` ≤ 256 B, `entity_search_keys` ≤ 10 × 128 B per-item, `key_themes` ≤ 1,024 B aggregate, `summary` ≤ remainder; total ≤ 4,096 B; char-boundary truncation; per-field `query_truncated` counts; the archived QueryPayload records original + rendered forms when truncation fires (accounting runs over the rendered expressions — what the selector saw). (v) Fat-`budget_exceeded` terminal's complete contract + two-row matrix + named F1 interaction (codex L3 ≡ opus5 L2).
-- **D8 — minimal wire: numbered expressions → indices; `evidence` dropped; allowances derived from the bounded compact schema (amends §2.2/§2.3; blueprint §2.3/§7/§12).** (i) The request's expressions are rendered **numbered** in the query; the fat selector returns **indices** (`matched: [int]`) and Python maps them back to the rendered expressions — out-of-range index = the unknown-expression coercion, R1's salvage posture intact (codex M1 option 2: no repeated expression strings on the wire). (ii) **`evidence` is dropped from the fat response schema** (Joseph's call — "who's going to use it? nobody"): nothing downstream consumed it (validation, the context build, and KPIs all read structured outcomes), it was the only free-prose field on the wire — unbounded, unverifiable, off-principle (*take the content, not the semantics*) — and ~60% of the worst-case response. (The fat-stage **evidence pool** — excerpts — is untouched: same word, different object.) (iii) Worst case now: 50 × ~170 B ≈ 8.5 kB ⇒ **`OUTPUT_ALLOWANCE_THIN` = 4,000** (100 × ≤ 96 B slugs ≈ ≤ 3.3k tokens at 3 B/token) and **`OUTPUT_ALLOWANCE_FAT` = 4,000** (≤ 2.8k) — derived from the bounded compact schema and asserted against **synthetic schema-maximum documents** (not fixture-average — codex M1). **Slug wire bound ≤ 96 B declared**; the controller asserts the materialized space's max slug length at render time (typed outcome, never silent truncation). (iv) The M=100 static guarantee stands with more margin (257 kB + 4k = 261k < 320k); the `tokens ≤ UTF-8 bytes` premise is recorded as a **named supported-route invariant** (`tokens_lte_bytes`, asserted at route resolution — codex — not an unqualified tokenizer-independent law). **Mechanics completed (v0.10, codex N1–N4 ≡ opus5 N1–N3):** `Hit.evidence` removed from the public result contract (the field D8 dropped from the wire); the wire states **every** numeric maximum (`max_results` ≤ 50 globally; `retained` ≤ 100 × ≤ 120 B; `matched` ≤ 10 unique indices ∈ [0, len(expressions)); `unresolved` ≤ 10 indices — indexed, keeping the `selector_accounting_delta` signal bounded); the slug bound is **`MAX_SLUG_LEN` (120 B), imported from `common.paths`** — not a re-declared 96 B that contradicted the canonical data model (a 97–120 B slug is valid graph data); the render-time assertion now fires only on non-canonical input (a real defect); allowances derive from the **exact max serialization under the invariant** — **THIN 13,000 / FAT 10,000** (12.3k / 9.6k exact max bytes + buffer; `tokens_lte_bytes` ⇒ tokens ≤ bytes — **no density step anywhere**, codex N2 option 1; the static guarantee: 257 kB + 10k = 267k < 320k); `tokens_lte_bytes` lives as a **validated boolean in `models.json` carried by `ModelSpec`** — a declared per-route premise with its theorem (byte-level BPE ⇒ content tokens ≤ content bytes), missing/false ⇒ typed config error at selector route resolution before any work, all three D4 candidates declared + test-covered (codex N4 + opus5 N3).
+- **D7 — M=100 + the honest budget settlement (supersedes D6's uniform-2,000 allowance and the 150-constant everywhere; blueprint §2.2/§7/§8).** (i) **M=100** (thin retention cap, fat hydration cap): the fat request's absolute worst case — evidence ≤ 100 × 2,500 B = 250 kB + query ≤ 4,096 B + system/template ≈ **≤ ~257 kB bytes ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + 10k visible + 16k hidden reserve = 26k provider-total reserved output** — **fits the configured pool's smallest budget (320k) by construction**, for any window ≥ ~355k: **no estimation in fat's safety path** (codex K1/L1 closed by sizing, not measurement; his 381 kB counterexample dies with the constant). Zero behavioral change at current scale (largest domain 51 < 100, retain-all intact); the reduced-M gate protocol is unchanged; **M=100 is the production constant**. (ii) **Thin stays estimate-guarded** — its input is the whole space, unbounded in N by Joseph's ratified searched-whole principle: the calibrated preflight is the operative guard; a density-driven underestimate is caught from the provider's context-length rejection and typed **`budget_estimation_miss`** (call attempted, spend possible, never retried — deterministic; watched per model — codex L1 option 2; re-typed as `budget_exceeded`/`detected: post_call` and excluded from the selector-failure gate by opus5 M1 — see §3.4). "Never a provider error" is withdrawn for thin and unnecessary for fat at pool windows. (iii) **Output allowances sized from response schemas** (opus5 L1): THIN/FAT per-schema — D6's uniform 2,000 sat fat's response at 95% of its own ceiling, a mid-JSON truncation ⇒ `unparseable` ⇒ retry ⇒ identical overflow ⇒ hard-gate `selector_failure` chain (final values and their derivation: D8(iii) as amended by **D9** — visible allowances plus a route-owned hidden-output reserve; the superseded 2,000 and 4,000/4,000 derivations are in the changelog). (iv) **Query ceiling per-field** (codex L2 fix 1): deterministic byte allocations per SD-1 field — `author` ≤ 256 B, `entity_search_keys` ≤ 10 × 128 B per-item, `key_themes` ≤ 1,024 B aggregate, `summary` ≤ remainder; total ≤ 4,096 B; char-boundary truncation; per-field `query_truncated` counts; the archived QueryPayload records original + rendered forms when truncation fires (accounting runs over the rendered expressions — what the selector saw). (v) Fat-`budget_exceeded` terminal's complete contract + two-row matrix + named F1 interaction (codex L3 ≡ opus5 L2).
+- **D8 — minimal wire: numbered expressions → indices; `evidence` dropped; allowances derived from the bounded compact schema (amends §2.2/§2.3; blueprint §2.3/§7/§12).** (i) The request's expressions are rendered **numbered** in the query; the fat selector returns **indices** (`matched: [int]`) and Python maps them back to the rendered expressions — out-of-range index = the unknown-expression coercion, R1's salvage posture intact (codex M1 option 2: no repeated expression strings on the wire). (ii) **`evidence` is dropped from the fat response schema** (Joseph's call — "who's going to use it? nobody"): nothing downstream consumed it (validation, the context build, and KPIs all read structured outcomes), it was the only free-prose field on the wire — unbounded, unverifiable, off-principle (*take the content, not the semantics*) — and ~60% of the worst-case response. (The fat-stage **evidence pool** — excerpts — is untouched: same word, different object.) (iii) **Visible-response allowances are the exact maximum serialization of the fully-bounded wire, covered by `tokens_lte_bytes` — no density step anywhere:** `VISIBLE_OUTPUT_ALLOWANCE_THIN` = **13,000** (exact max **12,315 B**) and `VISIBLE_OUTPUT_ALLOWANCE_FAT` = **10,000** (exact max **8,404 B**), each asserted against a **synthetic schema-maximum document** (not fixture-average — codex M1). The slug wire bound is **`MAX_SLUG_LEN` (120 B) imported from `common.paths`**; the render-time assertion fires only on non-canonical input. All wire maxima derive from **`MAX_EXPRESSIONS`** (D9), never from pass-1's schema. (iv) The M=100 static guarantee is recomputed on the **provider-total** envelope by D9 (257 kB + 10k visible + 16k hidden reserve = 283k < 320k); the `tokens ≤ UTF-8 bytes` premise is recorded as a **named supported-route invariant** (`tokens_lte_bytes`, asserted at route resolution — codex — not an unqualified tokenizer-independent law). **Mechanics completed (v0.10, codex N1–N4 ≡ opus5 N1–N3):** `Hit.evidence` removed from the public result contract (the field D8 dropped from the wire); the wire states **every** numeric maximum (`max_results` ≤ 50 globally; `retained` ≤ 100 × ≤ 120 B; `matched` ≤ 10 unique indices ∈ [0, len(expressions)); `unresolved` ≤ 10 indices — indexed, keeping the `selector_accounting_delta` signal bounded); the slug bound is **`MAX_SLUG_LEN` (120 B), imported from `common.paths`** — not a re-declared 96 B that contradicted the canonical data model (a 97–120 B slug is valid graph data); the render-time assertion now fires only on non-canonical input (a real defect); allowances derive from the **exact max serialization under the invariant** — **THIN 13,000 / FAT 10,000** (12.3k / 9.6k exact max bytes + buffer; `tokens_lte_bytes` ⇒ tokens ≤ bytes — **no density step anywhere**, codex N2 option 1; the static guarantee: 257 kB + 10k visible + 16k hidden = 283k < 320k); `tokens_lte_bytes` lives as a **validated boolean in `models.json` carried by `ModelSpec`** — a declared per-route premise with its theorem (byte-level BPE ⇒ content tokens ≤ content bytes), missing/false ⇒ typed config error at selector route resolution before any work, all three D4 candidates declared + test-covered (codex N4 + opus5 N3).
+
+- **D9 — provider-envelope output accounting + one declared expression bound (DRAFTED by opus5, pending Joseph's confirmation; absorbs codex O1/O3 ≡ opus5 O1; amends D7/D8 and §7.2 R2).** Two mechanics and one retraction.
+  1. **Visible ≠ provider-total output.** D8's proof bounds *visible JSON* (`visible tokens ≤ visible bytes ≤ allowance`), but `ModelRequest.max_tokens` reaches provider fields that cap the **whole completion, hidden reasoning included** — verified: `gpt-5.4-mini` carries `extra_body: {"reasoning_effort": "low"}` with `use_completion_tokens: true`, so it receives `max_completion_tokens` (`common/call_model.py:283`); the gemini path sends `ThinkingConfig(thinking_level="minimal")` — *"flash-lite floor is `minimal` (full-off unsupported)"* (`call_model.py:221-228`) — and `thoughts_token_count` is **added to output tokens** (`:248`); and `_THINKING_DISABLE_EXTRA_BODY` (`common/model_pool.py:32-38`) has **no openai or gemini mapping**, so the semantic `thinking: "disabled"` injects nothing for two of the three D4 candidates. Hidden output can therefore consume the allowance before the schema-maximum JSON is emitted, re-opening the truncation ⇒ `unparseable` ⇒ retry ⇒ identical-overflow chain the allowance work exists to prevent. **Resolution — codex's option 2 (split the envelope), because option 1 is unavailable:** requiring verified no-hidden-output routes would eject `gemini-3.6-flash`, the D4 interim default, since its family has no full-off — that is an owner decision on D4, not a mechanics fix. So: `VISIBLE_OUTPUT_ALLOWANCE_THIN/FAT` = 13,000/10,000 (D8(iii), unchanged); **`HIDDEN_OUTPUT_RESERVE` = 16,000**, route-owned; **`max_tokens` carries their sum** (thin 29,000 / fat 26,000 — each verified under every D4 route's `max_output_tokens`: gemini 65,536 is the binding one), and **the sum is what the R2 pre-flight reserves**. Neither provider exposes an enforceable hidden-token maximum, so the residual is typed honestly rather than claimed away: a length-finish-reason truncation is **`status: budget_exceeded`, `detected: post_call`, `budget_side: output`** — never retried (deterministic), watched per model, and **excluded from the §8.4 selector-failure gate** on the same grounds as its input-side sibling (our sizing, not the model's judgment — opus5 M1). It is additionally a **selector-admission signal**: a route that trips it is not a viable selector, which is a D7 finding, not a production gate. The reserve is sized by what the static guarantee can absorb — not by a prediction of reasoning length — and exceeds any plausible `low`/`minimal` trace for a selection task by orders of magnitude.
+  2. **`MAX_EXPRESSIONS` = 10, declared in the core `QueryPayload` and validated for every consumer before rendering.** D8's wire maxima (`matched` ≤ 10 unique indices, advisory `unresolved` ≤ 10, single-digit index width) and the FAT allowance arithmetic were all derived from `entity_search_keys`' `maxItems: 10` (`ingestion/enrich/pass1_schema.py:88`) — a **pass-1.5 adapter constraint** standing in for a consumer-neutral invariant, which R2 rules against directly. Measured consequence: the FAT wire's exact maximum is 8,404 B at 10 expressions but **10,414 B at 20 and 14,914 B at 50**, so the 10,000 allowance was a proof only under an unstated premise, and the P5b CLI/MCP surface would have re-opened the truncation chain. `MAX_EXPRESSIONS` is now the single derivation source for both index caps, the index digit width, and the allowance byte math; a caller exceeding it gets a typed outcome with telemetry, never silent truncation.
+  3. **§8.6 (selector task variants) is removed.** Its rationale was opus5's — *"the cohort runs thinking-disabled, so the per-hit prose field was the only in-band reasoning channel"* — and that premise is **false**: GPT reasons at `low` effort and Gemini at `minimal`, both billed as output tokens. The models already have an in-band reasoning channel; the dropped field was redundant with it, not the only one. With the premise gone the section has no argument, and codex O2's accounting is correct that it was never a free axis (a second response schema, its own numeric maxima and allowance, a research prompt and validator, and a predeclared run matrix). **D8's drop stands on its own reasoning.** (opus5 retracts N2.)
 
 ---
 
@@ -33,7 +38,10 @@ GraphSearchRequest
 
 QueryPayload
   text: str                        # the query as the selector sees it
-  expressions: [str]               # the discrete expressions hits are attributed to
+  expressions: [str]               # the discrete expressions hits are attributed to;
+                                   # len(expressions) <= MAX_EXPRESSIONS (10 — D9), validated
+                                   # before rendering for EVERY consumer; the sole derivation
+                                   # source for the wire's index caps and the FAT allowance
                                    # (pass-1.5: entity_search_keys; human: free-form, single expression)
 
 SearchSpaceRef
@@ -124,7 +132,7 @@ Rank the entities **relevant to the query**, best first, up to `max_results`; at
 }
 ```
 
-(`matched` holds **indices into the request's numbered expressions** — no repeated expression strings on the wire, no per-hit `evidence` field — D8. **Every numeric maximum is stated (D8 mechanics, codex N2):** `selections` ≤ `max_results` (≤ 50 globally); `slug` ≤ `MAX_SLUG_LEN` (120 B — `common.paths`); `matched` ≤ 10 unique indices, each ∈ [0, len(expressions)); `unresolved` ≤ 10 indices — the advisory list indexed, keeping it bounded and the `selector_accounting_delta` signal intact.)
+(`matched` holds **indices into the request's numbered expressions** — no repeated expression strings on the wire, no per-hit `evidence` field — D8. **Every numeric maximum is stated (D8 mechanics, codex N2):** `selections` ≤ `max_results` (≤ 50 globally); `slug` ≤ `MAX_SLUG_LEN` (120 B — `common.paths`); `matched` ≤ `MAX_EXPRESSIONS` unique indices, each ∈ [0, len(expressions)); `unresolved` ≤ `MAX_EXPRESSIONS` indices — the advisory list indexed, keeping it bounded. **Every maximum derives from `MAX_EXPRESSIONS` (D9), not from pass-1's schema**; the exact serialized maxima and the compact-JSON grammar that produces them are recorded in blueprint §7's constants table, with the synthetic schema-maximum tests as executable authority.)
 
 **Governing principle (Joseph's first-principle ruling): take the content, not the semantics — a parseable response is never discarded.** Python validates each entry independently against the closed world, drops only what is invalid, coerces what has a unique deterministic reading, and counts every deviation by class. Joseph's example is the rule: 10 returned slugs — 1 duplicate, 3 malformed → **the 6 good slugs are kept**. This supersedes v0.2's whole-response fail-closed (codex's F6 position; his dissent and the compensating controls are recorded in §11).
 
@@ -145,7 +153,7 @@ Separately, **before any call**, the §7.2 R2 pre-flight budget check can fail t
 - **duplicate slug** — keep first occurrence (the selector's own returned order is the authority), drop later ones; **duplicate `matched` index within an entry** — dedupe (D8 mechanics);
 - **over-cap** (`len(selections) > max_results`) — truncate to `max_results` in returned order.
 
-**Controller-computed expression accounting:** after validation, every request expression is *matched* (≥1 validated hit attributes it) or *unresolved*. The selector's advisory `unresolved` index list is input to that computation; discrepancies are counted (`selector_accounting_delta`), never a failure class. Two artifact annotations keep artifacts out of the abstention metric (§8.3 metric 6): cap-induced unresolveds are annotated `cap_exhausted_possible` when `len(hits) == max_results` (opus5 minor 2); and hits left with zero matched expressions after unknown-expression coercion are counted (`unattributed_hit_count`), with expressions whose only validated hits are unattributed annotated `unattributed_possible` and likewise excluded from abstention scoring (opus5 §2.4 — a genuinely relevant entity can be present in `hits` while its expression reports unresolved; that is an attribution artifact, not an abstention judgment).
+**Controller-computed expression accounting:** after validation, every request expression is *matched* (≥1 validated hit attributes it) or *unresolved*. The selector's advisory `unresolved` index list is input to that computation; discrepancies are counted (`selector_accounting_delta`), never a failure class. **Recorded consequence of D8's indexing (opus5 O3):** an *invented* expression is now unrepresentable — under the old string form a selector could return an unresolved value absent from the request, which the delta surfaced as a conformance signal; indexed, an out-of-range value is coerce-dropped and the delta narrows to a count mismatch. The trade is accepted (a bounded wire is worth more, and out-of-range indices are themselves the signal), but the series' meaning is narrower than pre-D8 and should be read that way. Two artifact annotations keep artifacts out of the abstention metric (§8.3 metric 6): cap-induced unresolveds are annotated `cap_exhausted_possible` when `len(hits) == max_results` (opus5 minor 2); and hits left with zero matched expressions after unknown-expression coercion are counted (`unattributed_hit_count`), with expressions whose only validated hits are unattributed annotated `unattributed_possible` and likewise excluded from abstention scoring (opus5 §2.4 — a genuinely relevant entity can be present in `hits` while its expression reports unresolved; that is an attribution artifact, not an abstention judgment).
 
 **Stage-1 (thin) validation:** the identical per-entry rule — foreign/malformed dropped, duplicates deduped, over-M truncated — applied to the retained-slug list. One rule, both stages.
 
@@ -171,7 +179,7 @@ Empty space (domain-empty or `domain_missing`) → `status: abstain_empty_space`
 
 ### 3.4 Retry, then honest empty — **no deterministic substitution (R1 — Joseph, 2026-07-25)**
 
-On **selector failure** — transport error, timeout, or any §2.3 response-level class (`unparseable_response`, `structurally_unusable_response`, `all_entries_dropped`; model-correctable/transient classes per D-119's "retries only for model-correctable failure classes"): **retry the LLM call**, bounded budget (2 attempts total, the #104/#106 precedent). `budget_exceeded` (§7.2 R2) is **not** a retry class — it is deterministic, fails before any API call, and is reported, not retried. **`budget_estimation_miss` (D7, re-typed per opus5 M1) is likewise never retried:** the provider's context-length rejection means the estimate under-called a rendered request; the same request fails identically. It is the same event as `budget_exceeded`, detected one step later — so it is typed **`status: budget_exceeded`, `detected: post_call`** (the preflight's is `detected: pre_call`), the call is recorded as attempted (spend possible), and its rate is watched per model. It is **excluded from the §8.4 selector-failure-rate gate** — an estimator under-call is *our* mechanics, not the model's behavior, and typing it `selector_failure` would contaminate the one hard gate that answers whether #123 works and penalize smaller-window models systematically (the `budget_exceeded`-is-not-`selector_failure` precedent; the `cap_exhausted_possible`/`unattributed_possible` exclusion principle).
+On **selector failure** — transport error, timeout, or any §2.3 response-level class (`unparseable_response`, `structurally_unusable_response`, `all_entries_dropped`; model-correctable/transient classes per D-119's "retries only for model-correctable failure classes"): **retry the LLM call**, bounded budget (2 attempts total, the #104/#106 precedent). `budget_exceeded` (§7.2 R2) is **not** a retry class — it is deterministic, fails before any API call, and is reported, not retried. **`budget_estimation_miss` (D7, re-typed per opus5 M1) is likewise never retried:** the provider's context-length rejection means the estimate under-called a rendered request; the same request fails identically. It is the same event as `budget_exceeded`, detected one step later — so it is typed **`status: budget_exceeded`, `detected: post_call`, `budget_side: input`** (the preflight's is `detected: pre_call`). **Its output-side sibling (D9)** — a length-finish-reason truncation, i.e. hidden reasoning plus visible JSON exceeding the reserved provider-total output — is typed identically with **`budget_side: output`**, is likewise never retried, and is likewise excluded from the §8.4 gate, the call is recorded as attempted (spend possible), and its rate is watched per model. It is **excluded from the §8.4 selector-failure-rate gate** — an estimator under-call is *our* mechanics, not the model's behavior, and typing it `selector_failure` would contaminate the one hard gate that answers whether #123 works and penalize smaller-window models systematically (the `budget_exceeded`-is-not-`selector_failure` precedent; the `cap_exhausted_possible`/`unattributed_possible` exclusion principle).
 
 After the retry budget is exhausted: **honest empty T2 + `selector_failure` telemetry with the failure class** — and nothing else. **There is no deterministic exact/alias fallback.** Rationale (Joseph's ruling): the deterministic resolver cannot be the catch-all — it resolves only string-identical keys, and `warren-buffett` string-resolves to *nothing* (#122 finding 4); if the deterministic routine were that good, the LLM search would not be needed. This **supersedes opus5's A1 degraded-mode fallback** (vision P1 v1.2–v1.3) and moots codex's F6 (fallback search-space question — there is no fallback).
 
@@ -266,7 +274,7 @@ Mid-run, the space is a function of intra-run compile order (source N reads bodi
 
 ### 6.3 SearchTelemetry (controller-produced)
 
-Counts — eligible-space size, stage-1 retained, stage-2 hydrated, stage-2 title-only, returned entries, **valid_entry_yield** (`None` when returned = 0, §2.3), **per-class attempted violations** (§2.3), `all_entries_dropped` occurrences, `unattributed_hit_count`, `query_truncated` occurrences (D6), `search_artifact_write_failures` (warn-only envelope sink — codex minor), retry attempts, `selector_failure` class — `status`, `execution`, per-stage model route + stamps, per-stage latency + cost, **pre-flight budget record per stage (`budget_estimate_tokens`, `selector_window`, `headroom_factor`, routing outcome, `detected: pre_call|post_call` — §7.2 R2 as amended by D6; D7/opus5 M1)**, **thin/fat concordance (fat top-10 ∩ thin top-20 — §8.3)**, search_snapshot_hash, artifact ref. Threads into the #122 record (§5.1) and the run's KPI emission; **selector-quality fields (yield, per-class violation rates, retry rate, failure rate) are watched KPIs per model — Joseph's leaderboard counter (R1 §3.2), promotion to scored is a later, separate decision.**
+Counts — eligible-space size, stage-1 retained, stage-2 hydrated, stage-2 title-only, returned entries, **valid_entry_yield** (`None` when returned = 0, §2.3), **per-class attempted violations** (§2.3), `all_entries_dropped` occurrences, `unattributed_hit_count`, `query_truncated` occurrences (D6), `search_artifact_write_failures` (warn-only envelope sink — codex minor), retry attempts, `selector_failure` class — `status`, `execution`, per-stage model route + stamps, per-stage latency + cost, **pre-flight budget record per stage (`budget_estimate_tokens`, `selector_window`, `headroom_factor`, routing outcome, `detected: pre_call|post_call`, `budget_side: input|output`, `visible_output_allowance`, `hidden_output_reserve`, provider `finish_reason` when post-call — §7.2 R2 as amended by D6/D9)**, **thin/fat concordance (fat top-10 ∩ thin top-20 — §8.3)**, search_snapshot_hash, artifact ref. Threads into the #122 record (§5.1) and the run's KPI emission; **selector-quality fields (yield, per-class violation rates, retry rate, failure rate) are watched KPIs per model — Joseph's leaderboard counter (R1 §3.2), promotion to scored is a later, separate decision.**
 
 ## 7. Vault-scale sizing (pre-implementation gate — corrected per codex F1, dual projection per F3)
 
@@ -281,7 +289,7 @@ Measured from the SD-6 snapshot (`benchmark/runs/gemini-3.6-flash-2026-07-25T09-
 | | entities | largest domain subtree | thin, whole graph | fat, stage-2 block (≤M=100 rendered — D7) |
 |---|---|---|---|---|
 | today (29 sources compiled) | 163 | 51 | ~3.6k tokens (14,343 B measured) | ~9.6k tokens largest domain (38,512 B measured; production renders ≤ 100) |
-| vault (~1,706 notes) | **~9,600** (5.6/src) | **~3,000** (31% share) | **~211k tokens** (~845 kB measured projection) | **~18k tokens expected** (100 × 719 B); **statically guaranteed to fit the pool** — ≤ ~257k tokens absolute worst case + 4k output < 320k smallest budget (M=100, D7) |
+| vault (~1,706 notes) | **~9,600** (5.6/src) | **~3,000** (31% share) | **~211k tokens** (~845 kB measured projection) | **~18k tokens expected** (100 × 719 B); **statically guaranteed to fit the pool** — ≤ ~257k tokens absolute worst case + 26k provider-total output < 320k smallest budget (M=100, D7) |
 
 **Blueprint duty (codex F1's required recompute, items 4–5) — discharged (v0.6):** serialized stage-1/stage-2 counts are fixture-measured (§7.1) and the estimator is calibrated against provider-reported usage per D5 (blueprint §7); the R2 guardrail routes on the measured serialized estimate (runtime-authoritative), not `space_size × excerpt_tokens` alone. (The "two-stage switch" phrasing is stale — R4 dissolved the switch; every executed search runs thin.)
 
@@ -300,7 +308,7 @@ Measured from the SD-6 snapshot (`benchmark/runs/gemini-3.6-flash-2026-07-25T09-
 
   **Corrected rationale (opus5 §C):** options 1 and 2 **both** cap recall; the operative distinction is **which cap is measured** — option 2's is measurable with the harness we are already building. **Predeclared measurement:** stage-1 recall at the §8.3 reduced-M points on the fixed truth set, run **before** vault ingestion; gate failure ⇒ options 1 or 3 revisited.
   **Vision impact:** P1 amended (v1.3, then v1.4 R4) — two-stage thin→fat is the uniform production path at every scale; no single-stage path, no activation threshold.
-- **R2 (oversized spaces) — RESOLVED (Joseph, 2026-07-25): one uniform pre-flight budget rule, no per-consumer distinction.** The contract treats every caller identically (Joseph's [2]: nothing special for pass-1.5, nothing special for human/CLI/MCP — the space is whatever the caller passed). Before **each selector call — thin and fat alike (D6: one calculation method for both stages)** — the controller estimates the serialized input tokens of the **fully rendered request** (evidence + system template + user wrapper + query + the stage's reserved output) and compares against **80% of the configured selector model's context window** (Joseph's headroom factor — covers provider margin and density variance). **The estimator is calibrated, not a proved bound (codex confirmation K1):** the exact per-model tokenizer where available; otherwise UTF-8 bytes ÷ 4, calibrated against provider-reported usage (D5) — the 0.8 headroom is the accepted margin for density variance (opus5 J3, explicit), and the "never underestimates" claim stays withdrawn. `words × 1.3` is **not** conservative for slug-heavy text (2.2k vs 3.8k measured on the real 163-entity block — a ~1.7× underestimate), and an underestimated guardrail can authorize a request it was meant to block. Over budget → **fail the search without invoking the API** — `budget_exceeded`, zero spend, typed telemetry, no retry (a deterministic condition; retrying changes nothing). Period — no silent degradation, no partial search, no consumer-specific escape hatch. (Stage 2 needs no estimate at the configured pool's windows — **D7**: with M=100 the fat request is **statically guaranteed** — evidence ≤ 100 × 2,500 B = 250 kB + query ≤ 4,096 B + system/template ≈ ≤ ~257 kB bytes ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + 10k reserved output (D8 mechanics) **< 320k, the pool's smallest budget** — by construction, for any window ≥ ~330k; no estimation in fat's safety path. **Thin remains the estimate-guarded stage** — its input is the whole space, unbounded in N by design (the space is searched whole): the calibrated preflight is the operative guard and catches what it catches; a density-driven underestimate surfaces as a typed **`budget_estimation_miss`** (provider context-length rejection caught; call attempted, spend possible; never retried — deterministic; watched per model — codex L1 option 2). "Never a provider error" is withdrawn for thin and unnecessary for fat at pool windows. The v0.4 premises (150 × ~366 ≈ 55k < 64k) and the v0.6/v0.7 128k-window token claims are superseded — M=100 settles the fat side by sizing.)
+- **R2 (oversized spaces) — RESOLVED (Joseph, 2026-07-25): one uniform pre-flight budget rule, no per-consumer distinction.** The contract treats every caller identically (Joseph's [2]: nothing special for pass-1.5, nothing special for human/CLI/MCP — the space is whatever the caller passed). Before **each selector call — thin and fat alike (D6: one calculation method for both stages)** — the controller estimates the serialized input tokens of the **fully rendered request** (evidence + system template + user wrapper + query + the stage's reserved output) and compares against **80% of the configured selector model's context window** (Joseph's headroom factor — covers provider margin and density variance). **The estimator is calibrated, not a proved bound (codex confirmation K1):** the exact per-model tokenizer where available; otherwise UTF-8 bytes ÷ 4, calibrated against provider-reported usage (D5) — the 0.8 headroom is the accepted margin for density variance (opus5 J3, explicit), and the "never underestimates" claim stays withdrawn. `words × 1.3` is **not** conservative for slug-heavy text (2.2k vs 3.8k measured on the real 163-entity block — a ~1.7× underestimate), and an underestimated guardrail can authorize a request it was meant to block. Over budget → **fail the search without invoking the API** — `budget_exceeded`, zero spend, typed telemetry, no retry (a deterministic condition; retrying changes nothing). Period — no silent degradation, no partial search, no consumer-specific escape hatch. (Stage 2 needs no estimate at the configured pool's windows — **D7**: with M=100 the fat request is **statically guaranteed** — evidence ≤ 100 × 2,500 B = 250 kB + query ≤ 4,096 B + system/template ≈ ≤ ~257 kB bytes ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + 10k visible + 16k hidden reserve = 26k provider-total reserved output (D9) **< 320k, the pool's smallest budget** — by construction, for any window ≥ ~355k; no estimation in fat's safety path. **Thin remains the estimate-guarded stage** — its input is the whole space, unbounded in N by design (the space is searched whole): the calibrated preflight is the operative guard and catches what it catches; a density-driven underestimate surfaces as a typed **`budget_estimation_miss`** (provider context-length rejection caught; call attempted, spend possible; never retried — deterministic; watched per model — codex L1 option 2). "Never a provider error" is withdrawn for thin and unnecessary for fat at pool windows. The v0.4 premises (150 × ~366 ≈ 55k < 64k) and the v0.6/v0.7 128k-window token claims are superseded — M=100 settles the fat side by sizing.)
 
   What fits is then an **empirical fact about the configured model, not a rule about consumers**: at vault scale, pass-1.5's largest domain space thin-projects to ~66k tokens and whole-graph human queries to ~211k (§7.1, measured projections) — both pass against a 1M-window selector (800k budget); against a 128k-window model the whole-graph query fails closed and *visibly*, which is exactly what the rule is for — the operator sees `budget_exceeded` and configures a bigger-window selector. (The earlier per-surface "window requirement for the whole-graph surface" framing is withdrawn: there is no whole-graph special case, only one budget rule.) Sharded thin selection stays recorded as contingency for a pool era with no long-window models; lexical candidate generation for oversized spaces stays rejected (an unmeasured recall cap cannot be load-bearing — codex's own v0.1 F4 premise).
 - **R4 (staging posture) — RESOLVED (Joseph, 2026-07-25): always two-stage — thin call then fat call, every source, every run; no single-stage path, no routing logic.** (As amended — **D3**: the thin-retained-zero N>M terminal **skips the fat call** (`thin_attempted`, honest empty); **opus5 F1**: the thin-failed/N≤M path proceeds to fat as `fat_after_thin_failure`. Branch-specific call counts — 1–2 thin + 0–2 fat per terminal path — are the executable contract, blueprint §8's table.) Rationale (Joseph's uniformity principle — the same one behind R2): the system never behaves conditionally when it can behave uniformly — no estimator-dependent path selection, no behavioral discontinuity as domains grow, and the stage-1 **code path** is exercised on 100% of production traffic from day one. (Stated precisely — opus5 §2.2: the *gate* becomes informative only once a domain exceeds M; below M, recall@M is 1.0 by controller-enforced retain-all construction, not by selector quality — §2.1's small-space enforcement, codex #3.) At today's scale the results equal a single fat call's **by construction** (largest domain 51 < M=100), for the price of one ~1k-token thin call per source. There is **no small-space skip** — a 200-token thin call is the price of one code path. Consequences: SD-5's routing threshold **dissolves** (no single/two-stage switch exists to tune — the estimator survives only as the R2 guardrail); `execution` records how far execution progressed (`thin_attempted | two_stage_attempted | fat_after_thin_failure`); the stage-1 prompt is recall-oriented (§2.1); the stage-1 recall hard gate **at the predeclared reduced-M points** guards the only path (predeclared per SD-4; **M=100 is the production constant (D7)** — codex K2's naming point: the gate lives on the §8.3 reduced-M curve, and at M=100 the 163-entity whole-graph fixture space is itself binding — opus5 §2.3/M2).
@@ -366,16 +374,12 @@ Plus: **selector-failure rate** (retry-exhausted) + retry rate; **thin/fat conco
 
 ### 8.4 Gates
 
-Numerical thresholds are set **after** Joseph's labeling exists (D7 — no grading against an undefined target). Gate *shapes* fixed now: **hard gates** — escaped foreign-identity rate = 0; semantic abstention accuracy ≥ threshold; person-class (A) recall@5 ≥ threshold (the motivating failure); **stage-1 recall at the reduced-M points of §8.3 metric 2 ≥ threshold (predeclared on that curve, run before vault ingestion — SD-4; M=100 is the production constant — D7)**; **selector-failure rate ≤ ceiling (the "is #123 delivering" measure — R1's compensating control; `budget_exceeded` in both `detected` forms — incl. `budget_estimation_miss` — is **excluded**: our mechanics, not model behavior — opus5 M1)**. **Watched** — precision@5, MRR, valid_entry_yield, per-class attempted violations, `all_entries_dropped` rate, unattributed-hit rate, thin/fat concordance, coverage, cost.
+Numerical thresholds are set **after** Joseph's labeling exists (D7 — no grading against an undefined target). Gate *shapes* fixed now: **hard gates** — escaped foreign-identity rate = 0; semantic abstention accuracy ≥ threshold; person-class (A) recall@5 ≥ threshold (the motivating failure); **stage-1 recall at the reduced-M points of §8.3 metric 2 ≥ threshold (predeclared on that curve, run before vault ingestion — SD-4; M=100 is the production constant — D7)**; **selector-failure rate ≤ ceiling (the "is #123 delivering" measure — R1's compensating control; `budget_exceeded` is **excluded in every form** — both `detected: pre_call|post_call` and both `budget_side: input|output` (D9's provider-total truncation included): our sizing, not model behavior — opus5 M1)**. **Watched** — precision@5, MRR, valid_entry_yield, per-class attempted violations, `all_entries_dropped` rate, unattributed-hit rate, thin/fat concordance, coverage, cost.
 
 ### 8.5 Cross-domain A/B cohort (opus5 C3, adopted)
 
 One cohort, selector run twice per source (domain-scoped space vs whole graph), delta recorded as telemetry. Answers whether the 2/486 cross-community-edge figure is corpus property or the gate's shadow. Read-only; no production change; runs after the v1 harness exists. **Retention-pressure confound (opus5 M2, D7-era):** the arms now differ in retention pressure as well as scope — the domain arm (51) is retain-all by controller enforcement while the whole-graph arm (163) must discard ~39% at M=100 (was ~8% at M=150); a whole-graph loss can read as a *scope* effect when it is *retention*. The A/B therefore reports the whole-graph arm's **stage-1 recall** alongside its result (variant: run the scope comparison at M ≥ 163 — non-binding on both arms — while M=100 stays the production-path measurement).
 
-### 8.6 Selector task variants (opus5 N2 — one axis, same runs)
-
-The selector A/B carries a **with-/without-`evidence` prompt axis** (D8's drop, measured rather than assumed): the cohort runs **thinking-disabled** across all three candidates (`models.json` + `model_pool.py`), so the per-hit prose field was the only in-band reasoning channel the selector had, and removing it may shift selection quality in either direction — we have zero evidence either way, and the change lands before any baseline. One variant axis on the already-parameterized runs; if the delta is nil, D8's drop is confirmed **on evidence**, not on the absence of a consumer — the same standard R3's truth-set-before-tuning gate applies to everything else in this design.
-
 ## 9. Test plan outline (TDD-first; expands in blueprint)
 
 - **Contract tests**: request/result shapes (incl. the D8 compact fat wire — `matched: [int]` indices, no `evidence`); result-state coverage (`status` × `execution` × `evidence_status` combinations, incl. empty-space `not_executed`/`not_applicable`/`None`, `budget_exceeded` in **both `detected` forms** — `pre_call` (thin/fat preflight) and `post_call` (`budget_estimation_miss`, excluded from the §8.4 gate), attempted-but-failed `thin_attempted`/`two_stage_attempted` paths, the D3 thin-retained-zero terminal, the `fat_after_thin_failure` path, and the fat-stage `budget_exceeded` terminal (`execution: thin_attempted` — D6)); the §2.3 four-way response classification (`unparseable` / `structurally_unusable` / `all_entries_dropped` retryable vs empty-`selections` honest-empty **not** a failure); `valid_entry_yield = None` on zero-returned.
@@ -383,7 +387,7 @@ The selector A/B carries a **with-/without-`evidence` prompt axis** (D8's drop,
 - **Per-entry salvage tests (R1 — the named case is Joseph's 6-of-10)**: 10 returned slugs with 1 duplicate + 3 malformed/foreign ⇒ the 6 good slugs survive in returned order, per-class counts recorded; out-of-range-index coercion (entry kept, stray attribution removed, unattributed hit stands — D8's wire form of unknown-expression); over-cap truncation keeps the selector's own top-`max_results`; **a parseable response is never wholesale-discarded**; `unparseable_response` is the only response-level failure.
 - **Zero-escape proof tests**: no matter the raw output (foreign slugs, fabrication attempts), the emitted hit set is always ⊆ search space.
 - **Retry-then-honest-empty tests (R1)**: transport error / timeout / `unparseable_response` / `structurally_unusable_response` / `all_entries_dropped` ⇒ retry once ⇒ success path records `attempt: 2`; exhausted budget ⇒ `status: selector_failure`, empty T2, typed failure class; **`budget_estimation_miss` (provider context-length rejection — D7) is typed `budget_exceeded`/`detected: post_call`, attempted-once, never retried, excluded from the §8.4 gate (opus5 M1)**; **no deterministic substitution ever fires**; the deterministic resolver is never invoked anywhere in the search path (no fallback, no annotation, no comparator); retried calls archive **one stage entry per attempt** (rendered messages + raw response per attempt — opus5 §2.6b).
-- **Pre-flight budget tests (R2/R4)**: thin estimate stubbed over 80% of a fake window ⇒ `budget_exceeded` with **zero API invocations** (call mock asserts never called); under ⇒ thin then fat in branch order (call counts per terminal path — blueprint §8); `budget_exceeded` is never retried **at either stage** (fat preflight ⇒ zero fat-call invocations — D6); identical routing regardless of caller (consumer-neutrality — no pass-1.5 vs human/CLI/MCP distinction); **no small-space skip** (a 5-entity space still runs both calls); estimator calibration (the ÷4 bound does not underestimate the measured fixture thin block — codex #2); **stage-2 static guarantee asserted** (100 × 2,500 B evidence + query ≤ 4,096 B + system/template ≈ ≤ ~257 kB bytes ⇒ ≤ ~257k tokens + 10,000 output < 320k — the pool's smallest budget — by construction, no estimation in fat's safety path — D7); **output allowances = exact max serialized bytes of the fully-bounded wire (THIN 13,000 / FAT 10,000), asserted against synthetic schema-maximum documents and proved sufficient via `tokens_lte_bytes` — no density step (codex N2)**; **`tokens_lte_bytes` resolution tests — missing/false ⇒ typed config error before any work; all three D4 candidates declared (codex N4)**; **slug bound = `MAX_SLUG_LEN` imported from `common.paths` — the render-time assertion fires only on non-canonical input (opus5 N1 ≡ codex N3)**; **`budget_estimation_miss` typed `budget_exceeded`/`detected: post_call`, attempted-once, never retried** (codex L1 option 2; excluded from the §8.4 gate — opus5 M1).
+- **Pre-flight budget tests (R2/R4)**: thin estimate stubbed over 80% of a fake window ⇒ `budget_exceeded` with **zero API invocations** (call mock asserts never called); under ⇒ thin then fat in branch order (call counts per terminal path — blueprint §8); `budget_exceeded` is never retried **at either stage** (fat preflight ⇒ zero fat-call invocations — D6); identical routing regardless of caller (consumer-neutrality — no pass-1.5 vs human/CLI/MCP distinction); **no small-space skip** (a 5-entity space still runs both calls); estimator calibration (the ÷4 bound does not underestimate the measured fixture thin block — codex #2); **stage-2 static guarantee asserted** (100 × 2,500 B evidence + query ≤ 4,096 B + system/template ≈ ≤ ~257 kB bytes ⇒ ≤ ~257k tokens + 10,000 output < 320k — the pool's smallest budget — by construction, no estimation in fat's safety path — D7); **visible allowances = exact max serialized bytes of the fully-bounded wire (THIN 13,000 vs 12,315 B / FAT 10,000 vs 8,404 B), asserted against synthetic schema-maximum documents and proved sufficient via `tokens_lte_bytes` — no density step (codex N2)**; **the four envelope quantities are tested separately (D9/codex O1): visible response tokens, hidden output tokens, the provider cap (`max_tokens` = visible + `HIDDEN_OUTPUT_RESERVE`, verified ≤ each route's `max_output_tokens`), and the context-budget reservation (the sum)**; **`budget_side: output` terminal on a length finish reason — typed, attempted-once, never retried, excluded from the §8.4 gate**; **`len(expressions) > MAX_EXPRESSIONS` ⇒ typed outcome before any rendering, for every consumer (D9)**; **`tokens_lte_bytes` resolution tests — missing/false ⇒ typed config error before any work; all three D4 candidates declared (codex N4)**; **slug bound = `MAX_SLUG_LEN` imported from `common.paths` — the render-time assertion fires only on non-canonical input (opus5 N1 ≡ codex N3)**; **`budget_estimation_miss` typed `budget_exceeded`/`detected: post_call`, attempted-once, never retried** (codex L1 option 2; excluded from the §8.4 gate — opus5 M1).
 - **Prompt-contract golden tests**: fixed space + fixed query ⇒ pinned rendered message bytes (archived verbatim); SELECTOR_PROMPT_VERSION / SELECTOR_THIN_PROMPT_VERSION + SHA guard (the #115 pattern).
 - **P10 adversarial fixtures** (class H) — must-pass.
 - **Stage-aware artifact tests**: two-stage execution records both stages (evidence, rendered messages, raw response text, failure class, per-class validation counts, retained identities, per-stage model/cost); `graph_ref` present; `search_snapshot_hash` + `artifact_integrity_hash` well-formed.
@@ -409,7 +413,7 @@ The selector A/B carries a **with-/without-`evidence` prompt axis** (D8's drop,
 | **SD-5** | two-stage threshold | **DISSOLVED by R4 as a routing decision** — no single/two-stage switch exists; survives as the R2 guardrail budget (80% of configured window) + entity count as tracked series (codex F3 preserved: measured tokens authoritative) |
 | **SD-6** | truth snapshot | **Choice RATIFIED; substrate GATED (R3):** fixture + smoke test → probe draft → Joseph's labels/gates → experiments (§8.1) |
 | **R1** | contract-violation posture | **RESOLVED (Joseph, 2026-07-25): per-entry salvage + retry, never wholesale-discard, no deterministic fallback** (§2.3/§3.4). Extends opus5's §1 (coerce 4/5) to all entry-level classes per Joseph's first principle ("take the content, not the semantics"; the 6-of-10 rule). **Supersedes** opus5's A1 fallback and v0.2's whole-response fail-closed; **codex's dissent recorded** (his v0.1 F6/v0.2 gate 6 held foreign slug, unknown expression, duplicate, over-cap, bad accounting as whole-response failures). Compensating controls: per-entry closed-world validation (D9 output invariant), per-class attempted-violation telemetry + valid_entry_yield → leaderboard, selector-failure-rate hard gate, escaped-foreign-identity = 0 hard gate |
-| **R2** | oversized spaces / context-window budget | **RESOLVED (Joseph, 2026-07-25): one uniform pre-flight budget rule** — estimate thin input tokens before the stage-1 call; >80% of the configured selector model's window ⇒ fail `budget_exceeded` **without invoking the API** (zero spend, no retry); identical for all consumers (Joseph's [2]: no pass-1.5 vs human/CLI/MCP distinction — the per-surface window-requirement framing is withdrawn). Sharded thin = recorded contingency; lexical candidate-gen rejected (§7.2). **Amended by D6 (2026-07-26): the same estimate-and-guard now covers the fat call — one calculation method for both stages, over the fully rendered request (system + template + query-capped-4,096 B + evidence + reserved output); the estimator is calibrated-with-headroom, not a proved bound; `OUTPUT_ALLOWANCE_FAT` declared.** **Amended by D7 (2026-07-26): M=100** — the fat request's absolute worst case (≤ ~257 kB bytes ⇒ ≤ ~257k tokens + 10k output — D8 mechanics) fits the pool's smallest budget (320k) **by construction**, no estimation in fat's safety path at pool windows; thin stays estimate-guarded with the honest `budget_estimation_miss` residual (codex L1 option 2) |
+| **R2** | oversized spaces / context-window budget | **RESOLVED (Joseph, 2026-07-25): one uniform pre-flight budget rule** — estimate thin input tokens before the stage-1 call; >80% of the configured selector model's window ⇒ fail `budget_exceeded` **without invoking the API** (zero spend, no retry); identical for all consumers (Joseph's [2]: no pass-1.5 vs human/CLI/MCP distinction — the per-surface window-requirement framing is withdrawn). Sharded thin = recorded contingency; lexical candidate-gen rejected (§7.2). **Amended by D6 (2026-07-26): the same estimate-and-guard now covers the fat call — one calculation method for both stages, over the fully rendered request (system + template + query-capped-4,096 B + evidence + reserved output); the estimator is calibrated-with-headroom, not a proved bound; `OUTPUT_ALLOWANCE_FAT` declared.** **Amended by D7 (2026-07-26): M=100** — the fat request's absolute worst case (≤ ~257 kB bytes ⇒ ≤ ~257k tokens + 10k output — D8 mechanics) fits the pool's smallest budget (320k) **by construction**, no estimation in fat's safety path at pool windows; thin stays estimate-guarded with the honest `budget_estimation_miss` residual (codex L1 option 2). **Amended by D9 (2026-07-26, drafted): the reserved output is the provider-total envelope** — `VISIBLE_OUTPUT_ALLOWANCE_THIN/FAT` 13,000/10,000 + `HIDDEN_OUTPUT_RESERVE` 16,000, their sum both sent as `max_tokens` and reserved in the pre-flight; the unbounded-hidden residual is typed `budget_exceeded`/`detected: post_call`/`budget_side: output`, gate-excluded; `MAX_EXPRESSIONS` = 10 declared in the core contract as the sole derivation source for the wire's maxima |
 | **R3** | ratification semantics | **RESOLVED (Joseph, 2026-07-25): codex's narrower gate adopted** — "spec design approved; evaluation substrate open"; D7 sequence recorded (§8.1). **Amended by D1/D5 (2026-07-26):** implementation (P1→P4-harness, canned/mocked, zero live selector spend) proceeds at blueprint ratification — it does not wait for labels; the sole pre-D7 live exception is the 3-call D5 calibration; experiments/tuning/vault ingestion still wait for labels + gates |
 | **R4** | staging posture | **RESOLVED (Joseph, 2026-07-25): always two-stage** — thin call then fat call, every source, every run (**as amended by D3**: thin-retained-zero N>M skips fat; the thin-failed/N≤M path runs fat as `fat_after_thin_failure`); no single-stage path, no routing logic, no small-space skip. Uniformity (Joseph's principle, same as R2): no estimator-driven behavior, no behavioral discontinuity at scale, stage-1 gate measured on 100% of traffic; result-identical to single-fat at today's scale (51 < M=100 ⇒ retention non-binding); stage-1 prompt recall-oriented; SD-5's routing threshold dissolved (§7.2); stage-1 recall gate at the predeclared reduced-M points guards the only path (**M=100 production constant — D7**) |
 
@@ -421,9 +425,10 @@ The selector A/B carries a **with-/without-`evidence` prompt axis** (D8's drop,
 
 ## Changelog
 
-- **v0.10 (2026-07-26)** — **D8 mechanics completed (codex N1–N4 ≡ opus5 N1–N3; all checkable claims repo-verified: `Hit.evidence` at §1.1, `MAX_SLUG_LEN=120` at `common/paths.py:27`, thinking-disabled cohort, no registry `tokens_lte_bytes` field).** (i) `Hit.evidence` removed from the public result contract — the field D8 dropped from the wire had survived in the public type (codex N1). (ii) **The wire now states every numeric maximum** (codex N2 option 1): `max_results` ≤ 50 globally; `retained` ≤ 100 × ≤ 120 B; `matched` ≤ 10 unique indices ∈ [0, len(expressions)); advisory `unresolved` **indexed** (≤ 10 — bounded, the `selector_accounting_delta` signal intact); duplicate-`matched`-index dedupe added to validation. (iii) **Allowances derive from the exact max serialization under `tokens_lte_bytes` — THIN 13,000 / FAT 10,000** (12.3k / 9.6k exact max bytes + buffer; tokens ≤ bytes by the invariant — **no 3 B/token density step anywhere**); the static guarantee: 257 kB + 10k = 267k < 320k. (iv) **Slug bound = `MAX_SLUG_LEN` (120 B), imported from `common.paths`** — D8's re-declared 96 B contradicted the canonical data model (97–120 B slugs are valid graph data, `summary_slug.py` budgets against 120); the render-time assertion now fires only on non-canonical input — a real defect (opus5 N1 ≡ codex N3). (v) **`tokens_lte_bytes` ownership:** a validated boolean in `models.json` carried by `ModelSpec` — a declared per-route premise with its theorem (byte-level BPE ⇒ content tokens ≤ content bytes; all pool providers byte-level BPE), missing/false ⇒ typed config error at selector route resolution before any work, all three D4 candidates declared + test-covered (codex N4 + opus5 N3). (vi) **§8.6 selector task variants (opus5 N2):** the with-/without-`evidence` prompt axis rides the selector A/B — the cohort runs thinking-disabled, so the dropped field was the only in-band reasoning channel; the drop gets confirmed on evidence, not on the absence of a consumer. Basis for blueprint v0.8.
+- **v0.11 (2026-07-26)** — **v0.8 confirmation round absorbed (codex REVISE O1–O3; opus5 CONCUR O1–O3); every claim repo-verified first.** New **D9 (drafted by opus5, pending Joseph's confirmation)**: (i) **provider-envelope output accounting** — codex O1 is correct that D8's proof bounds only *visible* JSON while `max_tokens` caps the whole completion (`gpt-5.4-mini`: `reasoning_effort: low` + `use_completion_tokens` ⇒ `max_completion_tokens`, `call_model.py:283`; gemini: `ThinkingConfig(thinking_level="minimal")`, full-off unsupported, `call_model.py:221-228`, with `thoughts_token_count` added to output at `:248`; `_THINKING_DISABLE_EXTRA_BODY` has no openai/gemini mapping, `model_pool.py:32-38`). Resolved by his **option 2** — visible 13,000/10,000 + `HIDDEN_OUTPUT_RESERVE` 16,000, sum sent as `max_tokens` (thin 29,000 / fat 26,000, both under every route's `max_output_tokens`) and reserved in the pre-flight; residual typed `budget_side: output`, gate-excluded, watched as a selector-admission signal. Option 1 (verified no-hidden-output routes) rejected: it would eject `gemini-3.6-flash`, the D4 interim default — an owner decision on D4, not a mechanics fix. Static guarantee recomputed on the provider-total envelope: **257 kB + 26k = 283k < 320k**, window threshold ~330k → **~355k**. (ii) **`MAX_EXPRESSIONS` = 10 declared in the core `QueryPayload`** and validated for every consumer before rendering (codex O3 ≡ opus5 O1): the wire's index caps, index width and FAT allowance had all been derived from pass-1's `maxItems: 10` — an adapter constraint standing in for a consumer-neutral invariant, and the FAT exact max is 10,414 B at 20 expressions vs the 10,000 allowance. (iii) **§8.6 removed** — its premise ('thinking-disabled cohort') is false for two of three candidates; opus5 retracts N2, and D8's drop stands on its own (codex O2's accounting that it was never a free axis is also correct). Consistency sweep (codex O3 ≡ opus5 O2): D7(iii)/D8(iii)–(iv) now state **only the final contract** — the superseded 96 B / 3 B-per-token / 4,000-4,000 and 2,000 derivations live here in the changelog, where history belongs; both `+ 4k output` instances and all three `≥ ~330k` window figures synced; exact serialized maxima (**12,315 B** thin, **8,404 B** fat) and the grammar that produces them named, with the synthetic schema-maximum tests as executable authority; `Basis` gains the v0.7 and v0.8 confirmation reviews. opus5 O3 recorded: D8's indexing narrows `selector_accounting_delta` — an invented expression is unrepresentable, so the series no longer signals hallucinated query terms. Basis for blueprint v0.9.
+- **v0.10 (2026-07-26)** — **D8 mechanics completed (codex N1–N4 ≡ opus5 N1–N3; all checkable claims repo-verified: `Hit.evidence` at §1.1, `MAX_SLUG_LEN=120` at `common/paths.py:27`, thinking-disabled cohort, no registry `tokens_lte_bytes` field).** (i) `Hit.evidence` removed from the public result contract — the field D8 dropped from the wire had survived in the public type (codex N1). (ii) **The wire now states every numeric maximum** (codex N2 option 1): `max_results` ≤ 50 globally; `retained` ≤ 100 × ≤ 120 B; `matched` ≤ 10 unique indices ∈ [0, len(expressions)); advisory `unresolved` **indexed** (≤ 10 — bounded, the `selector_accounting_delta` signal intact); duplicate-`matched`-index dedupe added to validation. (iii) **Allowances derive from the exact max serialization under `tokens_lte_bytes` — THIN 13,000 / FAT 10,000** (12.3k / 9.6k exact max bytes + buffer; tokens ≤ bytes by the invariant — **no 3 B/token density step anywhere**); the static guarantee: 257 kB + 10k visible + 16k hidden = 283k < 320k. (iv) **Slug bound = `MAX_SLUG_LEN` (120 B), imported from `common.paths`** — D8's re-declared 96 B contradicted the canonical data model (97–120 B slugs are valid graph data, `summary_slug.py` budgets against 120); the render-time assertion now fires only on non-canonical input — a real defect (opus5 N1 ≡ codex N3). (v) **`tokens_lte_bytes` ownership:** a validated boolean in `models.json` carried by `ModelSpec` — a declared per-route premise with its theorem (byte-level BPE ⇒ content tokens ≤ content bytes; all pool providers byte-level BPE), missing/false ⇒ typed config error at selector route resolution before any work, all three D4 candidates declared + test-covered (codex N4 + opus5 N3). (vi) **§8.6 selector task variants (opus5 N2):** the with-/without-`evidence` prompt axis rides the selector A/B — the cohort runs thinking-disabled, so the dropped field was the only in-band reasoning channel; the drop gets confirmed on evidence, not on the absence of a consumer. Basis for blueprint v0.8.
 - **v0.9 (2026-07-26)** — **D8 (Joseph): minimal wire — numbered expressions → indices, `evidence` dropped, allowances derived from the bounded compact schema** (codex M1 option 2 + Joseph's "who's going to use it? nobody"). (i) The fat selector's expressions are rendered numbered; hits carry `matched: [int]` indices (Python maps back; out-of-range index = the unknown-expression coercion, R1 intact) — no repeated expression strings on the wire. (ii) **`evidence` dropped from the fat response schema** — nothing downstream consumed it (validation, context build, KPIs all read structured outcomes); it was the only free-prose field, unbounded and off-principle; ~60% of the worst-case response. (The fat-stage evidence *pool* — excerpts — is untouched: same word, different object.) (iii) Allowances **THIN 4,000 / FAT 4,000** derived from the bounded compact schema (100 × ≤ 96 B slugs ≈ ≤ 3.3k tokens; 50 × ~170 B ≈ ≤ 2.8k at 3 B/token), asserted against synthetic schema-maximum documents (codex M1); slug wire bound ≤ 96 B with a render-time controller assertion. (iv) `budget_estimation_miss` re-typed **`budget_exceeded`/`detected: post_call`, excluded from the §8.4 selector-failure gate** (opus5 M1 — our mechanics, not the model's; would otherwise penalize smaller-window models). (v) §8.5 retention-pressure confound note + whole-graph arm's stage-1 recall reported alongside; reduced-M curve gains M≈5/163 (~3%) to bracket production's vault-scale regime (opus5 M2). (vi) `keys_emitted` carries original expressions, audit the rendered forms, indices flagged (opus5 M4). (vii) Stale-figure sweep (codex ≡ opus5 M3): R4 trailing clause, SD-4's last `recall@150`, blueprint §7's 381 kB → all synced to M=100/257 kB; §8 branch table names thin-preflight `budget_exceeded` + both `budget_estimation_miss` rows. (viii) `tokens ≤ UTF-8 bytes` recorded as the named `tokens_lte_bytes` supported-route invariant (codex). Basis for blueprint v0.7.
-- **v0.8 (2026-07-26)** — **D7 (Joseph): M=100 + the honest budget settlement.** (i) **M=100** — the fat request's absolute worst case (evidence ≤ 100 × 2,500 B = 250 kB + query ≤ 4,096 B + system/template ≈ ≤ ~257 kB bytes ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + 4k reserved output) **fits the configured pool's smallest budget (320k) by construction** — codex's K1/L1 counterexample dies by sizing, not measurement; no estimation in fat's safety path for any window ≥ ~330k. Zero behavioral change at current scale (largest domain 51 < 100; retain-all intact); **M=100 is the production constant**; the whole-graph fixture space (163 > 100) is now binding — recall@100 there is a real measurement, and the reduced-M gate protocol is unchanged. (ii) **Thin stays estimate-guarded** (its input is the whole space, unbounded in N by the ratified searched-whole principle); the residual density-underestimate is typed **`budget_estimation_miss`** (attempted-once, spend possible, never retried, watched per model — codex L1 option 2; "never a provider error" withdrawn for thin, unnecessary for fat at pool windows). (iii) **Output allowances sized from response schemas** — THIN 3,000 / FAT 4,000 (opus5 L1; D6's uniform 2,000 sat fat's response at 95% of its ceiling at 3 B/token — the truncation ⇒ unparseable ⇒ retry ⇒ identical-overflow ⇒ hard-gate chain recorded; codex's 2,000 closed-list superseded on this evidence). (iv) **Query ceiling per-field deterministic allocations** (codex L2 fix 1: author ≤ 256 B / keys ≤ 10 × 128 B / themes ≤ 1,024 B / summary ≤ remainder; per-field `query_truncated`; archived original+rendered forms). (v) Fat-`budget_exceeded` terminal's complete contract + two-row matrix + named F1 interaction (codex L3 ≡ opus5 L2). (vi) SD-5 "thin tokens" → per-stage (opus5 L3). Sizing/cost refreshed (fat ≤ 100 ≈ ~18k expected at vault; ~143M/$20/$107/$215 expected + 219M/$31/$164/$329 policy-ceiling re-ingest). Basis for blueprint v0.6.
+- **v0.8 (2026-07-26)** — **D7 (Joseph): M=100 + the honest budget settlement.** (i) **M=100** — the fat request's absolute worst case (evidence ≤ 100 × 2,500 B = 250 kB + query ≤ 4,096 B + system/template ≈ ≤ ~257 kB bytes ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + 10k visible + 16k hidden reserve = 26k provider-total reserved output) **fits the configured pool's smallest budget (320k) by construction** — codex's K1/L1 counterexample dies by sizing, not measurement; no estimation in fat's safety path for any window ≥ ~355k. Zero behavioral change at current scale (largest domain 51 < 100; retain-all intact); **M=100 is the production constant**; the whole-graph fixture space (163 > 100) is now binding — recall@100 there is a real measurement, and the reduced-M gate protocol is unchanged. (ii) **Thin stays estimate-guarded** (its input is the whole space, unbounded in N by the ratified searched-whole principle); the residual density-underestimate is typed **`budget_estimation_miss`** (attempted-once, spend possible, never retried, watched per model — codex L1 option 2; "never a provider error" withdrawn for thin, unnecessary for fat at pool windows). (iii) **Output allowances sized from response schemas** — THIN 3,000 / FAT 4,000 (opus5 L1; D6's uniform 2,000 sat fat's response at 95% of its ceiling at 3 B/token — the truncation ⇒ unparseable ⇒ retry ⇒ identical-overflow ⇒ hard-gate chain recorded; codex's 2,000 closed-list superseded on this evidence). (iv) **Query ceiling per-field deterministic allocations** (codex L2 fix 1: author ≤ 256 B / keys ≤ 10 × 128 B / themes ≤ 1,024 B / summary ≤ remainder; per-field `query_truncated`; archived original+rendered forms). (v) Fat-`budget_exceeded` terminal's complete contract + two-row matrix + named F1 interaction (codex L3 ≡ opus5 L2). (vi) SD-5 "thin tokens" → per-stage (opus5 L3). Sizing/cost refreshed (fat ≤ 100 ≈ ~18k expected at vault; ~143M/$20/$107/$215 expected + 219M/$31/$164/$329 policy-ceiling re-ingest). Basis for blueprint v0.6.
 - **v0.7 (2026-07-26)** — **D6 (Joseph): R2 amended — one budget calculation method for BOTH stages** (codex K1 option 2 ≡ opus5 J1): the fat call gets the same calibrated pre-flight estimate + 80%-of-window guard as thin (typed `budget_exceeded`, stage recorded, zero spend, never retried); full-request accounting (evidence ≤ 375 kB + query ≤ 4,096 B + system/template ≈ ≤ ~381 kB worst-case bytes; `OUTPUT_ALLOWANCE_FAT = 2,000` reserved); rendered-query byte ceiling 4,096 B with `query_truncated` telemetry (`summary`/`key_themes` schema-unbounded — verified `pass1_schema.py:77-88`); "conservative"/"provably fits ≥128k" relabeled calibrated-with-headroom (the headroom explicitly carries density variance — opus5 J3; D5 unchanged at 3 calls); codex option 1 (tokenizer preflight) rejected (no local tokenizer; gemini-only provider counters). **K2:** R3 decision row synced with D1/D5; SD-4/R4 name the gate "stage-1 recall at the predeclared reduced-M points" (M=150 production constant). **K3 ≈ J2:** fixture v1 immutable; sibling policy-v2 compatibility artifact (proof: max rendered block 2,209 B < 2,500 B); harness records effective policy `"2"`. **Minors:** byte ceiling takes precedence over the no-mid-sentence-cut rule; ~160M relabeled expected-case + 273M policy-max expense record ($38/$205/$410); envelope-count completeness qualified as successful-write (+ `search_artifact_write_failures`); §7.2 figures synced to measured 66k/211k (J4); §3.2's "two selector calls" branch-synced. Basis for blueprint v0.5.
 - **v0.6 (2026-07-26)** — **D5 (Joseph):** estimator calibration contract (one non-comparative call per candidate, 3-call ceiling, provider-reported usage, Joseph fires at end of P2, sibling calibration artifact — the checksummed fixture manifest is never mutated; narrow D1 amendment). **Normative-body sync (codex confirmation #2 ≡ opus5 H2):** `execution` enum gains `fat_after_thin_failure` (§1.1, §5.1); R4 body + decision row carry the D3/F1 branch contract (1–2 thin + 0–2 fat per terminal path); §8.1 step 4 carries D1's re-scope inline; §9 test language de-"exactly two calls" / de-recall@150; §10's blueprint-routed items retired (resolved by blueprint v0.4). **Stage-2 bound made enforceable (codex confirmation #3 remedy 1 ≡ opus5 H1):** excerpt policy v2 — the rendered per-entity fat block is hard-capped at 2,500 UTF-8 B (projector-enforced, char-boundary truncation); `150 × 2,500 B ≈ 94k < 102.4k` becomes a property of the code (the 55k/64k word-based premises superseded); fixture v1 untouched (max rendered block 2,209 B < ceiling — no regeneration, checksums unchanged). **Calibration artifact separated (codex confirmation #4).** **Sizing figures refreshed to fixture-v1 measurements** (§7.1: thin 88 B / fat 691 B mean per entity; cost-on-record updated; the uncapped single-stage fat vault cell withdrawn). Basis for blueprint v0.4.
 - **v0.5 (2026-07-26)** — **post-ratification amendments D1–D4 (Joseph, panel-informed)** added as §0: D1 D7-gate re-scope (implementation proceeds at blueprint ratification; experiments/tuning/ingestion + destructive P3b stay gated); D2 State C runs the search (`query_kind: state_c`; D-90-8 retired); D3 R4 amended (no fat call on thin-empty N>M; `thin_retained_zero` watched class; codex dissent recorded); D4 three-candidate screening cohort (deepseek-v4-flash included). Published with blueprint v0.3 per codex c-5 (synchronize the owner rulings into the ratified basis); ledger + North Star updated in the same batch.
diff --git a/docs/superpowers/specs/2026-07-26-task123-semantic-graph-search-blueprint.md b/docs/superpowers/specs/2026-07-26-task123-semantic-graph-search-blueprint.md
index c77df75..490b4de 100644
--- a/docs/superpowers/specs/2026-07-26-task123-semantic-graph-search-blueprint.md
+++ b/docs/superpowers/specs/2026-07-26-task123-semantic-graph-search-blueprint.md
@@ -1,9 +1,9 @@
-# #123 — Semantic Graph Search: Blueprint v0.8 (for Joseph's ratification)
+# #123 — Semantic Graph Search: Blueprint v0.9 (for Joseph's ratification)
 
-Date: 2026-07-26 · Task: **#123 Semantic graph search** · Status: **v0.8 — v0.7 confirmation round absorbed (codex REVISE N1–N4; opus5 CONCUR N1–N3; all checkable claims verified against the repo: `Hit.evidence` at §1.1, `MAX_SLUG_LEN=120` at `common/paths.py:27`, thinking-disabled cohort, no `tokens_lte_bytes` registry field); for Joseph's ratification**
-Basis: **spec v0.4 RATIFIED + v0.10 amendments (D1–D8, Joseph 2026-07-26)** · vision v1.5 · fixture v1 (`3d271e2`) · truth probes draft-v1 (39 probes, `a3c832a`) · v0.1 reviews + synthesis · v0.2 concurrence responses (`…-blueprint-v0.2-concurrence-codex.md`, `…-blueprint-v0.2-concurrence-opus5.md`) · v0.3 confirmation reviews (`…-blueprint-v0.3-review-codex.md`, `…-blueprint-v0.3-review-opus5.md`, `…-blueprint-v0.3-confirmation-diff-audit-opus5.md`) · v0.4 confirmation reviews (`…-blueprint-v0.4-review-codex.md`, `…-blueprint-v0.4-review-opus5.md`) · v0.5 confirmation reviews (`…-blueprint-v0.5-review-codex.md`, `…-blueprint-v0.5-review-opus5.md`) · v0.6 confirmation reviews (`…-blueprint-v0.6-review-codex.md`, `…-blueprint-v0.6-review-opus5.md`) · v0.7 confirmation reviews (`…-blueprint-v0.7-review-codex.md`, `…-blueprint-v0.7-review-opus5.md`).
+Date: 2026-07-26 · Task: **#123 Semantic graph search** · Status: **v0.9 — v0.8 confirmation round absorbed (codex REVISE O1–O3; opus5 CONCUR O1–O3); every claim repo-verified: `reasoning_effort: low` + `use_completion_tokens` on gpt-5.4-mini, gemini `thinking_level="minimal"` (full-off unsupported) with `thoughts_token_count` billed as output, no openai/gemini entry in `_THINKING_DISABLE_EXTRA_BODY`, FAT exact max 10,414 B at 20 expressions; for Joseph's ratification — **two calls need his confirmation (D9)****
+Basis: **spec v0.4 RATIFIED + v0.10 amendments (D1–D8, Joseph 2026-07-26)** · vision v1.5 · fixture v1 (`3d271e2`) · truth probes draft-v1 (39 probes, `a3c832a`) · v0.1 reviews + synthesis · v0.2 concurrence responses (`…-blueprint-v0.2-concurrence-codex.md`, `…-blueprint-v0.2-concurrence-opus5.md`) · v0.3 confirmation reviews (`…-blueprint-v0.3-review-codex.md`, `…-blueprint-v0.3-review-opus5.md`, `…-blueprint-v0.3-confirmation-diff-audit-opus5.md`) · v0.4 confirmation reviews (`…-blueprint-v0.4-review-codex.md`, `…-blueprint-v0.4-review-opus5.md`) · v0.5 confirmation reviews (`…-blueprint-v0.5-review-codex.md`, `…-blueprint-v0.5-review-opus5.md`) · v0.6 confirmation reviews (`…-blueprint-v0.6-review-codex.md`, `…-blueprint-v0.6-review-opus5.md`) · v0.7 confirmation reviews (`…-blueprint-v0.7-review-codex.md`, `…-blueprint-v0.7-review-opus5.md`) · v0.8 confirmation reviews (`…-blueprint-v0.8-review-codex.md`, `…-blueprint-v0.8-review-opus5.md`).
 
-**Joseph's rulings (2026-07-26), binding:** **D1** — coding starts at blueprint sign-off; live experiments/tuning/ingestion and the destructive P3b wait for labels+gates/experiments. **D2** — State C runs the search (`expressions: []`, `query_kind: state_c`); D-90-8 retired. **D3** — no fat call on thin-empty with N>M; `completed` + `thin_attempted` + `thin_retained_zero` (R4 amendment; codex's dissent recorded). **D4** — screening cohort: gemini-3.6-flash (interim default) + gpt-5.4-mini + deepseek-v4-flash. **D5** — calibration contract: one non-comparative call per candidate (3-call ceiling), provider-reported usage, Joseph fires at end of P2, sibling calibration artifact (fixture manifest never mutated) — a narrow D1 amendment. **D6** — R2 amended: one budget calculation method for BOTH stages (fat preflight + rendered-query ceiling 4,096 B + `OUTPUT_ALLOWANCE_FAT`; calibrated-with-headroom labeling; codex K1 ≡ opus5 J1). **D7** — M=100: the fat request's absolute worst case (≤ ~257 kB bytes ⇒ ≤ ~257k tokens + 4k output) fits the pool's smallest budget (320k) by construction; thin stays estimate-guarded with typed `budget_estimation_miss` (codex option 2); output allowances per response schema (opus5 L1); query ceiling per-field allocations (codex L2); fat-budget terminal's complete contract (codex L3 ≡ opus5 L2). **D8** — minimal wire: numbered expressions → indices (Python maps back; out-of-range = coerce-drop); **`evidence` dropped** (nothing consumed it); allowances THIN 13,000 / FAT 10,000 from the **exact max serialization** under `tokens_lte_bytes` (no density step — codex N2); slug bound = `MAX_SLUG_LEN` imported from `common.paths` (opus5 N1 ≡ codex N3); `Hit.evidence` removed from the public contract (codex N1); `tokens_lte_bytes` = validated `models.json` boolean carried by `ModelSpec`, missing/false ⇒ typed config error at selector resolution (codex N4 + opus5 N3); `budget_estimation_miss` re-typed `budget_exceeded`/`detected: post_call`, excluded from the §8.4 gate (opus5 M1).
+**Joseph's rulings (2026-07-26), binding:** **D1** — coding starts at blueprint sign-off; live experiments/tuning/ingestion and the destructive P3b wait for labels+gates/experiments. **D2** — State C runs the search (`expressions: []`, `query_kind: state_c`); D-90-8 retired. **D3** — no fat call on thin-empty with N>M; `completed` + `thin_attempted` + `thin_retained_zero` (R4 amendment; codex's dissent recorded). **D4** — screening cohort: gemini-3.6-flash (interim default) + gpt-5.4-mini + deepseek-v4-flash. **D5** — calibration contract: one non-comparative call per candidate (3-call ceiling), provider-reported usage, Joseph fires at end of P2, sibling calibration artifact (fixture manifest never mutated) — a narrow D1 amendment. **D6** — R2 amended: one budget calculation method for BOTH stages (fat preflight + rendered-query ceiling 4,096 B + `OUTPUT_ALLOWANCE_FAT`; calibrated-with-headroom labeling; codex K1 ≡ opus5 J1). **D7** — M=100: the fat request's absolute worst case (≤ ~257 kB bytes ⇒ ≤ ~257k tokens + 26k provider-total reserved output — D9) fits the pool's smallest budget (320k) by construction; thin stays estimate-guarded with typed `budget_estimation_miss` (codex option 2); output allowances per response schema (opus5 L1); query ceiling per-field allocations (codex L2); fat-budget terminal's complete contract (codex L3 ≡ opus5 L2). **D8** — minimal wire: numbered expressions → indices (Python maps back; out-of-range = coerce-drop); **`evidence` dropped** (nothing consumed it); allowances THIN 13,000 / FAT 10,000 from the **exact max serialization** under `tokens_lte_bytes` (no density step — codex N2); slug bound = `MAX_SLUG_LEN` imported from `common.paths` (opus5 N1 ≡ codex N3); `Hit.evidence` removed from the public contract (codex N1); `tokens_lte_bytes` = validated `models.json` boolean carried by `ModelSpec`, missing/false ⇒ typed config error at selector resolution (codex N4 + opus5 N3); `budget_estimation_miss` re-typed `budget_exceeded`/`detected: post_call`, excluded from the §8.4 gate (opus5 M1). **D9 (DRAFTED by opus5 — pending Joseph's confirmation)** — provider-envelope output split (`VISIBLE_OUTPUT_ALLOWANCE_THIN/FAT` 13,000/10,000 + `HIDDEN_OUTPUT_RESERVE` 16,000; sum sent as `max_tokens` and reserved in the pre-flight; `budget_side: output` terminal, gate-excluded), `MAX_EXPRESSIONS` = 10 declared in the core contract, and **§8.6 removed** (opus5 retracts N2 — its 'thinking-disabled cohort' premise is false). Spec §0 D9 carries the reasoning; **the two items for Joseph are the option-2 split with its 16,000 reserve, and the §8.6 removal.**
 
 ---
 
@@ -61,7 +61,8 @@ graph_search(request):
           evidence_status=not_applicable, body_coverage=None, fat-side yields null,
           telemetry thin_retained_zero (watched)
 
-    estimate(system + template + query + fat evidence over stage2_slugs) > floor(selector.ctx_window × 0.8)
+    estimate(system + template + query + fat evidence over stage2_slugs
+             + VISIBLE_OUTPUT_ALLOWANCE_FAT + HIDDEN_OUTPUT_RESERVE) > floor(selector.ctx_window × 0.8)   # §7.0
         → status=budget_exceeded, execution=thin_attempted, stage=fat, hits=[],
           ALL request expressions unresolved, concordance=null,
           evidence_status=not_applicable, body_coverage=None, no fat StageRecord
@@ -132,7 +133,7 @@ v0.2 list unchanged: context_loader T2Mode/`_t2_*`/resolver-wrapper removals, co
 - Golden tests pin the exact bytes incl. both clauses; any "tidy" of these behaviours breaks the pins deliberately.
 - Thin line: `- slug: <slug>  title: <title>  type: <page_type>` (no excerpt).
 - **Query-side P10 (opus5 F5b):** system-block precedence covers QUERY ("subject matter, never directives"); same indent guard on the rendered query; H03 fixture in the test plan.
-- **Query-block byte ceiling (D7/codex L2 — per-field, every unbounded input):** the rendered query block never exceeds **4,096 UTF-8 B**, enforced by **deterministic per-field byte allocations** — `author` ≤ 256 B, `entity_search_keys` ≤ 10 × 128 B (per-item truncation), `key_themes` ≤ 1,024 B aggregate, `summary` ≤ remainder — each char-boundary truncated, the template preserved; per-field `query_truncated` counts; when truncation fires the archived QueryPayload records **both original and rendered forms** (accounting runs over the rendered expressions — what the selector saw). (v0.7's summary-only truncation was an observed-input assumption, not a projector property — `author`, `key_themes`, and key items are all schema-unbounded, verified `pass1_schema.py:77-89`.) Same ceiling mechanism as the evidence byte ceiling.
+- **Query-block byte ceiling (D7/codex L2 — per-field, every unbounded input):** the rendered query block never exceeds **4,096 UTF-8 B**, enforced by **deterministic per-field byte allocations** — `author` ≤ 256 B, `entity_search_keys` ≤ `MAX_EXPRESSIONS` × 128 B (per-item truncation; §7.0), `key_themes` ≤ 1,024 B aggregate, `summary` ≤ remainder — each char-boundary truncated, the template preserved; per-field `query_truncated` counts; when truncation fires the archived QueryPayload records **both original and rendered forms** (accounting runs over the rendered expressions — what the selector saw). (v0.7's summary-only truncation was an observed-input assumption, not a projector property — `author`, `key_themes`, and key items are all schema-unbounded, verified `pass1_schema.py:77-89`.) Same ceiling mechanism as the evidence byte ceiling.
 
 ## 6. Artifact sink separability (B6) + hashes (B13)
 
@@ -140,7 +141,41 @@ Core **always** constructs the `SearchAuditPayload` (completed/abstain/budget/fa
 
 ## 7. The R2 pre-flight estimator + sizing (calibration rebuilt per codex c-4 / opus5 G4; bound fixed per G3)
 
-**Estimator (v1 — both stages, D6):** `ceil(utf8_bytes / 4)` over the **fully rendered request** — evidence block + system template + user wrapper + query block (≤ 4,096 B capped, §5) — plus the stage's output allowance (**`OUTPUT_ALLOWANCE_THIN` = 13,000 / `OUTPUT_ALLOWANCE_FAT` = 10,000**, reserved via `ModelRequest.max_tokens`; **derived from the exact max serialization of the fully-bounded wire, covered by `tokens_lte_bytes` — no density step anywhere (codex N2 option 1):** thin = `{"retained": [≤ 100 × ≤ 120 B]}` ≈ 12.3k B exact max (+ buffer); fat = `{"selections": [≤ 50 × {slug ≤ 120 B, matched ≤ 10 indices}], "unresolved": ≤ 10 indices}` ≈ 9.6k B exact max (+ buffer); `tokens_lte_bytes` ⇒ tokens ≤ bytes, so the allowance covers any schema-valid response, period. Slug bound = **`MAX_SLUG_LEN` (120 B), imported from `common.paths`** — `kdb_search` imports `common`, and a second slug constant is exactly the drift the boundary test exists to prevent (opus5 N1 ≡ codex N3: 97–120 B slugs are valid graph data; the render-time assertion now fires only on non-canonical input — a real defect). P1 asserts each allowance against a **synthetic schema-maximum document** — exact max bytes ≤ allowance, proved via the invariant, never ÷3. History: D6's 2,000 → opus5 L1's measured 3,000/4,000 → D8's derived 4,000/4,000 → **v0.8's invariant-derived 13,000/10,000**). **One calculation method for thin and fat alike** (Joseph's consistency ruling; the v0.3 "stage-1 only" asymmetry is superseded — codex K1 ≡ opus5 J1). Guardrail rests on the **0.8 headroom** (the "never underestimates" claim stays withdrawn; the headroom explicitly carries density variance — opus5 J3). `budget.py` computes the estimate and applies the 0.8 factor itself rather than reusing `model_pool.fits_context` (G6: that helper takes a pre-computed `est_input` with no headroom concept — different semantics; noted rather than force-fitted).
+### 7.0 Constants — the single source of truth (opus5 O2)
+
+Every other mention in this blueprint and in the spec **cites this table**; none restates a value. Three consecutive
+confirmation rounds each caught a stale-figure straggler because these numbers lived in five-plus places at once.
+
+| constant | value | derivation / authority |
+|---|---|---|
+| `M` (thin retention + fat hydration cap) | **100** | D7 — sized so the fat request's provider-total worst case fits the pool's smallest budget by construction |
+| `max_results` | **50** | spec §3.2 (the merged `page_cap`) |
+| `MAX_EXPRESSIONS` | **10** | **D9** — declared in the core `QueryPayload`; the sole derivation source for the wire's index caps, index digit width and the FAT allowance |
+| `MAX_SLUG_LEN` | **120 B** | imported from `common.paths` (opus5 N1 ≡ codex N3) — never re-declared here |
+| excerpt policy-v2 rendered-entry ceiling | **2,500 B** | spec §4 policy v2 (opus5 H1); fixture max 2,209 B ⇒ binds on nothing in fixture v1 |
+| rendered query-block ceiling | **4,096 B** | D7(iv) per-field allocations (`author` ≤ 256 B, keys ≤ 10 × 128 B, themes ≤ 1,024 B, `summary` ≤ remainder) |
+| `VISIBLE_OUTPUT_ALLOWANCE_THIN` | **13,000** | exact max serialization **12,315 B** (§7.0a) under `tokens_lte_bytes` |
+| `VISIBLE_OUTPUT_ALLOWANCE_FAT` | **10,000** | exact max serialization **8,404 B** (§7.0a) under `tokens_lte_bytes` |
+| `HIDDEN_OUTPUT_RESERVE` | **16,000** | **D9** — route-owned; hidden reasoning is unbounded and unenforceable at both providers, so the reserve is sized by what the static guarantee absorbs, and the residual is typed (`budget_side: output`) |
+| `max_tokens` sent to the provider | **thin 29,000 / fat 26,000** | visible + `HIDDEN_OUTPUT_RESERVE`; verified ≤ every D4 route's `max_output_tokens` (gemini 65,536 binding) |
+| fat provider-total static guarantee | **283k < 320k** | 257 kB input ⇒ ≤257k tokens (1-token/byte extreme) + 26k reserved output; holds for any window **≥ ~355k** |
+
+### 7.0a Exact serialized maxima + the grammar that produces them (codex O3)
+
+Compact JSON, no insignificant whitespace, every value at its declared maximum; the **synthetic schema-maximum
+tests in `test_budget.py` are the executable authority** for these integers:
+
+```text
+thin  {"retained": [<M=100 × ("<120 B slug>")>]}                                    = 12,315 B
+fat   {"selections": [<50 × {"slug": "<120 B>", "matched": [<MAX_EXPRESSIONS indices>]}>],
+       "unresolved": [<MAX_EXPRESSIONS indices>]}                                   =  8,404 B
+```
+
+Index width follows from `MAX_EXPRESSIONS` (single digit at 10). **The FAT figure is a function of
+`MAX_EXPRESSIONS`, not a free constant** — at 20 expressions the same grammar yields 10,414 B and at 50, 14,914 B,
+which is why D9 moved that bound into the core contract instead of inheriting pass-1's `maxItems`.
+
+**Estimator (v1 — both stages, D6):** `ceil(utf8_bytes / 4)` over the **fully rendered request** — evidence block + system template + user wrapper + query block (≤ 4,096 B capped, §5) — plus the stage's **provider-total reserved output** (§7.0: `VISIBLE_OUTPUT_ALLOWANCE` + `HIDDEN_OUTPUT_RESERVE` = thin 29,000 / fat 26,000 — sent as `ModelRequest.max_tokens` **and** reserved in the estimate). **The visible half is the exact max serialization of the fully-bounded wire under `tokens_lte_bytes` — no density step anywhere (codex N2 option 1); §7.0a carries the grammar and the integers.** **The hidden half is D9's answer to codex O1:** `max_tokens` reaches provider fields that cap the *whole* completion — `gpt-5.4-mini` receives `max_completion_tokens` (`call_model.py:283`) and carries `reasoning_effort: low`; the gemini path sends `ThinkingConfig(thinking_level="minimal")` (full-off unsupported — `call_model.py:221-228`) and adds `thoughts_token_count` to output tokens (`:248`); and `_THINKING_DISABLE_EXTRA_BODY` has no openai/gemini mapping (`model_pool.py:32-38`), so the pool's semantic `thinking: "disabled"` injects nothing for two of three candidates. Visible-JSON bounds therefore do not bound what the provider counts. Four quantities stay distinct and separately tested: visible response tokens, hidden output tokens, the provider cap, and the context-budget reservation. Slug bound = **`MAX_SLUG_LEN` (120 B), imported from `common.paths`** — `kdb_search` imports `common`, and a second slug constant is exactly the drift the boundary test exists to prevent (opus5 N1 ≡ codex N3: 97–120 B slugs are valid graph data; the render-time assertion now fires only on non-canonical input — a real defect). P1 asserts each allowance against a **synthetic schema-maximum document** — exact max bytes ≤ allowance, proved via the invariant, never ÷3. (Allowance history — D6's uniform 2,000 → L1's measured 3,000/4,000 → D8's 4,000/4,000 → v0.8's invariant-derived 13,000/10,000 → **v0.9's visible+hidden envelope** — lives in the changelog; §7.0 is the contract.) **One calculation method for thin and fat alike** (Joseph's consistency ruling; the v0.3 "stage-1 only" asymmetry is superseded — codex K1 ≡ opus5 J1). Guardrail rests on the **0.8 headroom** (the "never underestimates" claim stays withdrawn; the headroom explicitly carries density variance — opus5 J3). `budget.py` computes the estimate and applies the 0.8 factor itself rather than reusing `model_pool.fits_context` (G6: that helper takes a pre-computed `est_input` with no headroom concept — different semantics; noted rather than force-fitted).
 
 **Calibration gate (D5 — Joseph, 2026-07-26; mechanics per opus5 G4; timing per codex c-4; artifact separability per codex confirmation #4):** `count_tokens` endpoints exist only for gemini — so calibration reads **provider-reported usage** instead: **exactly one non-comparative real call per candidate (hard 3-call ceiling)** over the exact rendered fixture thin block, reading the returned input-token count `call_model` already surfaces — the billed number, authoritative for all three candidates. Fixed prompt; no ranking or selection effect. **Timing: end of P2** (after golden rendered bytes pass — the production renderer exists then; a pre-P1 measurement would calibrate a renderer that doesn't exist yet), **as a gate before D7 live experiments**. It is a paid run — **Joseph fires it**; D5 records this as a **narrow D1 amendment** (the only live selector spend before D7; experiments/tuning/ingestion still wait for labels + gates). Persisted per candidate as a **sibling artifact** `task123_search_calibration_v1.json` — `{counting_source: "provider_reported_usage", fixture_version, input sha256, prompt version, model id, input_tokens, measured bytes/token}`; **the checksummed fixture manifest is never mutated**. P1's budget tests are structural-only (H3); measurement assertions live at this gate.
 
@@ -154,18 +189,18 @@ Core **always** constructs the `SearchAuditPayload` (completed/abstain/budget/fa
 | fat, value-investing | 51 | 38,512 | ~9.6k | 755 B |
 | fat, production cap (M=100 — D7) | ≤100 | ≤ 250,000 bound (exp. ~71,900 at the 719 B mean) | ≤ 62.5k (exp. ~18k) | 719 B fixture mean |
 
-- **Stage-2 static guarantee (D7 — codex K1/L1 closed by sizing, not measurement):** with **M=100**, the fat request's absolute worst case is bounded: evidence ≤ 100 × 2,500 B = 250 kB + query ≤ 4,096 B + system/template ~3 kB ≈ **≤ ~257 kB bytes** ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + 10k reserved output **< 320k — the pool's smallest budget (gpt-5.4-mini 400k × 0.8)**. For any selector window ≥ ~330k — the entire configured pool — **fat fits by construction; no estimation in its safety path.** The guarantee rests on a **named supported-route invariant: `tokens_lte_bytes`** — content token count ≤ UTF-8 byte count, a **theorem for byte-level BPE** (every token maps to ≥ 1 byte; all pool providers use byte-level BPE — opus5 N3), recorded as a **validated boolean in `models.json`, carried by `ModelSpec`, presence-checked at selector route resolution** — missing/false ⇒ typed config error before any work; all three D4 candidates declared + test-covered (codex N4). (The D6 fat preflight still runs — one calculation method for both stages, Joseph's consistency ruling — but at pool windows its estimate, ≤ ~68k, never reaches the guard; below ~330k windows fat reverts to the same honest estimate-guard as thin, with `budget_estimation_miss` typed.) The v0.4 "holds by construction ≥128k" and v0.7's ~96–98k / ≤ ~381 kB figures are superseded (M 150→100). §12 asserts the accounting against the projector ceilings, cross-checked against the fixture maximum rendered block 2,209 B (`value-investing-as-owner-mindset…`, 251w — binds on nothing in fixture v1). No architecture consequence (pool is 400k/1M/1M).
+- **Stage-2 static guarantee (D7 — codex K1/L1 closed by sizing, not measurement):** with **M=100**, the fat request's absolute worst case is bounded: evidence ≤ 100 × 2,500 B = 250 kB + query ≤ 4,096 B + system/template ~3 kB ≈ **≤ ~257 kB bytes** ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + **26k provider-total reserved output (D9: 10k visible + 16k hidden)** = **283k < 320k — the pool's smallest budget (gpt-5.4-mini 400k × 0.8)**. For any selector window ≥ ~355k — the entire configured pool — **fat fits by construction; no estimation in its safety path.** The guarantee rests on a **named supported-route invariant: `tokens_lte_bytes`** — content token count ≤ UTF-8 byte count, a **theorem for byte-level BPE** (every token maps to ≥ 1 byte; all pool providers use byte-level BPE — opus5 N3), recorded as a **validated boolean in `models.json`, carried by `ModelSpec`, presence-checked at selector route resolution** — missing/false ⇒ typed config error before any work; all three D4 candidates declared + test-covered (codex N4). (The D6 fat preflight still runs — one calculation method for both stages, Joseph's consistency ruling — but at pool windows its estimate, ≤ ~68k, never reaches the guard; below ~355k windows fat reverts to the same honest estimate-guard as thin, with `budget_estimation_miss` typed.) The v0.4 "holds by construction ≥128k" and v0.7's ~96–98k / ≤ ~381 kB figures are superseded (M 150→100). §12 asserts the accounting against the projector ceilings, cross-checked against the fixture maximum rendered block 2,209 B (`value-investing-as-owner-mindset…`, 251w — binds on nothing in fixture v1). No architecture consequence (pool is 400k/1M/1M).
 - **Both stages guarded (D6 — this supersedes the v0.3/v0.4 "stage-1 only" asymmetry):** G3's optional fat-stage estimator is **adopted** as R2's amendment (codex K1 ≡ opus5 J1, convergent): the same estimate-and-guard covers the fat call. Thin remains the unbounded-in-N case the guard exists for; fat is byte-capped by construction (≤ ~257 kB full request — D7's M=100) and now estimate-guarded at runtime as well — and, at pool windows, statically guaranteed (see above).
-- Vault-scale projections (measured): thin whole-graph ~9,600 × 88 B ≈ 845 kB ≈ **~211k tokens**; largest-domain ~3,000 ≈ 264 kB ≈ **~66k**; fat ≤ 100 (D7) ≈ ~18k expected — and **statically guaranteed to fit the pool**: ≤ ~257 kB worst-case bytes ⇒ ≤ ~257k absolute-worst tokens + 4k output < 320k (the pool's smallest budget). All pass the configured pool's 80% budgets.
+- Vault-scale projections (measured): thin whole-graph ~9,600 × 88 B ≈ 845 kB ≈ **~211k tokens**; largest-domain ~3,000 ≈ 264 kB ≈ **~66k**; fat ≤ 100 (D7) ≈ ~18k expected — and **statically guaranteed to fit the pool**: ≤ ~257 kB worst-case bytes ⇒ ≤ ~257k absolute-worst tokens + 26k provider-total reserved output = 283k < 320k (the pool's smallest budget — §7.0). All pass the configured pool's 80% budgets.
 - Cost per source (largest domain): ~10.7k input tokens today → ~$0.01–0.02; 1,706-source re-ingest ≈ **~143M expected-case ⇒ $20 deepseek / $107 gpt-5.4-mini / $215 gemini**; **policy-ceiling case 1,706 × ~128.5k ≈ 219M ⇒ $31 / $164 / $329** (D7: M=100; the pathological 1-token/byte worst case is a fit guarantee for the pool, not a cost scenario).
 
 ## 8. Selector route (B10) + retry composition (B11 — contract frozen per codex c-1; policy label per opus5 G5)
 
-**B10:** `ModelSpec` via `resolve_models_json` (fail-hard incl. `ctx_window` present — §2.1); `DEFAULT_SELECTOR_MODEL_ID = "gemini-3.6-flash"` (interim); cohort per D4; production default post-D7. **`tokens_lte_bytes` ownership (codex N4 + opus5 N3):** a validated boolean in `models.json` per model (all pool providers byte-level BPE — the invariant is a theorem there: every token maps to ≥ 1 byte ⇒ content tokens ≤ content bytes), carried by `ModelSpec`; the selector route asserts it at resolution — missing/false ⇒ typed config error **before any rendering or calling**; all three D4 candidates declared + test-covered. Pass-1/pass-2 routes don't require the field (the invariant is load-bearing only for the selector's static guarantees).
+**B10:** `ModelSpec` via `resolve_models_json` (fail-hard incl. `ctx_window` present — §2.1); `DEFAULT_SELECTOR_MODEL_ID = "gemini-3.6-flash"` (interim); cohort per D4; production default post-D7. **`tokens_lte_bytes` ownership (codex N4 + opus5 N3):** a validated boolean in `models.json` per model (all pool providers byte-level BPE — the invariant is a theorem there: every token maps to ≥ 1 byte ⇒ content tokens ≤ content bytes), carried by `ModelSpec`; the selector route asserts it at resolution — missing/false ⇒ typed config error **before any rendering or calling**; all three D4 candidates declared + test-covered. Pass-1/pass-2 routes don't require the field (the invariant is load-bearing only for the selector's static guarantees). **`HIDDEN_OUTPUT_RESERVE` is route-owned too (D9):** selector route resolution asserts `VISIBLE_OUTPUT_ALLOWANCE + HIDDEN_OUTPUT_RESERVE ≤ max_output_tokens` for the resolved model (gemini's 65,536 is the binding case against thin's 29,000) — typed config error before any rendering or calling, never a runtime surprise. No pool provider exposes an *enforceable* hidden-token maximum (`reasoning_effort` and `thinking_level` are levels, and `thinking_budget` is not the 3.x knob — `call_model.py:221`), which is exactly why the residual is typed rather than claimed away.
 
 **B11 — the stage/attempt contract:**
 
-- **Up to two logical attempts per executed stage; attempt 2 only after an allowed retry class** (transport, timeout, `unparseable_response`, `structurally_unusable_response`, `all_entries_dropped`). Each logical attempt = one `call(ModelRequest)` (production: single `call_model` — not the 3-attempt wrapper). `budget_exceeded` and **`budget_estimation_miss`** are never retried (deterministic — the same request fails identically; the miss is typed `budget_exceeded`/`detected: post_call`, call attempted, spend possible, watched per model — D7; **excluded from the §8.4 selector-failure gate** — our mechanics, not the model's, and it would otherwise penalize smaller-window models systematically — opus5 M1).
+- **Up to two logical attempts per executed stage; attempt 2 only after an allowed retry class** (transport, timeout, `unparseable_response`, `structurally_unusable_response`, `all_entries_dropped`). Each logical attempt = one `call(ModelRequest)` (production: single `call_model` — not the 3-attempt wrapper). `budget_exceeded` and **`budget_estimation_miss`** are never retried (deterministic — the same request fails identically; the miss is typed `budget_exceeded`/`detected: post_call`/`budget_side: input`, call attempted, spend possible, watched per model — D7; **its output-side sibling `budget_side: output`** — a length-finish-reason truncation, i.e. hidden + visible output exceeding the reserved envelope — is typed the same way, never retried, and additionally read as a **selector-admission signal** (a route that trips it is not a viable selector: a D7 finding, not a production gate); **excluded from the §8.4 selector-failure gate** — our mechanics, not the model's, and it would otherwise penalize smaller-window models systematically — opus5 M1).
 - One `StageRecord` per logical attempt incl. failures; `logical_call_count == archived StageRecords` (§6).
 - **The stage entry records the provider's *actual* SDK sub-retry policy** (G5 — verified: openai-family `max_retries=2`, `common/call_model.py:192/:273`; **gemini has none** — `call_model.py:212`: no such constructor kwarg, `HttpOptions.retry_options` default None). Never counted as selector attempts.
 - **Backoff posture: deliberately pass-1's** (G5's option b — bare `call_model`, no wrapper; precedent `ingestion/enrich/pass1_caller.py:179`): the kdb_search loop retries immediately on the allowed classes. Recorded as a posture adoption, not an oversight; a Retry-After/exponential layer is a later refinement if telemetry shows transport flakes dominate.
@@ -180,7 +215,9 @@ Core **always** constructs the `SearchAuditPayload` (completed/abstain/budget/fa
 | thin exhausted, N>M | 2 thin, 0 fat |
 | thin exhausted, N≤M → fat (F1) | 2 thin + 1–2 fat = 3–4 |
 | fat exhausted | 1–2 thin + 2 fat = 3–4 |
-| fat budget_exceeded (D6) | 1–2 thin, 0 fat |
+| fat budget_exceeded, pre-call (D6) | 1–2 thin, 0 fat |
+| budget_estimation_miss, `budget_side: input` (D7) | 1 attempted at the missing stage |
+| output truncation, `budget_side: output` (D9) | 1 attempted at the truncated stage |
 | fat `budget_estimation_miss` (sub-330k windows only — D7) | 1–2 thin + 1 fat attempted |
 
 ## 9. Replay modes (spec §5.2 — unchanged)
@@ -218,7 +255,7 @@ Record replay (default; integrity hash validated; no calls/reads) and historical
 
 ## 12. Test plan (TDD-first; concurrence items folded)
 
-**P1 (`kdb_search/tests/`):** `test_contract.py` (status × execution × evidence_status matrix incl. the D3 terminal's complete contract: all expressions unresolved, concordance null, evidence_status `not_applicable`, body_coverage None; `fat_after_thin_failure` naming; **the fat-`budget_exceeded` terminal's complete contract (D6/D7, codex L3 ≡ opus5 L2: hits [], all expressions unresolved, concordance null, `not_applicable`/None, no fat StageRecord; `budget_exceeded` as a two-row entry — `not_executed` thin-preflight / `thin_attempted` fat-preflight; the F1 interaction named — `budget_exceeded` + `thin_failed_nonbinding` on the thin-failed/N≤M path)**); `test_response.py` (four-way, 6-of-10, coercion incl. **out-of-range + duplicate `matched` indices and indexed advisory `unresolved` (D8 compact wire — no `evidence` field; every numeric maximum tested at its bound)**, accounting); `test_zero_escape.py` (property); `test_budget.py` (zero-invocation `budget_exceeded` **at both stages** (D6 — fat preflight ⇒ zero fat-call invocations + typed `budget_exceeded`/`execution=thin_attempted`); never retried; **full-request accounting — system + template + query + evidence + output allowance** (codex K1); **the M=100 static guarantee asserted — 100 × 2,500 B + 4,096 B + system/template ≤ ~257 kB ⇒ ≤ ~257k tokens + 10k output < 320k, the pool's smallest budget (D7)**; **output allowances = exact max serialized bytes of the fully-bounded wire (THIN 13,000 / FAT 10,000), asserted against synthetic schema-maximum documents and proved sufficient via `tokens_lte_bytes` — no density step (codex N2)**; **`tokens_lte_bytes` resolution tests — missing/false ⇒ typed config error before any work; all three D4 candidates declared (codex N4)**; **slug bound = `MAX_SLUG_LEN` imported from `common.paths`, assertion fires only on non-canonical input (opus5 N1 ≡ codex N3)**; **`budget_estimation_miss` typed `budget_exceeded`/`detected: post_call`, attempted-once, never retried, excluded from the §8.4 gate series (D7/opus5 M1)**; **estimator structure only — threshold arithmetic against synthetic windows (H3: measurement assertions live at the calibration gate, not P1)**; **`ctx_window=None` route ⇒ typed config error at resolution, before any work**); `test_projection.py` (policy v1 caps/extension + **policy-v2 byte ceiling — truncation at a character boundary, total rendered entry ≤ 2,500 B** + **query ceiling per-field allocations (D7/codex L2: author ≤ 256 B, keys ≤ 10 × 128 B, themes ≤ 1,024 B, summary ≤ remainder; oversized author/themes/keys cases, not just summary) with per-field `query_truncated` counts + archived original/rendered forms**; **§5 grammar incl. both G7 clauses — trailing-newline whitespace line, indented blank lines**; `delimiter_collision_guard` counts); `test_artifact.py` (payload on all paths; `logical_call_count == StageRecords`; hashes); packaging smoke tests.
+**P1 (`kdb_search/tests/`):** `test_contract.py` (status × execution × evidence_status matrix incl. the D3 terminal's complete contract: all expressions unresolved, concordance null, evidence_status `not_applicable`, body_coverage None; `fat_after_thin_failure` naming; **the fat-`budget_exceeded` terminal's complete contract (D6/D7, codex L3 ≡ opus5 L2: hits [], all expressions unresolved, concordance null, `not_applicable`/None, no fat StageRecord; `budget_exceeded` as a two-row entry — `not_executed` thin-preflight / `thin_attempted` fat-preflight; the F1 interaction named — `budget_exceeded` + `thin_failed_nonbinding` on the thin-failed/N≤M path)**); `test_response.py` (four-way, 6-of-10, coercion incl. **out-of-range + duplicate `matched` indices and indexed advisory `unresolved` (D8 compact wire — no `evidence` field; every numeric maximum tested at its bound)**, accounting); `test_zero_escape.py` (property); `test_budget.py` (zero-invocation `budget_exceeded` **at both stages** (D6 — fat preflight ⇒ zero fat-call invocations + typed `budget_exceeded`/`execution=thin_attempted`); never retried; **full-request accounting — system + template + query + evidence + output allowance** (codex K1); **the M=100 static guarantee asserted — 100 × 2,500 B + 4,096 B + system/template ≤ ~257 kB ⇒ ≤ ~257k tokens + 10k output < 320k, the pool's smallest budget (D7)**; **visible allowances = exact max serialized bytes of the fully-bounded wire (THIN 13,000 vs 12,315 B / FAT 10,000 vs 8,404 B — §7.0a), asserted against synthetic schema-maximum documents and proved sufficient via `tokens_lte_bytes` — no density step (codex N2)**; **the four envelope quantities separately (D9/codex O1): visible response tokens, hidden output tokens, the provider cap (`max_tokens` = visible + `HIDDEN_OUTPUT_RESERVE`, asserted ≤ the route's `max_output_tokens` at resolution), and the context reservation (the sum)**; **`budget_side: output` terminal on a length finish reason — typed, attempted-once, never retried, gate-excluded**; **`len(expressions) > MAX_EXPRESSIONS` ⇒ typed outcome before any rendering, every consumer; the FAT exact-max test parameterized on `MAX_EXPRESSIONS` (10 ⇒ 8,404 B, 20 ⇒ 10,414 B) so the bound's load-bearing role is pinned (D9 ≡ opus5 O1)**; **`tokens_lte_bytes` resolution tests — missing/false ⇒ typed config error before any work; all three D4 candidates declared (codex N4)**; **slug bound = `MAX_SLUG_LEN` imported from `common.paths`, assertion fires only on non-canonical input (opus5 N1 ≡ codex N3)**; **`budget_estimation_miss` typed `budget_exceeded`/`detected: post_call`, attempted-once, never retried, excluded from the §8.4 gate series (D7/opus5 M1)**; **estimator structure only — threshold arithmetic against synthetic windows (H3: measurement assertions live at the calibration gate, not P1)**; **`ctx_window=None` route ⇒ typed config error at resolution, before any work**); `test_projection.py` (policy v1 caps/extension + **policy-v2 byte ceiling — truncation at a character boundary, total rendered entry ≤ 2,500 B** + **query ceiling per-field allocations (D7/codex L2: author ≤ 256 B, keys ≤ 10 × 128 B, themes ≤ 1,024 B, summary ≤ remainder; oversized author/themes/keys cases, not just summary) with per-field `query_truncated` counts + archived original/rendered forms**; **§5 grammar incl. both G7 clauses — trailing-newline whitespace line, indented blank lines**; `delimiter_collision_guard` counts); `test_artifact.py` (payload on all paths; `logical_call_count == StageRecords`; hashes); packaging smoke tests.
 
 **P2 (`kdb_search/tests/`):** `test_prompts_golden.py` (pinned bytes; version/SHA guard); `test_two_stage.py` (thin→fat order; retain-all variants; **branch-specific call counts per the §8 table**; F1 path (proceed + `thin_failed_nonbinding` + concordance null + `fat_after_thin_failure`); D3 terminal (no fat call + `thin_retained_zero`); concordance formula + null cases; retry classes ⇒ attempt 2 archived; exhausted ⇒ typed `selector_failure`; stage entry records the provider's actual sub-retry policy); `test_adversarial.py` (H01/H02 evidence-side, H03 query-side); `test_replay.py`.
 
@@ -233,9 +270,9 @@ Record replay (default; integrity hash validated; no calls/reads) and historical
 | # | Question | Disposition |
 |---|---|---|
 | **B1** | Package boundary | **Selected, pending blueprint ratification** (label corrected per codex c-5): `kdb_search/`, `{common}`; packaging per §1 |
-| **B3** | Estimator | `ceil(utf8_bytes/4)` over the full rendered request + output allowances **= exact max serialization of the bounded wire, covered by `tokens_lte_bytes` (THIN 13,000 / FAT 10,000 — no density step, codex N2)** vs 80% window — both stages (D6); **fat additionally statically guaranteed for the pool via M=100 (D7)**; `budget_estimation_miss` = `budget_exceeded`/`detected: post_call`, watched, excluded from the §8.4 gate (opus5 M1); calibration = provider-reported usage at end of P2, Joseph fires (**D5**), sibling artifact (§7) |
+| **B3** | Estimator | `ceil(utf8_bytes/4)` over the full rendered request + **the provider-total output envelope — visible 13,000/10,000 (exact max serialization under `tokens_lte_bytes`, no density step) + `HIDDEN_OUTPUT_RESERVE` 16,000, sum sent as `max_tokens` and reserved (D9/codex O1); all constants in §7.0** vs 80% window — both stages (D6); **fat additionally statically guaranteed for the pool via M=100 (D7)**; `budget_estimation_miss` = `budget_exceeded`/`detected: post_call`, watched, excluded from the §8.4 gate (opus5 M1); calibration = provider-reported usage at end of P2, Joseph fires (**D5**), sibling artifact (§7) |
 | **B4** | Prompt storage | `kdb_search/prompts/*.txt`, version + SHA guard, package-data + wheel smoke test |
-| **B5** | Escaping | Frozen grammar (§5) incl. G7 clauses + **policy-v2 byte ceiling (2,500 B rendered entry)** + **query ceiling per-field allocations (4,096 B total — D7/codex L2)**; JSON-array amendment rejected |
+| **B5** | Escaping | Frozen grammar (§5) incl. G7 clauses + **policy-v2 byte ceiling (2,500 B rendered entry)** + **query ceiling per-field allocations (4,096 B total — D7/codex L2, per-field caps keyed to `MAX_EXPRESSIONS`)**; JSON-array amendment rejected |
 | **B6** | Artifact sink | Core always constructs; caller persists; `artifact_path` null on failure; `logical_call_count == StageRecords` |
 | **B7** | Adapter placement | `compiler/search_adapter.py` in `compile_source` step 1; explicit `vault_root`/`intra_run_order`/`ModelSpec`; §3.1 plumbing named |
 | **B8** | Record evolution | V2 + sibling envelope; dispatching loader; `context_failed.search` preserved; **hit-level provenance + representative per-expression projection**; KPI resolver read removed (no scored axis touched) |
@@ -244,10 +281,11 @@ Record replay (default; integrity hash validated; no calls/reads) and historical
 | **B11** | Retry composition | Up-to-2 logical attempts/executed stage; per-attempt StageRecords; actual sub-retry policy recorded; pass-1 no-backoff posture adopted (§8) |
 | **B12** | FTS track | P5b, independent CLI/MCP-surface track |
 | **B13** | Refs + hashes | Caller-computed `GraphSnapshotRef`; canonical-JSON sha256s |
-| **B14** | Gate + edges | D1–D8 per Joseph (binding); plus F1 (`fat_after_thin_failure`), F4/c-3 (§11 measurement contract), F5b (query-side P10), codex #12 (concordance); confirmation rounds absorbed: codex 1–4 + K1–K3 + L1–L3 + M1 + N1–N4, opus5 H1–H9 + J1–J4 + L1–L3 + M1–M4 + N1–N3 (D6, D7, D8) |
+| **B14** | Gate + edges | D1–D8 per Joseph (binding) + **D9 drafted, pending his confirmation**; plus F1 (`fat_after_thin_failure`), F4/c-3 (§11 measurement contract), F5b (query-side P10), codex #12 (concordance); confirmation rounds absorbed: codex 1–4 + K1–K3 + L1–L3 + M1 + N1–N4 + O1–O3, opus5 H1–H9 + J1–J4 + L1–L3 + M1–M4 + N1–N3 + O1–O3 (D6, D7, D8, D9) |
 
 ## Changelog
 
+- **v0.9 (2026-07-26)** — v0.8 confirmation round absorbed (codex REVISE O1–O3; opus5 CONCUR O1–O3; every claim repo-verified first). **New D9 (drafted by opus5 — Joseph confirms two calls).** codex O1: D8's allowance proof bounds *visible* JSON, but `max_tokens` caps the whole completion — `gpt-5.4-mini` carries `reasoning_effort: low` with `use_completion_tokens` ⇒ `max_completion_tokens` (`call_model.py:283`), the gemini path sends `ThinkingConfig(thinking_level="minimal")` with full-off unsupported (`:221-228`) and bills `thoughts_token_count` as output (`:248`), and `_THINKING_DISABLE_EXTRA_BODY` has no openai/gemini mapping (`model_pool.py:32-38`). Resolved by his **option 2**: `VISIBLE_OUTPUT_ALLOWANCE_THIN/FAT` 13,000/10,000 + **`HIDDEN_OUTPUT_RESERVE` 16,000**, sum sent as `max_tokens` (thin 29,000 / fat 26,000, asserted ≤ each route's `max_output_tokens` at resolution — gemini's 65,536 binding) and reserved in the pre-flight; the unbounded-hidden residual typed `budget_exceeded`/`detected: post_call`/**`budget_side: output`**, never retried, gate-excluded, watched as a selector-admission signal. Option 1 rejected because it would eject `gemini-3.6-flash` (D4's interim default) — an owner decision on D4, not a mechanics fix. Static guarantee recomputed on the provider-total envelope: **283k < 320k**, window threshold ~330k → **~355k**. opus5 O1 ≡ codex O3: **`MAX_EXPRESSIONS` = 10 declared in the core `QueryPayload`**, validated for every consumer before rendering, and the sole derivation source for the wire's index caps, index width and the FAT allowance — which had all been inheriting pass-1's `maxItems: 10` (FAT exact max is 10,414 B at 20 expressions vs a 10,000 allowance). **§8.6 removed** — opus5 **retracts N2**: its premise ('the cohort runs thinking-disabled, so the prose field was the only in-band reasoning channel') is false for two of three candidates, and codex O2's accounting that it was never a free axis is correct; D8's drop stands on its own reasoning. opus5 O2 → **§7.0 constants table + §7.0a exact serialized maxima and grammar** (12,315 B / 8,404 B, `test_budget.py` named as executable authority) — the structural fix for three consecutive rounds of stale-figure stragglers; every other mention now cites §7.0. Sweep (codex O3): both `+ 4k output` instances, all `≥ ~330k` window figures, D7(iii)/D8(iii)–(iv) rewritten to state only the final contract (superseded 96 B / 3 B-per-token / 4,000-4,000 / 2,000 derivations moved to the spec changelog), spec `Basis` gains the v0.7 + v0.8 reviews. opus5 O3 recorded: indexing narrows `selector_accounting_delta` (an invented expression is unrepresentable).
 - **v0.8 (2026-07-26)** — v0.7 confirmation round absorbed (codex REVISE N1–N4; opus5 CONCUR N1–N3; every checkable claim verified against the repo first: `Hit.evidence` present at spec §1.1; `MAX_SLUG_LEN = 120` at `common/paths.py:27` with `summary_slug.py` budgeting against it; thinking-disabled cohort in `models.json`; zero `tokens_lte_bytes` registry fields). codex N1: `Hit.evidence` removed from the public result contract (the field D8 dropped from the wire had survived in the public type). codex N2 (option 1): the wire now states **every** numeric maximum (`max_results` ≤ 50 globally; `matched` ≤ 10 unique indices; advisory `unresolved` indexed, bounded, delta signal intact); **allowances derive from the exact max serialization under `tokens_lte_bytes` — THIN 13,000 / FAT 10,000** (12.3k / 9.6k exact max bytes + buffer; tokens ≤ bytes by the invariant — **no density step anywhere**, closing the K1→L1→M1→N2 lineage); static guarantee 257 kB + 10k = 267k < 320k. codex N3 ≡ opus5 N1: slug bound = **`MAX_SLUG_LEN` imported from `common.paths`** (97–120 B slugs are valid graph data; a re-declared 96 B contradicted the canonical model; the render-time assertion now fires only on non-canonical input — a real defect; `kdb_search` imports `common`, no second constant). codex N4 + opus5 N3: **`tokens_lte_bytes` ownership** — a validated `models.json` boolean carried by `ModelSpec`, a declared per-route premise with its byte-level-BPE theorem, missing/false ⇒ typed config error at selector route resolution; all three D4 candidates declared + test-covered. opus5 N2: **§8.6 selector task variants** — the with-/without-`evidence` prompt axis rides the selector A/B (the cohort runs thinking-disabled; the drop gets confirmed on evidence).
 - **v0.7 (2026-07-26)** — v0.6 confirmation round absorbed (codex REVISE M1 + consistency sweep; opus5 CONCUR M1–M4; every checkable claim verified against the repo first: fat `evidence` field numerically unbounded at spec §2.2; three stale M=150/381 kB figures located and fixed). codex M1 + Joseph's drop call → **D8: minimal wire** — numbered expressions ⇒ `matched: [int]` indices (Python maps back; out-of-range = coerce-drop, R1 intact), **`evidence` dropped from the fat response** (nothing consumed it; the only free-prose field; ~60% of the worst-case response); allowances **THIN 4,000 / FAT 4,000 derived from the bounded compact schema** (100 × ≤ 96 B slugs ≈ ≤ 3.3k tokens; 50 × ~170 B ≈ ≤ 2.8k at 3 B/token) and asserted against **synthetic schema-maximum documents**; slug wire bound ≤ 96 B with a render-time controller assertion. opus5 M1: `budget_estimation_miss` re-typed **`budget_exceeded`/`detected: post_call`** — **excluded from the §8.4 selector-failure gate** (our mechanics, not the model's; would otherwise penalize smaller-window models). opus5 M2: §8.5 retention-pressure confound note (whole-graph arm discards ~39% at M=100) + arm's stage-1 recall reported alongside; reduced-M curve gains **M≈5/163 (~3%)** to bracket production's vault-scale regime. opus5 M4: `keys_emitted` carries original expressions, audit the rendered forms, affected indices flagged. codex sweep ≡ opus5 M3: R4 trailing clause + SD-4's last `recall@150` + the §7 381 kB figure synced to M=100/257 kB; §8 branch table names thin-preflight `budget_exceeded` and adds both `budget_estimation_miss` rows. codex's tokenizer premise recorded as the **named `tokens_lte_bytes` route invariant** (asserted at route resolution, not an unqualified law).
 - **v0.6 (2026-07-26)** — v0.5 confirmation round absorbed (codex REVISE L1–L3; opus5 CONCUR L1–L3; checkable claims repo-verified: all four SD-1 text fields schema-unbounded, `pass1_schema.py:77-89`; fixture slug distribution mean 24.9/max 64). codex L1 → **D7 (Joseph): M=100** — the fat request's absolute worst case (≤ ~257 kB bytes ⇒ ≤ ~257k tokens + 4k output) fits the pool's smallest budget (320k) **by construction** — codex's counterexample dies by sizing, not measurement; zero behavioral change at current scale (largest domain 51 < 100); **M=100 is the production constant** (reduced-M gate protocol unchanged). Thin stays estimate-guarded by design (unbounded N — the space is searched whole, Joseph's ratified principle); the residual underestimate is typed **`budget_estimation_miss`** (attempted-once, never retried, watched per model — codex option 2; "never a provider error" withdrawn for thin, unnecessary for fat at pool windows). opus5 L1: output allowances sized from response schemas — **THIN 3,000 / FAT 4,000** (D6's uniform 2,000 sat fat's response at 95% of ceiling at 3 B/token; the truncation ⇒ unparseable ⇒ retry ⇒ identical-overflow ⇒ hard-gate failure chain recorded; codex's 2,000 closed-list noted as superseded on evidence). codex L2: query ceiling now **per-field deterministic allocations** (author ≤ 256 B / keys ≤ 10 × 128 B / themes ≤ 1,024 B / summary ≤ remainder), per-field `query_truncated`, archived original+rendered forms. codex L3 ≡ opus5 L2: fat-`budget_exceeded` terminal's **complete contract** + two-row matrix + named F1 interaction. opus5 L3: SD-5 "thin tokens" → per-stage. Sizing/cost refreshed: fat ≤ 100 ≈ ~18k expected at vault; ~143M/$20/$107/$215 expected + 219M/$31/$164/$329 policy-ceiling re-ingest.
