# #123 P2.1a — Codex round-3 confirmation

**Date:** 2026-07-27  
**Commit reviewed:** `37be899`  
**Verdict:** **APPROVE-WITH-ITEMS**

The round-3 absorption closes the substantive R1–R4 findings. P2.1f is released to proceed. The sole item below is clerical and does not require another review round or delay the prompt pin.

## Confirmation

### R1 — H04 coverage

Confirmed. `_single_line()` is applied at every metadata interpolation in `render_thin_line()`:

- `slug`
- `title`
- `page_type`

This holds for every boundary in the production set, including `CRLF`; the sanitizer emits spaces without allowing a second physical line. There is no fourth metadata interpolation in the thin line. `render_fat_block()` inherits the same protection by calling `render_thin_line()`.

I also exercised all 3 fields against all 11 hard-coded test cases (the 10 single-character boundaries plus `CRLF`): all 33 cases remained one physical line.

### R2 — Independent oracle

Confirmed. `H04_BREAKS` is hard-coded in the test rather than derived from `_LINE_BREAKS`, is checked against the current Python interpreter's `splitlines()` behavior, and is asserted to be a subset of the production boundary set. Therefore narrowing `_LINE_BREAKS` by removing a known boundary fails independently of the production constant.

The parameterized test covers every field × every boundary and checks both the one-line property and exact space replacement.

### R3 — Corrected sanitizer property

Confirmed. The correct claim is:

- character count is preserved; and
- UTF-8 byte length does not increase.

That claim is stated consistently in the implementation docstring, the round-2 disposition and P2.7 plan text, and the property test. The test also correctly handles `CRLF` as two input characters becoming two spaces.

### R4 — Fat ranking instruction

Confirmed. The replacement asks the model to rank the entire supported candidate set by directness and strength before applying the cap, with tie-breaking by broader key/theme support. It no longer implies an unconditional full ranking of irrelevant candidates, does not privilege incoming evidence order, and is not over-instructed.

## Verification

- `kdb_search/tests`: passed with the authorized non-live, no-cache invocation; 32 expected skips and no failures.
- Direct H04 runtime probe: 33/33 field-boundary combinations passed.
- The disclosed mutation-harness restoration defect does not change this conclusion: the current source and tests were inspected directly, and the clean package suite plus the independent runtime probe confirm the absorbed behavior.

## Non-blocking item

The historical P2.7 narrative in
`docs/superpowers/specs/2026-07-27-task123-p2.1a-plan.md` still says `_single_line` is applied to “`slug` and `title`.” It should include `page_type` so the older section matches the implementation and the newer round-2 disposition. This is a documentation-only cleanup and is not a prompt-pin blocker.

## Release decision

**R1–R4 are closed. P2.1f is released to proceed. No further behavioral or prompt-prose change is required before pinning.**
