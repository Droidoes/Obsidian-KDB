# Phase B Migration — Qwen Review

## Verdict: GO-WITH-FIXES

The migration is **architecturally sound and behaviorally correct**. The six-package split honors the B.3 dependency contract, the resp_stats_writer split is clean (the lifted gate is byte-identical), and the guard tests are non-vacuous. **One HIGH-severity defect blocks v0.5.2 tag:** two historical migration scripts still import from the removed `kdb_compiler.manifest_update` and will crash on import. This is not a live-pipeline risk (the scripts are documented as dead), but it violates the "move-don't-duplicate, surface-don't-delete" constraint — the scripts should either be updated to the new paths or explicitly marked as broken with a `sys.exit(1)` at the top.

---

## Findings

### (a) Correctness bugs

**[HIGH] `scripts/migrate_task64_supersession.py:27` and `scripts/migrate_task66_compiled_hash.py:37` — broken imports from removed `kdb_compiler.manifest_update`**

- **What:** Both scripts import from `kdb_compiler.manifest_update`:
  ```python
  # migrate_task64_supersession.py:27
  from kdb_compiler.manifest_update import (
      _supersede_omitted_pages,
      assert_manifest_invariants,
  )
  
  # migrate_task66_compiled_hash.py:37
  from kdb_compiler.manifest_update import assert_manifest_invariants
  ```
  The `kdb_compiler` package no longer exists; these imports will raise `ModuleNotFoundError` on any attempt to run the scripts (even `--dry-run`).

- **Why it matters:** The scripts are documented as "HISTORICAL one-shot migration (already applied); references modules that no longer exist. Not part of the live pipeline." This is accurate — they're dead code. However:
  1. If a developer tries to run them (e.g., to understand a past migration), they'll get an unhelpful `ModuleNotFoundError` at import time, not a clear "this script is dead" message.
  2. The comment says "references modules that no longer exist" but doesn't say "this script will crash on import" — the comment is ambiguous about whether the script is runnable.
  3. The brief's §B.2 relocation map says `scripts/*` → "keep / `tools/migrations/`" with no decision. These scripts are in `scripts/` and will break the moment someone tries to use them.

- **Suggested fix (pick one):**
  1. **Update imports** to the new paths (if `manifest_update` was relocated to `orchestrator/manifest_writer` or similar). Verify the functions still exist and have the same signatures.
  2. **Add a hard guard** at the top of each script:
     ```python
     import sys
     sys.exit("ERROR: This is a historical migration script. The kdb_compiler package was retired in Phase B. This script is not runnable.")
     ```
  3. **Move to `docs/archive/migrations/`** (or similar) and remove from the repo's executable surface.

- **Verification:** `grep -rn "from kdb_compiler" scripts/` finds exactly these two lines. No other scripts are affected.

---

### (b) Test-fidelity gaps (vacuous/false-green/lost coverage)

**None found.**

- The **parsed_summary gate test** (`compiler/tests/test_parsed_summary_gate.py`) is non-vacuous — it tests the lifted condition (`parse_ok and isinstance(parsed_json, dict)`) at the compiler level, not just the default-arg behavior.
- The **resp_summary split test** (`compiler/tests/test_resp_summary_split.py`) is non-vacuous — it asserts `common/llm_telemetry` is a leaf by AST-walking the imports and checking no `compiler`/`ingestion`/`orchestrator`/`tools`/`kdb_compiler` imports exist.
- The **package-boundary guard** (`tools/tests/test_package_boundaries.py`) is non-vacuous — it asserts the B.3 contract via AST analysis and parametrizes over all six packages.
- The **D34 producer-agnostic guard** (`f13fecb`) was already de-vacuumed to check live `compiler/ingestion/orchestrator` names, not the removed `kdb_compiler`.
- **Test count:** 1191 non-live tests pass (prompt says 1175 → 1191, +16 new guard/split/render/gate tests). Verified: `pytest -q -m "not live"` passes with 1 skip (live-API smoke).

**Verification:** I ran the three new test files and all pass. The guard tests are substantive, not trivially true.

---

### (c) Packaging & data-file risks

**[LOW] Editable install metadata stale — will regenerate on reinstall**

- **What:** `.venv/lib/python3.12/site-packages/__editable___obsidian_kdb_0_1_0_finder.py` still references `kdb_compiler` in its `MAPPING` and `NAMESPACES` dicts:
  ```python
  MAPPING: dict[str, str] = {'common': '...', 'compiler': '...', ..., 'kdb_compiler': '...'}
  ```
  This is the editable install's path finder, generated by `pip install -e .` at the time of the last install (pre-Phase B).

- **Why it matters:** **It doesn't** — this file is regenerated on `pip install -e .` or `pip install .`. It's not checked into the repo (it's in `.venv/`). The prompt's §4 says "`pip install -e .` does **not** exercise `[tool.setuptools.package-data]` globs" — but that's a different concern (data files in wheels, not editable installs).

- **Suggested fix:** None. This is expected behavior for editable installs. The next `pip install -e .` will regenerate it with the new package names.

**Verification:** `pyproject.toml` `[tool.setuptools.packages.find]` include list is correct: `["kdb_graph*", "common*", "ingestion*", "compiler*", "orchestrator*", "tools*"]`. The `[tool.setuptools.package-data]` globs match actual files (verified via `ls`).

---

### (d) Cleanliness/naming nits

**[LOW] Cosmetic mentions of old package names in comments/docstrings**

- **What:** A few comments/docstrings mention the old package names:
  - `conftest.py` — comment: "Originally scoped to kdb_compiler/tests; promoted here"
  - `compiler/tests/test_resp_stats_writer.py` — docstring: "The kdb_compiler.resp_stats_writer module is removed"
  - `kdb_graph/tests/test_queries_context.py` — comment: "Cypher reads formerly authored inside kdb_compiler.graph_context_loader"

- **Why it matters:** **It doesn't** — these are historical/contextual comments explaining the migration. They're accurate and helpful for understanding the code's history.

- **Suggested fix:** None. These are intentional historical notes, not stale references.

**Verification:** `grep -r "kdb_compiler\|graphdb_kdb" --include="*.py"` finds only these cosmetic mentions, plus the two broken scripts in (a), plus the guard tests (which correctly check for the old names as string literals to ensure they're NOT imported).

---

## Additional checks (prompt §2–§8)

**§2 — The resp_stats split:**
- **Is `common/llm_telemetry` truly a leaf?** Yes. Imports only from `common.*` (atomic_io, call_model, run_context, types). No TYPE_CHECKING imports from compiler/ingestion/orchestrator/tools. Verified via `grep -n "^from\|^import" common/llm_telemetry.py`.
- **Is the lifted gate byte-identical?** Yes. Original (internal to `build_resp_stats`):
  ```python
  if parse_ok and isinstance(parsed_json, dict):
      summary = build_parsed_summary(parsed_json)
  else:
      summary = None
  ```
  New (lifted to `compiler/compiler.py:423`):
  ```python
  parsed_summary = (
      build_parsed_summary(state["parsed_json"])
      if (state["parse_ok"] and isinstance(state["parsed_json"], dict))
      else None
  )
  ```
  The condition is byte-identical. The only change is that `parsed_summary` is now a parameter to `build_resp_stats`, not computed internally.
- **Any telemetry field/value subtly different?** No. The `build_resp_stats` function signature and logic are unchanged except for the new `parsed_summary` parameter (which replaces the internal computation). All other fields (hashes, capture-full behavior, stop_reason, token fields) are computed identically.

**§3 — Vacuous tests:** None found (see (b) above).

**§4 — Data-file / path resolution:** All package-data files exist and match the globs in `pyproject.toml`. The Jinja template (`ingestion/enrich/pass1_prompt.j2`), schemas (`compiler/schemas/*.json`), vocab data (`ingestion/config/*.json`, `ingestion/config/*.yaml`), benchmark data (`tools/benchmark/models.json`), diagnostics schema (`tools/diagnostics/last_scan.schema.json`), and viewer template (`tools/viewer/kdb_graph_viewer_template.html`) all exist. **No risk of a wheel shipping missing data files.**

**§5 — Packaging:** `pyproject.toml` is correct. Entry points are updated to the new paths. `[tool.setuptools.packages.find]` include/exclude is correct. `[tool.setuptools.package-data]` globs are correct. `[tool.pytest.ini_options].testpaths` is updated to the new package structure.

**§6 — Import hygiene:** No leftover `kdb_compiler`/`graphdb_kdb` imports in live code. The two broken scripts in (a) are the only exceptions, and they're documented as dead code. No circular-import risk introduced by the split.

**§7 — The `orchestrator→tools.cleanup` exception:** Acceptable deferral. The import is isolated to one line (`orchestrator/kdb_orchestrate.py:37`), documented in the dependency contract (`tools/tests/test_package_boundaries.py:35`), and the brief explicitly says "decoupling deliberately deferred as a behavior change out of move-only scope." This is a known, bounded violation, not a layering problem.

**§8 — Anything the suite structurally cannot catch:**
- **The broken scripts in (a)** — the test suite doesn't run `scripts/*.py` (they're not in `testpaths`), so the broken imports are not caught by pytest. This is the one real defect the automated gates missed.
- **Editable install metadata** — not a defect (see (c) above).

---

## Bottom line

**Safe to merge + tag v0.5.2, with one fix:** update or guard the two broken scripts in `scripts/migrate_task{64,66}*.py`. The migration is behaviorally correct (the lifted gate is byte-identical, no logic drift in moves), the dependency contract is enforced by guard tests, and the packaging is sound. The broken scripts are not a live-pipeline risk (they're documented as dead), but they violate the "surface, don't delete" constraint — either fix the imports or add a hard guard so they fail clearly instead of with a cryptic `ModuleNotFoundError`. Once that's fixed, this is ready to ship.
