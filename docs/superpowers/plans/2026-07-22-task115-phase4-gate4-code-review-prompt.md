# Task #115 — Phase 4 (cross-boundary parity + system tests) — Gate-4 CODE Review Prompt (Codex)

> Sent **verbatim** to Codex CLI. Repo root: `/home/ftu/Droidoes/Obsidian-KDB`, branch `feat/115-pass2-contract`, base commit `15f16ff` (Gate 3). The change under review is the **uncommitted working-tree diff** on top of `15f16ff` — 3 modified files (+161/−9) plus 4 new paths. **This HEAD is the comparison-cohort anchor for Phase 5** — the last code gate. Write your review to `docs/superpowers/plans/2026-07-22-task115-phase4-gate4-code-review-codex.md`.

---

You are a senior staff engineer doing a **pre-commit CODE review** at the final phase gate. Phases 1–3 are reviewed and committed (Gates 2–3). Your job: verify the **Phase-4 implementation** — the wikilink parity corpus + system tests (blueprint §362–377) — is correct, complete, and has no regressions or scope creep. Be skeptical and specific; cite `file:line`.

## HARD GUARDRAIL — read first, non-negotiable
- **Read-only.** Do NOT modify, create, rename, or delete ANY file **except** the single output file named above. Write your entire review there and nowhere else.
- No state-changing git (`add`/`commit`/`checkout`/etc.), no `pip install`, no formatters.
- You MAY run tests: `.venv/bin/python -m pytest` (use the venv python — bare `pytest` resolves to a broken system install). Full suite currently claimed green: **1436 passed / 1 live-skip / 1 deselected**.

## Ground truth (ratified design — the diff must match THIS)
- Spec v1.6: `docs/superpowers/specs/2026-07-21-task115-pass2-contract-audit-findings.md` — **D-115-9** (canonical collisions: body is absolute authority), **D-115-10** (graph owns body→LINKS_TO derivation; compiler/graph parsers share a fixture corpus; system test: *final wiki-body wikilinks = live graph LINKS_TO = rebuilt graph LINKS_TO*), **D-115-14** (historical read-compat = graph rebuild + `kdb-validate`), validation matrix §D.
- Blueprint v1.10, **Phase 4**: `docs/superpowers/plans/2026-07-21-task115-pass2-contract-revision-blueprint.md` §362–377 (parity corpus with TWO expected outputs per case; live-vs-rebuild LINKS_TO equality scoped to LIFECYCLE-NEUTRAL NEW/CHANGED; mixed historical+new journal rebuild; replay new-shape end-to-end + legacy-negative classification).

## What the change is (intended behavior)
1. **Parity corpus** (`tests/fixtures/wikilink_parity/cases.json`, 12 cases): each case pins TWO expected outputs — extracted slugs AND post-rewrite body — driven against FOUR consumers: the compiler extractor (`compiler/validate_source_response.py::body_wikilink_slugs`), the mirrored graph extractor (`kdb_graph/intake.py::body_wikilink_slugs`), the canonicalizer rewrite (`compiler/canonicalize.py::_remap_body_wikilinks`), and the coercion rewrite (`compiler/repair.py::coerce_slugs_and_propagate`). Test-only shared data — no cross-package code import.
2. **Coercion rewriter token-semantics fix** (`compiler/repair.py`): building the corpus exposed that coerce had NO escape lookbehind and NO code-span protection (it rewrote `[[...]]` inside code spans and escaped brackets, unlike the other three consumers). Now aligned: `(?<!\\)` lookbehind + fenced/inline code-span segmentation for both the slug-value scan and the rewrite.
3. **Live-vs-rebuild LINKS_TO equality** (`kdb_graph/tests/test_rebuilder.py::test_live_vs_rebuild_links_to_equality_new_shape`): lifecycle-neutral NEW/CHANGED batch; three-way D-115-10 equality — final wiki-body wikilinks == live LINKS_TO == rebuilt LINKS_TO.
4. **Replay legacy-negative** (`tests/fixtures/response_replay/case04_legacy_negative/`): a pre-#115 old-shape response must extract+parse but reject at schema (`additionalProperties: false`), semantic never reached.
5. **Mixed historical+new journal rebuild (D-115-14)**: claimed ALREADY COVERED (Gate-2 F6 mixed-pair test, Gate-3 full-graph corpus, historical-sidecar validator fixtures) — no new code; verify the claim.

## How to see the diff
- Modified: `git diff HEAD` — `compiler/repair.py`, `kdb_graph/tests/test_rebuilder.py`, `tools/tests/test_response_replay.py`.
- New (untracked): `compiler/tests/test_wikilink_parity.py`, `kdb_graph/tests/test_wikilink_parity_graph.py`, `tests/fixtures/wikilink_parity/cases.json`, `tests/fixtures/response_replay/case04_legacy_negative/`.

## Pressure-test these (the load-bearing decisions)
1. **Corpus expectation correctness — independently recompute, don't trust the pins.** For EVERY case in `tests/fixtures/wikilink_parity/cases.json`, recompute by hand (or probe): (a) the extractor slug set under the strict-kebab + heading + escape + code-span contract; (b) the canonicalizer's post-rewrite body under the case's `resolve` map; (c) the coercion rewrite's output — remembering coerce derives its rename from `collapse_slug` (lowercase + collapse hyphens + strip edges) and REFUSES entirely on any un-coercible value. Flag any case whose pinned expectation is wrong or whose semantics hide a consumer disagreement the corpus should instead expose. Is the case set complete for the blueprint's list (plain / `|alias` / `#heading` / escaped / fenced-code / inline-code / duplicates / malformed)? Is anything important missing (e.g., heading+display combined, `[[Foo Bar]]` un-coercible refusal, uppercase fold)?
2. **The coerce fix — behavior-change audit.** `compiler/repair.py` now segments code spans for BOTH `_all_slug_values` and the rewrite. Consequences to audit: (a) a malformed slug that appears ONLY inside a code span no longer participates in the value set — could a payload that previously coerced-then-passed now fail schema (or vice versa)? (b) the escaped `\[[...]]` non-link is no longer rewritten — consistent with the extractors? (c) collision-refusal semantics change when a colliding value lives only in a code span — is the new behavior the intended one? (d) does the retry/gate flow in `compiler/compiler.py` still re-validate after coercion exactly as before? Check `compiler/tests/test_coerce_slugs.py` and the compile_one recovery tests still pin the gate flow.
3. **Live-vs-rebuild system test soundness.** `test_live_vs_rebuild_links_to_equality_new_shape`: does it actually prove the D-115-10 three-way equality (wiki-body == live == rebuilt), or does it over-constrain (assertions that pass only because all targets exist)? Are the negative edges (inline-code, escaped) genuinely exercised — would the test FAIL if the intake extractor regressed to counting code-span tokens? Is the lifecycle scoping really NEW/CHANGED-only (no MOVED/DELETED, which belongs to #116/#94)?
4. **Replay legacy-negative classification.** `case04_legacy_negative`: confirm the OLD-shape payload rejects specifically at the SCHEMA gate (`additionalProperties: false` on removed fields) and that replay short-circuits semantic — the expected flags (extract/parse True, schema/semantic False) and the `schema:` error prefix are the right classification. Do the updated count-dependent tests (fixture list, CLI `4/4`) match the new fixture set?
5. **D-115-14 coverage claim.** Mixed historical+new journal rebuild is claimed covered by: `test_rebuilder_mixed_shape_journal_pair` (Gate-2 F6), the Gate-3 corpus (`kdb_graph/tests/fixtures/gate3_mixed_corpus/`), and historical-sidecar validator fixtures (`tests/fixtures/compile_result.minimal.valid.json` et al). Verify these actually exercise BOTH shapes through rebuild AND `kdb-validate` — or is there a gap Phase 4 should have closed?
6. **Scope discipline.** Phase 4 is test-corpus + the coerce token-semantics fix ONLY. Flag any production-code change beyond `compiler/repair.py`, any Phase-5 cohort machinery, any #116 lifecycle work, any change to Gate-2/Gate-3 surfaces (canonicalize, intake, snapshot) that the corpus should have left untouched.

## Output — write ONLY to `docs/superpowers/plans/2026-07-22-task115-phase4-gate4-code-review-codex.md`
1. **Verdict:** `GO` (commit Gate 4 as-is) / `GO-WITH-CHANGES` (commit after specific fixes) / `REWORK`.
2. **Findings**, each: `[Severity: Critical | High | Medium | Low]` · `file:line` · the flaw · why it matters · concrete suggested change.
3. Group under: **(a) corpus correctness & completeness**, **(b) coerce behavior-change audit**, **(c) system-test soundness (live-vs-rebuild / replay)**, **(d) coverage claims & scope**. If a group is empty, say "none".
4. **One-paragraph bottom line:** is this diff safe to commit as Gate 4 — the Phase-5 comparison-cohort anchor — and what (if anything) must change first.
