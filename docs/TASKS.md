# Project Task Ledger

Canonical, version-controlled index of every numbered project task. This is the
**source of truth** for task numbering and status — daily notes link into it,
memory notes reference it by number, session TODO trackers are ephemeral and
must not be used as the authoritative list.

## Conventions

- **Stable IDs.** Once assigned, a task's number never changes and is never reused.
- **Status values.** `open` (not started) · `in-progress` (actively being worked) · `closed` (landed).
- **Closure.** `closed` is terminal — on landing, move the row to the `Closed` table (see "How to update this file"); terminal rows don't linger in the Open table.
- **Working statuses** (non-terminal; the row stays in the Open table):
  - `proposed` — idea filed; design pass still needed.
  - `hypothesis` — unvalidated design idea; not yet blueprinted.
  - `vX ratified` — blueprint ratified at version X (e.g. `v0.4 ratified`); implementation not yet landed.
  - `blocker (<scope>)` — known blocking defect, scope in parentheses (e.g. `blocker (pre-production)`).
  - `deferred` — consciously postponed; the Notes name the revive trigger.
  - `parked (2.0)` — shelved with the data-gated 2.0 tier; revisit only after corpus scale grows.
- **Closure proof.** Every closed task links to the commit SHA, doc, or memory note that carries the resolution.
- **Assignment.** New tasks take the next free ID. Gaps in the numbering are preserved — don't backfill.
- **Decomposition.** When a task is split, the parent stays at `in-progress` and its sub-tasks get their own IDs.
- **Separate tracks.** Architectural open questions live in `docs/CODEBASE_OVERVIEW.md` as `Open-1..Open-8`; they are NOT task-numbered here.

---

## Open / In-Progress

| #  | Status        | Title                                                         | Notes                                                                                         |
|----|---------------|---------------------------------------------------------------|-----------------------------------------------------------------------------------------------|
| 2  | open          | Scalability discussion                                        | Thinking-work. **Cost/latency baseline now produced by #109** (per-model cost-per-source-word + latency diagnostics in `measurements.json`) → the deferral-gate ("defer until benchmark baseline establishes real cost/latency numbers") is satisfied once #109 runs land. The discussion itself stays low-priority thinking-work given the single-user / low-volume workload ([[feedback_no_imaginary_risk]]). See #109. |
| 16 | open          | Stand up durable task ledger (`docs/TASKS.md`)                | **This doc.** Root-cause fix for losing track of numbered tasks between sessions              |
| 83 | parked (2.0)  | Hypothesis Promotion Contract — first-class boundary operator | **⏸ PARKED 2026-07-07 (data-gated 2.0)** — deferred with the whole Claim/Learn metacognition tier per the state-of-the-system review (`docs/2026-07-07-state-of-the-system.md` §8); same scale-hedge logic that abandoned the stress test (#113 Ph 3b); revisit only after corpus scale grows. Filed 2026-05-22 from Round 6 §9.4.2 closure (path = (a+) per §9.3.8 lean → Joseph ratification). Cross-cutting architectural contract that mediates every Analysis → Learn state transition. **NOT a Learn slot** per Round 6 (a+) decision (taxonomy hygiene + slot-inflation prevention per §9.3.8). Owns: input candidate shape (link-prediction candidates / community-detection partitions / structural-hole signals / LLM contradiction-or-reinforcement signals); output mutation types (belief-edge update / identity-split / abstraction-node-or-rule); confidence + provenance + supporting-path requirements; conflict checks against existing graph state; human-review thresholds (auto-promote vs. surface-for-review); predeclared eval criteria mirroring Task #75. **Designed in parallel with #84 (Belief Revision)** per §9.3.8 sequencing nuance + [[feedback_concrete_first_extract_later]] — the contract crystallizes from the concrete Belief-Revision case rather than being specified in isolation. Blueprint precedent: #74 (Canonicalization) + #75 (predeclared evals). External review preview: Codex + Antigravity pre-review on (a+) vs (b) at `docs/archive/tasks/task82-learn-architectural-options-{codex,gemini}.md`. Empirical hedge to watch: vanity-graph failure mode (§9.4.7 #1) if gates ship weakly. |
| 84 | parked (2.0)  | Belief Revision — Round 6 Learn slot #1                       | **⏸ PARKED 2026-07-07 (data-gated 2.0)** — deferred with the whole Claim/Learn metacognition tier per the state-of-the-system review (`docs/2026-07-07-state-of-the-system.md` §8); same scale-hedge logic that abandoned the stress test (#113 Ph 3b); revisit only after corpus scale grows. Filed 2026-05-22 from Round 6 §9.4.2 closure. First Learn mechanism (slot 1 of 3). AGM-philosophy + continual-KGE-engineering (per §9.3.2 convergent finding #2). Operates on edge / assertion weight + version state. **Folds in as sub-operations:** forgetting + temporal validity + decay + contraction (per §9.3.2 #2 caveat — engineering form is belief-base revision over finite syntactic state; classical AGM deductive closure intractable). **Designed in parallel with #83** — let the contract crystallize from this concrete case (memory [[feedback_concrete_first_extract_later]]). Anchors: §9.3.2 convergent finding #2 (M1 cleanest match), §9.3.6 candidate (a+) slot 1, working-hypothesis M1 from §9.1.1. |
| 85 | parked (2.0)  | Identity Refinement — Round 6 Learn slot #2 (deferred)        | **⏸ PARKED 2026-07-07 (data-gated 2.0)** — deferred with the whole Claim/Learn metacognition tier per the state-of-the-system review (`docs/2026-07-07-state-of-the-system.md` §8); same scale-hedge logic that abandoned the stress test (#113 Ph 3b); revisit only after corpus scale grows. Filed 2026-05-22 from Round 6 §9.4.2 closure. **Deferred until #83 + #84 land** — inherits the formalized Promotion Contract. Anchors: Opus minority split of working-hypothesis M4 per §9.3.3 Fork B (canonicalization=hygiene stays in #74 as compile-stage [6]; temporal/contextual splits=Learn here). Operates on identity state — committing the graph to claims like "Buffett-1990s ≠ Buffett-2020s" as distinct entities. Architectural separation from #74 preserved per §9.4.3 reclassification. Literature anchors: HyTE (Dasgupta et al. EMNLP 2018), Lin et al. 2012 NIL clustering, Kobren et al. 2019. Empirical hedge to watch: under-counted-Learn-surface (§9.4.7 #3) if #74 canon merges turn out to host genuine epistemic decisions. |
| 86 | parked (2.0)  | Abstraction / Principle Induction — Round 6 Learn slot #3 (deferred) | **⏸ PARKED 2026-07-07 (data-gated 2.0)** — deferred with the whole Claim/Learn metacognition tier per the state-of-the-system review (`docs/2026-07-07-state-of-the-system.md` §8); same scale-hedge logic that abandoned the stress test (#113 Ph 3b); revisit only after corpus scale grows. Filed 2026-05-22 from Round 6 §9.4.2 closure. **Deferred until #83 + #84 land**. Anchors: §9.3.2 convergent finding #4 (Abstraction is the missing slot all four research models endorsed). Three literature paths to evaluate per blueprint: (1) rule mining — AMIE (Galárraga et al. WWW 2013) / AnyBURL (Meilicke et al. 2020); at personal scale LLM-assisted is the viable form (§9.3.7 caveat); (2) schema induction — DL-Learner (Bühmann & Lehmann 2013); (3) LLM-driven summary-with-commit — extends GraphRAG (Edge et al. arXiv:2404.16130) past indexed-summary into materialized graph element. Empirical hedge to watch: **stranded-summary failure mode** (§9.4.7 #2) — blueprint MUST specify summary→graph-element materialization as a first-class step, not an optional output. |
| 87 | parked (2.0)  | Predeclared eval criteria for #83/#84 (Promotion Contract + Belief Revision) | **⏸ PARKED 2026-07-07 (data-gated 2.0)** — parked with its subjects #83/#84 (Claim/Learn metacognition tier) per the state-of-the-system review (`docs/2026-07-07-state-of-the-system.md` §8); the eval criteria + probe set stay ratified and ready, but implementation waits on corpus scale. Filed 2026-05-22 from #83/#84 blueprint v2 closeout (commit `01c6373`). Analog of Task #75 for step-3 ops — defines per-op pass/fail/gate criteria + hedge-watch rules BEFORE implementation lands. Owns the empirical-tuning OQs surfaced in the #83/#84 v2 blueprint: **OQ-6** (corroboration threshold N for `reinforces`); **OQ-9** (drift eval thresholds for fingerprint fast-path-skip eligibility); **OQ-20** (confidence map calibration `{low: 0.3, medium: 0.5, high: 0.8}`); **OQ-26** (confidence aggregation function tuning — bounded-mean-with-recency-decay default per D-83/84-12); **OQ-27** (drift action matrix knob calibration per D-83/84-8 Part D); **OQ-29** (`tau` default for confidence decay function — ~365 days starting guess). External review: Codex + Deepseek + Qwen (per `docs/external-review-panel.md`). **Blueprint v1 drafted 2026-05-22** at `docs/archive/tasks/task87-promotion-belief-revision-eval-criteria-blueprint.md` (~500 lines, mirrors Task #75 spine adapted for mutation-eval vs retrieval-eval frame per advisor input). **Critical adaptation:** Task #75 is retrieval-eval ("did the op return the right thing?"); #87 is mutation-eval ("did the op make the right state change?") — pre-state + input → expected post-state + invariants preserved. v1 specifies 3 operations (Promotion pipeline / Upgrade-from-LINKS_TO / Belief-sensitive read), per-op pass/fail/gate criteria (P-On-N / F-On-N indexing), 7 hedge-watch rules HW-1..HW-7 with shaped thresholds, invariants-reference (no re-litigation of #83/#84 §6 GraphDB contract delta), and 5 new OQs (#87 OQ-1..OQ-5). **Sequencing:** eval criteria SHIP BEFORE implementation per Task #75 precedent. **Sub-tasks:** ✅ #87.1 v1 ratified 2026-05-23 — probe-set curation — **20 scenarios** at `docs/archive/tasks/task87.1-probe-set-curation-blueprint.md` (~3000 lines). **Spike (S1–S4) drafted 2026-05-22**; **§6 D-87.1-1..10 ratified 2026-05-22** (D-87.1-5 with vocabulary correction — `implied_by_links_to` not in #83/#84 enum; O2 dispatch is derived from candidate_counterpart_found + null claim_id + non-null LINKS_TO ref); **expansion completed 2026-05-23 in 4 batches** — Batch 1 (S5–S9: 5 action-table cells `reinforces`/`qualifies-with-truth`/`qualifies-without-truth`/`supersedes`/`orthogonal`), Batch 2 (S10–S11: 2 upgrade tiers — Tier-2 SUPPORTS-overlap + Tier-3 synthesized-marker), Batch 3 (S12–S14: 3 drift cells), Batch 4 (S15–S19: state-transition idempotency + alias-canonicalized-between-runs + retracted-counterpart × 2 + sequential multi-candidate); **spot-check pass complete 2026-05-23** — added S20 to close F-O3-3 gap, patched S1–S4 missing `op_under_test:` field, registered OQ-S8 (F-O1-1 classification non-determinism structurally non-scenario-exercisable; lean = defer to classifier-test suite). **All 7 §7.1 coverage axes closed; 32 of 33 P/F criteria exercised in scenarios (F-O1-1 deferred per OQ-S8).** **Sub-tasks new OQs:** OQ-S1..OQ-S8 — implementation-detail follow-ups. Blocks #83/#84 implementation start until v1 ratified. **v1 review fired** at Codex + Deepseek + Qwen 2026-05-22; responses landed at `docs/archive/tasks/task87-blueprint-v1-review-{codex,deepseek,qwen}.md` (16 substantive findings; 3-reviewer convergence on O3 hybrid framing + P-O1-6 determinism + §9.4.7 hedge reconciliation). **v2 synthesized 2026-05-22:** O3 reframed as hybrid op (retrieval-eval + 1 mutation invariant); HW roster expanded to HW-1..HW-11 (added vanity-graph attrition / promotion latency / audit-trail growth / LINKS_TO↔Claim divergence); §7.2 probe template extended with `eval_config` block (clock + thresholds); new P-O1-7 disposition correctness + P-O1-8 retracted-counterpart resolution + F-O1-5 unauthorized-mutation guard; new F-O3-4 lazy-rewrite corruption; deferred-transition probe rows removed pending retraction trigger contract; 13 new OQs registered (OQ-6..OQ-18 — 4 HW-threshold + 9 reviewer-raised). **Status:** v1 ratified 2026-05-23 (v2 of #87 + v1 of #87.1 both ratified by Joseph). Unblocks #83/#84 implementation start. |
| 88 | v0.4 ratified | **Ingestion System** (renamed from "Ingestion Pipeline" per D-88-9 in v0.2) — multi-source-stream platform (end B of tunnel-from-both-ends) | Filed 2026-05-23 evening from Joseph's strategic pivot at session close. Project metaphor: "tunnel from both ends" — **end A (compile pipeline)** has had ~6 weeks of investment and is near-mature (#74 canonicalization + #76 Domain infrastructure + #83/#84 O1 Promotion Pipeline 15/15 GREEN); **end B (ingestion pipeline)** is essentially unstarted (files appear in `~/Obsidian/KDB/raw/` by hand; no preprocessing). Continued deepening of end A has diminishing returns when end B doesn't exist. **Pause end A deepening; focus all architectural design effort on end B.** End B is a **PLATFORM, not a single LLM pass** — pluggable adapters for multiple source streams (manual md drops today; future: RSS, podcasts, YouTube transcripts, web scrapes, PDFs, emails). All streams converge to a normalized internal form compile consumes. **Three "big things" identified:** (1) the ingestion framework/platform itself, (2) **domain/sub_domain classification — MOVED from end A** (was in flight as a compile-side prompt amendment after Path 0 surfaced #76 dormancy on real corpus; now lives in ingestion's metadata extraction layer with predefined enums + "others" fallback + domain ledger), (3) the LLM preprocessing pass that embeds frontmatter at top of source markdown + suggests wiki links at end. **Design discipline:** review the compile pipeline continuously during ingestion design to identify what should MOVE from end A to end B — **move-don't-duplicate**. Domain classification is the first concrete example. **Iteration goal:** ONE tunnel (not two parallel + not perpetually-unfinished); ship v1 end-to-end, then refine. **Open question this arc must answer:** does the Analysis-op (producer of `CandidateEnvelope` for #83/#84) live IN ingestion, IN compile, or as a 3rd pipeline? **Pattern:** brainstorm → multi-model parallel design (Gemini + GPT + Grok + Opus per Round 6 / #82 precedent) → 3-reviewer external panel (Codex + Deepseek + Qwen per `docs/external-review-panel.md`) → blueprint v1 → review → v2 → ratification → implementation arcs. Anchors: memory `tunnel-from-both-ends-pivot-2026-05-23`, `docs/archive/handoffs/session-handoff-2026-05-23-saturday-afternoon.md` §Strategic pivot. **Path 0 fire 2026-05-23 PM** (raw/ recompile exercising #74/#76 on real corpus) surfaced the empirical input that drove this pivot: #76 Domain field dormant in production despite shipped infrastructure (root cause: prompt under-instructs LLM to extract domain — confirmed via gemini-3.1-flash-lite vs deepseek-v4-flash:direct A/B test, same null output across both). The fix MOVES to ingestion, not patched in compile-side prompt. **Sub-tasks (filed):** ✅ **NW-4 v0.4 ratified 2026-05-25** (`9de41f7`) — 23-domain canonical Pass-1 vocabulary at `docs/archive/tasks/task88-nw4-domain-list-v0.4.md`; three-iteration arc in one session (v0.2 → 5-reviewer panel → v0.3 + new **D-NW4-5** (no pre-declared edges/hints) + parent blueprint `sub_domain` bug fix → v0.4 + new **D-NW4-6** (boundary axis: vertical/horizontal/temporal) + 3 renames + 1 merger). Memory generalization: [[feedback_no_edge_predeclaration_no_hints]]. #89 **Component #1 (Enrichment) deep-design** filed as next blocker. **Status:** scoping → blueprint v0.2 ratified → NW-4 v0.4 ratified → Component #1 next. |
| 91 | v0.2 ratified | `kdb-orchestrate`: E2E ingestion orchestrator + extended `kdb_scan.py` (subsumes Component #3 + #6 per v1 simplification) | Filed 2026-05-27 evening from Joseph's v1 simplification brainstorm. **Sub-task of #88** (ingestion system); **subsumes Component #3 (Trigger) + Component #6 (Orchestrator)** from #88 v0.2 per `[[feedback_no_imaginary_risk]]` + `[[feedback_concrete_first_extract_later]]`. Component #5 (move-from-compile survey) stays orthogonal. **Core idea:** manual-trigger + manifest-diff inside extended `kdb_scan.py` collapses v0.2's elaborate Component #3 design (filesystem-watching + 8-event taxonomy + batching) into a single scan-and-diff-then-route operation appropriate for single-user infrequent workload. Thin `kdb-orchestrate` CLI wires `[optional] feeders → kdb_scan → kdb-enrich → kdb-compile → kdb-clean orphans`. **14 decisions ratified 2026-05-27 evening across v0.1 + v0.2 (D-91-1..D-91-14):** D-91-1 single unified `manifest.json` / D-91-2 `.md`-only / D-91-3 orphan-cascade hands-off / D-91-4 `kdb-clean orphans` as final step / D-91-5 command name `kdb-orchestrate` / D-91-6 subsumes #88 Components #3 + #6 / D-91-7 real-time/scheduled OUT of v1 / D-91-8 fail-fast (Joseph overrode skip-and-continue lean) / **D-91-9 per-root MOVED detection** (2/2 panel β convergence) / **D-91-10 slim `state/last_orchestrate.json`** per-run summary (Deepseek payload spec) / **D-91-11 `--dry-run` skips feeders** (Deepseek Probe 3 unique) / **D-91-12 orchestrator uses Python API direct, not subprocess** (2/2 panel β convergence) / **D-91-13 two-phase failure model** (pre-commit vs post-manifest graph-sync per Codex F-4 critical unique catch — respects existing D50 trade-off) / **D-91-14 OQ-91-1 RESOLVED Shape A** (source-retraction via existing `event_type: "compile"` event with empty `compiled_sources` + DELETED `to_reconcile`; zero new mutation channel; Joseph picked over Deepseek's Shape B per v1 simplification spirit). **v0.2 blueprint:** `docs/task91-kdb-orchestrate-blueprint.md` (~700 lines post-fold, 14 sections + §13 amendments table + §14 reviewer-convergence summary). **Panel β:** Codex + Deepseek (2/2 guardrail-clean; 17 amendments — 2 critical + 6 high + 7 medium + 2 low; 2 deferred to post-v0.2 OQ-91-8 (MOVED+CHANGED edge case) + OQ-91-9 (`last_orchestrate.json` running marker)). Zero Joseph vetoes on panel findings; one architectural fork (OQ-91-1 Shape A vs B) resolved by Joseph. **Status:** v0.2 ratified; ready for TDD implementation plan write. **Next:** implementation plan at `docs/superpowers/archive/plans/2026-05-27-task91-kdb-orchestrate-implementation.md` (Phase A.0 source_state_update per-source API + A.1 ScanResult accessor + A.2 _rel_to_vault generalize + A.3 kdb_scan multi-root + B CLI skeleton + C source-retraction + D feeder triggering + E live smoke). Reviews: `docs/task91-v0.1-review-codex.md`, `docs/task91-v0.1-review-deepseek.md`. **Plan 1 (`kdb-compile` rebuild) written + reviewed 2026-05-29:** plan at `docs/superpowers/archive/plans/2026-05-29-task91-plan1-kdb-compile-rebuild.md`; workflow review (4-lens, 2 lenses succeeded — caught the `single_scan` crash) + 5-model external panel (Codex+Deepseek+Qwen+Grok+Gemini, all proceed-with-changes; reviews `docs/task91-plan1-review-{codex,deepseek,qwen,grok,gemini}.md`, synthesis `docs/task91-plan1-review-synthesis.md`). Panel convergence: 5/5 wiki-write-before-commit, 4/5 cross-source page-merge loss on one-element `cr` (code-confirmed — Grok dissent refuted), 4/5 thin error model, 3/5 context-snapshot seam; 1/5 import-cycle refuted. **Two forks ratified (Joseph, my-leans):** (1) `compile_source` = produce-don't-write (orchestrator owns wiki writes + provenance at commit boundary); (2) accept cross-source wiki last-writer-wins as single-user trade-off (graph stays authoritative; `kdb-audit`/#93 reconciles). Spec + Plan 1 revision pending. |
| 92 | hypothesis | NW-9 — EXISTING CONTEXT list T2/T3 redesign (domain-scoped key-driven T2 + 1-hop T3) | Filed 2026-05-29 from the `kdb-orchestrate` E2E walk (Pass-2 ingress "is T2 effective?" deliberation). Revives Task #26's parked effectiveness-measurement scope; revises #90's shipped STRUCTURED T2. **Hypothesis A:** scope T2 candidates by `domain` (BELONGS_TO sub-cluster) then fuzzy-match `entity_search_keys` as regex within scope — converts blind exact-slug match into scope-then-fuzzy. **Hypothesis B (coupled):** T3 strictly 1-hop (remove #71 cold-start 2-hop widening) — only safe if A delivers enough seeds; measure together. `source_type` drops out (no entity-level scoping edge — it's on `Source` only). Honest scope: structural retrieval, NOT semantic quality. Measured via NW-9 (new `DOMAIN_SCOPED` `T2Mode` alongside STRUCTURED/LEGACY/LAYERED + D-90-12 sunset gate). First validation = domain-edge-coverage check on real graph. Doc: `docs/nw9-context-list-t2-t3-redesign-hypothesis.md`. Memory: [[feedback_graph_over_vector_for_kdb]]. **Status:** hypothesis (not yet blueprinted). |
| 94 | blocker (pre-production) | `kdb-orchestrate` resume-correctness — fail-fast abort strands cross-source LINKS_TO | Filed 2026-05-29 from the pre-live-run advisor gate (Plan 5+6 execution). **The gap:** `_finalize` (which runs the C1 deferred `wire_links` batch pass) is inside `if abort is None`, so a mid-run fail-fast (D-91-8) leaves already-committed sources with entities+SUPPORTS in the graph but **zero LINKS_TO**, and no `compile_result.json`. A naive re-run sees those sources as `current_hash == last_compiled_hash` → `to_skip` → never recompiled → absent from the resume's `accumulated_crs` → resume's `wire_links` wires only new sources → **prior sources' cross-source links permanently lost**. Root cause: C1 defer-to-finalize assumed finalize always runs over the full batch; fail-fast breaks that, and the deferred per-source-`cr`-sidecar decision removed the resume path. **First-run unblock (throwaway sandbox only): on abort, `rm -rf` graph + `manifest.json` and re-run clean — do NOT resume.** **Fix options (before any non-throwaway vault):** (a) finalize-on-abort that wires `accumulated_crs` for what committed; (b) restore per-source `cr` sidecars so resume re-wires over the full set; (c) incremental prefix-rewire (Codex). Memory: [[project_orchestrate_e2e_design_spec]]. **Status:** blocker filed; NOT gating the first sandbox run. |
| 93 | proposed | `kdb-audit` — cross-store consistency auditor (GraphDB ↔ manifest ↔ wiki) + opt-in `--fix` | Filed 2026-05-29 from the Plan-1 panel review (Joseph's "GraphDB-audit-cleanup outside the loop" question). Generalizes three existing primitives — `kdb-clean orphans` (entity orphan reaping, D-91-4), `graphdb-kdb rebuild` (journal-replay graph reconstruction), and the #73 Phase G replay-to-temp structural verifier — from graph-internal to **cross-store referential reconciliation**. Read-only `audit` + opt-in `--fix`: orphan entities; orphan wiki pages (no graph/manifest entry — Finding-B dirty-disk residue); **wiki↔graph `source_refs` drift** (the accepted Finding-A last-writer-wins residue — graph stays authoritative, audit reconciles wiki from `SUPPORTS` truth); manifest↔graph staleness (case-b); dangling `SUPPORTS`/manifest entries. **Scope: structural/referential only, NOT semantic quality.** Keeps the orchestrator loop forward-only; run between/before runs as a pre-run gate. The reconciler home for #91's accepted cross-source wiki trade-off. Sibling to `kdb-clean`, not a replacement. Likely reuses Phase-G replay-to-temp for the cross-store diff. Memory: [[feedback_obsidian_wikilinks_are_vanity]], [[feedback_name_must_match_contents]]. **Status:** proposed (design pass needed). |
| 98 | deferred | Pass-1 classification benchmark — GT-based label *correctness* residual (distributional axis subsumed by #109) | **Narrowed + largely subsumed by #109 (2026-06-05).** #109's GT-free cross-model Pass-1 diagnostics — **domain breadth (÷23)**, **domain-null rate**, **signal/noise ratio**, **`entity_search_key_resolution`** — now cover the **distributional / model-comparison** purpose that was #98's revival trigger, surfacing gross classification-*behavior* differences (e.g. `science-technology` catch-all over-use) **without labels**. **Residual — the one axis #109 cannot do:** GT-based label **correctness** (the `software`-over-`ai-ml` call needs Joseph's hand-labels; + `source_type` correctness). **Explicitly not pursued:** no label corpus, low value with the standing Pass-1 model already settled on `deepseek-v4-flash`. **Revive only on** a concrete Pass-1 *correctness* complaint that justifies building a labeled set. **Dependency retained:** the #95 handling-policy half (per-field repair vs all-or-nothing) was data-gated on this benchmark and stays deferred — current two-stage all-or-nothing validation stands. (Original deferral 2026-05-30: proceed without a benchmark gate; classification lean was quality not robustness, deepseek 20/20 clean.) See #109. |
| 107 | open | Phase B follow-up polish (deferred panel-review items) | Filed 2026-06-02 from the Phase B panel review (synthesis: `docs/superpowers/archive/specs/2026-06-02-phase-b-review-synthesis.md`). Three non-blocking items deferred from v0.5.2: **(1) viewer packaging** — `tools/viewer/` has no `__init__.py`/console-script and package-data ships only `viewer/*.html` not `*.py`; a real wheel would omit the viewer module (moot today — editable-install only). Either make `tools.viewer` a real package + a `kdb-graph-viewer` binding, or document source-only + drop the HTML-only package-data (Codex). **(2) `compiler.compiler` double-name** — `from compiler.compiler import …` is awkward in traces/IDE; rename to `compiler/pass2.py` w/ `__init__` re-export (Grok). **(3) `orchestrator→tools.cleanup` decoupling** — move cleanup's core to `kdb_graph` so neither orchestrator nor the CLI couples to a tool; ratified deferral (behavior change — alters when the cleanup journal is written), out of move-only scope (Gemini). |
| 116 | proposed | Source-lifecycle convergence + durability (carved from #115; paired with #94) | Filed 2026-07-21 evening from the #115 blueprint review arc (Codex R8–R13). The reservation/MOVED/durability subsystem that accreted across 4 Codex rounds is its own task: global summary-slug reservation + occupancy policy (fail-closed incl. unowned/non-summary-occupant rejection); NEW/CHANGED/MOVED/DELETED + signal/noise/reactivation transitions (one complete lifecycle design — R12 F1 showed NOISE→signal reactivation breaks MOVED-only thinking); per-run replay material + matched-pair write protocol; crash-safe startup recovery; run-journal evolution; live==rebuild invariants. **Design seed (CANDIDATE, not ratified):** `docs/superpowers/specs/2026-07-21-task116-source-lifecycle-design-seed-v1.7.md` (the #115 blueprint v1.7, archived verbatim after R13 F1 caught it unrecoverable from git) — its write-ahead record + five-state recovery table are ONE candidate architecture; immutable commit bundles or a simpler model remain live alternatives, to be chosen AGAINST evidence collected read-only during WS2/WS3 preflight (R12 list: normalized derived-slug collision inventory, tombstone/move frequency, signal↔noise cases, interrupted-run artifact shapes, live-vs-rebuild divergences). The North Star simplicity rule ("no two-phase commits") makes the choice a first-class architectural decision, not a foregone WAL. **Pre-production gate for WS3, jointly resolves #94.** Status: proposed (design pass when WS3 opens). |
| 118 | in-progress | Split-model runs — cheap Pass-1 + quality Pass-2 in one orchestration | Filed 2026-07-22 from the same WS1 discussion as #117 (Joseph: "most models clear Pass-1 with flying colors — consider deepseek-v4 for Pass-1, gpt-5.4-mini for Pass-2"). Requires: orchestrator per-pass model selection (CLI / pool roles), `RunMeasurementHeader` per-pass model stamps, leaderboard row-key semantics for model pairs (key by pair? by Pass-2 model?), emit + recompute paths that honor per-pass models. (Parking "revive after #116" superseded by the 2026-07-24 revival below; #117's split boards already collect the per-pass evidence.) **Scope extension (2026-07-24, from #120 spec v1.3):** the Pass-1 search-key three-way decomposition — for each Pass-1 `entity_search_key`, classify resolved-to-pre-cohort-entity / materialized-during-cohort / unresolved-after-cohort (run-aware via canonical `first_run_id`; all buckets watched-not-scored) — is part of #118's evaluation-harness design, deferred from #120 (Codex R2/R3). Also the home for Pass-1 metadata-quality diagnostics (cross-model disagreement, signal flip rate, text-support). **REVIVED 2026-07-24 (Joseph) with the #120 three-model 4.0.2 matrix as evidence seed:** deepseek — clean but capped (0.883/0.257, 0% dangling, ~$0.10); gpt — dense (1.521) with amplification side effects (9.9% dangling, 4 self-links, ~$0.75); **gemini-3.6-flash — best of the 4.x era** (1.621/0.685, 0.7% dangling, both named sources clean, ~$1.37). **Fourth column (2026-07-24):** qwen3.6-flash — eliminated from Pass-2 (0.368/0.110, 33.3% dangling fabrication, 1 Pass-1 compliance block; family pattern holds) though the #119/#120 boundary absorbed its July contract failures (0 Pass-2 quarantines, both named sources clean); second to deepseek as cheap-P1 candidate. Acceptance experiment per Joseph: deepseek-P1 + [gpt\|gemini]-P2 vs gpt/gpt + gemini/gemini — closeness on Pass-2 KPIs, graph KPIs, named-source behavior; adopt the cheap Pass-1 if close. Also owns: Pass-1 metadata diagnostics (cross-model disagreement, signal flip rate, text-support); the Pass-1 search-key three-way decomposition was **carved to #122** (2026-07-24, Joseph — "do 2 first"). Status: in-progress (design pass opens next). |
| 121 | closed | Config-driven provider wiring — `ModelRoute` per pool entry + Class-B registry; `Settings` deleted; dual timeout knobs | Filed 2026-07-24 (Joseph: "one field for openai endpoint and another for api key name, to use less hardcoding and improve commonality"). **Architecture ratified 2026-07-24 (D1–D8, → D-121)** through Codex options R1–R3 + blueprint R1–R3: layered `ModelRoute(api_call_type, endpoint, api_key_env)` — required + whole-pool-validated per active entry, authoritative — plus a 9-row Class-B registry factory for route-less calls only; `api_call_type` sole closed dispatch selector; `provider` = free-form canonical identity; secrets resolved late by env-var name for all transports; `Settings` class deleted (config keeps `.env` loading + late-bound helpers); fail-hard two-gate validation with ordered runtime-type checks (D4); timeouts split connect/write/pool (`LLM_TIMEOUT_SECONDS`=120) vs read-silence watchdog (`LLM_INACTIVITY_TIMEOUT_SECONDS`=900, not a wall-clock bound); retry budgets documented (corrected matrix) + `max_retries=2` explicit. Artifacts: options v1.4 + blueprint v0.4 `docs/superpowers/archive/specs/2026-07-24-task121-config-driven-provider-wiring-{options,blueprint}.md`. Consumed by #118 (per-pass routes thread these kwargs). Status: **CLOSED 2026-07-24** — 5-commit chain `13816c5`→`208cf6f` (flash-lite drop → docs/P0 → P1 engine → P2 pool+threading → P3 timeouts+docs), suite 1659→1768 green (+109). North Star milestone + D-121 on record; production `.env` hand-migrated to `LLM_INACTIVITY_TIMEOUT_SECONDS=900`; optional live smoke not run (Joseph's call). |
| 122 | closed | Pass-1 search-key decomposition — **event-time context capture** (carved from #118/#120) | Filed 2026-07-24 (Joseph: "can we do 2 first — maybe task 122"). Deferred from #120 (Codex R2/R3), carved out of #118's evaluation-harness scope. **Architecture ratified 2026-07-24 (options v1.3, Codex R1–R3, → D-122):** the legacy `entity_search_key_resolution` resolves keys against the final post-run graph — self-fulfilling (Joseph: "you recalculate T2/T3 after the run and you always get the hit — it becomes meaningless"). The fix: **measure at the moment of identification** — a per-source `state/runs/<run_id>/context/*.json` record at context-build time (dedicated namespace; `pass2/*.json` belongs to the RespStatsRecord loader); two-product split (`ContextSnapshot` prompt-facing byte-identical / `ContextTelemetry` persistence-facing) keeps telemetry out of the prompt by construction; per-key dispositions (`resolved_t2_seed`/`already_t1`/`out_of_scope`/`duplicate_seed`/`unresolved`) with pre-cap `search_key_t2_seed_rate` + headline `context_t{1,2,3}_delivered_mean` ("count T1/T2/T3 like links"); age partition by `first_run_id` **equality** (pre-run/cohort/age_unknown); late-vs-never over `unresolved` keys only (final graph's two declared reads: legacy continuity + miss explanation); identity-based completeness reconciliation (coverage ≠ substantive population; aggregates fail-closed to `None`); both resolver paths enriched, parity pinned; legacy metric retained unchanged. Status: **CLOSED 2026-07-24** — 4-commit chain `fc60507`→`92108af` (docs/P0 → blueprint v0.5 → P1 capture → P2 aggregation), suite 1768→1956 green (+188). North Star milestone + D-122 on record; prompt bytes pinned identical by golden fixtures; legacy metric retained unchanged. |
| 123 | in-progress — **spec v0.16 + blueprint v0.14, D1–D11 all binding** (2026-07-27: D10 prompt ordering + D11 letter key-labels landed, suite 2511 green); **P1 COMPLETE 2026-07-26 (P1.0–P1.5); P2 IN PROGRESS 2026-07-27 — P2.0 (shared fake selector) + P2.1a (prompt templates, loader, D10 ordering) done + **codex review REVISE absorbed** — F1 thin cap-aware two-step retention, F3 fat ordering criterion + positive-evidence rule, F2/H04 identity-line line-break escaping fixed at the serializer (byte-neutral; Joseph authorized), F4 one checked version parser, F5 filed as #127. **Round-2 re-review REVISE also absorbed** — R1 `page_type` was still unsanitized (the `PageType` `Literal` is a type hint, not a runtime check: `schema.py:64` is `STRING`, `intake.py:325` writes it through, and a forged header got out — now all three identity fields are sanitized; the materializer-vocabulary half filed as #128), R2 the H04 oracle was derived from the production constant so a narrowed set stayed green (now hardcoded, interpreter-anchored, every field × every boundary), R3 "byte-count preserving" was false (`U+2028` is 3 bytes → 1; the real property is character-preserving + UTF-8 **non-expanding**, which is what the ceilings need and what is now asserted), R4 fat's "then the rest" removed + the rank-the-whole-set rule added. Codex judges the fat block **not** over-instructed. System+wrapper re-measured at fat 2,998 B / thin 2,507 B against the 3,072 B reserve — holds, but **74 B headroom means raising it to 4,096 B is an open owner call** (codex concurs, on the stronger ground of architectural capacity: the golden pin already forces version discipline, so the reserve need not) — **corrected 2026-07-27: this does NOT gate the P2.1f pins**, which freeze the *measured* rendered bytes and the version/SHA guard; the constant's only consumers are `fat_worst_case_request_bytes()` and two headroom assertions, so raising it later costs exactly what it costs now. **Round-3 confirmation CLOSED (codex APPROVE-WITH-ITEMS, `37be899`): R1–R4 all confirmed, P2.1f released, no further behavioral or prose change required.** He re-derived rather than trusting us — 3 fields × 11 boundaries = 33/33 one-line by direct runtime probe, no fourth interpolation site, `render_fat_block` inherits by delegation, and the R2 oracle now fails independently if `_LINE_BREAKS` narrows; on our disclosed mutation-harness restoration defect he inspected source and tests directly instead of relying on the 29/29. His one item was clerical (the P2.7 narrative still said `slug` and `title`) — fixed at both sites, plus a third staleness he missed (12 → **50** H04 tests, verified by `--collect-only`); suite 2677, mutation 29/29. **P2.1f DONE 2026-07-27 — golden byte pins (+18 tests):** hardcoded-literal pins (never computed from what they check) on digest/version/`repo_path`/half-bytes/overhead per stage, with the D-115-13 obligation stated in the failure message. Two pins the bullet did not imply: (i) the **constants** folded into the overhead are pinned separately, because the overhead is *not* a pure function of the template — widening `MAX_RESULTS` 50→100 moves fat's wrapper with every template byte identical, so the digest pin would stay green while the figure `fat_worst_case_request_bytes()` consumes drifts; a second test proves that coupling is real so the pin can't be "simplified" away; (ii) **D10 re-asserted on the rendered bytes**, since a hash plus byte counts survives a length-preserving EVIDENCE/QUERY swap. `git_commit` deliberately unpinned (moves every commit ⇒ would train blind re-blessing). Mutation 12: 10 caught by the file, 12 at package scope, both boundaries documented. **P2.2 DONE 2026-07-27 — `search.py` spine + zero-call terminals (+33 tests, mutation 18/18):** ends at one executable seam (`STAGE_CALL_SEAM`) so a zero-call terminal is reachable without the machinery that spends; every test drives `NeverCalled` + a raising `body_reader`, so zero-work is pinned structurally rather than by status. **Gate order recorded as a decision** (request before route — the caller's own input needs no config to judge; ratified text orders neither), pinned by the one case that violates both. `json_mode` **split**: the anthropic-cannot-honour check lands at resolution here (silent free-forming is the Pass-2 failure), the per-request assertion moved to P2.3. `domain_missing` stamped narrowly (closed `WatchedClass` admits one of spec §3.4's two reasons; fires only for `domain_subtree`). A second window fixture proves the **rendered evidence is in the estimate** — the 20k-window cases bust on the 29k envelope alone and so pin the terminal, not the inputs. `assert_result_contract` proved **wired** at each return site by injecting a non-conforming result (17/18 → 18/18). **Open, not decided:** full audit *delivery* — ratified §1.1 fixes `GraphSearchResult` at seven fields and `audit` is not one, so blueprint §2.1's "audit (always)" is an obligation, not a field; P2.2 builds it on every path and surfaces `search_snapshot_hash`, delivery deferred to P2.4. Full repo suite **2,727 passed / 32 skipped** | Semantic graph search | Filed 2026-07-25 from the #122 baseline cold/warm runs (evaluation: `docs/superpowers/evaluations/2026-07-25-task122-metric-baseline-cold-warm.md`). **The project's single objective: identifying relevant knowledge sources in terms of graphDB elements. That capability does not exist.** Observed: pass-1 search keys for well-known entities (`warren-buffett`, `charlie-munger`, `mohnish-pabrai`) get zero hits from a graph that contains rich knowledge about all three — the context loader impersonates semantic search with exact-slug matching, and the measured symptom is T2 delivered ≈ 0 when it should be full. Chicken/egg acknowledged and **solution discussion deferred**: the build loop already consumes the missing search on every compile (pass-2 context loading), so search cannot simply wait for a finished graph — but vision comes first, per Joseph's four-point problem statement (2026-07-25). **Sequence:** vision → spec → blueprint → implementation plan, all in this task; **#118 resumes after #123** (honest pass-1 quality reading is downstream of semantic resolution); #122's T1/T2/T3 event-time metrics stand unchanged as the measurement. **Vision v1.2 RATIFIED 2026-07-25 (D1–D10; opus5 + codex APPROVE):** `graph_search(query_text, search_space, k) → ordered [entities]` — graphDB-or-subtree as explicit input, caller-side scoping; pass-1.5 first adapter (batched wholesale selection per source); identity = return type, relevance = criterion; domain gate always on for context-build (Joseph's 3/3 override, on record); read-only; SearchSnapshot + persisted search artifacts for determinism/replay; P10 untrusted evidence; truth-set-before-tuning (#75 pattern); vault-scale sizing = pre-implementation gate. Doc: `docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-vision.md`. **Spec v0.4 + vision v1.5 RATIFIED 2026-07-25** (panel: codex REVISE×2→CONCUR-WITH-ITEMS, all 7 gate conditions closed/by-R3-sequence, dissent-for-record preserved; opus5 CONCUR-WITH-ITEMS ×3 rounds): **R4** always two-stage thin→fat (recall-oriented thin ≤M=150 ranked → fat over retained; no single-stage/routing; controller-enforced retain-all for N≤M ⇒ today equals single-fat by construction); **R1** per-entry salvage (6-of-10 rule; four-way response classes; `valid_entry_yield` → leaderboard) + retry-once → honest empty, **no deterministic machinery anywhere** (exact/alias declared not-a-valid-method — no fallback/annotations/delta/foregone-hits; all 3 T2Modes retire); **R2** uniform pre-flight budget rule (thin estimate > 80% of configured window ⇒ `budget_exceeded`, zero spend, no retry, no per-consumer distinction); **R3** narrower gate ("spec design approved; evaluation substrate open") with the D7 sequence: fixture + smoke test **first** (blueprint work item 1, `benchmark/truth/`) → probe draft → Joseph's labels + gates → experiments. SD-1 author included; SD-4 gate = reduced-M protocol (M=10/20 VI, 20/40 WG) predeclared before vault ingestion. Thin-visibility measurement (person-key recall ceiling 0–57%) recorded as D10's measured confirmation. Doc: `docs/superpowers/specs/2026-07-25-task123-semantic-graph-search-spec.md` (+ reviews/syntheses/concurrence prompt, commit `71e1951`). Blueprint: package boundary P9, selector model, fixture layout, estimator, T2Mode retirement. **Blueprint v0.3 + spec v0.5 amendments 2026-07-26** (panel rounds: codex REVISE→CONCUR-WITH-ITEMS ×2, opus5 F1–F8→G1–G8 — every checkable claim repo-verified): `kdb_search/` consumer-neutral sibling (`{common}` only), frozen P10-safe serializer (fat largest-150 = 107,885 B ≈ 27.0k tok; stage-2 policy-max bound 94k < 102.4k), ContextRecordV2 + hit-level provenance (`first_run_id` Entity property, no resolver), up-to-2-logical-attempt stage loop (one StageRecord per attempt), measurement contract (pass-1.5 OUT of scored union + cost-centre diagnostics + reconciliation invariant), calibration = provider-reported usage at end of P2 (Joseph fires). **Joseph's D1–D4 (spec §0):** D1 coding proceeds at blueprint ratification (experiments/tuning/ingestion + destructive P3b stay gated); D2 State C runs the search (`query_kind: state_c`; D-90-8 retired; 2/36 real empty-key sources); D3 no fat call on thin-empty N>M + `thin_retained_zero` (R4 amendment, codex dissent recorded); D4 screening cohort = gemini-3.6-flash/gpt-5.4-mini/deepseek-v4-flash. SearchSnapshot fixture v1 (`3d271e2`, 163 identities) + truth probes draft-v1 (39 probes, `a3c832a`) await Joseph's adjudication (his schedule). Docs: `docs/superpowers/specs/2026-07-26-task123-*`. **Confirmation round 2026-07-26 (codex REVISE 1–4, opus5 H1–H9 incl. the removal audit — all repo-verified) folded as blueprint v0.4 + spec v0.6:** stage-2 bound made enforceable (excerpt policy v2 — rendered fat entry hard-capped 2,500 B, projector-enforced; binds on nothing in fixture v1, max 2,209 B; 94k < 102.4k now a code property, not a corpus statistic); spec normative body synced (enum gains `fat_after_thin_failure` at both sites, branch call contract replaces "exactly two calls", 55k/64k superseded, stale gating retired, sizing refreshed to fixture measurements); calibration artifact separated from the frozen fixture; V2 kept-fields / #119-pin / never-surfaced enumeration restored. **Joseph's D5:** calibration = one non-comparative provider-usage call per candidate (3-call ceiling), Joseph fires at end of P2 — narrow D1 amendment. **v0.4 confirmation round 2026-07-26 (codex REVISE K1–K3 + minors, opus5 CONCUR J1–J4 — all repo-verified) folded as blueprint v0.5 + spec v0.7:** **Joseph's D6** — R2 amended: one budget calculation method for BOTH stages (fat gains the same calibrated pre-flight estimate + 80% guard; typed `budget_exceeded`, stage recorded, zero spend, never retried); full-request accounting (evidence ≤ 375 kB + query ≤ 4,096 B + system/template ≈ ≤ ~381 kB worst-case bytes; `OUTPUT_ALLOWANCE_FAT` = 2,000); rendered-query byte ceiling 4,096 B with `query_truncated` telemetry (`summary`/`key_themes` schema-unbounded — verified); "conservative"/"provably fits ≥128k" relabeled calibrated-with-headroom (codex K1 ≡ opus5 J1; tokenizer-preflight option rejected — no local tokenizer, gemini-only counters). R3 row synced with D1/D5; gate named "recall at the predeclared reduced-M points"; fixture v1 immutable + sibling policy-v2 compat artifact (2,209 < 2,500 proof); 160M expected-case + 273M/$38/$205/$410 policy-max expense record; envelope-count = successful-write completeness. **v0.5 confirmation round 2026-07-26 (codex REVISE L1–L3, opus5 CONCUR L1–L3 — all repo-verified) folded as blueprint v0.6 + spec v0.8:** **Joseph's D7 — M=100 + the honest budget settlement.** Fat's absolute worst case (≤ ~257 kB bytes ⇒ ≤ ~257k tokens + 4k output) fits the pool's smallest budget (320k) **by construction** — codex's counterexample dies by sizing, not measurement; zero behavioral change at current scale (largest domain 51 < 100); M=100 is the production constant. Thin stays estimate-guarded (unbounded N, searched-whole principle) with the residual typed `budget_estimation_miss` (attempted-once, never retried, watched — codex option 2). Output allowances sized per response schema (THIN 3,000 / FAT 4,000 — opus5's measured truncation-into-hard-gate chain; codex's 2,000 closed-list superseded on evidence); query ceiling per-field allocations (author/keys/themes/summary); fat-budget terminal's complete contract (codex L3 ≡ opus5 L2). Cost refreshed: ~143M/$20/$107/$215 expected + 219M/$31/$164/$329 policy-ceiling re-ingest. **v0.6 confirmation round 2026-07-26 (codex REVISE M1 + sweep, opus5 CONCUR M1–M4 — all repo-verified) folded as blueprint v0.7 + spec v0.9:** **Joseph's D8 — minimal wire:** numbered expressions → `matched: [int]` indices (Python maps back; out-of-range = coerce-drop, R1 intact); **`evidence` dropped from the fat response** ("who's going to use it? nobody" — the only free-prose field, ~60% of the worst-case response); allowances THIN 4,000 / FAT 4,000 **derived from the bounded compact schema** (synthetic schema-max tests — codex M1); slug wire bound ≤ 96 B + render-time controller assertion. opus5 M1: `budget_estimation_miss` re-typed `budget_exceeded`/`detected: post_call`, **excluded from the §8.4 selector-failure gate** (our mechanics, not the model's). opus5 M2: §8.5 retention-pressure note + reduced-M curve gains M≈5/163 (~3%) to bracket production's vault regime. opus5 M4: `keys_emitted` = original forms. Stale sweep: last `recall@150`/381 kB/M=150 figures synced; `tokens_lte_bytes` named route invariant. **v0.7 confirmation round 2026-07-26 (codex REVISE N1–N4, opus5 CONCUR N1–N3 — all repo-verified) folded as blueprint v0.8 + spec v0.10 (D8 mechanics completed):** `Hit.evidence` removed from the public contract (codex N1); the wire states **every** numeric maximum (`max_results` ≤ 50 globally; `matched` ≤ 10 unique indices; advisory `unresolved` indexed); **allowances = exact max serialization under `tokens_lte_bytes` — THIN 13,000 / FAT 10,000, no density step anywhere** (closing the K1→L1→M1→N2 lineage; static guarantee 257 kB + 10k = 267k < 320k); slug bound = **`MAX_SLUG_LEN` imported from `common.paths`** — the re-declared 96 B contradicted the canonical 120 (opus5 N1 ≡ codex N3); `tokens_lte_bytes` = validated `models.json` boolean carried by `ModelSpec` with its byte-level-BPE theorem, missing/false ⇒ typed config error at selector resolution (codex N4 + opus5 N3); §8.6 with-/without-`evidence` axis rides the selector A/B (opus5 N2 — the cohort runs thinking-disabled, the drop gets confirmed on evidence). **Blueprint v0.9 + spec v0.11 2026-07-26** (v0.8 confirmation absorbed by opus5 taking the drafting seat: codex O1–O3, opus5 O1–O3): **D9 drafted** — provider-envelope output split (visible 13,000/10,000 + `HIDDEN_OUTPUT_RESERVE` 16,000, sum sent as `max_tokens` and reserved; `budget_side: output` terminal, gate-excluded; static guarantee 283k < 320k, window ≥ ~355k), `MAX_EXPRESSIONS` = 10 declared in the core contract as the sole derivation source for the wire's maxima, and **§8.6 removed** (opus5 retracts N2 — its thinking-disabled premise is false: gpt runs `reasoning_effort: low`, gemini `thinking_level: minimal`, both billed as output). Plus the §7.0 constants table + §7.0a exact serialized maxima (12,315 B / 8,404 B) as the structural fix for three rounds of stale-figure stragglers. **Blueprint v0.10 + spec v0.12 2026-07-26** (codex REVISE P1–P4 absorbed; he APPROVES both pending D9 decisions and `MAX_EXPRESSIONS`=10): D9's *executable* contract completed — post-call output terminal's three complete outcomes (thin / fat-after-thin / fat-after-F1; F1 confined to retry-exhausted failures), **stop-reason normalization** (a live gap: gemini returns uppercase `MAX_TOKENS` while both existing predicates match only `length`/`max_tokens`, so truncation on the interim default selector would have misgraded into the selector-failure hard gate), cap-stop-AND-no-usable-document predicate preserving R1's salvage rule, `InvalidGraphSearchRequest(code="max_expressions_exceeded")` named, and a seven-item consistency sweep (all opus5's own defects, incl. §7.0's false no-restatement claim). Filed out of scope: `common/llm_telemetry.py:152`'s `token_overrun` predicate misses gemini for pass-1/pass-2 too — pre-existing, independent of #123. **Blueprint v0.11 + spec v0.13 (2026-07-26) — panel review COMPLETE:** codex returns **APPROVE / CONCUR-WITH-ITEMS** on v0.10, re-affirms both D9 decisions and `MAX_EXPRESSIONS` = 10, and states no further concurrence round is needed before ratification and P1. One correction absorbed, opus5's own: the wire's exact maxima were computed from a *one-based* index list under `separators=(",", ": ")` while §2.3 is zero-based — **8,251 B / 12,314 B**, break-even **22 expressions** (not 8,404/12,315 and ~15). No decision, allowance or guarantee moves (the true margin is wider); the serializer is now a named constant in blueprint §7.0 and the §12 test parameterization repaired to a fits/exceeds pair (10 / 22) that actually demonstrates the bound. New **D9.6** (codex's ruling): a truncated attempt never enters `valid_entry_yield`'s denominator; `None` stands; no new exclusion marker; the obligation is presentational. Gemini stop-reason defect filed as **#124**. **D9 CONFIRMED by Joseph 2026-07-26 — both decisions (the option-2 envelope split with its 16,000 owner-selected policy reserve; the §8.6 removal). D1–D9 are all binding; spec v0.14 + blueprint v0.12 carry no open design item, and codex's v0.10 APPROVE already stated no further concurrence round is needed before ratification and P1.** Next: **Joseph's explicit Proceed opens P1** (per D1, coding does not wait on probe labels or numerical gates — those run in parallel on his schedule; live experiments and the destructive P3b still wait for labels+gates). **P1 COMPLETE 2026-07-26 — `kdb_search/` is the repo's eighth package, everything that does not call a model:** P1.0 packaging + boundary rows (`kdb_search` → `common` only); P1.1 projection — the §5 grammar byte-exact incl. both G7 clauses, excerpt policy v2's 2,500 B ceiling, and the query block's per-field allocations (option C: the renderer lives in the core, the ratified `QueryPayload` untouched); P1.2 response salvage + accounting (four-way classification, per-entry drop, per-field coerce, `valid_entry_yield` `None` at zero returned); P1.3 budget estimator + the four D9 envelope quantities + `tokens_lte_bytes` on `ModelSpec` + the M=100 static guarantee (283,168 < 320,000, and asserted to FAIL at M=150 so the constant is provably load-bearing); P1.4 audit payload + the two hashes (snapshot = what was searched, integrity = what happened), built on **every** path including the zero-call ones; P1.5 the terminal contract matrix — 13 named terminals in `contracts.py` with `assert_result_contract` as P2's fail-closed return-site guard, three rows driven through real producers today and ten marked as shape. Every §7.0a figure reproduces from the implementation rather than being copied (12,314 / 8,251 / the whole break-even table). **Mutation testing was adopted as standing practice mid-phase** after tests were found passing without exercising their subject; it subsequently caught `bool`-as-a-wire-index (a wrong attribution that looks correct), three P1.4 hash gaps, and one unreachable check in P1.5 that was deleted rather than kept. Suite **1963 → 2494**. Two ratified-text gaps recorded, not papered over: blueprint §8 carries two *input*-side post-call terminals the P1.5 bullet did not enumerate (now in the matrix), and three cells (`fat_exhausted`'s evidence side and concordance, `fat_input_estimation_miss`'s evidence side) are genuinely unenumerated and are marked `UNENUMERATED` rather than inferred. Plan: `docs/superpowers/plans/2026-07-26-task123-p1-implementation-plan.md`. **Spec v0.16 + blueprint v0.14 (2026-07-27) — two owner amendments landed as one batch before P2, deliberately: both touch the same sections, and D11 had been held pending #125's Q1, which Joseph closed the same day.** **D10** — invariant-first prompt ordering: `EVIDENCE` before `QUERY` in the USER message, both stages, asserted by test. The ratified order put the ≤ 4 kB per-source query ahead of the ~250 kB block that is identical for every source sharing a space at a given graph state, forfeiting prefix reuse and sitting the query ~250 kB from the generation point. **Zero code cost** — `kdb_search/prompts/` is empty, so P2 now builds it right the first time instead of rewriting later. **D11** — expressions addressed by **letter key-label** (`A`, `B`, …) rather than zero-based index (amends D8): a letter is not an ordinal, so the 0-vs-1 base ambiguity — a *protocol* ambiguity yielding *systematic silent* mis-attribution — ceases to exist, and every wire identifier becomes a verbatim echo of something printed in the prompt. **This retires by construction the exact hazard mutation testing caught in P1** (`bool`-as-a-wire-index: a wrong attribution that looks correct), so D8's `isinstance(bool)` guard is dropped — a `bool` is not a `str`. Lowercase labels are **case-folded**, not counted. Re-derived by test: FAT exact max **8,251 → 9,271 B** (+1,020 B = 51 quoted label lists), break-even **22 → 14 expressions**. **Nothing else moves** — the `"A. "` marker is byte-identical to `"0. "`, so the 4,096 B query ceiling, the ≤ ~257 kB request bound, `max_tokens` 26,000 and the **283k < 320k static guarantee** all stand, thin stays 12,314 B, and the FAT allowance stays 10,000 (9,271 ≤ 10,000 is a proof, not a buffer). The one real cost — 4 under the cliff instead of 12 — **strengthens** D9.2's consumer-neutral bound. **R4's thin-stage rationale replaced** with Joseph's stronger argument (no behavioural change): the largest domain projects to **~490 vs M=100** post-vault-ingest, and retain-all currently *masks* every thin defect — so the development window is the only window in which finding them is free, since after N > M the same defect is silent, unrecoverable data loss. **One near-miss recorded:** `test_zero_escape.py`'s fuzz generator emitted integer wire values; under labels every one would have become an unknown coercion, leaving the sweep behind the project's only non-diagnostic gate passing while testing nothing. Generator widened + a liveness test now asserts it reaches both decode outcomes. Suite **2498 → 2513** — including a projector↔validator seam test (printed markers fed back as `matched`), both new behaviours mutation-checked, and `_bounded_labels` given a *total* vocabulary helper so an over-long request cannot turn the never-discard contract into discarded-by-exception. **P2.3 DONE 2026-07-28 — `stage_call` (+62 tests, mutation 28/28):** shipped as its own module (`kdb_search/stage.py`) — `graph_search` decides *which* stages run, `stage_call` owns what happens inside one; `search.py` untouched, so P2.2's zero-call terminals stay provable without the machinery that spends. Four outcomes leave it and only one is ordinary (`usable` / `output_truncation` / `input_estimation_miss` / `exhausted`). **Three things the bullets did not say.** (i) There was **no thin-side document classifier** — `validate_thin_retention` takes an already-parsed list, so nothing could classify a thin response at all; added `response.validate_thin_response`, whose classification reads the RETURNED entry count and never the validated one, because `{"retained": []}` and an all-foreign document both validate to `()` and take opposite branches (D3's honest empty, never retried, vs `all_entries_dropped`, an allowed retry class) — the fixture had been carrying that warning since P2.0 with no production counterpart to warn about. (ii) `StageRecord` had **nowhere to archive a stop reason**, though §8 requires raw + normalized per record; added both (plus `sdk_sub_retries`, §8 G5) and folded them into the integrity hash, not the snapshot hash. (iii) `StageOutcome.validated` is `None` on `exhausted` **deliberately** — a retry-exhausted stage produced no answer, and exposing its last parsed document invites the exact F1 bug the path exists to prevent. D9.3's conjunction is tested from both sides (cap stop + complete document = R1 salvage; cap stop + no usable document = the terminal), and the stop-reason map is driven **directly as a unit** because the anthropic row is unreachable through `graph_search`. **P2.4 DONE 2026-07-28 — the two-stage flow (+42 tests, mutation 27/27):** thin→fat wired end to end — retain-all at N≤M (controller-enforced, manifest order so fat stays unanchored to thin), the F1 path, the D3 terminal, both fat budget terminals, concordance, and the audit built on every path through **one `finish` helper**, so neither the audit nor `assert_result_contract` can be omitted from a return site. **Two findings.** (i) **A gap in the ratified contract matrix:** the F1 treatment reached the fat PRE-flight and OUTPUT terminals but not the fat INPUT one, which reads as an ordering artifact (the D7 rows predate D9.3's F1 treatment) rather than a decision — and the state is reachable, so a legitimate search would die fail-closed on a `ContractViolation`. Added `FAT_INPUT_ESTIMATION_MISS_ON_F1` with every cell **copied** from the two rows that bracket it, the evidence side left UNENUMERATED as its parent leaves it, and `EXTENSION, NOT RATIFIED TEXT` as the note's first words — **OPEN: wants owner ratification.** (ii) The contract guard's budget check required *every* matching record to be a non-fit, which P1.5 could write safely with no producer; a fat pre-flight rejection legitimately carries thin's PASSING record too, and `BudgetRecord` exists to keep exactly those. Narrowed to the last record of the class. Concordance gained a third null case the bullet does not name — thin produced no validated retention at all (F1) — distinguished from a thin that ran and honestly retained nothing, where 0.0 is a real measurement. **One honest sweep correction:** the `request.max_results` mutation first reported caught, but by a `NameError`; re-run with a literal it left the suite green, because every test used the default cap. Two tests added at a non-default cap — the prompt states the cap and the validator counts `over_cap` against it, so a call site rendering the global constant charges the selector under a rule it was never given. **P2.5 DONE 2026-07-28 — §8's branch table as a parameterized oracle (+119 tests):** 19 cases, each asserting the table's call range, `logical_call_count == len(StageRecords)` off the archive, the guard passing, the script fully consumed, **the terminal the row NAMES** (several rows share a call count, so a count-only oracle passes on a search that took the other branch), and the archived attempt counts against P1.5's matrix bounds — which is where the two ratified artifacts meet. **The table prints 12 rows and names 11 distinct paths**: row 9 is a generic restatement of rows 2 and 12, so its claim is asserted directly rather than a case invented for a path the controller does not have. Ranged rows are covered at BOTH ends, listed explicitly after a first attempt inferred them from a hyphen and matched `thin-preflight` — and that check found a real hole (`fat exhausted` covered only at 3 calls). The archive is reached by wrapping `build_audit_payload` / `assert_result_contract` with pass-through recorders, so the open audit-delivery question is not pre-answered. Verified independent: a one-spurious-retry mutation fails 42 cases in this file alone. **P2.6 DONE 2026-07-28 — `replay.py`, the two non-live modes (+22 tests, mutation 14/14):** record replay validates the integrity hash and returns **`ReplayedSearch`, not a `GraphSearchResult`** — §5.2's words are "the persisted historical selection", and the archive holds exactly that: six of the seven result fields reconstruct, `telemetry` does not, and `budget_records` **cannot** be derived at all since pre-flight verdicts were never archived. A result with `budget_records=()` would read as "the estimates were taken and were zero" and enter the D5 series as a measurement. No-call/no-body-read is pinned **structurally** — `replay_record` takes neither, so a mode that cannot be handed a selector cannot invoke one. Historical re-call sends the **archived bytes verbatim**, stamped `historical_recall` on the outcome itself (the rule is about presentation, so the stamp travels with the result); it needed **no accommodation in `stage_call`** — P2.3's decision to take `messages` as a parameter makes "bypasses the projector by construction" literally true — and it therefore inherits the whole attempt/retry/stop-reason contract rather than becoming a second place for D9.3 to drift. **Fat's re-call pool is the archived EVIDENCE, not the manifest** (above M they differ; the manifest would accept a slug the selector was never shown). Also fixed from advisor review of shipped work: **`ARTIFACT_SCHEMA_VERSION` 1 → 2** (P2.3 changed `StageRecord` and the integrity digest; free now, a migration once the adapter writes a file), and an **exhausted fat stage now reports a real 0.0 yield over its returned entries** instead of `None` — a selector that hallucinated its whole answer twice was filing its strongest quality signal into the §8.4 series as "no data". The blueprint's #119 `context_snapshot` bullet is **misfiled** — that is `build_context_snapshot` on the compiler side — and its disposition is now asserted as an absence in `kdb_search`'s surface. **P2.7 DONE + P2 CLOSED 2026-07-28 (+32 tests, mutation 8/8; repo suite 2,513 → 3,021):** P10 adversarial fixtures — 5 payloads, each a different MECHANISM (imperative, forged section header, forged block delimiter, template slot, forged system turn) across H01 (title), H02 (excerpt) and H03 (query side). **Each fixture asserts two things**: structural containment (injected text arrives as content, SYSTEM half byte-identical, no slot substituted) AND output-side fail-closed — every fixture also scripts a selector that **obeys** the injection and asserts nothing foreign leaves the function. Testing only the first proves the prompt is tidy while leaving a compromised selector unexercised. One assertion was rewritten after failing for the right reason: "no `QUERY:` at column 0" fails on a CLEAN render, since the template legitimately has one — the checkable claim is that injection cannot ADD one, so the fixtures compare against a benign control. Recorded non-finding: `Hit.title` carries the space's value verbatim; sanitization is render-side containment, not a mutation of data — a result that rewrote a title would report what the vault does not contain. Close-out mutation check 8/8: both D10 swaps at both stages, both stop-reason mutations, plus four P10 containment mutations the new fixtures are what catch. **P2 CLOSED — P2.0–P2.7 all shipped, every behaviour swept (12/18/28/27/14/8), the §8 branch table verified as an oracle independent of the flow tests. TWO ITEMS FOR JOSEPH, neither blocking: (1) the audit-DELIVERY surface — `graph_search` builds the payload on every path and does not return it; the P3a adapter needs the whole payload to write its envelope, and it changes a public signature; nothing in P2 presupposes an answer. (2) the `FAT_INPUT_ESTIMATION_MISS_ON_F1` matrix extension. Plus the parked `SYSTEM_TEMPLATE_BUDGET_BYTES` 3,072 → 4,096 call. D5 CALIBRATION — HARNESS READY, AWAITING THE OWNER (2026-07-28). Branch pushed to origin (19 commits). `tools/task123_calibrate_estimator.py` built and dry-run verified (+27 tests, mutation 16/16): dry run is the default and `--fire` is required to spend, the 3-call ceiling is an object that raises rather than a loop that happens to run three times, the artifact is rewritten after every candidate so a late failure cannot lose an earlier paid measurement, and the checksummed fixture is fingerprinted before and after and asserted unchanged. Dry run at the current pins: 16,849 rendered B (system 2,446 + user 14,403), ÷4 estimate 4,213 tokens, all three D4 candidates pre-flight `fits`. **BLOCKED ON the selector prose review** — published at `docs/superpowers/specs/2026-07-28-task123-selector-prompt-prose-review.md`; blessing the prose is what freezes the bytes, and a word changed afterwards renames `_v1` → `_v2`, moves `GOLDEN_DIGESTS` and invalidates three paid measurements. That doc also carries the one input decision calibration needs (the query slot — the harness defaults to empty, the only choice re-derivable from the fixture alone). Predicted finding, recorded in advance: slug-heavy identity text tokenizes nearer 3 B/token than 4, so the ÷4 estimator is expected to be shown UNDERESTIMATING — which is what the gate exists to produce.** **PROSE REVIEW CLOSED + D5 CALIBRATION FIRED 2026-08-02 (Joseph).** Three prose findings absorbed and both templates re-versioned `_v1` → `_v2` per D-115-13 (rename + pin update + prose in ONE commit): (i) **fat's opener carried thin's "supplied to you in full" — false for fat whenever N > M**, since `search.py:371` filters stage 2 to thin's retained pool, and false on the D5/D7 fixture itself (163 vs M=100); it also left fat the only stage never declaring what evidence it receives. The concrete risk was `unresolved` reading as "the graph has nothing" when it means "thin's shortlist has nothing". Rewritten. (ii) **fat's unseen-body IMPERATIVE cut as redundant; the premise REMAINS** — the excerpt policy caps at 250 words and only **2 of 163** fixture excerpts are capped (median 65, max 262), but **the precision tax is NOT closed by this edit**: it comes from the surviving "silence is weak evidence against" sentence, which still applies to all 163 including the 161 complete bodies, and `render_fat_block` still does not render `projected.truncated` so the discount cannot be conditioned — **open observation for a future `_v3`**, recorded not claimed-closed; the **title-only half STAYS** and is clean (defends the corpus-independent `body_missing` degrade). (iii) thin's "the second pass reads each retained entity's body" corrected to *excerpt*. **"BEST FIRST" KEPT** on Joseph's challenge: the cap binds at 163-vs-100 so thin must rank regardless, and emitting the order it already computed is the only thing keeping `_concordance`'s `retained[:20]` non-trivial — dropping it means dropping concordance. **`SYSTEM_TEMPLATE_BUDGET_BYTES` RAISED 3,072 → 4,096** (blueprint **v0.15**) — the fix did not fit in 74 B of headroom, so the parked call became a prerequisite, not an independent one; guarantee recomputed **258,192 B ⇒ 284,192 < 320,000** (35,808 slack), no allowance/ceiling/wire figure moved. Fat overhead 2,998 → 3,074, thin 2,507 → 2,521; suite **3,051 passed / 34 skipped** green. **D5 RESULT — the recorded prediction was directionally right and materially WRONG, and the gate turns on the difference:** predicted ~3 B/token (1.333x underestimate, would break the 0.8 headroom); **measured gemini-3.6-flash 4,542 tok / 3.713 B-per-token and deepseek-v4-flash 4,481 tok / 3.763 B-per-token — a ~6-8% shortfall (~1.07x), comfortably inside the 1.25x the headroom absorbs.** Re-worked at each candidate's own pre-flight boundary both FIT (901,529 vs 1,048,576; 848,515 vs 1,000,000), so the earlier "authorizes a request that does not fit" claim is **withdrawn on evidence**. **Ruling PROVISIONAL — `ESTIMATOR_BYTES_PER_TOKEN = 4` STANDS on the two measured candidates**, headroom unchanged, `budget_estimation_miss` stays typed and watched; **the D5 gate is two-thirds discharged, not closed** (a third candidate would have to tokenize below ~3.2 B/token to threaten the 1.25x headroom, far outside the 3.71-3.76 observed, so the no-change decision is safe either way). Artifact `benchmark/truth/task123_search_calibration_v1.json`; record `docs/superpowers/specs/2026-07-28-task123-selector-prompt-prose-review.md`. **gpt-5.4-mini UNMEASURED — the OpenAI account has no credits (429 `insufficient_quota`); one of the 3 D5 calls was spent on it. It is the pool's smallest window (400k), so thin's ESTIMATOR boundary is reached soonest there — a claim about the estimator, NOT about the D7 static guarantee, which holds by `tokens_lte_bytes` and is sizing not measurement. **Re-run trap:** `--models gpt-5.4-mini --fire` costs one call but `write_artifact` REWRITES the artifact from the current run's list and does not merge, so a single-candidate re-run would DISCARD the two paid measurements. Fix `write_artifact` to merge by `model_id` first (filed as the prerequisite), or copy-aside-and-hand-merge.** **PANEL ROUND 1 (codex + kimi) ABSORBED + SIMPLIFICATION AMENDMENT RATIFIED 2026-08-02.** Panel 2/2 killed our own claim that a prose change "invalidates the three paid measurements" — **false for fat**, since the calibrator renders thin only (`tools/task123_calibrate_estimator.py:199-201`); the error had inflated the cost of the open fat item. Also absorbed: P2 plan gate checklist synced (it still read BLOCKED), the provisional ruling softened off cross-provider extrapolation, and our merge follow-up corrected — merge-by-`model_id` **replaces** copy-aside rather than preceding it, and needs a **full artifact-header fingerprint guard** or it silently combines incomparable rows. Pushed back on kimi's zero-cost tiktoken proxy: not installed, needs a network install (#127's blocker), and `encoding_for_model('gpt-5.4-mini')` has no registry entry — a guessed encoding presented as a measurement is what D5 exists to prevent. **D-123-A…E RATIFIED (Joseph — chief objective: *simplify*):** `M` **100 → 150**, narrowed to thin's retention ceiling; stage-2 pool **dynamic 1–150**, filled from thin's rank to the 0.8 budget then presented in manifest order (**§3.4 preserved, not amended** — the cut was always thin's judgment; §3.4 governs presentation, and this gives `BEST FIRST` a second load-bearing consumer); **all body-text truncation DELETED**; D7's static guarantee **withdrawn** for a stronger property — *a request that does not fit is never constructed*; **fat pre-flight and fill become one operation**; `excerpt` → `body`; fat prose → `_v3`. **The evidence that justified deletion:** the byte ceiling fired **0/163** fixture and **0/83** live — *never*; the word cap 2/163 and 0/83; `ProjectedEntity.truncated` was computed and **read nowhere**. Live compiled bodies max at **129 words** because pass-2 writes them, so the ceiling was sized for a corpus that does not exist — while forcing the fat prompt into a claim false for 161 of 163 entities, which *was* codex's HIGH precision finding. Deleting the mechanism deleted the defect. Recorded as blueprint **v0.16** + spec amendments; amendment doc `docs/superpowers/specs/2026-08-02-task123-simplification-amendment.md`. **Deliberately NOT removed:** output truncation (`stop_reason`, D9.3/D9.4) and the query-block ceiling — the latter filed as an open candidate to be decided by measurement, since pass-1 metadata is model-authored and genuinely unbounded unlike compiled bodies. **NEXT: implement the amendment** (its own commit, on top of the v2/v0.15/D5 work), then a fresh `_v3` prose review. OPEN owner calls: audit DELIVERY surface (changes `graph_search`'s signature) and the `FAT_INPUT_ESTIMATION_MISS_ON_F1` extension.** **v0.16 IMPLEMENTED 2026-08-02 (suite 3,058 green, uncommitted).** Fork A + Fork B code landed against the ratified D-123-A…F. Deleted: `_excerpt_policy_v1`, `_truncate_to_block_ceiling`, `ProjectedEntity.truncated`, `EXCERPT_WORD_CAP`/`SENTENCE_EXTENSION`/`BLOCK_CEILING_BYTES`, `EXCERPT_POLICY_VERSION` (it was live on every fat artifact and in the snapshot hash, not just the fixture manifest), `fat_worst_case_request_bytes`, `fat_static_guarantee_tokens`, `ValidatedResponse.advisory_unresolved`, `selector_accounting_delta`. Added: `budget.fat_input_byte_allowance` (derived from `preflight`'s own inequality, boundary-asserted both directions), the fill loop, `stage2_pool_size`/`stage2_budget_bound`. `M` = 150; `excerpt` → `body` (field AND rendered label — the label is 3 B shorter, so every hydrated block shrank); fat prompt `_v3`. **Two unanticipated consequences, both recorded not absorbed:** (1) `VISIBLE_OUTPUT_ALLOWANCE_THIN` **13,000 → 20,000** — M=150 carries thin's `M × MAX_SLUG_LEN` exact max to 18,464 B, straight through the old allowance; D-123-A's "no longer an input to any guarantee" is true of the input budget and false of the output wire (`max_tokens` thin → 36,000, under gemini's binding 65,536). (2) `FAT_PREFLIGHT_BUDGET` changed shape — it is no longer reachable by a small window, since any route passing thin's pre-flight has room for an entity; only a single oversized body reaches it, and the branch-table rows were rebuilt on that. **NOT committed — the gate is open**; three commits planned (v2 prompts+D5 / v0.16 docs+Fork A / `_v3`+Fork B), fat `_v3` prose awaiting the owner read. **P3a LANDED (phases 0–4), 2026-08-03→08-04:** six-commit chain `63dd795`→`128631b` on `feat/123-semantic-graph-search`, each phase TDD-gated — **P3a.0** foundations (64K envelope, `entity_first_run_ids`, cost projection) · **P3a.1** `compiler/search_adapter.py` (unwired) · **P3a.2a/2b** ContextRecordV2 + pass-1.5 wired into `compile_source` step 1 — the PK/regex T2 seeding family is DELETED whole (REPLACE ruling) · **P3a.3** dispatching context-record loader (V1+V2) + KPI consumers on V2 · **P3a.4** §4.7 `SearchPassMeasurement` + header counters + board columns. Suite 3,021 → 3,102 passed (exit 0). **⚠ KPI re-baseline:** all seven #122 `search_key_*` series are cut in P3a.3 (one rename, one per-key→per-hit re-baseline, three clean cuts per blueprint §4.6) — **historical `search_key_*` values are NOT comparable across the re-baseline**; the V2 series (`search_expression_matched_rate`, `search_hit_recency_*_rate`, `search_stage2_budget_bound_rate`) start fresh. **P3a.5 = the docs/close-out pass** (North Star milestone + AGENTS.md sweep + this row + session handoff 2026-08-04). Outstanding: the sandbox full-chain run (Joseph fires `scripts/sandbox-run.sh` under the split gate — Pass-1 prompt-1.3.0 output inspected/accepted BEFORE pass-1.5 results are read); production graph `~/Obsidian/KDB/graph` is EMPTY (P3a.0 projection) — first real run populates it. Post-ratification amendment recorded below. |
| 124 | open | `token_overrun` / truncation predicates miss gemini's `MAX_TOKENS` stop reason | Filed 2026-07-26 out of #123's v0.9 confirmation round (codex P2; both panel seats concur it is a live defect needing its own ID rather than a fix inside #123's feature diff — changing shared telemetry there would mix a pre-existing defect and a watched-series re-baseline into semantic search). **Defect:** `common/llm_telemetry.py:152` computes `token_overrun = stop_reason in ("max_tokens", "length")` and `compiler/compiler.py:411` gates its truncation terminal on the same tuple, but `_call_gemini` surfaces the enum *value* — uppercase `"MAX_TOKENS"` (`common/call_model.py:250-254`). **Consequence: gemini truncation is invisible to `token_overrun` and to the compiler's truncation guard for pass-1 and pass-2 today**, and gemini-3.6-flash is the current interim default. Scope: decide whether normalization becomes a shared `common.call_model` boundary primitive (returning both raw and normalized) or stays consumer-side; table-test every pool route; note that fixing it **re-baselines a watched KPI series**, so the change needs its own before/after record. Must preserve `compiler.py:405-411`'s ruling that a cap stop is carrier metadata, not proof of absence (`test_compiler_recovery.py:200-224`). Sequenced after #123. |
| 125 | **BOUNDARY CLOSED 2026-07-27 (Joseph)** — residual question is capacity/performance, not architecture | The Pass-1 / Pass-1.5 boundary: does the metadata stage earn its keep? | **OWNER DECISION 2026-07-27 — Joseph, explicit and final:** *"we will keep the current workflow as-is, Pass-1 ↔ Pass-1.5 ↔ Pass-2, this is decided and closed… it makes a lot of sense logically, and architecturally to do it this way… cost is not the only consideration."* (Rationale not elaborated on record by his choice; he offered to if needed.) This closes the merge question in **all** its forms — the Pass-1↔Pass-1.5 merge (already closed by domain circularity + thin→fat) **and** the Pass-1.5↔Pass-2 adjacency surfaced 2026-07-27 after the Pass-2-input correction, which was blocked only by costs (cadence, failure coupling, ContextSnapshot ordering) rather than structure. Do not reopen either without an explicit owner reversal. **Reclassified with his concurrence:** the residual live question — *does body access improve stage-2 selection?* — is **no longer an architecture question**; it is a capacity/performance question, localized to **Pass-1.5 stage-2**, and settled by experiment. Predictions pre-registered before firing: `docs/superpowers/specs/2026-07-27-task125-stage2-representation-predictions.md`. | Raised by Joseph 2026-07-26 mid-#123 P2 planning ("why go through the trouble of Pass-1, if the only purpose of Pass-1 is to produce metadata … why not just use Pass-1 with the source doc to produce T1/T2/T3?"), with his own note that it should have been asked before Pass-1.5 was designed. **Problem statement: `docs/superpowers/specs/2026-07-26-task125-two-pass-boundary-question.md`** — deliberately options-free and recommendation-free per Phase 1, written to double as an external-panel brief (every claim carries `file:line`). **Why it became askable only now:** `entity_search_keys` was introduced by D-89-20 with one documented consumer — "sole consumer = Task #90 context-loader T2-rewrite" (`ingestion/enrich/pass1_schema.py:34-35`) — and #123 *retires* that consumer while silently repurposing the field as the selector's query expressions. While the keys fed a deterministic PK lookup, imperfect keys were harmless by design ("imperfect slugs simply miss, no harm" — `pass1_schema.py:79-84`, after a strict regex rejected real emissions like `see's-candies` in a 2026-05-26 live fire). As LLM query input they are load-bearing, so the separate metadata call needs re-justifying rather than inheriting. **Framing narrowed (the question is not answerable as posed):** T1 (SUPPORTS) and T3 (structural BFS) are deterministic graph traversals, not LLM outputs — only **T2** comes from search. Answerable Q1: *should T2 selection happen in a second call over a ≤4 kB metadata projection, or in the same call that already holds the full source body?* Pass-1 receives `{{ source_text }}` in full (`pass1_prompt.j2:141`); Pass-1.5's selector never sees the source, only a 4,096 B rendered projection of five Pass-1 fields. **Record-correction inside the doc:** an earlier claim in discussion that Pass-1 output makes a source *findable* is false — `compile_source` is produce-don't-write (`compiler/compiler.py:688`) and graph intake is `apply_compile_result`, i.e. Pass-2 output (`kdb_graph/intake.py:1-7`), so findability requires Pass-2. There is no independent write-side justification for the split. **Q2:** review the Pass-1 prompt's `entity_search_keys` instructions (`pass1_prompt.j2:68-80`). **Q2b:** `author`'s sentinel defect, root-caused to prose — "null otherwise" (`pass1_prompt.j2:60-61`) makes the model emit the 4-char *string* `"null"`, which `{"type":["string","null"]}` accepts (`pass1_schema.py:76`); 9 of 39 #123 probes carry it, 5 carry genuine null, 19 a real author. Pass-1.5-side normalization already decided (Call 2, 2026-07-26); the upstream prompt fix belongs here. **Coupled to the headroom question (queue item [2]):** the static guarantee leaves ~37 kB spare (283k of 320k). Measured over the population that matters — the **25 probe sources that resolve to real vault files** — bodies are median **34,029 B**, max **96,311 B** (the `benchmark/sources/` median of 45,442 B agrees), so a full-body query would consume nearly all the spare at the median and break the guarantee above ~37 kB. A first reading that used all 1,909 vault `.md` (median 2,398 B) and concluded the cap was unforced is **corrected in the doc** — that population is mostly generated pages, not ingestion sources. The merged alternative therefore has to trade evidence budget against body budget (smaller M, fewer hydrated candidates, or body truncation); it is not a free substitution. **Decidable on evidence:** each probe carries `payload_origin: sandbox-frontmatter:<path>` and 25 of 39 resolve in `~/Obsidian` (the other 14 are kimi-drafted synthetic queries with no originating source), so a projection-vs-body arm is runnable on classes A/B/C against the normative precision@5 and recall. **Blocks:** the batched spec v0.16 / blueprint v0.14 amendment (Call-1 letter labels) is held until Q1 settles — no point re-deriving §7.0a if the answer restructures the passes. **Does not block:** #123 P2 selector implementation, whose contract is settled. **Formal response filed 2026-07-26: `docs/superpowers/specs/2026-07-26-task125-two-pass-boundary-response.md`** — position: *keep the stage, re-derive the interface*. The stage re-earns its keep on three grounds independent of the retired consumer (envelope factorization — the merged call is graph-aware × source-complete, the one call the 320k guarantee cannot hold; the boundary is the ratified intrinsic/relational frontmatter line D-89-16 expressed in call structure; cadence asymmetry — merged, every re-selection at N>M steady state re-pays ~34 kB of body to recompute fields that cannot change). Answer space priced as an escalation ladder A (prompt fix, current 4 kB interface) ⊂ B (widen projection from ~37 kB spare) ⊂ C (honest merge = classifier + body-carrying selector; exchange rate ~14 hydrated candidates at median source, ~39 at max). Predeclared decision rule set before data: §6.1 paired arms P/F(+optional P−k keys-ablation), materiality delta ≥0.10 on a normative metric with paired corroboration; panel convenes only if the gap survives both Q2 revision and B's widest budget. Q2/Q2b prompt review unconditional under every branch. **Codex response (`…-response-codex.md`, `CONCUR-WITH-ITEMS`) and SYNTHESIS (`…-two-pass-boundary-synthesis.md`) filed 2026-07-26.** 2/2 convergent: ratify the *boundary*, not the projection; Q1 conflates boundary vs representation (a body-informed selector needs no merge); C not viable under D1–D9; predeclared decision rule required; keep A as baseline / run B as experiment; fix the Pass-1 prompt regardless. **Codex's 4 unique catches, all independently verified:** (1) **thin→fat** — final T2 selection *is* the fat stage, "always preceded by the thin call, R4" (spec:105), so a merged call given only identities does *thin* selection, a second structural invalidation of "one call" needing no byte argument; (2) **append vs replace** — the brief said "substituting" but used the append ceiling: append ≤36,832 B, **replace ≤40,928 B** (257,168 `test_budget.py:316` + 26,000 = 283,168 `:323` vs `SMALLEST_POOL_BUDGET_TOKENS` 320,000, itself already 400k×`BUDGET_HEADROOM` 0.8 so the spare is *not* double-discounted); (3) **probe denominators** — 25 resolvable = A18/B3/C2/**F2**, and F01/F02 are `empty_override` (abstention structurally forced, spec §283) so the scorable denominator is **23 over 13 unique documents**, not 25; the 14 unresolved are D3/E5/G3/**H3** (H omitted from the brief's enumeration); (4) **hold M constant** in the initial arm or query quality confounds with candidate retention. **Synthesis's own findings:** (a) **fable's C-pricing was falsified twice, and the second time is the load-bearing one** — the first error divided body bytes by 2,500 while ignoring both the 36,832 spare and the 4,096 freed by replacing the query; the correction then quoted the **wrong gate**. `budget.py` keeps two quantities apart and warns that conflating them *"would turn a by-construction argument into a measurement"*: `worst_case_input_tokens` (`tokens == bytes`, the **static guarantee's pathological bound** → max body 40,928 B) vs `estimate_input_tokens` (`ceil(bytes/4)`, **what `preflight` actually runs** → max body **922,928 B**, a **22.5× spread** exceeding the largest vault file at 199,092 B). Measured with the shipped functions: **all 13 scorable documents pass pre-flight with M=100 and no truncation**, largest 96,311 B → **113,346 of 320,000** tokens. So the candidate cost of a full-body query is **zero on every probe document**; what the largest 5 forfeit is the **by-construction guarantee**, not the request. **Consequences:** the exclude-or-truncate fork for arm F **dissolves** (and would have biased toward A, since the largest docs are where a 4 kB projection should lose most) → run all 13 whole; arm F is a **production-viable configuration**, not just an instrument; and the envelope is **retracted as an argument against the merge at either gate** — C must be answered with thin→fat + domain circularity, never with bytes. **Answer to [2], strongest supported form:** the headroom concern is **almost entirely a property of the proof discipline, not the calls** — shipped fat worst case is **28%** of the usable window at the estimator vs **88.5%** at the pathological bound (window itself already discounted 20% by `BUDGET_HEADROOM`); the operative question is whether the by-construction guarantee must cover the query block or may rest on the estimator+headroom guard every other request uses; (b) **codex's B ≡ fable's C** — the merge **saves no call at all** (classifier stands per domain circularity, thin stands per thin→fat, and `summary` must still be produced by someone for the GraphDB `Source.summary` property) → **C is empty, not merely dominated: closed independently by domain circularity and thin→fat; external panel retired**. **CORRECTION 2026-07-26 (Joseph caught it):** the synthesis first claimed *"`Source.summary` is Pass-2's authoritative input"* and treated it as a third structural blocker — **both wrong**. Pass-2's user prompt is `source_name` + a 4-field PASS-1 METADATA block + **the entire source body** + **the T1/T2/T3 ContextSnapshot** + schema + exemplar (`compiler/prompt_builder.py:190-207`); "authoritative" is only a *trust instruction* — *"do NOT re-derive … no need to rewrite or merge it"* (`:132-145`). `summary` is one line, not the principal input. Downgraded from blocker to **call accounting** in both docs; the merge's closure now rests entirely on domain circularity + thin→fat. **This correction opened the live question up:** Pass-2 is a working precedent for presenting an LLM **both** the full body **and** a distillation with trust labels, so stage-2 representation is **not** the binary projection-XOR-body both responses assumed — a **PF arm (projection AND body)** was added to the experiment, and without it a P-vs-F result cannot separate *"the body helps"* from *"the projection hurts"*; (c) part of the 36,832 spare is **unbanked** — `SYSTEM_TEMPLATE_BUDGET_BYTES` 3,072 is *"a declared reserve, not a measurement"* with a P2 obligation to assert real templates and raise it (`constants.py:128-135`), costing 4 B of proof-bound ceiling per byte of overrun — so this binds B **only if** B elects to preserve the static guarantee, and is immaterial to arm F. **Disagreement resolved in codex's favour:** the materiality threshold is an owner exchange rate, not a technical fact (D7 diagnostic status is Joseph's own ruling) — structure predeclared, number Joseph's (proposal: ≥0.10 on either normative metric with paired corroboration, guarding a now-cheap change). Question doc corrected in place for all of the above. **Ladder collapses to A vs B**, and B's one real design question is the **proof obligation** (keep the by-construction guarantee over the query block, capped at 40,928 B less any P2 template overrun — or rest it on the estimator+headroom guard, effectively unbounded for real sources). OWNER DECISIONS: set the materiality threshold; confirm C stays closed. NEXT: size the harness delta (query side only — alternate query-block builder + a way to run arm F outside the guarantee while still honouring pre-flight), Joseph fires round 1 (**P / F / PF** + optional P−k keys-ablation) on the 23 A/B/C probes, **all 13 documents whole, M=100 held**. **Stage scoping (established in walkthrough 2026-07-26):** the experiment measures **stage-2 only** — when `eligible_space_size ≤ M` the controller feeds stage-2 every eligible identity *regardless of thin's response* (spec:123 small-space enforcement) and the largest domain is 51, so stage-1's representation has no effect on results until N>100 and cannot be measured before then. Also established: **Pass-1 does not depend on search at all** — `kdb_signal` (admission gate) and `Source.summary` (GraphDB property) would keep it alive if #123 never existed; `domain` scoping is a third job. So no query-representation decision can threaten Pass-1's existence. |
| 126 | open | Pass-1 prompt — revisit `entity_search_keys` for semantic selection (Q2) + the `author` null sentinel (Q2b) | Filed 2026-07-27 (Joseph: *"i think it may need optimization"*), **carved out of #125's Q2/Q2b so it survives that row's `BOUNDARY CLOSED` status** — both were recorded there as "unconditional under every branch", i.e. they happen regardless of how the #26 stage-2 representation experiment lands, and neither is blocked by it. Target: `ingestion/enrich/pass1_prompt.j2:68-85`. **Why the prompt is now mis-specified rather than merely improvable:** `entity_search_keys` was introduced by D-89-20 with one documented consumer — "sole consumer = Task #90 context-loader T2-rewrite" (`ingestion/enrich/pass1_schema.py:34-35`) — a **deterministic PK/regex lookup** in which imperfect keys were harmless by design (*"imperfect slugs simply miss, no harm"*, `pass1_schema.py:79-84`, written after a strict regex rejected real emissions like `see's-candies` in a 2026-05-26 live fire). #123 **retires that consumer** and repurposes the field as the selector's **semantic query expressions** (`MAX_EXPRESSIONS` = 10, rendered `A.`–`J.` per D11), where keys are load-bearing: a key that fails to express intent costs a whole expression slot and shows up as an unresolved expression, not as a silent miss. The prompt still instructs for the retired contract. **Two concrete defects already identified (2026-07-27, not yet a design pass):** (1) *"These seed a later entity lookup, so emit the canonical form a reader would search by"* (`:71-72`) optimizes for **string identity** — exactly right for an exact-match PK lookup, and the wrong objective for an LLM selector that needs the key to *express what to look for*; (2) Guidance rule 1, *"Include each `key_themes` slug"* (`:73`), makes the expression list a partial **copy of `key_themes`** — but the Pass-1.5 query block renders `key_themes` **and** `entity_search_keys` as separate fields (`QUERY_FIELD_ALLOCATIONS`, `kdb_search/constants.py`), so under the new contract that rule spends scarce expression slots re-stating something the selector is already shown. **Measured 2026-07-27 over the 39 ratified probes (`benchmark/truth/task123_search_probes_v1.json`), which carry expressions captured from real Pass-1 output under the current prompt — this is evidence, not inference: 138 of 263 expression slots (52%) are verbatim `key_themes` copies, and 30 of the 39 probes are at ≥50%.** A01 is typical: `key_themes` = owner-earnings / return-on-equity / free-cash-flow / depreciation-and-amortization / financial-statement-analysis, and 4 of its 8 expressions repeat four of those, one rendered line above. Sharpest instance: **all five E-class probes are 1-of-1 duplicates** (single expression, and it is a themes copy) — and E01–E05 + A10 are exactly the `semantic_abstention_denominator`, so on the abstention set the expression list contributes nothing the query did not already state. **Q2b (`author`):** *"null otherwise"* (`:60-61`) makes the model emit the 4-character **string** `"null"`, which `{"type":["string","null"]}` accepts (`pass1_schema.py:76`) — 9 of the 39 #123 probes carry it, 5 carry a genuine null, 19 a real author. The **downstream** normalization is separate and already decided (ledger #30 / #125 Call 2, Pass-1.5 projector); **#126 owns the upstream prompt fix**, which is where the defect actually originates. **Sequencing (settled 2026-07-27):** independent of #123 **P2** — P2's oracles are `contracts.py`, the §8 branch table and the byte ceilings, `call` is injected, and no real key flows through any P2 test; #126 changes key *content*, not the field, its cardinality (≤10), its type, or D11's label rendering, so P2 rebuilds nothing. It **is** a prerequisite of the **D5 calibration gate** and **P5a**, which consume real keys. Design pass therefore happens at the P2.1a selector-prompt review: #126 (what keys Pass-1 emits) and the selector prompt (how the selector reads them) are two halves of one query contract, and the 52% finding is what a joint review catches and a separate one does not. A prompt change bumps `PASS1_PROMPT_VERSION` and must land before the P5a A/B or key quality and selector quality confound. Worth pairing with #26's `P−k` keys-ablation arm, which measures how much the keys contribute at all. **OWNER DECISION owed (fixture drift, not sequencing):** after #126 the truth set measures the selector on queries production will no longer emit. Checked, and it bounds the cost — **no scored metric is keyed per-expression**: `class_a_recall_aggregation` is `micro_relevant_slug` and `stage_1_recall_aggregation` is `micro_relevant_slug_at_each_point`, both slug-level, and `target_expression` is descriptive metadata rather than a metric key — so duplicate slots do **not** distort the numbers, and regeneration is not required for the metrics to mean what they say. The residual is narrower: `label_policy.relevant` is defined as *"supported by selector-visible evidence **for the exact query**"*, so changed keys technically re-open adjudication. Practical read: `summary` + `key_themes` carry most of the query's semantic content and #126 does not touch them, and the keys mostly duplicate themes today, so de-duplication removes the least informative part. Options: regenerate the 25 resolvable probe payloads under the revised prompt (a Pass-1 re-run — Joseph fires) and spot-check that labels hold, or accept the drift on record. **Q2 + Q2b IMPLEMENTED 2026-08-02 (`PASS1_PROMPT_VERSION` 1.2.0 → 1.3.0, suite 3,076 green, uncommitted).** Three edits, one objective: the `:71-72` framing (*"these seed a later entity lookup, so emit the canonical form a reader would search by"* — an identifier objective) is replaced with **"these are the search's query terms"**, each naming a distinct thing to look for and reported back key by key; guidance rule 1 (*"Include each `key_themes` slug"*) is **deleted** and replaced by an explicit **do-NOT-repeat** rule; and **all three worked examples are rewritten**. That third edit is the load-bearing one and was NOT in the original filing: every example opened by restating `key_themes` verbatim, so the examples *taught* the duplication the rule merely licensed — deleting the rule alone would have changed nothing. Re-measured independently before the change: **138/263 slots (52.5%), 30/39 probes ≥50%, all five E-class probes 1-of-1** — the filed figures are exact. Q2b: *"null otherwise"* now asks for the JSON literal explicitly, naming the four-character string as the wrong value. Slug **format retained** — deliberately: natural-language phrases would express intent better to an LLM, but slugs match the evidence's own shape, keep D11's label rendering unchanged, and the field's *existence* is still under test by #26's `P−k` ablation; don't redesign the type of something you may delete. Fixing the keys first sharpens that ablation (good keys vs none, not broken keys vs none), so the ordering is not a deadlock. **Tests:** 8 added, including a parse-the-examples oracle asserting no example repeats a theme — mutation-checked (reintroducing one duplicate fires it), plus a guard test so a regex that matched nothing could not make the others vacuously true. **OWNER RULING (Joseph, 2026-08-02) — fixture drift ACCEPTED and recorded, not repaired:** the 39 probes keep the `query_payload` captured under prompt 1.2.0, so they now measure the selector on queries production will no longer emit. Bounded, and checked: no scored metric is keyed per-expression. **New consequence recorded:** dropping the copy rule means a source engaging nothing beyond its themes may now emit ZERO keys (valid — schema has no `minItems`, and the query block simply omits the field line). §8.3 metric 6 reads *unresolved expressions*, so a zero-key source makes it degenerate — a future-probe/production concern only, since the frozen 39 keep their expressions. |
| 127 | open | Built-wheel prompt-loading smoke test — needs offline build tooling | Filed 2026-07-27 as codex F5 out of the #123 P2.1a review (`docs/superpowers/specs/2026-07-27-task123-p2.1a-review-codex.md`). Blueprint §1 calls for an **offline built-wheel prompt-loading smoke test**; P2.1a could not build a wheel because `.venv` carries no `setuptools`, `build` or `wheel` and building one needs a network install. Two substitutes shipped instead and both are real coverage: the packaging **declaration** is parsed out of `pyproject.toml` and its globs expanded against the package directory (so a new non-`.py` file the loader opens, added without a packaging update, fails), and module-relative resolution is proved by a subprocess with `cwd` outside the repo and `PATH` emptied — which also pins `git_commit == "unknown"`, the installed-wheel case. **What is still unproven:** that a built wheel actually *contains* `kdb_search/prompts/*.txt` and that import + resource lookup work after installation. Parsing `pyproject.toml` is not an execution of the build backend. **Codex's disposition, adopted:** this blocks neither the P2.1f byte pins nor D5 calibration — `pyproject.toml:46-50` is correctly declared and the loading path is correct — so it belongs in release CI or the next moment offline build tooling exists, not on #123's critical path. Remedy: `pip install build` (a network op Joseph fires), then a wheel-build + install + import smoke over both templates. |
| 128 | open | Validate `page_type` vocabulary at the graph→search-space materializer boundary | Filed 2026-07-27 as codex R1's second layer, out of the #123 P2.1a re-review (`docs/superpowers/specs/2026-07-27-task123-p2.1a-rereview-codex.md`). **The finding:** `SpaceEntity.page_type` carries the `PageType` `Literal`, which is a **type hint, not a runtime check** — nothing on the path enforces it. `kdb_graph/schema.py:64` declares `page_type STRING`, `kdb_graph/intake.py:325` writes the producer's value through unexamined, and `SpaceEntity` validates nothing, so a `page_type` of `"concept\nQUERY:"` reached the prompt and forged an unindented section header. **Already fixed locally** in `kdb_search`: `projection._single_line` now sanitizes all three interpolated identity fields (H04), which is the containment layer and is not in question. **What this task is:** the *semantic* half — membership in the canonical page-type vocabulary — belongs at the materializer that builds a `SearchSpaceRef` from the graph, where it protects MCP, CLI, the viewer and any future consumer instead of making each one rediscover the rule. **No such materializer exists yet** (the caller materializes the space today; P5b names an owner), which is why this is a forward design constraint rather than a defect with a site. Codex is explicit that it does **not** replace the local fix — two layers. **General lesson worth carrying:** P10 containment must never rest on a type annotation. |
| 129 | open | Pass-2 tier-structured context — separate T1/T2/T3 lists in the snapshot JSON + per-tier prompt instructions | Filed 2026-08-04 from the post-P3a sandbox-run review (Joseph: "we should separate out T1, T2, and T3 from the context JSON, and in the prompt give different instructions regarding those 3 lists"). **The problem:** EXISTING CONTEXT renders as one flat `pages` list (`common/types.py` `ContextPage` = slug/title/page_type/outgoing_links only) and the system prompt misdescribes it as "pages from sources compiled before this one" — T1 (the source's OWN previous pages) is indistinguishable, so warm recompiles re-roll instead of steward: run 2 (`2026-08-04T13-10-43_EDT`, 28 unedited sources) churned 46% (184→162 pages: 99 kept / 63 new / 85 dropped; drops verified in-T1 — Pabrai 11/11, React/Tailwind 4/4). The tier of every selected page is computed then discarded at projection (`compiler/context_loader.py:182`); telemetry records it, the prompt never sees it. **Design (ratified in discussion):** split at projection only — tier assignment, scoring, strict tier order, and the global `page_cap=50` untouched; `ContextSnapshot` gains t1/t2/t3 lists; the prompt renders three labeled sections with per-tier instructions — T1: re-emit/extend same-slug and justify each drop in `compilation_notes`; T2: link-don't-duplicate; T3: weak context. Explicitly out: per-tier caps, selection changes. **Success measure:** churn vs the 46% baseline on a sandbox re-run. Blast radius: `common/types.py`, `context_loader.py` projection, `prompt_builder.py`, system prompt doc (+ version bump), snapshot-constructing tests; the snapshot JSON is never persisted (context records persist telemetry), so no migration. Sequenced BEFORE #130. |
| 130 | open | Dormant entity lifecycle — option C: stop graph deletion of support-loss orphans ("zombie pages") | Filed 2026-08-04 from the zombie-page investigation (Joseph: "after prompt changes for pass-2, we can adopt option C with 'dormant' for zombie pages"). **The problem:** when a recompile drops a page, finalize's reap deletes the node graph-side while the wiki file survives `status: active` — dead in the graph, alive on disk. Run 2 produced 85 (`runs/2026-08-04T13-10-43_EDT/retraction.json`): all 85 files on disk, zero slugs anywhere in the graph. Root cause: `_finalize` (`orchestrator/kdb_orchestrate.py:212-223`) applies the retraction graph-only; `kdb-clean orphans` enumerates FROM the graph, so it can never see zombies. **Design (option C, ratified):** on SUPPORTS-loss, transition to `dormant` instead of delete — the mechanics mostly exist: all readers already filter `status='active'` (`kdb_graph/queries.py:264,526,581-582`), revive-on-re-support exists (`kdb_graph/intake.py:735`); rename/alias `orphan_candidate` semantics to dormant. Sequenced AFTER #129 — stewardship reduces the churn rate first; dormancy makes the residual harmless. **Open sub-decisions (Joseph pending):** (1) hard-delete still fires when the SOURCE is deleted — distinguish "LLM dropped it" from "user deleted it" (recommend yes; intersects #116's source-lifecycle scope); (2) one-off backfill re-creating run-2's 85 dropped nodes as dormant from the surviving files' frontmatter + SUPPORTS via `raw_path` (pattern: `scripts/backfill_cleanup_journal.py`). Cross-ref: #93 (a cross-store auditor would catch this divergence class). |
| —  | open          | Resolve `Open-1..Open-8` in `docs/CODEBASE_OVERVIEW.md`       | Tracked there, not here. Target: close before end of M2                                       |

Task #123 D7 progress note (2026-07-26): Joseph selected and smoke-verified the
static D7 localhost reviewer (`tools/task123_probe_adjudicator.html`), then
completed all 39 probe labels and 14 special-outcome confirmations. The
canonical fixture-valid result is `benchmark/truth/task123_search_probes_v1.json`
(review/export record: `docs/superpowers/specs/task123_search_probes_v1.json`).
The final policy is diagnostic: micro Class-A and Stage-1 aggregation, Class
E+A10 abstention denominator, M=5 watched; the three relevance floors are 0%
and the selector-failure ceiling is 100%. Escaped foreign identities remain
zero-tolerance. No live selector experiment was run by this adjudication.

Post-adjudication amendment (2026-07-26, Joseph's call, annotation-only —
recorded in the artifact's own `amendments` array with the prior sha256):
**A09 and A12 are marked `scoring_role: "precision_only"`** and named in
`gates.metric_rules.class_a_precision_only_probes`. Both were exported as
scored class-A probes with zero relevant slugs but non-empty
`acceptable_alternatives`, so they contributed to neither side of micro class-A
recall while carrying nothing saying that was intended — harmless
arithmetically, indistinguishable from an oversight to a later reader. They are
**not** abstention probes and must not join that denominator: unlike A10 (zero
acceptable alternatives, a true domain-gated abstention), returning one of
A09/A12's labeled alternatives is *correct*, so scoring it as a failed
abstention would invert the label. That makes **three** scoring roles, not two —
required-set / precision-only / abstention — now stated in spec §8.3 metric 3.
Verified: all 39 probes, every label and slug, all five thresholds and every
aggregation rule byte-identical to the ratified version; totals still 89
relevant / 216 acceptable.

Task #123 P3a session rulings (2026-08-03, Joseph — settles the open questions
from `docs/session-handoff-2026-08-02.md` §3–§4 ahead of the P3a blueprint):

- **Strategic fork (handoff #8) — SETTLED: continue #123 through P3a.** The
  1,586-note vault ingestion waits, and then runs instrumented (full chain).
- **Replace, not supplement (handoff item 2) — SETTLED: REPLACE.** Semantic
  selection is the sole T2 seeding path; deterministic PK/regex seeding keeps
  no seat at the table (founding evidence: PK/regex could not identify search
  entries for warren-buffett / charlie-munger). Full deletion scope ratified:
  STRUCTURED / LEGACY / LAYERED T2 modes, the regex machinery, and the
  cold-start 2-hop widening. T1 (SUPPORTS edges) stays. **T2 and T3 are either
  Pass-1.5-generated or empty** — empty T1/T2/T3 is valid; Pass-2 compiles cold
  (already proven by benchmark cold-compilation on an empty ContextSnapshot).
  This supersedes spec D1's P3b timing ("deletion lands only after truth-set
  experiments pass") — owner ruling, 2026-08-03.
- **First sandbox run (handoff item 5) — full chain only:** the first
  Vault-in-place-test-run E2E from now on is Pass-1 + Pass-1.5 + Pass-2; no
  interim current-pipeline run. That run doubles as the prompt-1.3.0 first
  live fire and the pre-vault smoke.
- **Model seat — single model across all three passes initially**;
  deepseek-v4-flash / qwen3.7-flash the named candidates. Split-model stays
  parked in #118. Owner-stated envelope: 1M ctx in / 64K out-token (pool
  entries currently register 384K / 128K max_output — adjust to 64K during
  P3a implementation). **SEAT SETTLED 2026-08-03: qwen3.7-flash.** Decision
  input: the P3a.0 pre-run cost projection
  (`docs/superpowers/specs/2026-08-03-task123-p3a-cost-projection.md`) —
  cost does not constrain the seat ($2–$40 vault-scale), qwen3.7-flash is
  ~2.8× cheaper at EXPECTED with identical token density; quality-neutral
  default per the projection read. The 64K envelope edit landed in P3a.0.

Task #123 post-ratification amendment (recorded 2026-08-04, P3a close-out —
accepted during P3a.2b, commit `59d930e`): **`Pass15Outcome` carries two
fields beyond blueprint §4.1** — `keys_emitted: list[str]` and
`key_outcomes: list[KeyOutcomeV2] | None`. The blueprint's §4.1 outcome
shape listed `search_ran / t2_selection / search_summary /
envelope_written / t1_slugs`; the §4.5 positional projection (per-key
matched/unresolved dispositions aligned 1:1 with the emitted expressions)
was specified as flowing into the context record, but no carrier was named.
The adapter returns it on the outcome — `None` when no search ran
(pre-Pass-1 / replay), distinct from a searched-but-keyless State C's `[]`
— and `compile_source` hands it straight to `build_context_snapshot`. No
behavior change beyond the addition; recorded here per the
deviations-on-record convention.

Task #123 P3a run-review residue (recorded 2026-08-04 — P3a merged to main
PR #1, `4214667`; validated by two Vault-in-place-test-run sandbox runs:
cold `2026-08-03T22-52-44_EDT`, warm `2026-08-04T13-10-43_EDT`, exit=0
both, warm selector hit rate 14/22). Two residues carried out of the run
review: **(1) graph/file divergence on warm recompile (zombie pages)** —
46% page churn across 28 unedited sources; the 85 dropped slugs were
deleted graph-side while their files survive `status: active`; root-caused
to T1 blindness in the Pass-2 prompt (flat context list, tier discarded at
projection) plus graph-only retraction in `_finalize`. Filed as **#129**
(tier-structured context + T1 stewardship) and **#130** (dormant
lifecycle, option C), sequenced #129 then #130 (Joseph, 2026-08-04).
**(2) person-entity modeling — DEFERRED** (Joseph: settle zombies first).
Persons have no node of their own; they resolve diffusely to idea-pages
via `matched_expressions` (verified warm: warren-buffett 5/8 envelopes,
mohnish-pabrai 2/2, charlie-munger 1/4). Parked options: A first-class
person page type; B `kind: person` marker on the existing machinery; C do
nothing (status quo resolves, diffusely). Revive trigger: post-#129/#130
sandbox review.

---

## Closed

| #  | Title                                                       | Resolution                                                                                                                          |
|----|-------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------|
| 1  | CLI progress banners (7-stage → 8-stage)                    | `b377bfc` original 7-stage; `88ee8fc` promoted to 8-stage with reconcile banner                                                     |
| 3  | Wiki type & graph case study                                | `docs/reference/wiki.md` — three-type ontology rationale, concept lifecycle, graph topology intuition                                         |
| 4  | Prompt & JSON output structure                              | Direction A via Task #11 synthesis + 2026-04-21 Option A refinements; Direction B's goal met by validator+reconciler (auto-heal). See memory `project_task4_prompt_schema_directions.md` |
| 6  | `patch_applier` empty-plan crash                            | `92aa778` code fix (both cases closed by single Option-1 change); `7e28297` ticket closure. See `docs/archive/early/bug-patch-applier-empty-plan.md` |
| 7  | Drop `index.md` (D23)                                       | `b708d63`. Obsidian file explorer is the TOC; `manifest.json` is the machine index                                                  |
| 8  | Drop `log.md` (D24)                                         | `48f1666`. `state/runs/<run_id>.json` is the authoritative per-run journal                                                          |
| 9  | 1-sentence ask for compiler prompt                          | Locked in `docs/archive/handoffs/session-handoff-2026-04-20.md`                                                                                      |
| 10 | 5-bullet high-level spec (v3)                               | Locked in `docs/archive/handoffs/session-handoff-2026-04-20.md`                                                                                      |
| 11 | Expand 5-bullet spec into detailed behavioral sections      | Completed as part of the Task #12–#15 multi-model synthesis workflow                                                                |
| 12 | Claude draft of compiler system prompt                      | Part of the parallel drafting pool → synthesis                                                                                      |
| 13 | Grok draft of compiler system prompt                        | Part of the parallel drafting pool → synthesis                                                                                      |
| 14 | Gemini draft of compiler system prompt                      | Part of the parallel drafting pool → synthesis                                                                                      |
| 15 | QWEN + GPT5.4 drafts + synthesis + install + rename         | Synthesized prompt installed to `~/Obsidian/KDB/KDB-Compiler-System-Prompt.md` (renamed from `CLAUDE.md`, commit `1312757`). See memory `project_task11_completed.md` |
| 17 | Terminology sweep: aspirational "eval" → mechanical names   | `b7ea53a`. Code/tests/fixtures/CLI/blueprint/memory all renamed: `eval_replay → response_replay`, `kdb-eval → kdb-replay`, `KDB_EVAL_CAPTURE_FULL → KDB_RESP_CAPTURE_FULL`, `llm_eval/ → llm_resp/`, `EvalRecord → RespStatsRecord`. Reserves "benchmark" for Task #5 cross-model work |
| 18 | Decide benchmark directory structure (D2)                   | `5825d0f`. D2 picked: top-level `kdb_benchmark/` engine package + top-level `benchmark/` data dir (sources/truth/scores tracked; runs/inspect gitignored). One-way import boundary. 9 unit tests green |
| 29 | `RespStatsRecord` telemetry plumbing for benchmark scorer   | `26b345a`. `RespStatsRecord` gained `stop_reason`/`token_overrun`/`source_words`. `ModelEntry` gained `price_in`/`price_out` (USD per 1M tokens; `0.0` for local). `cost_usd` intentionally NOT persisted — scorer derives from `(input_tokens, output_tokens) × registry` at score time, respecting the `kdb_compiler -/-> kdb_benchmark` one-way import boundary from #18. +10 tests. Phase 3 gate for #19 |
| 28 | M5 (`body_link_syntax_match`) implementation — symmetric Jaccard | `0bcc2b6`. Added `body_link_check(payload) -> (intersection, union)` to `validate_compiled_source_response` (~58 LOC w/ docstrings). Per-page set semantics: `declared = set(outgoing_links)`, `body = slugs in [[…]] tokens` after stripping fenced/inline code spans (negative-lookbehind handles `\`-escapes). Returns `Σ\|D_p ∩ B_p\|`, `Σ\|D_p ∪ B_p\|` summed across pages. Wired into `build_resp_stats` → persisted on `RespStatsRecord.body_link_intersection` / `body_link_union`; both default to 0 on parse failure. Scorer derives Jaccard ratio at score time. +16 tests. Last Phase 3 gate for #19 |
| 32 | Benchmark corpus v1 (`benchmark/sources/`)                  | `d31d054`. Curated 5 license-clean public sources (~28.6K words, ~250 KB total): (1) Attention Is All You Need (arXiv 1706.03762v7) — license caveat in meta.yaml for personal-use repo; (2) Distill "Why Momentum Really Works" (CC-BY-4.0); (3) The Twelve-Factor App (CC-BY-4.0); (4) Distill "Research Debt" (CC-BY-4.0); (5) Rust Book Ch. 4 — Understanding Ownership (MIT/Apache-2.0). Each source paired with `.meta.yaml` carrying source URL, version, license, attribution, conversion command, sha256 checksum, fetched_at, word/char counts. HTML→MD via `html2text` (pandoc not installed); math notation doubles cosmetically in Attention + Momentum but doesn't affect Borda-ranked KPIs. Estimated cost ~$1.50 per scorecard for haiku-4.5 + sonnet-4.6 |
| 22 | Design KDB scorecard format                                 | `2780f63`. `kdb_benchmark/scorecard.py` — `build_scorecard(runs)` orders post-Borda RunScores by final_score desc, stamps a scorecard_id (local-ISO-timestamp + candidate set), embeds the Round 4 DC4 disclaimer. `render_terminal()` outputs rank \| model_id \| S0 \| M1..M5 \| M6_b \| M7_b \| FINAL \| RAN_AT plus raw-rates inspection footer plus diagnostics plus disclaimer. `write_scorecard()` writes JSON to `benchmark/scores/<scorecard_id>.json`. 8 unit tests |
| 30 | Benchmark runner (`kdb_benchmark/runner.py`)                | `2780f63`. Isolation-contract orchestrator. Invokes `compile_one` directly (NOT the production `kdb-compile` pipeline — structurally cannot touch the user's vault); empty `ContextSnapshot` per source for cold-compilation determinism; sets `KDB_RESP_STATS_CAPTURE_FULL=1` (§3 mandate); writes `RespStatsRecord` JSONs under `<runs_root>/<run_id>/state/llm_resp/`. run_id format `<model_id>-<local-ISO-timestamp_TZ>`. Snapshots system prompt into `<run_id>/vault/KDB/` for re-runnability. 9 unit tests covering registry lookup, .meta.yaml skipping, capture-full env, system-prompt copy, isolated state_root |
| 31 | Benchmark scorer (`kdb_benchmark/scorer.py`)                | `b16525d`. Implements `docs/archive/tasks/task19-kpi-design.md` § Phase 3 § 5–§ 9: `MeasureScore` + `RunScore` dataclasses; per-measure functions (S0 `pipeline_success_rate`, S1/S2/S3 diagnostics, M1 link_target_resolution, M2/M3 concept/article Jaccard, M4 semantic_pass_rate, M5 body-link Jaccard, M6/M7 cost/latency per 1K source-words, 3 diagnostics); `borda_normalize` (average-rank with all-equal policy + drop-None-rate); `final_score` (weighted sum + pro-rata redistribution); `score_run(state_root, run_id, model_id)` + `score_runs(runs)`. Edge cases per § 4 (parse-fail M6/M7 inclusion via source_words>0; non-dict parsed_json guards; non-list slug coercion; model-vs-corpus zero-denom split; retry_load clamp; M1 list semantics). 57 unit tests via TDD |
| 34 | Parameterize `source_id` prefix for benchmark schema validation | `bfa09d4`. Surfaced 2026-05-07 by smoke-test fire. `validate_compiled_source_response.validate(payload, *, source_id_prefix="KDB/raw")` patches the schema's `$defs.sourceId.pattern` to `^<prefix>/.+`. `compile_one` threads the kwarg through; `kdb_benchmark.runner` derives the prefix from `sources_dir` (POSIX-normalized) and constructs each `source_id` as `<prefix>/<filename>`. Production default `KDB/raw` unchanged. Re-fired smoke confirmed S0/M4/M1 signal recovered (real haiku-4.5 `duplicate_slug` defect now visible instead of masked by wiring failure). +9 tests |
| 35 | `kdb-benchmark --verbose` per-measure scoring trace          | `371fb7a` (v1: initial flag, mid-flight prints); `af8aa13` (v2: sink-based, post-scorecard render, always-on per-source S0 breakdown w/ rich finding `.detail` via new `check_compiled_source_findings()` helper); `259d743` (v3: per-source S0 KPIs numbered `[1]`–`[4]` on own lines, weights rendered `x/100` instead of decimal). CLI flag `--verbose`/`-v`; internal API `trace_sink: list[str] \| None`. Surfaces evidence for S0 cause, M1 target_set size, M6 cost/words, M7 latency/words, Borda raw→ranked → final_score |
| 36 | Persist score trace to per-run dir                          | `f3ced89`. CLI ALWAYS captures per-run + cross-run trace sinks (regardless of `--verbose`) and writes each run's complete trace (per-run section + cross-run Borda/final_score section) to `benchmark/runs/<run_id>/score_trace.txt`. Self-contained — opening any run dir tells the whole story for that run. `--verbose` narrows to "mirror trace to stdout" — disk persistence is unconditional. +3 tests in `test_cli.py` covering persistence-without-verbose, verbose-mirrors-stdout, no-verbose-stays-quiet |
| 37 | Reserve `summary-` prefix for summary slugs                 | `a21c2a8`. Surfaced 2026-05-07 from two-model smoke analysis: both haiku-4.5 and sonnet-4.6 reproducibly hit `duplicate_slug`/`pairing_type_mismatch`/`summary_slug_wrong_type` on sources where the central concept name matched a topic-derivable summary slug. Root cause: pre-#37, summary and concept/article slugs shared one flat namespace. Fix: schema gains `summarySlug` def (pattern `^summary-[a-z0-9]+(?:-[a-z0-9]+)*$`) used by top-level `summary_slug` in both `compiled_source_response.schema.json` and `compile_result.schema.json`. System prompt (in `~/Obsidian/KDB/`, outside git) rewritten to mandate `summary-<stem>` verbatim with explicit examples; concept/article slugs explicitly forbid the prefix. Production `prompt_builder.exemplar_response()` updated; tests + fixtures updated across 6 files. **Verified 2026-05-08 two-model smoke** (`haiku-4.5-2026-05-08T00-02-44_EDT` + `sonnet-4.6-2026-05-08T00-03-12_EDT`): both models produced clean output (`summary_slug=summary-04-research-debt`, prefix preserved, `research-debt` cleanly used as concept slug). S0 went 0→1 for both; haiku final 0.800→0.9955, sonnet final 0.350→0.7000. Defect class eliminated. Side benefit: wikilink semantics now unambiguous (`[[research-debt]]` always concept, `[[summary-X]]` always artifact) |
| 38 | Persist rendered scorecard table alongside JSON             | `6af4f1c`. `write_scorecard()` now writes both `<scorecard_id>.json` (existing) and a sibling `<scorecard_id>.txt` containing the full `render_terminal()` output. Two artifacts written together — never drift. Future scorecards automatically get the .txt; pre-#38 scorecards (seven from 2026-05-07/08) only have JSON. +1 test in `test_scorecard.py` |
| 39 | M5 verbose trace per-page asymmetry detail                  | `80debbc`. Surfaced 2026-05-08 from haiku M5=10/11 investigation: identifying the offending declared/body slug required post-run JSON spelunking. Same self-explanatory-trace pattern as #36: when M5 rate < 1.0, scorer's `_emit_verbose_trace` renders per-page asymmetry under the M5 line (which source, which page, which `declared_only` / `body_only` slugs); skip block when M5=1.0 or no asymmetry found. New helper `body_link_per_page_asymmetry(payload) -> list[dict]` in `validate_compiled_source_response` (same payload-tolerance contract as `body_link_check`). Records already carry `parsed_json` via capture-full → no schema/persistence/prompt change. Trace-only. +10 validator tests, +3 scorer trace tests. **Verified end-to-end on real fires:** 2026-05-08 00:02 haiku run named `summary-04-research-debt → ['research-exposition']`; 10:08 sonnet run named `interpretive-labor → ['research-debt']` (case-mismatch slip — body had `[[Research-debt]]` capitalized) |
| 40 | Runner fails loud on missing/empty sources_dir              | `d81eed1`. Surfaced 2026-05-08 when `/tmp/kdb-smoke/` was wiped post-reboot: `run_benchmark` silently produced zero records and the misleading "no records found" error only surfaced 30s later from the scorer. Fix: validate `sources_dir` up front in `run_benchmark`, BEFORE any side effects (run dir creation, system prompt snapshot, env var) — no orphan run dirs / vault snapshots on bad input. `FileNotFoundError` when missing/not-a-directory; `ValueError` when exists-but-zero-`.md`-files. CLI's existing `except (ValueError, RuntimeError, FileNotFoundError)` already surfaces both as `error: {exc}` to stderr — no CLI change. +2 runner tests verifying both failure modes raise as advertised AND that no side effects occur |
| 41 | Drop source_id-space from LLM response surface              | `f6bf607`. Surfaced 2026-05-08 from sonnet-4.6's reproducible 2-3/5 parse failures on the canonical corpus: the model reacted to the user-message schema's `^KDB/raw/.+` source_id pattern with prose preamble explaining the perceived mismatch with runtime source_ids like `benchmark/sources/...`, breaking JSON extraction. Task #34's prefix override only patched the validator, not the schema embedded in the prompt. Hybrid α/β fix: **α** rename top-level `source_id` → `source_name` (LLM emits filename only; runner constructs full source_id post-parse). **β** drop `supports_page_existence` per page + `related_source_ids` per log_entry from the LLM contract entirely; runner injects them post-parse from runtime knowledge. The LLM owns slug-space; the runner owns source-id-space. Schema's `$defs.sourceId` removed; Task #34's prefix-override superseded. Also fixes a latent bug where the model couldn't fully populate `supports_page_existence` for extended pages because EXISTING CONTEXT doesn't carry prior source attribution. **Verified end-to-end on real fire 2026-05-08 14:50** (`sonnet-4.6-2026-05-08T14-50-49_EDT`): sonnet's parse-failure rate dropped from 2-3/5 (across two prior fires) to 0/5. Both models now hit S0=5/5 on the canonical corpus; cross-model signal now lives entirely in M5/M6/M7 as designed. Headline scorecard `2026-05-08T14-58-08-04-00-haiku-4.5_sonnet-4.6`: haiku final 0.997, sonnet final 0.700 |
| 19 | Define KPIs + gate thresholds for KDB benchmark             | **Phase 5 closed 2026-05-08.** Architecture promoted into `docs/CODEBASE_OVERVIEW.md` §7 (Benchmark Architecture) — covers package layout & one-way import boundary, RespStatsRecord input contract w/ capture-full mandate, Tier-1 stage success rates (S0 weighted; S1/S2/S3 diagnostic), Tier-2 measures (M1–M7 with formulas + domains), locked weights table (S0=20/M1=20/M2=5/M3=5/M4=15/M5=5/M6=15/M7=15), average-rank Borda for M6/M7 with rank-latest-pick-best workflow, end-to-end data flow. Decisions ledger gains D25–D28 capturing the four load-bearing benchmark architecture calls. `archive/tasks/task19-kpi-design.md` retained as historical record (rounds 1–4 + Phase 3 spec mechanics + Round 4 corrections) with a closure banner pointing readers at CODEBASE_OVERVIEW §7 first. Dead `score_response` stub + its test removed from `validate_compile_result.py` / `kdb_compile.py` / `test_validate_compile_result.py` / `test_kdb_compile.py` (cross-model scoring now lives in `kdb_benchmark.scorer`). Phase 3 + Round 4 had previously closed 2026-05-06 (function signatures, edge-case policies, average-rank tie handling, retry_load clamp, model_id-as-identity, S0 renamed `pipeline_success_rate`). Phase 2 + Round 3 closed 2026-05-04 (page-spam exploit closed via source-words denominator, Jaccard formulas, M5 symmetric, weights re-allocated). Phase 1 closed 2026-04-30 |
| 23 | Document benchmark architecture in `CODEBASE_OVERVIEW.md`   | Closed 2026-05-08 alongside Task #19 Phase 5 — benchmark architecture summary now lives at `docs/CODEBASE_OVERVIEW.md` §7. Same outcome as #19 Phase 5; tracking under both ledger entries was redundant |
| 21 | Port/adapt `models.json` registry                           | `b3d7afc`. Lifted the proven shape from `~/Droidoes/Code-projects/youtube-comment-chat/src/eval/models.json`: optional `ctx_window`, `max_output_tokens`, `use_completion_tokens` (bool), and `extra_body` (dict) added to `ModelEntry`. The flag/body fields thread through `runner` → `compile_one` → `ModelRequest` → `call_model`'s openai-compat path, where `use_completion_tokens=true` switches the SDK call from `max_tokens` to `max_completion_tokens` (GPT-5+ family requirement) and `extra_body` forwards to the SDK's `extra_body=` kwarg for provider-specific knobs. The "21-model lift" framing was aspirational — what actually mattered was the schema shape; future model additions are now pure JSON edits to `kdb_benchmark/models.json`. Registry currently carries 3 entries (haiku-4.5, sonnet-4.6, gpt-5.4-mini), the third restoring Borda gradient on M6/M7 (binary 1.0/0.0 → graded ordinals when n_candidates≥3) and as the first cross-provider participant. +11 tests: 7 in `test_registry.py`, 4 in `test_call_model.py` |
| 42 | Cross-run scorecard auto-merge                              | `e6f71c1`. Surfaced 2026-05-08 from "I'm just running gpt-5.4-mini only — combine with prior haiku/sonnet results". Pre-#42, every invocation wrote a one-shot scorecard limited to the models in that run; cross-run merging meant ad-hoc scripts. Fix: split `benchmark/scores/` into `runs/` (per-invocation, model_id suffix only when 1-model run) and `final/<ts>.json` (versioned merged view, never overwritten — prior versions remain as history). Each final entry carries `source_scorecard_id` pointing at the per-run that produced it. Merge: union by model_id, new wins on overlap, Borda + final_score recomputed across the union. New `--no-merge` flag skips the final write. `MeasureScore.from_dict` / `RunScore.from_dict` reverse loaders; `build_per_run_scorecard` / `build_final_scorecard` / `latest_final_scorecard_path` / `load_runs_from_scorecard` in scorecard.py; `write_scorecard(scores_dir=, subdir=)` directs to runs/ or final/. Bootstrap: pre-#42 scorecard 2026-05-08T14:58 split into runs/+final/ with `source_scorecard_id` backfilled. +15 tests |
| 43 | Add xAI provider + expand model registry                    | `dfc794d`. New `xai` provider route in `kdb_compiler/call_model.py` — dispatch via openai SDK at `base_url=https://api.x.ai/v1`, api_key from `XAI_GROK_API_KEY` env (`Settings.xai_api_key`). `xai` added to `Provider` literal in `call_model.py` and to `ModelEntry.provider` docstring in `kdb_benchmark/registry.py`. `models.json` gains two entries: `gemini-3-flash-preview` (Gemini provider, existing route — no code change) and `grok-4-1-fast-reasoning` (xAI). Extends Task #21's 3-candidate baseline to 5 candidates; M6/M7 Borda gradient now spreads across 5 ordinals (binary→graded). Verified end-to-end via fires 2026-05-08T23:01/23:59 (gemini-3-flash-preview) and 2026-05-09T00:20 (grok-4-1-fast-reasoning); 5-candidate scorecard `2026-05-09T00-20-39-04-00` confirms gradient: gpt-5.4-mini 0.922 / haiku-4.5 0.847 / grok-4-1-fast-reasoning 0.837 / sonnet-4.6 0.737 / gemini-3-flash-preview 0.483. +2 tests in `test_call_model.py`: xai dispatch path (verifies `base_url` + `api_key` + plain-model-id — no `models/` prefix unlike Gemini); xai missing-key guard (verifies `ModelConfigError` parity with other providers) |
| 44 | "Dropped Models" scorecard section                          | `155fc16`. Surfaced 2026-05-08 from gemini-3-flash-preview's pathology (high run-to-run variance, intermittent token-overrun) — without a drop mechanism, gemini was permanently sticky in the auto-merge lineage and skewing other models' Borda ranks. `ModelEntry` gains `dropped: bool = False` + `dropped_reason: str = ""`; `Scorecard` gains `dropped_runs` + `dropped_reasons` fields. JSON to_dict emits a separate `dropped_models` array (entries inline `drop_reason`); `load_runs_from_scorecard` combines `models` + `dropped_models` into one returned list so the partition is decided fresh at merge time against the current registry, not the prior scorecard's snapshot. `cli._merge_with_prior_final` returns `(active, dropped, source_map, dropped_reasons)`; partitions union by registry.dropped, runs score_runs over active only, forces m6_borda/m7_borda/final_score = None on dropped subset. `cli.main` fail-fast guard: when every selected `--models` is registry-dropped, exits 2 with "all selected models are marked dropped..." BEFORE any runner invocation. `render_terminal` adds "Dropped Models (excluded from active Borda)" section (model_id, S0, M1..M5, M6_raw $, M7_raw ms, DROP_REASON) — only when `dropped_runs` non-empty (backward-compat regression-guarded). models.json marks `gemini-3-flash-preview` dropped; existing committed scorecards (4b37469) stay as historical record. +17 tests covering registry round-trip + type validation, Scorecard dropped fields + JSON shape, render with/without dropped section, load_runs combined + legacy pre-#44 fallback, cli all-dropped guard, merge partition, end-to-end registry-flip across two fires |
| 45 | Register gemma4 (Ollama) in model registry                  | `60753da`. Adds `gemma4-obsidian-bench` as the 6th model and first Ollama participant. Provider `ollama` (existing dispatch route from #18; no code change). Prices 0.0/0.0 (local execution — gemma4 will dominate M6 cost-per-1K-words by default in the next final scorecard). `ctx_window: 65536` reflects the user's RTX 4080 operational ceiling set via Modelfile `PARAMETER num_ctx 65536` — gemma4's native is 131072 but the 4080 caps at 64K. Modelfile also pins `num_gpu 999` (full GPU offload) and `num_batch 1024`; sampling params (temperature 0.7, top_p 0.9, repeat_penalty 1.15) are partially overridden at request time — `compile_one` hardcodes `temperature=0.0` (`compiler.py:219`) keeping gemma4 apples-to-apples with the other 4 active candidates. Fire command: `kdb-benchmark --models gemma4-obsidian-bench --sources benchmark/sources --max-tokens 24000` (24K output cap fits within 64K ctx after worst-case ~37K input on source #02 Momentum, leaving ~3K margin). Other models continue to use default `--max-tokens 32768` |
| 47 | Register qwen3.5 ollama variants (fast-structured + 4b-rag) | `558b629`. Adds two Qwen3 4B variants from the youtube-comment-chat registry as candidates 7 and 8 in active set: `qwen3.5-fast-structured` (structured-output-tuned Modelfile — best fit for KDB JSON-emit task) and `qwen3.5-4b-ollama-rag` (RAG-tuned — comparison candidate for "does structured-output tuning matter on this task?"). Both: provider=ollama, `ctx_window: 49152` (Modelfile-baked num_ctx; smaller than gemma4's 65K), prices 0.0/0.0, `extra_body: {"think": false}` to disable Qwen3 chain-of-thought (threaded through to Ollama via #21's schema port). Skipped `qwen3.5-4b-ollama-rag-01` from the YT registry: at our forced `temperature=0.0` (compile_one hardcodes this in compiler.py:219), the rag-vs-rag-01 differences (temperature 0.1, top_p 0.85, seed 42) are all overridden by the request; the remaining difference (num_batch 1024→512) only affects M7 latency and rag's larger batch is faster, so rag-01 would produce byte-identical correctness output to rag — redundant. Fire commands (single-model, per #45 pattern): `kdb-benchmark --models qwen3.5-fast-structured --sources benchmark/sources --max-tokens 12000` and same for `qwen3.5-4b-ollama-rag`. 12K output cap fits within 49K ctx after worst-case ~37K input on source #02 (gemma4's actual output on src #02 was 3.9K, so 12K is comfortable headroom) |
| 46 | kdb-benchmark UX overhaul: single-model + per-run progress + persisted run metadata | `72a0f54`. Surfaced 2026-05-09 from "I'm running the test but I don't see per run status" — silent multi-minute Ollama fires were anxiety-inducing. Three connected UX improvements: (1) `--models` enforces single value (comma → exits 2 with guidance to use cross-run merge); main() shrinks as the per-model loop is gone. (2) `runner.run_benchmark` now reads each just-written RespStatsRecord (compile_one writes in `finally`) and prints `[<model>]   N/M  filename  XX.Xs  in=NNNNN  out=NNNNN  [⚠ stop=length]` per source — surfaces truncation IMMEDIATELY, ahead of scorer-side detection (blind for Ollama). Return shape: `(run_id, state_root, compile_metrics)` where `compile_metrics = {compile_seconds, n_sources, n_source_words}`. (3) `Scorecard` gains `run_config` + `run_timing` dicts persisted in per-run scorecard JSON — config carries provider/model/ctx/max_tokens/prices/sources_dir/n_sources/n_source_words/system_prompt_path; timing carries compile/score/total seconds. `render_terminal` shows Config + Timing lines between header and table when populated; skips them when empty (backward-compat regression-guarded). Final scorecards intentionally leave both empty — aggregations have no single config/timing. New shared helper `scorecard.fmt_duration(seconds)` (`2m 14s` / `12.3s` / `n/a`). cli.py emits config header before runner output + KPI summary line + total run time at end. +18 tests across runner/scorecard/cli covering 3-tuple return, progress + truncation warning, run_config/run_timing round-trip, Config/Timing render conditional, fmt_duration, comma rejection, persisted metadata in per-run / empty in final, config header + KPI summary + timing footer printed |
| 48 | Register qwen3.5-9b-64k-rag (Ollama, 9B variant)            | `078fc9d`. Adds `qwen3.5-9b-64k-rag` as the 9th candidate and second active Ollama participant (alongside gemma4-obsidian-bench). Provider=ollama, ctx_window=65536 (matches the 4080 hardware ceiling, same as gemma4), prices 0.0/0.0 (local), `extra_body: {"think": false}` defensive. Surfaced after qwen3.5-fast-structured (4B, #47) ran pathologically slow on canonical corpus (26+ min stuck on source #01; GPU 90% util; most likely Qwen3 thinking-mode tokens dominating output — Modelfile thinking control may not be set, and Ollama's OpenAI-compat endpoint may not forward `extra_body.think`). The 9B Modelfile is explicitly user-tuned for the benchmark with thinking control. Fire: `kdb-benchmark --models qwen3.5-9b-64k-rag --sources benchmark/sources --max-tokens 24000` (24K cap, 64K ctx — same headroom math as gemma4-obsidian-bench). Existing 4B qwen entries kept untouched — still valid candidates if/when their Modelfile thinking-control is verified or fixed |
| 49 | kdb-benchmark live progress timer during compile_one        | `e78ef32`. Surfaced 2026-05-09 from "display 1/5 and a running timer during the run, not wait for llm to return" — Task #46's per-source progress line only printed AFTER compile_one returned, so 10+ minute Ollama calls were silent. Two additions to `runner.run_benchmark`: (1) `⏳ starting...` line emitted BEFORE compile_one is invoked. (2) Background-thread ticker `_periodic_progress_ticker` prints `[<model>]   N/M  filename  ⏳ Xm Ys elapsed` every 60s while compile_one is in flight; stops on `stop_event.set()` from main thread when compile_one returns (daemon thread, 2s join timeout). Interval configurable via `KDB_BENCHMARK_PROGRESS_INTERVAL_SECONDS` env var. Fast API models (sub-60s/source) see only the starting line + existing completion line. Slow Ollama models see `starting → 1m elapsed → 2m elapsed → ... → final stats`. Best-effort interleave with main-thread prints — acceptable vs adding `\r` overwriting + tty-detection machinery. +3 tests: starting line precedes completion; ticker emits multiple ⏳ ticks at tiny interval; ticker emits zero ticks when stopped before first interval |
| 50 | Remove qwen3.5 ollama variants from registry (#47 + #48)    | `05502df`. All three qwen entries (`qwen3.5-fast-structured`, `qwen3.5-4b-ollama-rag`, `qwen3.5-9b-64k-rag`) removed from `models.json` after proving unworkable on the canonical 5-source corpus on the RTX 4080: 4B fast-structured stuck 26+ min on source #01 (likely Qwen3 thinking-mode tokens dominating output — Ollama OpenAI-compat may not forward `extra_body.think`); 9B 64k-rag stuck 16+ min on source #01 even at 64K ctx (likely 9B + near-cap VRAM pressure on the 4080); 4b-rag never fired (skipped after fast-structured failure). Clean removal rather than `dropped: true` because none produced any successful RunScore — the Dropped Models section (Task #44) requires actual scorecard entries to display. Git history at #47 + #48 preserves the experiment. Registry now: 5 active + 1 dropped (gemini-3-flash-preview). May revisit qwen3.5 if (a) Modelfile-baked `think: false` is verified to work reliably, or (b) we accept partial-corpus signal as benchmark-meaningful (gemma4 at S0=0.4 already demonstrates this) |
| 51 | Add Alibaba provider + register qwen-flash-us / qwen3.5-flash | `198fb1d`. New `alibaba` provider route in `kdb_compiler/call_model.py` — dispatch via openai SDK at `base_url=https://dashscope-us.aliyuncs.com/compatible-mode/v1` (DashScope intl/US OpenAI-compat endpoint), api_key from `QWEN_US_API_KEY` env (`Settings.qwen_us_api_key` — same env name as youtube-comment-chat for consistency). `alibaba` added to `Provider` literal in `call_model.py` and `ModelEntry.provider` docstring. Two cloud-hosted Qwen entries replace the local Ollama qwen experiments removed in #50: `qwen-flash-us` (1M ctx, 32K max_output, $0.05/$0.40 per 1M tok) and `qwen3.5-flash` (1M ctx, 64K max_output, $0.029/$0.287 per 1M tok — cheapest API model in registry alongside grok). 1M ctx leaves massive headroom on canonical corpus; default `--max-tokens 32768` works without special cap. +2 tests in `test_call_model.py`: `test_alibaba_dispatch_uses_dashscope_endpoint` (base_url + api_key + plain-model-id) and `test_missing_qwen_us_key_raises` (mirrors xAI missing-key guard). Pre-existing `test_unknown_provider_raises` updated from `alibaba` → `moonshot` since `alibaba` is now a real route |
| 52 | TTY-aware running-clock progress ticker (refines #49)       | `bf9f24f`. Surfaced 2026-05-09 from "is it possible to do a running clock... I thought we did that in youtube project" — Task #49's per-60s newline ticker worked but felt like a periodic log dump rather than a live clock. In TTY mode (`sys.stdout.isatty()=True`), switched to `\r` in-place overwriting at 1s intervals so each source shows a single line that ticks like a stopwatch (`⏳ 0.0s elapsed → ⏳ 5m 14s elapsed → final stats`). Non-TTY mode (tests, log redirect, CI) keeps the per-60s newline behavior — robust under stdout redirect, no terminal-control codes. Two ticker functions: `_tty_progress_ticker` (1s, `\r`) and `_newline_progress_ticker` (60s, `\n`); back-compat alias `_periodic_progress_ticker` retained. After ticker stops, TTY branch clears the line (`\r` + spaces + `\r`) so the final-stats line starts on column 0. Both intervals tunable via env vars (`KDB_BENCHMARK_TTY_TICK_SECONDS`, `KDB_BENCHMARK_PROGRESS_INTERVAL_SECONDS`). +2 tests: direct unit test of `_tty_progress_ticker` verifying `\r` (not `\n`); end-to-end with isatty monkey-patched True asserting `\r` appears alongside final stats |
| 53 | Fix body-wikilink emission via exemplar + schema descriptions | `6acb408`. Surfaced 2026-05-09 from qwen-flash-us back-to-back fires (13:18, 13:29) both scoring M5 body_link_jaccard = 0.000 across all 5 sources — every page populated `outgoing_links` metadata but emitted zero `[[…]]` in body prose, structurally reproduced. Diagnosis (single-variable): prime suspect was the in-prompt exemplar at `kdb_compiler/prompt_builder.py:exemplar_response` — a 1-page summary with empty `outgoing_links` and a wikilink-free body. For instruction-following-weak models, recency + concreteness of the user-message exemplar overrides §5.4's prose rule in the system prompt; the exemplar literally taught "empty outgoing_links and bodies without `[[…]]` are valid." Replaced with a 2-page exemplar (summary + concept) where the summary body contains `[[example-concept]]` matched in `outgoing_links: ["example-concept"]` — demonstrates the bidirectional rule by example. Tightened schema field descriptions for `body` (mentions `[[slug]]` convention + bidirectional contract) and `outgoing_links` (spells out "build by collecting body wikilinks, not as separate metadata") since both re-render every call. Skipped §5.4 reword to keep the experiment isolated. Validated: qwen-flash-us 2026-05-09T13:55 fire scored M5 = 0.150 (24/160 union), source #03 twelve-factor-app hitting 12/13 aligned — proves the model can apply the rule once primed. Sources #02/#04/#05 still mostly empty → motivates Task #54 (§5.4 generation-order hint). Methodology: prompt change affects every model so post-#53 Borda strictly not comparable to pre-#53; full re-baseline deferred per single-user scope |
| 54 | Generation-order hint in §5.4 — write body first, collect outgoing_links from `[[…]]` | **Reverted (no SHA — vault edit rolled back).** Vault file `~/Obsidian/KDB/KDB-Compiler-System-Prompt.md` restored to pre-#54 wording (no `### Generation order` subsection; self-check item #4 back to original `Every outgoing_links slug appears as [[slug]]...` form). Reasoning: cross-model post-#54 survey on 2026-05-09 found that the procedural prose helped weaker models (qwen-flash-us +0.256, qwen3.5-flash +0.144) but regressed stronger models on body-wikilink density (gpt-5.4-mini −0.268 M5; declared count 246→151, intersection 232→102, implying body wikilinks dropped from ~232 to ~102 — a real Obsidian-graph UX cost). Net body-wikilink change across 4 models was ≈ −124. The qwen-flash-us cure (0 → 31 body wikilinks) was overwhelmingly delivered by Task #53's exemplar fix (0 → 24); #54 added only +7 marginal wikilinks at the cost of −130 on gpt. After Task #57 made the bidirectional invariant a post-processing guarantee, #54's prompt prose became vestigial for M5 (system enforces what it teaches) and the only remaining motivation was body-wikilink density UX — for which the cross-model evidence shows it's net-negative. Methodology lesson logged: prompt prose that names a specific output convention can over-constrain models that already had a working pattern. Future iterations should target capability gaps rather than try to homogenize working patterns |
| 57 | Body-wins outgoing_links reconciler in compile pipeline      | `7602bdf`. Surfaced 2026-05-09 from cross-model post-#54 survey: even after two rounds of prompt engineering, models drifted in different directions (gpt-5.4-mini regressed −0.268 on M5 while qwen-flash-us improved +0.256). Conclusion: bidirectional consistency between `outgoing_links` and body `[[…]]` is a system-correctness concern, not a model-quality one — should be guaranteed in post-processing rather than relied on from raw model output. Body-wins normalization (Strategy A): for each page after `semantic_check` passes, `outgoing_links = sorted(body_wikilink_slugs(body))`. Aligns with the post-#53 schema description ("build outgoing_links by collecting the distinct slugs you wrote in `[[…]]` tokens in the body"). User chose reconcile BEFORE M5 measurement so M5 reads 1.000 by construction across all models — retires M5 as a model discriminator (acceptable trade: 5%-weight signal often dominated by per-source variance anyway, and the system-correctness guarantee is the more durable answer). New `reconcile_body_links(parsed_json)` in `kdb_compiler/reconcile.py`, separate from the existing finding-driven `reconcile()` since this is unconditional normalization. Wired into `compiler.py:compile_one` immediately after `semantic_ok` success, before `CompiledSource` enrichment, so both `resp_stats_writer.body_link_check` (M5 measurement) and `patch_applier` (disk write) see the reconciled state. Side effects: pages with wikilink-free bodies get `outgoing_links: []`; raw model output preserved in `raw_response_text` for forensics. Validated 2026-05-09T14:50 on qwen-flash-us: M5 = 1.000 (18/18), S0/M1-M4 = 1.000, no regression. +13 tests in `test_reconcile.py` covering drops/adds/aligned/empty-body/alias/heading/code-stripping/idempotency/multi-page/tolerance/sorted/dedup. Supersedes Task #54 (vault prompt edits reverted) |
| 58 | Add `ollama-cloud` provider; register `deepseek-v4-flash:cloud`; rename `ollama` → `ollama-local` | `116755d`. New `ollama-cloud` provider route in `kdb_compiler/call_model.py` — dispatch via openai SDK at `base_url=https://ollama.com/v1` (Ollama Cloud's OpenAI-compat endpoint), api_key from `OLLAMA_API_KEY` env (`Settings.ollama_api_key`). Existing `ollama` provider renamed to `ollama-local` for disambiguation — same dispatch logic and `ollama_base_url` setting (default `http://localhost:11434/v1`), only the literal name changes. `Provider` literal in `call_model.py` and `ModelEntry.provider` docstring in `kdb_benchmark/registry.py` updated. `models.json`: gemma4-obsidian-bench provider flipped to `ollama-local`; new `deepseek-v4-flash:cloud` entry with provider=`ollama-cloud`, ctx_window=1M, prices=0.0/0.0 (cost bundled in subscription, not per-token). `max_output_tokens` unset (server default applies). +2 tests in `test_call_model.py`: `test_ollama_cloud_dispatch_uses_ollama_com_endpoint` (verifies base_url + api_key + verbatim model id with the `:cloud` tag) and `test_missing_ollama_api_key_raises` (mirrors xai/alibaba missing-key guards via `_call_openai_compat`'s built-in empty-key check). Existing ollama dispatch test renamed for `ollama-local`. Validated 2026-05-09T15:53 fire on deepseek-v4-flash:cloud: S0=1.000, M1=0.991, M2-M5=1.000 (M5=1.000 by Task #57 reconciler), final_score=0.998, pages_per_1k=1.50, M7=13.2s/1K-words (slower than direct API but faster than 4080-local Ollama). Discovery: deepseek-v4-flash:cloud requires a paid Ollama Cloud subscription tier — first fire at 15:41 hit 403 with "this model requires a subscription"; plumbing was correct from the start, only the subscription gate blocked. User upgraded mid-session, re-fired successfully. Worth noting for future cloud-Ollama model additions: free tier ≠ all listed models |
| 59 | M5 replacement: `body_emit_set_coverage`                       | `060c915` (docs-first: CODEBASE_OVERVIEW §7.3 + D29 ledger + TASKS.md) → `154ab54` (promote `_body_wikilink_slugs` → `body_wikilink_slugs`) → `094da69` (new `_compute_body_emit_set_coverage` helper, 13 unit tests) → `fcd9dbb` (m5() rewrite reads parsed_json; per-source coverage verbose trace; imports cleaned). Retired `body_link_jaccard` (M5=1.000-by-construction post-#57) replaced by per-source coverage of declared emit-set in body wikilinks (self-links excluded), computed in scorer from parsed_json — preserves D25 one-way boundary. Live-verified on haiku-4.5 / canonical 5-source corpus 2026-05-10T10:13:14_EDT: M5=0.930 (53/57), final_score=0.9965. Per-source coverage trace renders missing slugs (e.g. `twelve-factor-methodology` declared but never wikilinked). See `docs/archive/tasks/task59-m5-replacement-design.md` (D29.1–D29.9 sub-decisions) and `docs/archive/tasks/task59-m5-replacement-plan.md` (7 tasks). Deferred cleanup items resolved in #60. |
| 60 | M5 helper consolidation + `body_link_*` orphan cleanup         | `a600659` (orphan removal: deleted `body_link_check` + `body_link_per_page_asymmetry` helpers from `validate_compiled_source_response.py`, `body_link_intersection`/`body_link_union` fields from `RespStatsRecord`, call site in `resp_stats_writer.py`, 22 tests across two test files, and `body_link_*` kwargs from `fake_record` test fixture — clean break per D29.8) → `fdc6649` (DRY consolidation: extracted `_emit_set_components(parsed_json) -> tuple[set[str], set[str]]` shared by `_compute_body_emit_set_coverage` and `_m5_per_source_coverage_lines` in `kdb_benchmark/scorer.py`, eliminating ~15 lines of duplicated extraction logic and lifting `_slugs` out of the per-record loop in the trace helper). All 21 M5 tests stay green; full kdb_benchmark suite at 188 passing post-refactor. |
| 61 | M5 reweight: 5% → 15%, M6/M7 each 15% → 10% (D30)              | `462284d` (initial weight bump in m5/m6/m7 constants + CODEBASE_OVERVIEW §7.4 + D30 ledger + TestLockedWeights regression guard) → `ca0826e` (architectural fix from Codex code review: `final_score()` had a duplicate hardcoded weight table at scorer.py:875-884 that didn't get updated; production FINAL silently still used D27 weights despite measure constants reporting D30). Refactored `final_score()` to source weights from MeasureScore objects directly (single source of truth — eliminates the duplicate-weight-table anti-pattern). Companion: stale weights in `_rs()` test fixture and `test_final_score_raises_when_all_components_none` updated to D30; new `test_d30_weights_drive_final_score` added as regression guard. 190/190 tests green. Post-#61 baseline scorecards (2026-05-10T13-27 and earlier) computed with the buggy weights — recommend a fresh single-merge fire to recompute. |
| 62 | Outlier penalty for FINAL (D31)                                  | `ebf5423`. `RunScore` gains `final_score_pre_penalty` + `penalty` fields; `final_score` remains the post-penalty rank-by value. `_compute_outlier_penalty(runs)` helper: for each in-scope measure (S0, M1, M2, M3, M4, M5), units = floor(((norm − value)/norm × 100) / 10) when value < norm; total = Σ units × 0.05; FINAL_post = max(0, FINAL_pre − total). M6/M7 excluded (Borda-relative). Score_runs() refactored to three-pass: pass-1 (Borda + pre-penalty FINAL), pass-2 (penalty computation across candidate set), pass-3 (apply penalty). Scorecard render gains PENALTY column between M7_b and FINAL (shows `-0.40` or `-` for zero). `TestOutlierPenalty` class: 8 tests (no-outlier, qwen-pattern, multi-axis cumulative, one-sided, norm-zero, rate-None, integration, floor). `TestScorecardTerminalRender` gains 3 PENALTY-column tests. CODEBASE_OVERVIEW gains new §7.6 (outlier penalty), existing data-flow renumbered to §7.7, pointer-to-spec to §7.8; §7.5 amended to mention penalty; D31 ledger entry added. 190 baseline → 201 tests green. Cross-generation FINAL comparison invalidated again per D29.9 doctrine; a subsequent merge fire produces the corrected post-#62 baseline. |
| 26 | Systematic review of EXISTING CONTEXT list design               | **Superseded by #63** (commit `963e8f7`). Folded into the GraphDB-KDB refoundation. Original scope (rationale, algorithm, objective, effectiveness measurement) becomes a graph-traversal query on top of GraphDB-KDB once `context_loader.py` is rewired — a post-#63 follow-up outside this task's scope. See `docs/reference/task-graphdb-kdb-blueprint.md` §1 + D32. |
| 27 | Manifest scalability assessment + industry-standard review      | **Superseded by #63** (commit `963e8f7`). Resolved by separation of concerns: GraphDB-KDB (Kuzu, embedded graph DB) holds graph state; `manifest.json` remains the JSON ledger for file metadata + system state. Both consume `compile_result` independently per D34 (independence-by-shared-upstream). |
| 20 | Decide ground-truth source                                      | **Closed 2026-05-21 as won't-do.** All 7 benchmark measures are ground-truth-free by design: S0 (pipeline_success_rate) is schema-gate driven; M1 (link_target_resolution) is internal to the parsed_json's own emit-set; M2/M3 (concept/article Jaccard) are internal consistency between declared slug lists and pairing-typed pages; M4 (semantic_pass_rate) is semantic_check finding-driven; M5 (body_emit_set_coverage, post-#59) is internal coverage of declared emit-set in body wikilinks; M6/M7 (cost/latency-per-1K-words) are Borda average-rank normalized **within the candidate set** (D28). The original GT-A..E options were drafted in 2026-04-19 before the Round-4 weights + Borda design locked the no-GT property in CODEBASE_OVERVIEW §7.3 + §7.5. No code change; ledger move only. Future re-open trigger: adding a measure that compares model output against a human-authored "correct" answer would require revisiting GT-D. |
| 25 | Capture exception type+message in resp-stats on pre-response failures | **Closed 2026-05-21.** Added `failure_stage`/`failure_exception_type`/`failure_exception_message` triplet to `RespStatsRecord` — additive optional fields (verified non-breaking against `kdb_benchmark` consumers, which all read via `.get()`). Scope extended beyond the literal "pre-response" wording to all 6 non-validation pipeline halts: `source_read`, `prompt_build`, `model_call`, `truncation`, `extract`, `parse`. Schema/semantic stay out — they own structured `schema_errors`/`semantic_errors` list surfaces. Co-presence invariant (all three populated, or all three None) enforced via private `FailureTelemetry` NamedTuple + `Literal` stage taxonomy + `_set_failure` helper in `compiler.py`; `_truncate_msg` caps messages at 2000 chars + `...[truncated]`. Truncation uses synthetic `exception_type="TokenOverrun"` (names the cause, better for grouping than `"TruncationError"`). 3-reviewer convergence (Opus + Codex + Gemini) on **Option B** (flat fields) over A (nested `FailureInfo`) / C (stringly-typed list); decision context preserved inline here, no separate blueprint doc per mechanical-patch scope. 16 new tests (4 resp_stats + 9 compiler per-stage/invariant + 3 helper unit), suite 891 passed / 1 skipped. Commit: `46a3c59`. |
| 90 | Context-loader T2-rewrite — `entity_search_keys` as structured seed signal | **Closed 2026-05-27 with E.1 live-fire GREEN** (11.33s; Pass-1 emitted 6 keys; ContextSnapshot pages=4; 4/4 seed entities resolved). Closes Pass-1↔context-loader tunnel-meeting-in-the-middle per D-89-20. v0.2 blueprint ratified via 5-CLI panel (Codex + Deepseek + agy/gemini-3.5-flash-high + Grok + Qwen; 5/5 guardrail-clean; **agy 4-for-4** on post-strike re-trial cycle); 3 bugs caught (B-1 planner→compiler circular import / B-2 dead canonical_id targets not active-status-verified / B-3 §2.5↔§3.3 N+1 contradiction); 5 new decisions (D-90-8 State-C honored / D-90-9 simple resolver default + Codex batch escape hatch / D-90-10 shared `source_io.py` / D-90-11 T2Mode stays in graph_context_loader / D-90-12 3-part sunset AND-gate); Pass-1 prompt v1.0.0→v1.1.0 amendments shipped; `PASS1_PROMPT_VERSION` constant for §10 watch-for #7 correlation. STRUCTURED ships as v1 default; LEGACY retained behind T2Mode dispatch pending D-90-12 sunset gate (corpus 100% enriched + NW-9 cold-start ≥ + NW-9 precision ≥). **Drift-from-plan disclosure** (commit `5e01856`): simple resolver implemented as 1-query with Python-decided path precedence (vs plan's 2-query) for Path 3 dead-target disambiguation correctness; parity-test enforces simple ≡ batch functional identity across 12 probe cases. Implementation: `71f8ef9` (Phase A — `kdb_compiler/source_io.py` shared module + 11 tests) → `5e01856` (Phase B+C+D — T2Mode dispatcher + STRUCTURED A/B/C + LAYERED + LEGACY + simple/batch resolvers + planner env-var helpers + 19 parity + 13 dispatch tests) → `68ee371` (Phase E — prompt v1.1.0 deployed + E.1 live smoke test + E.2 non-live plumbing test). Blueprint: `docs/archive/tasks/task90-context-loader-t2-rewrite-blueprint.md` v0.2. Plan: `docs/superpowers/archive/plans/2026-05-27-task90-context-loader-t2-rewrite-implementation.md`. Memory: `[[feedback_user_fires_api_cost_runs]]` reinforced (Joseph fires live smoke per discipline). |
| 89 | Component #1 (Enrichment) deep-design — Pass-1 ↔ Pass-2 ↔ GraphDB integration | **Closed 2026-05-26 (late night) — tunnel ends meet (live-verified).** v0.2.2 closure session: D-89-18 retracted; D-89-19 (mechanical append of key_themes to Source.summary, persisted) + D-89-20 (drop `key_entities`; add `entity_search_keys` ≤10 — Task #90 input contract) ratified; Bug #1 fixed (`Source.source_type` hardcode at `graphdb_kdb/ingestor.py:144`); Bug #2 dissolved. E.1 acceptance test green (`kdb_compiler/tests/test_pass1_end_to_end.py::test_tunnel_ends_meet`, 39.19s, 5/5 §10.5 contract checks: domain / author / source_type (Bug #1 fix verified) / Source.summary contains "Themes: …" append (D-89-19 verified) / frontmatter has entity_search_keys & no key_entities (D-89-20 verified)). v0.1 → v0.2 → v0.2.1 → v0.2.2 ratification arc spanned 2026-05-25 brainstorm → 5-CLI panel review → Joseph-led architectural deliberation; agy completed 3-for-3 on post-strike re-trial across Task #89 v0.1 + round-2 + NW-7 v0.1. Sub-tasks: NW-7 v0.2 (21-entry source_type vocab, `docs/archive/tasks/task89-nw7-source-type-list-v0.2.md`); NW-4 v0.4 (23-domain vocab, via parent #88, `docs/archive/tasks/task88-nw4-domain-list-v0.4.md`). Blueprint: `docs/archive/tasks/task89-component1-enrichment-blueprint.md` v0.2.2 (§12 decision log captures D-89-12..D-89-20 + retraction markers; §14 amendments table). Session handoffs: `archive/handoffs/session-handoff-2026-05-26-task89-pass1-impl-checkpoint.md` + `archive/handoffs/session-handoff-2026-05-26-task89-evening-v0.2.2-key_themes-loop-close.md`. Memories: `[[feedback_obsidian_wikilinks_are_vanity]]`, `[[feedback_sources_stay_static_intrinsic_frontmatter_only]]`, `[[feedback_integration_preconditions_are_architectural]]`, `[[feedback_prompt_template_definition_plus_examples]]`, sharpening of `[[feedback_no_edge_predeclaration_no_hints]]` and `[[feedback_no_imaginary_risk]]`. Follow-up: Task #90 (Context-loader T2-rewrite) input contract locked; consumer-purpose test for Pass-1 fields established as architectural discipline. |
| 110 | `common/models.json` — user-owned model pool + cost/ctx diagnostics | **Closed 2026-06-07 — merged to `main` via `feat/110-models-json-pool`** (16 commits, 1209 non-live tests, live-smoke clean on default `deepseek-v4-flash`). Reinstated the user-owned model pool (retired with the #5 engine) re-homed to `common/models.json` + `common/model_pool.py` `resolve_models_json(id)→ModelSpec` (alias→provider+knobs, **absolute** dropped-guard via `UnknownModelError`/`DroppedModelError`); `kdb-orchestrate --model <id>` resolves the pool, `--provider` demoted to escape hatch + conflict-check. Restored **`cost_usd`** (pricing × **aggregated** tokens) on BOTH telemetry paths (Pass-2 `RespStatsRecord` + Pass-1 sidecar). Proactive **ctx-overrun pre-flight guard** (`words×1.3`, `est_in+out≤ctx_window`) in both passes → skip-and-quarantine, no API spend (synthetic `TokenOverrun`, persisted in the sidecar). Holistic-review **must-fix**: thread `use_completion_tokens`/`extra_body` to both passes (makes thinking-disable real + unbreaks `gpt-5.4-mini`). Enhancement: semantic **`thinking` field** (per-provider translation; verified alibaba `enable_thinking:false` + deepseek `thinking:disabled`; gemini/openai/xai no-op+TODO), **un-dropped `deepseek-v4-pro`** (permanent discount). Subagent-driven TDD; per-task spec+quality reviews + final holistic review. **Deferred (documented):** `max_output_tokens` clamp (untriggered, ceilings≫defaults); Pass-1 failed-source `cost_usd=0.0`; per-model reasoning-disable verification → **#111**. Spec/plan `docs/superpowers/{specs,plans}/2026-06-06-models-json-pool*`; reference `docs/reference/model-provider-api-calls.md`. |
| 5  || LLM benchmarking                                              | Parent task closed 2026-05-17. Benchmark engine landed (`kdb_benchmark/`, CODEBASE_OVERVIEW §7): Borda-normalized cross-model scorecard, 7 measures (S0/M1–M7), outlier penalty (D31), 5-candidate registry (haiku-4.5, sonnet-4.6, gpt-5.4-mini, grok-4-1-fast-reasoning, deepseek-v4-flash:cloud + 1 dropped gemini-3-flash-preview), TTY progress ticker, per-source verbose traces. Child task #21 (registry expansion) closed implicitly via 5-candidate registry; #20 (ground truth) closed 2026-05-21 as won't-do — all 7 measures are GT-free by design (internal consistency for S0/M1–M5, Borda relative-ranking for M6/M7). |
| 33 || Benchmark orchestrator (future)                               | Closed 2026-05-17 alongside parent #5. v1 sequential orchestration (`--models a,b,c`) works and has been exercised across 5+ candidates. Parallel execution / resume-after-failure deferred indefinitely — YAGNI for single-operator workflow. |
| 63 || GraphDB-KDB Layer (Kuzu-backed multi-source ontology)         | Refoundation. Supersedes #26 + #27. Blueprint: `docs/reference/task-graphdb-kdb-blueprint.md` (locks D32–D40 + D-A1/A2/B1/S0/S1/S2/S3/S4/S5/S6 + L8 + TR-1/2/3; Codex 3-round reviewed). Companion docs: `docs/reference/graphdb-kdb-extraction-roadmap.md`, `docs/reference/manifest-succession-arc.md`, `docs/reference/graphdb-kdb-producer-contract.md`. Phase 3 impl blueprint: `docs/archive/tasks/task63-phase3-implementation-blueprint.md`. Sub-tasks: ✅ #63.0 (`4b1aa02`); ✅ #63.1 (`11eba55`); ✅ #63.2 (`437769f`); ✅ #63.3 (`d1a3641`); ✅ #63.4 (`09e4130`); ✅ #63.5 (`7edf2c4`); ✅ #63.5b rename pass (`eba5f8d`); ✅ #63.6 B-lite rebuilder (`fd92eed`); ✅ #63.7-pre Stage 9 via adapter (`2f42d5c`); ✅ #63.7 live integration validation A1→A4 across 3 providers (anthropic/gemini/alibaba) — commit chain `74a38aa`, `9db0826`, `f87ec04`, `a7ef341`, `04bdbc7`; ✅ #63.8 docs (CODEBASE_OVERVIEW §8); ✅ #63.9 snapshot/export — JSONL+manifest+schema.cypher with per-file sha256 (Codex Round 1 reviewed, design at `docs/archive/tasks/task63.9-snapshot-codex-prompt.md`) |
| 64 || Recompile page supersession + #37 summary-prefix data repair  | Surfaced 2026-05-15 from the Buffett recompile. Recompiling a pre-#37 source spawns a duplicate summary + strands omitted pages — `apply_compile_result()` unions support but never removes it. Two parts: (A) code fix — supersession step in `manifest_update.apply_compile_result()` + status-aware `source_refs` invariant (D41–D44); (B) one-time no-API migration repairing the 3 already-crossed sources (Buffett, EP1, Howard-Marks) + GraphDB-KDB resync. Blueprint: `docs/archive/tasks/task64-recompile-supersession-blueprint.md` (Codex 2-round reviewed). **Closed 2026-05-16:** (A) code fix landed `47c927e`→`27d67e4` (supersession + D41–D44). (B) migration **obsoleted** — the 2026-05-16 full canonical recompile of all 4 sources runs `_supersede_omitted_pages` live per source, achieving the migration's goal organically; `migrate_task64_supersession.py` was never applied. Recompile orphan residue cleared via `kdb-clean orphans` (#67). |
| 65 || `pairing_type_mismatch` should be reconcilable, not a hard gate | **Closed.** Option B implemented (D45): `reconcile_slug_lists()` rebuilds `concept_slugs`/`article_slugs` from `pages[].page_type` unconditionally in `compile_one` (after `reconcile_body_links`); `pairing_type_mismatch` demoted `gate`→`measure` and removed from `HARD_ZERO_FINDING_TYPES`; finding-driven reconcile rule as safety net. Commits: `8cae06a`→`5c454ab`. EP1 no longer gate-fails on the 2 borderline slugs. Blueprint: `docs/archive/tasks/task65-pairing-reconcilable-blueprint.md`. |
| 66 || Compile-trigger model: remove the `error_retry` kludge        | Surfaced 2026-05-16. `kdb_scan.py` had an `error_retry` side-channel — `UNCHANGED` sources recompile if `manifest.compile_state == "error"` — which let EP1 be force-recompiled by hand-editing the manifest. Replaced with the honest invariant: compile iff `current_hash != last_compiled_hash` (new source field, advanced only on successful compile). `compile_state` is now informational, no longer a trigger; force-recompile = real content change, no flag. D46. Blueprint: `docs/archive/tasks/task66-compile-trigger-model-blueprint.md`; plan: `docs/superpowers/archive/plans/2026-05-16-task66-compile-trigger-model.md` (Codex-reviewed, Opus final-review APPROVED). Code: 6 commits `160220e`→`0ca91db` (TDD, suite 504 green). Live migration applied 2026-05-16 (`task66-migration-2026-05-16T08-14-40`): EP1 un-kludged, 4 sources backfilled `last_compiled_hash`; post-migration no-op scan classifies all 4 `to_skip`. |
| 67 || `kdb-clean orphans` — first-class orphan-cleanup command      | Promoted `scripts/reap_orphans.py` → `kdb_compiler/kdb_clean.py`: a `kdb-clean <mode>` maintenance CLI (mode `orphans`), sibling to `kdb-compile`/`graphdb-kdb`, NOT a `kdb-compile` flag (cleanup is destructive, keeps its own review gate). Dry-run default / `--apply`; archives orphan pages to `state/orphan-archive/<run-id>/`, removes them from `manifest.pages`+`orphans`, reports active dead links (does not rewrite active pages). TDD, 8 tests, suite green. Used 2026-05-16 to reap 16 orphans after the canonical recompile. **Landed with a known GraphDB-KDB replay gap — tracked by #68:** the cleanup mutates `manifest.json` only; `graphdb-kdb rebuild` replays compile-history and re-introduces reaped pages as orphan entities, so the graph does not resync. Memory: `feedback_kdb_clean_naming`. |
| 68 || `kdb-clean` cleanup is not graph-replayable — HIGH PRIORITY   | Surfaced 2026-05-16; **closed 2026-05-16.** `kdb-clean orphans` mutated `manifest.json` only — `graphdb-kdb rebuild` replayed compile-history journals that still contained the reaped pages, re-introducing them as orphan entities (the cleanup was **not replayable**). Fix: candidate (b) — a typed, replayable cleanup/retraction event. Codex-approved blueprint (`docs/archive/tasks/task68-cleanup-retraction-event-blueprint.md`) → 9-task TDD plan (`docs/superpowers/archive/plans/2026-05-16-task68-cleanup-retraction-event.md`) → 8 implementation commits (`ff59ed1`→`f20fd39`) → final review **APPROVED**. `kdb-clean orphans --apply` now writes a `cleanup` run journal (`schema_version: "2.1"`, `event_type: "cleanup"`) + `retraction.json` sidecar into `state/runs/` and live-syncs through the adapter; `graphdb-kdb rebuild` replays the cleanup event chronologically and `apply_cleanup` `DETACH DELETE`s entities by `retracted_slugs` (reaped slugs with no surviving active page — slug-safe). Crash-consistent write order (sidecar→manifest→journal). Producer-contract amended (§3.3/§3.4/§4). One-shot backfill (`scripts/backfill_cleanup_journal.py`) applied to the pre-#68 `f23c74b` reap: 16 reaped pages → 14 retracted slugs (2 slug-safe-excluded). Post-rebuild `verify`: **reap-residue class = 0** (was 25); drift 34→10, all 10 remaining in the two out-of-scope classes — 8 `compile_count` attribute drift (D39 tax, possible #69) + 2 dead links (`confucianism→mencius`, `yield-chasing→risk-management` — manifest content fix, separate cleanup mode). Suite green (835 passed / 1 skipped). |
| 69 || `compile_count` attribute-drift audit — INVESTIGATION FIRST   | Surfaced 2026-05-16 on the post-#68 `graphdb-kdb verify`: all 4 sources show a uniform `compile_count` drift of −1 (manifest > kuzu by exactly 1) plus a matching `compile_state` mismatch (`recompiled` vs `compiled`) — 8 `attribute_mismatch` issues total. **Closed 2026-05-16 — D39 tax confirmed, not a counter bug. Audit: `docs/archive/tasks/task69-compile-count-drift-audit.md`.** Determination: the −1 is the **D39 replay-eligibility tax** — both counters are correct single-site `+1`-per-compile; the graph `ingest_count` provably equals the eligible-journal count per source (2/1/1/6); each source's missing compile is its *first*, whose journal predates #63.7 sidecar archival and is permanently ineligible by D39 (`unsupported_version` / `payload_missing`). Uniform because every source has exactly one pre-archival first compile. No code change (investigation scope satisfied). Sub-finding: the `compile_state` mismatch is a **separate mechanism** — the producer never writes `compile_state` into `compile_meta`, so the graph always falls back to literal `"compiled"` regardless of replay history; a future verifier-classification refinement must handle the two drifts with separate rules. Possible follow-up (not filed — YAGNI): teach `graphdb-kdb verify` to classify both as a known-benign band. The 2 dead links remain out of scope (future `kdb-clean links`). |
| 70 || GraphDB-backed context loader (manifest-succession Stage M1)  | Replace `context_loader.py`'s manifest-based `EXISTING CONTEXT` construction with graph queries from GraphDB-KDB. Core implementation landed (`1d05b0b`→`7e7efd4`): `graph_context_loader.py` + planner env-switch. **Gates passed:** context-set parity (cap≥20), small-cap stress (graph keeps hubs), failure modes, rebuild reproducibility, steady-state compile parity. **Cold-start gap closed (#71, `8f6ae11`).** **D49 shipped:** GraphDB is the only context authority; `KDB_CONTEXT_SOURCE=manifest` errors explicitly; planner always uses `graph_context_loader`; manifest fallback removed. Plan: `docs/superpowers/archive/plans/2026-05-16-task70-graphdb-context-loader.md`. |
| 71 || Cold-start widening rule for graph context loader             | Surfaced 2026-05-16 from #70 gate testing. When a source has no SUPPORTS edges (T1 empty), T2 slug-in-text matching alone is too narrow — new sources get 3 pages vs manifest's 6. Fix: (A) title-in-text matching widens T2 on cold-start (exact phrase, normalized, skip titles ≤ 3 chars + 2-token/6-char guardrail); (B) conditional 2-hop T3 if widened T2 still < 5 seeds. D48: graph loader must be self-sufficient — no manifest fallback, ever. Live-validated: cold-start produces 23 pages (was 3; manifest gives 8). Steady-state sources unaffected. `8f6ae11`. Blueprint: `docs/archive/tasks/task71-cold-start-widening-blueprint.md`. |
| 72 || Remove legacy `context_loader.py` (manifest-as-context dead code) | Follow-up to D49. Subsumed by #73 Phase H (`5274b94`). `context_loader.py`, `manifest_update.py`, `patch_applier` CLI command, and their tests all removed. |
| 73 || Manifest ontology removal (D50)                               | `manifest.json` stops being an ontology store. GraphDB owns Entity, LINKS_TO, SUPPORTS, orphan status. Manifest becomes source-file metadata ledger only. **Phase H done (`5274b94`):** remove legacy context_loader, manifest_update, patch_applier CLI dead code. **Phase G done (`315f0fa`):** replay-to-temp structural equality verifier; proves graph integrity by replaying journals into temp Kuzu then diffing replay (expected) vs live (actual); two layers (source-state preflight + full replay structural diff); all 7 Codex corrections applied; 10 tests. **Phase F done (`886958d`):** manifest becomes source-state-only ledger (schema v3.0); `source_state_update` is sole manifest writer with auto-migration v1→v3; `kdb-clean orphans` no longer reads/writes manifest; `apply_reap_to_manifest` removed (dead transitional code); +19 new migration/invariant tests. **Phase E done (`8063d99`):** `kdb-clean orphans` reads from GraphDB (ontology authority); GraphDB REQUIRED even for dry-run; `reap_orphans_from_graph`; 10 new graph-backed tests. **Phase D done (`7823185`):** `source_state_update.py` — source-meta-only ledger extraction (make-before-break; 17 tests). **Phase C done (`5e4d022`):** patch_applier derives frontmatter from compile_result + scan (no manifest.pages dependency). **Phase B done (`349ce55`):** Stage 9 graph_sync fatal; `replayable_payload` field; D39 amended. Blueprint: `docs/archive/tasks/task73-manifest-ontology-removal-blueprint.md`. |
| 74 || GraphDB-KDB Canonicalization Stage (Round 5 §8 implementation) | Implements Round 5 §8 — canonicalization as a first-class compile stage between [5] reconcile and renumbered [7] build_source_state. Wiki ≡ graph on entity names. Lineage: continuation of #63 family architecturally; new top-level task because #63 is closed in this ledger. Blueprint: `docs/archive/tasks/task74-canonicalization-blueprint.md` (Codex 2-round + Antigravity/Gemini 2-round reviewed; D-R5-1 through D-R5-13 locked; 7 OQs resolved 2026-05-20). Anchors: `docs/what-is-ontology-for-V1.md` §8.2 + §8.4. Closing summary in CODEBASE_OVERVIEW §11 (M4) + D52 ledger entry. Sub-tasks (all shipped 2026-05-20..2026-05-21): ✅ #74.1 schema delta (Entity.canonical_id + ALIAS_OF + migration, `6a5f929`); ✅ #74.2 aliases.json ledger loader (`d60f1aa`); ✅ #74.3 canonicalize.run() — string-norm + chain flattening (D-R5-13) + page merging (OQ-F) + body wikilink remap (D-R5-11) + canonical_meta emit + atomic write-back (D-R5-10) (`94b0018`); ✅ #74.4 kdb_compile.py Stage [6] wiring + stage renumber [6]→[10] + journal schema 2.1→2.2 + compile_result.schema.json optional fields (`faa2bd7`); ✅ #74.5 adapter Phase 3.5 — writes alias Entity rows + ALIAS_OF (algorithm property) + canonical_id; supported_journal_versions += "2.2"; orphan-detection scoped to canonical_id IS NULL (`12a540f`); ✅ #74.6 graphdb-kdb verify Layer 3 — C1–C4 live-graph invariants + `--canonicalization-only` flag (`e4cf80d`); ✅ #74.7 snapshot format v2 + canonical_meta replay tests + schema back-compat test (`1697d6c`); ✅ #74.8 docs (CODEBASE_OVERVIEW §5 + §8 + §11 M4 + D52 ledger entry, this commit). |
| 75 || Predeclared evaluation criteria for step-3 graph operations    | **Closed 2026-05-21 with user sign-off.** Satisfies Round 5 §8.5/§8.6 path-forward precondition (Codex Q6): define success/failure tests for step-3 ops BEFORE building them. Blueprint at `docs/archive/tasks/task75-predeclared-eval-criteria-blueprint.md` — v1 (`d674590`) → Codex + Gemini external review → v2 (`f7679ae`) with R1–R8 + new OQ-9 applied (see §9 change log). Locks operations roster (V1: PPR + community routing [algorithm-neutral; current Louvain, Leiden TBD #78] + subgraph extraction; V0: typed traversal + shortest-path; V2 defer scored-multi-hop), predeclares per-op pass/fail/gate criteria with structural-vs-semantic hub-guard scoping, defines hedge-watch rules HW-1..HW-7 (symptom → §8.3 hedge), narrows step-3 preconditions (only community/domain-ratio gate depends on Task #76 `domain` field; PPR + subgraph + probe set proceed in parallel), lists OQ-1..OQ-9 including bounded (a)→(b)→(c) failure-disposition ladder with HW-5 lock + iteration ceiling (2 / 1 provisional per OQ-9) + post-pivot preservation clause. Pattern mirrors Task #19 (compile-side KPI predeclaration → CODEBASE_OVERVIEW §7) and Task #74 (Codex 2-round + Gemini 2-round review). |
| 76 || Round 5 §7.3 `domain` field implementation                     | Step-3 critical-path enabler for the community/domain-ratio acceptance gate (Task #75 §4.2) + HW-3 hedge (Task #75 §5). Adds an LLM-extracted `domain` (+ optional `sub_domain`) coordinate to the graph per Round 5 C2. Blueprint: v1 (`8a25f03`) → Codex + Gemini external review (Gemini feedback archived `601c032`) → v2 (`0194e43`) with R1–R12 applied. **Adopts Option B** (Domain node + BELONGS_TO edge); `sub_domain` omit-when-plural (R5 consensus lock). **Closed 2026-05-21.** Sub-tasks: ✅ #76.1 producer pipeline (`2f3530f`) — per-call schema + `compile_result` schema + `PageIntent` dataclass + `compiler.py:367` all patched for `domain`/`sub_domain` with `{"type":"null"}` in `oneOf`; ✅ #76.2 schema v2.1 + chained migrations (`1cfcb18`) — `Domain` node table + `BELONGS_TO` rel table + `_migrate_2_0_to_2_1` + chained `_ensure_schema` walk + `stats()` gains `domains`/`belongs_to` counters + `rebuilder._DROP_ORDER` patched; ✅ #76.3 ingestor Phase 3.6 (`d236150`) — `_normalize_domain` (two-stage regex) + `_ingest_page_domains` (canonical-only, R5 omit-when-plural, post-normalize dedupe, alias_to_canonical defensive map); ✅ #76.4 CLI `domains` subcommand (`e83580b`) — tabular + `--json` output of Domain nodes sorted by entity count (blueprint §6.6); ✅ #76.5 domain ingestion tests (`3854179`) — 15 tests covering normalization, single/multi-domain, sub_domain propagation, R5 omit-when-plural, dedup, canonical-only tagging, no-domain no-op, idempotency; ✅ #76.6 sub_domain normalization fix (`8e268f6`) — Opus review caught R12 gap (`sub_domain` stored verbatim though blueprint §6.3 + Decision-Log R12 call for `_normalize_domain` applied to it too); 2 added tests cover normalization round-trip + whitespace→NULL coercion. Full suite 779 passed. Deferred follow-ups (filed): #79 verifier v2.1 coverage, #80 snapshot v2.1 coverage. Other deferred (not in #76): per-source `source_id` on BELONGS_TO (R6 promotion), `analytics.py:29` canonical-only filtering, prompt + producer-contract amendments. |
| 79 || `graphdb-kdb verify` coverage for schema v2.1 (Domain + BELONGS_TO) | Filed 2026-05-21 from the #76 Opus code review. Blueprint §3.1 scoped verifier out of #76, but `verifier.py` performs the replay-to-temp structural-equality check (D50) and currently did NOT compare Domain nodes or BELONGS_TO edges between replay and live — so any future ingestor drift on the domain pipeline would go undetected. **Closed 2026-05-21.** Implementation: `verifier.py` gains `_graph_domains` (name-keyed) + `_graph_belongs_to` ((slug,name)-keyed); `_diff_domains` (existence-only) + `_diff_belongs_to` (attribute diff via `_diff_attrs` on `sub_domain`); both collectors wired into `verify()` for replay+live with new `domains_checked` + `belongs_to_checked` counters. Conftest `make_page` extended with `domain`/`sub_domain` kwargs. 5 new tests in `test_verifier.py`: perfect-agreement with domains, missing-domain-in-live, extra-domain-in-live, BELONGS_TO sub_domain attribute_mismatch, pre-#76 no-domain still passes. Full suite 989 passed. **Deviation from blueprint:** ledger called for attribute checks on `sub_domain` AND `run_id`; ships with `sub_domain` only — `run_id` is set ON MATCH on every re-write (provenance, not state) and follows the existing SUPPORTS/LINKS_TO pattern of being skipped in attribute diffs (same precedent as Entity skipping `first_run_id` and Source skipping `last_seen_at`). Domain attribute diff is existence-only for the same reason (`created_at`/`first_run_id` are creation provenance). |
| 80 || `graphdb-kdb snapshot` coverage for schema v2.1 (Domain + BELONGS_TO) | Filed 2026-05-21 from the #76 Opus code review. `snapshot.py` (the belt-and-suspenders JSONL+manifest backup per D35) did NOT export `Domain` nodes or `BELONGS_TO` edges — so a "journals AND Kuzu both lost" disaster after #76 would lose all domain tags. **Closed 2026-05-21.** Implementation: `snapshot.py` gains `_write_domains` (lexical-by-name ordering; cols: name/created_at/first_run_id) + `_write_belongs_to` (total-order by slug+name+run_id+created_at; cols: entity_slug/domain_name/sub_domain/run_id/created_at). `SNAPSHOT_FORMAT_VERSION` bumped 2→3 (purely additive; all v2 files still emitted). `counts` dict + `files_meta` extended; CLI `snapshot` print line extended with `domain=N belongs_to=N`. 4 new tests in `test_snapshot.py`: domain.jsonl content + manifest counts + v3 assertion, belongs_to.jsonl content + sub_domain nullable handling (omit-when-plural verified), empty files for pre-#76 graphs (parity with alias_of pre-#74 test), and dedicated `SNAPSHOT_FORMAT_VERSION == 3` check. Existing file-list / counts / byte-identical-ordering tests extended to include the two new files. Full suite 989 passed. **Deviations from blueprint:** (1) ledger called for a "back-compat read path for v2 snapshots" and (2) "round-trip tests" — both deferred because `snapshot.py` is write-only today (no `load-snapshot` exists). Precedent: #74.7 (v1→v2 alias_of bump) shipped without a reader for the same reason. Future `load-snapshot` will own format-version dispatch. "Round-trip" is here interpreted as write→parse-JSONL→assert-fields, which the new tests cover. |
| 81 || Step-3 V0 ops regression foundation                            | Filed 2026-05-21 (formerly `#78c` per Task #75 §OQ-5 — renumbered post-#79/#80). Strand 1 of step-3 V0 foundation per Task #75 §3.1 — V0 ops (typed traversal + shortest-path) already exist in production; this task formalises the regression-guardrail eval surface around them so they become proper members of the §4 predeclared-criteria framework. **Two-reviewer synthesis (Codex + Gemini) before scope lock:** Codex caught (i) typed traversal is **two distinct implementations** — query-time `graphdb_kdb.queries.neighbors()` uses Kuzu variable-length paths `[:LINKS_TO*1..depth]` (`queries.py:72`), compile-time `_t3_neighbors()` is hand-rolled Python BFS (`graph_context_loader.py:202`) — both need separate coverage; (ii) `neighbors()` has **no edge-type filter** in current impl (signature is `(slug, direction, depth)`, hardcodes LINKS_TO) — do NOT include edge-type filter tests (would be feature expansion not regression); (iii) shortest-path **runtime guard belongs in a marked bench test** (`@pytest.mark.bench`), not unit tests (flakiness risk). Gemini structural breakdown (A compile-time / B query-time / C shortest-path) adopted with Codex corrections applied. **Closed 2026-05-21.** Implementation: **(A) Compile-time** — 4 tests in new `TestT3NeighborsRegression` class direct-unit-test `_t3_neighbors` (determinism, bidirectional expansion, candidate filter honored, seed exclusion); 1 test in new `TestColdStartWideningInvariant` locks the #71 ≥5-seed gate via a dedicated 8-entity `cold_start_gdb_rich` fixture (existing `cold_start_gdb` capped at 4 reachable after title-eligibility filtering — too small for the invariant). **(B) Query-time** — 2 tests added to `test_queries.py` for explicit direction-exclusivity: `out` does NOT leak inbound, `in` does NOT leak outbound (closes §4.3 fail test ii). **(C) Shortest-path** — `test_shortest_path_runtime_guard` with `@pytest.mark.bench` + 1 s ceiling on the 4-node linear chain. `bench` marker registered in `pyproject.toml` `[tool.pytest.ini_options].markers`; `addopts` extended with `-m 'not bench'` so default `pytest` runs exclude bench (opt-in via `pytest -m bench`). 996 passed (+7 new), 1 bench deselected, 1 live-API skipped. **Coverage map for §4.3 "binary parity for unchanged sources":** the natural reading of the blueprint phrase is the **manifest-skip-path** behavior (source hash unchanged → kdb-compile preserves prior `compile_result.json`, no recompile) — this surface is already covered by `kdb_compiler/tests/test_kdb_scan.py` (`test_unchanged_already_compiled_is_skipped` line 347, `test_moved_with_compiled_content_is_skipped` line 401). What #81 adds is the complementary **traversal-primitive determinism** guard (`_t3_neighbors` direct unit tests) — different surface, both now locked. **§4.3 "≥5 seeds" interpretation:** pinned to ≥5 unique entities in `snapshot.pages` (user-visible artifact), not ≥5 T2 seeds specifically (internal). The `cold_start_gdb_rich` topology makes both interpretations hold, but the test asserts on `snapshot.pages` to nail the surface for future readers. **Strand 2 still deferred:** the §4.3 "probe-set correctness ≥95% match" gate remains blocked on Task #77 (curate step-3 probe set, OQ-3 questions pending user decisions). No separate blueprint — predeclared criteria already live in `archive/tasks/task75-predeclared-eval-criteria-blueprint.md` §4.3 + §3.1 line 164. |
| 82 || Round 6 — operationalize "Learn" for the second-brain goal ([B] of §6.1) | Filed 2026-05-22 to close the goal-discussion gap from Round 4 §6.3 (Joseph's [2] over-reach callout). §9.1 framing + working hypothesis (4-mechanism decomposition); §9.2 multi-model research dispatched (Gemini 3.1 Pro, GPT, Grok 4.3, Opus 4.7) — `docs/archive/rounds/round6-research-{prompt,gemini,gpt,grok,opus}.md`; §9.3 synthesis landed cross-model convergence on Learn = persistent state evolution + M2/M3 reclassified as Analysis-feeding-Create + Abstraction added as missing slot + Devil's Advocate gate surfaced. §9.3.7 external reviews (Codex + Antigravity 2026-05-22) at `docs/archive/tasks/task82-learn-architectural-options-{codex,gemini}.md` — both reject candidate (c); Codex recommends (a+), Antigravity recommends (b). §9.3.8 synthesizer's lean filed: (a+) with parallel-design sequencing nuance + devil's-advocate self-check + honest residual uncertainty. **Closed 2026-05-22 with Joseph's ratification of (a+).** §9.4 closeout at `docs/what-is-ontology-for-V1.md` §9.4: three Learn mechanisms + Hypothesis Promotion as first-class boundary contract via separate task; M2 + M3 reclassified as Analysis-feeding-[C] Create; M4 split per Opus (canon=#74 hygiene, temporal/contextual splits=#85); anti-goal [5] adopted as value-alignment criterion not philosophical disqualifier; [C] Create role assignment recorded. Follow-up: Tasks #83 (Promotion Contract) + #84 (Belief Revision) parallel-designed; #85 + #86 deferred. Milestone Changelog entry filed in `docs/CODEBASE_OVERVIEW.md` per [[feedback_milestone_closure_rule]]. |
| 95 || Pass-1 prompt/schema contract fix — two-stage validation + arrow→prose boundaries | Filed + closed 2026-05-30 (task A of `docs/orchestrate-real-run-1-to-2-tasklist.md`). Root finding from real-run-1: 4 code-owned fields (`override`/`model`/`prompt_version`/`schema_version`) leaked into the LLM output contract; a null `override.llm_original` fail-fasted the batch at source #21. **Fix (atomic):** LLM now returns **11 content fields only**; the deterministic layer owns provenance + audit. `pass1_schema.py` split into **Stage 1** `validate_llm_content()` (11 fields, no code-owned — the retry gate) + **Stage 2** `validate_envelope()` (full assembled envelope — the gap-closer; nothing validated the final object before). `pass1_caller.py` reordered: parse → Stage-1 validate → stamp `prompt_version`/`model`/`schema_version` (no longer builds `override`). `overrides.py` gains `build_override_block()` = **sole producer** of the override block (3 call sites: success path + enrich.py empty/failed paths). `enrich.py` adds Stage-2 validation before embed. `pass1_prompt.j2` → 11-field content-only template (Joseph-reviewed); `PASS1_PROMPT_VERSION` 1.1.0→**1.2.0**. Boundary-rule arrow notation (↑ ↔ ⇄) → "vs" + prose per DeepSeek panel finding #4. 5-model panel review (Codex+Deepseek+Gemini+Grok+Qwen) at `docs/task95-pass1-review/`; C-1 (4 code-owned fields) 5/5 CRITICAL convergent. TDD: +7 Stage-1 schema tests, +3 `build_override_block` tests, new `test_pass1_caller.py` (3 two-stage-flow tests); fixed a latent `source_type="essay"` fixture bug Stage 2 surfaced. Suite green (754 passed / 1 live-skip). Sketch: `docs/task95-pass1-review/two-stage-validation-sketch.md`. **The handling-policy half (per-field repair vs all-or-nothing) stays DATA-GATED on #98 benchmark** — this fix only corrects WHAT is validated + WHERE. |
| 96 || `kdb-orchestrate` error-handling architecture — severity taxonomy + structured event log, then quarantine-and-continue | Filed 2026-05-30 from Joseph's re-evaluation of the E2E orchestrator after real-run-1. **Chosen path ratified:** **B then C** — first ship the structured observability foundation, then revise fail-fast into quarantine-and-continue using that severity model. Scope B: first-class `OrchestratorEvent` JSONL at `state/runs/<run_id>/orchestrator_events.jsonl`; severity taxonomy (`debug` / `info` / `warning` / `source_quarantine` / `run_fatal` / `invariant_violation`); explicit production invariant checks (typed errors, not bare Python `assert`, always on regardless of CLI log level); CLI logging controls (`--log-level {warning,info,debug}`, with `--verbose`/`--debug` aliases); richer `last_orchestrate.json` with event-log pointer, warning/quarantine counts, and visible alarm surface; raw-response persistence or explicit missing-artifact event on model failures. Scope C: replace D-91-8 fail-fast with source quarantine for source-local failures; continue the loop; finalize always runs over the committed set, dissolving #94's stranded deferred `LINKS_TO` class; `run_fatal` and `invariant_violation` still abort. Blueprint: `docs/archive/tasks/task96-orchestrator-error-handling-blueprint.md`. Implementation plan: `docs/superpowers/archive/plans/2026-05-30-task96-orchestrator-error-handling.md`. **Progress:** B1-B2 implemented 2026-05-30 — `kdb_compiler/orchestrator_events.py` adds `OrchestratorEvent`, `EventRecorder`, severity/log-level filtering, best-effort JSONL writes, `OrchestratorInvariantError`, and `check_orchestrator_invariant`; 15 focused tests green. B3 implemented 2026-05-30 — `kdb-orchestrate` accepts `--log-level {warning,info,debug}` with `--verbose`/`--debug` aliases, `run(...)` constructs an `EventRecorder`, `OrchestrateResult` and `last_orchestrate.json` expose `event_log_path`/`event_log_failed`, CLI prints the event log path. B4 implemented 2026-05-30 — run/scan/dry-run/source/enrich/gate/compile/commit/reconcile/finalize/finish events are emitted, source-local failures are labelled `source_quarantine` while preserving fail-fast, and unexpected exceptions emit `run_fatal`; targeted event + orchestrator tests green (32 passed). B5 implemented 2026-05-30 — `last_orchestrate.json` now includes event-derived warning/quarantine/invariant counts plus slim `quarantined_sources`, event-log write failure is surfaced, and CLI prints a quarantine alarm; targeted tests green (34 passed) and broader non-live `kdb_compiler/` suite green (1 live skip). B6 implemented 2026-05-30 — Pass-1 failure sidecars preserve the last raw response when present and normalize model exceptions into the same failure path; Pass-2 failed response records preserve raw response text in `resp_stats`; failure events include artifact paths; missing raw responses emit `raw_response_unavailable`; focused tests green (117 passed) and broader non-live `kdb_compiler/` suite green (1 live skip). B7 implemented 2026-05-30 — orchestrator-owned Pass-2 response stats now live under `state/runs/<run_id>/pass2/` while legacy `state/llm_resp/<run_id>/` remains the default for older compile/benchmark surfaces; production invariant checks are wired into Pass-1 success, compile result, and commit result contracts; invariant failures emit `invariant_violation` events and write `last_orchestrate.json` with `exit_reason="invariant:<code>"` instead of becoming generic crashes; focused tests green (80 passed). B8 implemented 2026-05-30 — active source-state writers now use `run_state` instead of deprecated `compile_state`; source-state schema is v3.1 with explicit lifecycle/error values (`pending`, `no_graph_db`, `in_graph_db`, `error_ingest`, `error_compile`, `error_commit`); legacy `compile_state` plus old lifecycle values migrate forward; deprecated `compile_state` is rejected if still present after migration; GraphDB keeps graph-side `ingest_state` and bridges from producer `run_state`, with legacy fallback only for old replay payloads; focused source-state/scan/orchestrator/GraphDB tests green and broader non-live `kdb_compiler/` plus focused GraphDB suite green (1 live skip). C1 implemented 2026-05-30 — Pass-1 source-local failures now write `run_state=error_ingest` plus structured `last_failure`, Pass-2 failures write `error_compile`, source-local pre-manifest commit failures write `error_commit`; all keep `last_compiled_hash` unchanged so retry eligibility remains hash-honest; source-local quarantines continue to the next source and completed runs exit `0` with `exit_reason=completed_with_quarantines` plus summary/event/CLI alarms; Stage-2 Pass-1 assembled-envelope validation failures now route through `enrich_failed` instead of unexpected crashes; focused C1 suite green. C2 implemented 2026-05-30 — finalization now runs whenever at least one source committed and no run-fatal/invariant abort occurred, even after later quarantines; all-quarantined runs skip finalization with a visible `finalize_skipped` event while still writing summary/event log; #94 regression test proves a committed source with deferred `LINKS_TO` is wired and `compile_result.json` contains the committed set after a later source quarantines. **C3 DEFERRED 2026-05-30** (decision Joseph + assistant) — circuit-breaker from severities deferred: runs are attended and `--limit N` (#99) caps blast radius; C1/C2 already surface all-quarantined runs loudly with no masking; thresholds need a measured baseline ("data before principle"); B's severity model makes the breaker a pure additive policy layer so deferral is reversible. Trigger to revisit: a measured quarantine-rate baseline from the first real multi-source run, or any move to unattended/scheduled runs. Resolves open ratification point #3 = *none for first C pass*. **D-96-1** recorded: D-91-8 fail-fast is *narrowed* to run-fatal scope (source-local `source_quarantine` continues; `run_fatal`/`invariant_violation` still abort). **CLOSED 2026-05-30** — B (B1-B8) + C1 + C2 complete; full non-live `kdb_compiler/` + `graphdb_kdb/` suite green (1 live smoke skip). |
| 97 || GraphDB viewer — multi-model render-only bake-off | Filed 2026-05-30. Completed 2026-05-31. Six models submitted single-file offline HTML viewers (Cytoscape/Opus baseline + Gemini/Qwen/Codex/Grok/DeepSeek) over a neutral graph export (`tools/viewer/bakeoff/export_graph.py` → `graph-export-run3.json`). **Winner: Gemini** (D3 force-directed; cohesive central-seed constellation); Qwen + Codex kept as references; Grok + DeepSeek dropped. Promoted to the **official single-command viewer** `tools/viewer/kdb_graph_viewer.py` (reads Kuzu directly → neutral JSON → injects into `tools/viewer/kdb_graph_viewer_template.html`, the Gemini template). Cytoscape `kdb_graph_viewer-opus.py` **retired**. Refinements on the base: nodes 2/3 scale, Entity=blue/Source=green, Elastic-only layout (Clusters removed), self-loops dropped, solid BELONGS_TO / dashed SUPPORTS, smaller arrowheads, arrowheads dim with ego-focus, hover ego-focus, combined right panel (search + node/edge filters + reset + node detail) with a header show/hide toggle. Original Gemini submission + 2-stage `build_gemini.py` pipeline + a 2/3 reference copy preserved under `tools/viewer/bakeoff/` as fallback. Design: `docs/superpowers/archive/specs/2026-05-31-opus-viewer-refinement-design.md`. Live runs need `graphdb-kdb rebuild` (graph v2.3 → code v2.4). |
| 101 || `kdb-orchestrate` console progress display (#96 follow-up) | Filed 2026-05-30 after run-3 — an attended ~30-min run produces **zero console output** until the final summary (felt like a hang). #96 events go only to `orchestrator_events.jsonl`; the CLI prints just a final summary. **Design (ratified with Joseph, build pending):** `EventRecorder` tees a **per-source count snapshot** to **stderr** after each source completes — `⏱ <elapsed> [N] · enriched · compiled · committed · noise · quarantined ▸ <current source>`. N = recorder self-count on `source_started` (no M/denominator — Joseph's call); elapsed = now − run-start; counters tallied in the recorder (single source of truth, already sees every event). Alarms (`source_quarantine`/`run_fatal`/`finalize`) print their own persistent line. **Scrolling plain `print`, no `\r`/`isatty` machinery** (Joseph: snapshot-per-source, not in-place dashboard). **DONE 2026-05-30:** `EventRecorder` gains `console`/`clock` params + a `_render_console` tee (counters tallied as events are recorded; snapshot after each terminal per-source event; alarms get a `⚠` line; best-effort, never a second failure path). CLI wires `console=sys.stderr` only at `--log-level info`/`debug` (default `warning` stays quiet). TDD: 6 unit (`test_orchestrator_events.py`) + 2 integration (`test_kdb_orchestrate.py` info-streams / warning-quiet). Full non-live suite green. **Status:** done. **Superseded by #102** (the stderr/info-gated console approach was replaced by default-on stdout per-stage progress). |
| 102 || `kdb-orchestrate` live progress on stdout (full console contract) | Filed + done 2026-05-31. The 0.5.0 "stdout/progress messaging" item; **supersedes #101's console approach** (per-source snapshot to stderr, gated at `--log-level info/debug` — a default run was silent for ~30 min). **Console contract:** live blow-by-blow narrative streams to **stdout, on by default**; `--quiet` silences it; `--log-level {warning,info,debug}` now governs **only** JSONL verbosity (decoupled from the console). Per source: a `[n/total] ▸ source` header, then `pass-1 enrich…`/`pass-2 compile…` printed **at stage start** and completed with `✓ <elapsed>` (deltaed via the recorder's injectable clock), the noise→skip path, a trailing running `done · skipped · noise · quarantined` count, and inline `⚠` alarm lines; reconcile/finalize get a `⏱ MM:SS` line. Plain scrolling `print` (no `\r`/`isatty`). **Mechanism:** `EventRecorder._render_console` rewritten + moved ahead of the `should_record` gate so it renders regardless of file verbosity; `set_progress_plan(total, skipped)` prints the run header (denominator reinstated, which #101 omitted); two new `pass1_enrich_started`/`pass2_compile_started` info events; CLI `--quiet` + `run(quiet=…)`, `progress_console = None if quiet else sys.stdout`. Event JSONL + `last_orchestrate.json` unchanged. Spec `docs/superpowers/archive/specs/2026-05-31-orchestrator-live-progress-design.md`, plan `docs/superpowers/archive/plans/2026-05-31-orchestrator-live-progress.md`. TDD: 4 renderer unit + 2 integration (default-streams / `--quiet`-silent) replacing the #101 tests; full non-live suite green (1209 passed). Commits `704438d`, `91add83`. **Next 0.5.0:** D3 domain-scoped T2/T3 retrieval → **run-4** (the orchestration-reliability gate). |
| 103 || T2/T3 domain-scoped retrieval (D3 consumer; same-domain gate) | Filed + done 2026-05-31. Implements **D3** as a **hard same-domain gate** — **overrides** the blueprint's ratified C (coordinate-not-gate); override + rationale recorded in `ontology-blueprint-V1.md` §7. **Why:** T1/T2/T3 context exists for entity anti-entropy (show Pass-2 the existing entities to reuse); off-domain entities are noise for that job. **Decision:** the Pass-2 context is a **positive pull from the source's Pass-1 domain only** — not a filter with edge cases (Joseph). T2 (`entity_search_keys`) + T3 (neighbors) candidate universe = entities `BELONGS_TO` the source's domain (joined on `Domain.name = frontmatter.domain`); T1 (the source's own `SUPPORTS`) unchanged, same-domain by construction. No padding — short/empty same-domain context preferred over off-domain noise (priority: full > short/empty > padded). A source with no Pass-1 domain (`frontmatter=None`) falls back to the full graph. Dropped from variant D: the 70/30 budget, cross-domain weighting, exact-T2-global. **Out of scope:** cross-domain link *creation* (unchanged — LLM's call) and cross-domain Discover (future, query-time). **Mechanism:** `_domain_pool(conn, domain)` helper + `build_context_snapshot` passes `pool` as the candidate universe to `_build_t2`/`_t3_neighbors` (both already filter by `candidate_slugs`); ~10 lines total. Spec `docs/superpowers/archive/specs/2026-05-31-t2-t3-domain-scoping-design.md`, plan `docs/superpowers/archive/plans/2026-05-31-t2-t3-domain-scoping.md`. TDD: 5 new gate tests (off-domain key dropped, same-domain key kept, cross-domain neighbor excluded, no-padding/all-same-domain, no-domain fallback) + t2-mode-dispatch fixture updated to model domains; full non-live suite green (1214 passed). Commits `d1a62df`, `9c581b6`. **run-4 → findings → #104 fixes → run-5 clean → `v0.5.0` tagged.** |
| 104 || Pass-1 coercion + Pass-2 retry (run-4 findings) | Filed + done 2026-05-31 from run-4 (1 quarantine: Borda doc, Pass-2 `JSONDecodeError` — LaTeX `\(…\)` written unescaped into a JSON string; stochastic, not a regression — same source compiled in run-3). Three fixes, principle = **coerce benign deviations, retry recoverable emissions, reject only the unrecoverable** ([[feedback_coerce_dont_reject]]): (1) Pass-1 `normalize_llm_content` truncates >10 `entity_search_keys` to 10 *before* validation (schema stays strict); (2) Pass-1 drops the OQ-NW7-7 `other_reason`-required cross-field rule (audit field; null coerced-through, both stages); (3) Pass-2 `compile_one` retries on a recoverable bad-JSON emission (extract/parse/schema), mirroring Pass-1's `call_pass1` retry (`_MAX_COMPILE_ATTEMPTS=2`; truncation/hard-model-error/semantic stay terminal). Findings + audit: `docs/run-4-findings.md`; plan `docs/superpowers/archive/plans/2026-05-31-pass1-coerce-pass2-retry.md`. TDD: 5 new tests (+2 existing other_reason tests flipped). **Run-5 validated the fixes** — the Borda doc retry-recovered, a Balance-Sheet `summary_slug` schema slip (`---`) retry-recovered, 0 quarantined. 1219 non-live tests green. Commits `554d7de`, `e1ca3e7`. Out of scope (no live evidence): confidence-clamp, nullable-default. |
| 105 || Codebase architecture realignment — vocabulary + structure reflect architecture | Filed + Phase A shipped 2026-06-02. Architecture-level refactor so the implementation reflects the decided architecture (two pipelines — *ingestion* / *compiler* — over a *graph* substrate, conducted by an *orchestrator*, + out-of-band *tools* and a *common* leaf). Thesis (Joseph): **terminology debt IS the architecture problem** — names still carried monolithic-`kdb_compile`-era meaning. 5-panel ratified (Codex/Deepseek/Qwen/Gemini/Grok-build). One refactor, two **sequential** mandatory phases (A then B). **Phase A (fix-in-place) DONE** — merged `8fac79f`, **run-6 clean** (29 compiled / 7 noise / 0 quarantined / 478 links / 0 orphans; graph 180 Entity / 29 Source / 10 Domain / 100% `BELONGS_TO`): retired legacy `kdb_compile.py` + 427-ln `run_journal.py` + dead `planner`/`run_compile` + 5 CLI bindings; fixed 2 layering inversions (`common` is now a leaf, guard-tested); renames `reconcile→repair` · `patch_applier→page_writer` · `source_state_update→manifest_writer` · `validate_compiled_source_response→validate_source_response` · `ingestion/→enrich/` (+`run_journal→enrich_journal`); **single Kuzu door** (10-fn read API in `graphdb_kdb/queries.py`; `graph_context_loader→context_loader`, byte-identical Cypher port); North Star §5 rewrite + stale-ref sweep. 1175 non-live tests green; tagged `v0.5.1`. **Phase B (package split) DONE** — merged `f28220e`, tagged **`v0.5.2`** (2026-06-02); **run-7 clean** (`exit_reason=ok`; graph 193 Entity / 29 Source / 10 Domain / 195 BELONGS_TO / 202 SUPPORTS / 468 LINKS_TO ≡ run-6 standard, zero-behavior-change). Split into six peer packages `common`/`ingestion`/`compiler`/`kdb_graph`/`orchestrator`/`tools`, leaf-first move-don't-rewrite (executed subagent-driven, 13 tasks); **B.3 dependency contract guard-tested**; `resp_stats_writer` split (`common/llm_telemetry` + `compiler/resp_summary`, lifted gate byte-identical); deferred Phase-A cleanups landed (`failure_stage="repair"`, `JOURNEY` noun). 1191 non-live tests green. **5-panel code review GO-WITH-FIXES** (Codex/Deepseek/Qwen/Gemini/Grok; all must-fixes folded F1–F6; synthesis `docs/superpowers/archive/specs/2026-06-02-phase-b-review-synthesis.md`); 3 non-blocking follow-ups → **#107**. Plan: `docs/superpowers/archive/plans/2026-06-02-phase-b-package-split.md`. Blueprint: `docs/superpowers/archive/specs/2026-06-01-codebase-realignment-*`. [[project_codebase_realignment]] |
| 106 || JSON-escape → retry → coerce → quarantine ladder (LLM-output robustness) | **CLOSED — shipped `v0.5.3`, merged `2fb5d0e`, run-8 clean** (`exit_reason=ok`, 0 quarantined; 1213 non-live tests). Deterministic re-validation-gated Pass-2 repair ladder. **Rung-1 = targeted backslash-escaping** (`common/util/json_escape_fix`, content-preserving — the 5-panel design review unanimously replaced `json-repair` with this; **no pip dep**). **Rung-2 = slug coercion** `common/paths.collapse_slug` (**lowercase**+collapse+strip — Joseph's decision B, over my conservative lean A) + `compiler/repair.coerce_slugs_and_propagate` (rename across 7 fields, regex wikilink rewrite preserving `\|display`/`#anchor`, all-values collision guard). **LB2: `semantic_check` moved in-loop**; compositional telemetry (`compile_attempts`/`syntax_repaired`/`slug_coerced`/`final_status`). 5-panel design review (GO-WITH-CHANGES, all folded; synthesis `…task106-review-synthesis.md`); implemented subagent-driven (8-task TDD plan `…2026-06-02-task106-json-escape-slug-coercion.md`); two-stage review caught 2 real bugs (telemetry sticky-flag; semantic-gate gap). run-8 telemetry: all 29 Pass-2 compiles `final_status='clean'` (malformations didn't recur — rungs stayed correctly dormant). Spec v0.2: `docs/superpowers/archive/specs/2026-06-02-json-repair-slug-coercion-ladder-design.md`. [[project_json_repair_coerce_ladder]] |
| 108 || Pass-1 robustness: outcome telemetry + shared `json_escape_fix` (defer full ladder until data) | **CLOSED 2026-06-06 — shipped, merged to `main` `610e2c8`** (with #109; run-9 `--emit-kpis` validated end-to-end, clean; 1338 non-live tests). All three settled pieces landed: **(1)** Pass-1 outcome telemetry — `final_status` + aggregate token/latency/`call_count`/`final_attempt_index` persisted into the sidecar `raw_response` on every write path (clean / repaired / quarantined / Stage-2 failure), mirroring Pass-2's #106 compositional telemetry and feeding #109's `PassCallMeasurement.from_pass1`; **(2)** shared `common/util/json_escape_fix.escape_stray_backslashes` wired into `pass1_caller` before `json.loads` (re-validation-gated, single call site, bare re-raise); **(3)** full ladder correctly deferred (no nullable null-fill, no temp-bump — data-before-principle holds, Pass-1 still has zero repairable failures). Two-stage review caught + fixed the Stage-2 telemetry-loss bug (`5e0dba1`). **Scope settled 2026-06-05** (retitled from "Extend the repair ladder into Pass-1" — name-must-match-contents: we are NOT porting #106's multi-rung ladder; that would be premature abstraction, the trap #106 itself avoided). **The reframe:** run-8's two Pass-1 failures were both **retry-only, not repairable** — empty/non-JSON response (`Expecting value: line 1 col 1`, Borda doc — nothing to escape) and `summary: None` (Berkshire doc — `summary` is required/non-nullable, no benign default; a fabricated summary is lossy). The #104-era `call_pass1` retry already rescues both, and **every retry across runs has succeeded with no temp bump** → no evidence of a deterministic-failure class to escape ([[feedback_no_imaginary_risk]]). The json-escape/backslash class IS real and recurring but has only ever hit **Pass-2** (run-4 quarantine → run-5 retry-recovery, the Borda-doc LaTeX `\(…\)` slip); #106 hardened it there. Pass-1 has had **zero** repairable failures. **Settled scope (3 pieces):** (1) **Pass-1 outcome telemetry** — give Pass-1 a `final_status`-style record mirroring Pass-2's #106 compositional telemetry; load-bearing because it (a) feeds #109's unified `PassCallMeasurement` and (b) makes Pass-1 failures observable so a future rung is *data*-justified; (2) **wire the shared `common/util/json_escape_fix` into Pass-1** before `json.loads` (re-validation-gated, ~1 call site) — one preventive rung, justified only because the class is proven + pass-agnostic (a model can emit LaTeX in a Pass-1 summary as in a Pass-2 body); (3) **defer the full ladder** — nullable null-fill + any further rungs wait until a Pass-1 repairable class actually appears (data-before-principle). **Not doing:** temp-bump on retry (no deterministic-failure evidence); `summary=None`/empty-response coercion (retry-only, not coercible). Next: blueprint → `writing-plans`. [[project_json_repair_coerce_ladder]] · [[feedback_coerce_dont_reject]] · [[feedback_name_must_match_contents]] |
| 109 || Benchmark redesign — quality-only, two-family KPIs (processing + graph) over Borda | **⚠️ CLI REFACTOR + WEAK-SPOT PENALTY 2026-06-06 (branch `feat/benchmark-cli-refactor-penalty`, spec `docs/superpowers/archive/specs/2026-06-06-benchmark-cli-refactor-penalty-design.md`, plan `docs/superpowers/archive/plans/2026-06-06-benchmark-cli-refactor-penalty.md`).** Legacy **#5 run engine RETIRED** — deleted `runner`/`scorer`/`scorecard`/`registry` + `models.json` (~1,900 LOC, self-contained island) + their tests; `kdb-benchmark` is now single-purpose — `score` is a **real argparse subcommand** (visible in `--help`), and "running" a model = `kdb-orchestrate --emit-kpis`. Added a **weakest-link penalty** to `score_models` (the one idea carried over from the old D31 outlier penalty, re-adapted for an all-Borda leaderboard): `weakest = min` over the **4 composite axes** (quarantine / graph_score / recovery / latency, equal treatment), `penalty = 0.10·max(0, (0.5−weakest)/0.5)` **capped at 0.10**, subtracted from the composite; `weakest_kpi` recorded; **leaderboard-only** (cross-model Borda is undefined for a single run → not in per-run `report.md`). Headline score now rendered **0–100** with a `PENALTY` column (terminal + `leaderboard.md`); `per_kpi_borda`/`graph_score` stay [0,1] components. **λ=0.10 PINNED; τ=0.5 PARKED** for cohort calibration alongside the §6 weights. Subagent-driven TDD, two-stage review per task, all green. **No `CODEBASE_OVERVIEW` Milestone Changelog entry yet — lands when #109 fully closes after weight calibration.** **Doc debt to clear at #109-close:** `CODEBASE_OVERVIEW.md §7` still describes the deleted engine (`runner`/`scorer`/`scorecard`/`registry`, the `run_benchmark→score_run→write_scorecard` dataflow, `models.json`, old D26/D30/PENALTY) — already stale at the framework merge, now describes non-existent files; rewrite §7 for the orchestrate-emit → score-only architecture when the Milestone Changelog entry lands. &nbsp; **⚠️ KPI-REFINED 2026-06-06 (branch `feat/benchmark-kpi-refine`, spec `docs/superpowers/archive/specs/2026-06-06-benchmark-kpi-refinement-design.md` §1–§6) — supersedes the scored-set described below.** Three rounds, all on the branch, **1355 non-live tests**, end-to-end smoke-validated: **(R1)** `dangling_link_rate` **DELETED** (degenerate); per-pass `latency_pass1`/`latency_pass2` diagnostics; per-run dir `benchmark/runs/<model>-<run_id>/` + rendered `report.md`. **(R2)** `score` redesigned as an **incremental model leaderboard** (`{model→run_dir}` pointers + ranking; no KPI values stored), **no `corpus_fingerprint` gate** (comparability = user judgment), one row per `header.model` (`group_key` removed; emit `provider`+`model`), reset = delete leaderboard file. **(R3 = §6 combined graph score)** the **four graph KPIs scored together** (`entity_reuse` alone *inverted* the signal vs the 2-model gemini/deepseek cohort) into a weighted **combined graph score**; `intervention_burden`→**`recovery_rate`** (retry∨repair; `token_overrun` dropped to its own diagnostic). **Net scored set: `quarantine_rate ↓ · recovery_rate ↓ · latency ↓` + graph `entity_reuse ↑ · graph_connectivity ↑ · link_density ↑ · supports_density ↑`.** **Weights (§6 starting point, in `compiler.kpi.score.DEFAULT_WEIGHTS`):** quarantine 40 / **graph 40** (within-graph connectivity 35 / link 30 / supports 20 / reuse 15) / recovery 10 / latency 10 — rationale-based, validated by reproducing the independent deepseek>gemini read (deepseek 0.79 vs 0.21), tuned with ≥3 models later (Spearman supports↔link redundancy; volume-normalized reuse). **Still open / next:** Path B chosen — merge the branch, then **re-run all models with §6 code** (the 2 prior gemini/deepseek runs are pre-§6, not scoreable as-is) → leaderboard → tune weights. **Deferred:** corpus-independent model-quality KPI brainstorm. — **FRAMEWORK LANDED 2026-06-06 — merged to `main` `610e2c8`; weights/final-selection/promotion PARKED to post-run-1 calibration** (the only step needing live cross-model spread — data-before-principle). Shipped per the 13-task plan (`docs/superpowers/archive/plans/2026-06-05-benchmark-implementation.md`), blueprints B1/B2/B3, subagent-driven TDD, 1338 non-live tests: **`common/measurement.py`** (`PassCallMeasurement` P1+P2 projection + `RunMeasurementHeader` + `load_run_measurements` — logical view over telemetry, NOT a new store); aggregate discarded-attempt telemetry on `RespStatsRecord`; **`compiler/kpi/`** (`processing.py` + alias-aware `graph.py` + Borda `score.py`); **`orchestrator --emit-kpis`** → repo `benchmark/runs/<id>/measurements.json` (reset-surviving); **`kdb-benchmark score <run-id…>`** (latest-per-`group_key`, same-`corpus_fingerprint` gate) + **watched-diagnostic promotion rule** (`tools/benchmark/promotion.py` — CoV>0.2 ∧ IQR-floor ∧ Spearman<0.7). **run-9 validated the emit path end-to-end** (clean `measurements.json`: header/group_key + processing.scored {quarantine 0 / intervention 0 / latency} + graph.scored {dangling 0} + all 4 watched + diagnostics, all populated). **Calibration watch (run-9 made concrete):** `dangling_link_rate=0.0` looks perfect but `entity_reuse 2.6%` / `connectivity 14.5%` / `search-key-resolution 24.6%` reveal a sparse fragmented graph → dangling-rate must be co-interpreted with density/reuse (prime promotion candidates); single-model run can't tell corpus-inherent from model-specific. **To close #109:** Joseph fires ≥3-model cohort `--emit-kpis` → `kdb-benchmark score` → set weights + run promotion (B3 §9). _Filed 2026-06-05. Redesign of the benchmark (supersedes the #5 engine's Pass-2-only scope)._ **Purpose lock:** model selection is settled on cost (`deepseek-v4-flash` wins on asymmetric per-run cost regardless), so this measures **quality only** — "among these models, which builds the best graph?" **Architecture converged** (directions doc `docs/superpowers/archive/specs/2026-06-03-benchmark-redesign-directions.md`, `⭐ Convergence 2026-06-04`): `kdb-orchestrate` is the run vehicle that *emits* per-run `measurements.json` (processing inline; graph via opt-in CLI post-process) + builds the graphDB; `tools/benchmark/` *scores only* (cross-model Borda). GT-free · per-token normalization (ratio pass-through for 0–1) · two families kept SEPARATE · latest-run-per-model (no historical averaging) · full-corpus sandbox (`Vault-in-place-test-run`, 36 src). **KPI list v0.2 — 5-panel reviewed** (codex/deepseek/qwen/gemini/grok; brief `docs/superpowers/archive/specs/2026-06-05-benchmark-kpi-enumeration-brief.md`, commit `7d2df24`): scored set reshaped **7→4** (`quarantine_rate` · `intervention_burden` · `dangling_link_rate` · `latency`); BELONGS_TO coverage + semantic-pass **demoted** (verified dead/narrow vs code); graph axis ships as **watched diagnostics** (entity-reuse/fragmentation · connectivity · orphan · search-key-resolution) promoted on first multi-model run; `link_resolution` corrected to **hybrid compute** (ingestor silently drops dangling links). **Framework prerequisites (blocking):** (1) aggregate discarded-attempt telemetry · (2) unified `PassCallMeasurement` (P1+P2) · (3) per-pass denominators. **Next:** anchors/weights + framework spec → writing-plans → impl. **Deferred:** always-on per-invocation processing-KPI telemetry. **Subsumes** #98's distributional Pass-1 axis (GT-free domain breadth / domain-null / signal-noise / search-key-resolution); **provides** #2's cost/latency baseline. Shares the unified `PassCallMeasurement` record with #108. [[project_benchmark_redesign_architecture]]. **CLOSED 2026-06-07** — baseline-1 confirmed weights valid; τ=0.5 PINNED; § 7 North Star doc rewritten for the new architecture. |
| 111 || Optimal per-model calls → clean-slate re-benchmark (`json_object`→`json_schema` + per-model reasoning config) | **Spec ratified `docs/superpowers/archive/specs/2026-06-07-optimal-model-calls-design.md`; two-phase tagged de-risk (baselines `v0.5.5`/`v0.5.6`/`v0.5.7`); batch-1 = deepseek-v4-flash / qwen3.5-flash / gpt-5.4-mini / gemini-3.1-flash-lite. **PHASE 0 SHIPPED** (merged `f82ca4c`, tagged **`v0.5.5`**, pushed): release_version provenance + per-run `console.log` + leaderboard keyed on (provider, model, release_version); plan `…phase0-run-provenance-leaderboard-key.md`. Decision: prior 3-model cohort preserved as the **`@v0.5.4` baseline-0** (no separate v0.5.5 benchmark). **PHASE 1 DONE on `feat/111-phase1`** (subagent-driven, Tasks 1–6, 1228+ non-live tests, NOT yet merged/tagged): pool split `models.json`(active)/`models_dropped.json`(human archive); retired dead dropped-guard (dropped id → `UnknownModelError`); roster — dropped grok-4-1→archive, added grok-4.20-0309-non-reasoning + gemma-4-12b-qat; **gpt-5.4-mini `reasoning_effort:"low"`** (Joseph-verified for structured output). **Gemini NATIVE handler SHIPPED in Phase 1** (`google-genai` SDK `_call_gemini`, off the openai-compat shim; json-mode `response_mime_type=application/json` + `thinking_config.thinking_level="minimal"` — Gemini 3.x uses thinking_level not thinking_budget; flash-lite floor=minimal, full-off unsupported); telemetry maps usage_metadata (output=candidates+thoughts); 1232 non-live tests. **`response_json_schema` → Phase 2** (the schema variable stays isolated). Plans `…phase1-pool-prep-per-model-config.md` + `…phase1-gemini-native-handler.md`. **PHASE 1 SHIPPED + tagged `v0.5.6`** (tag moved in-place 3× to fold mid-cohort fixes, roadmap unchanged): also includes **retry-telemetry fix** (re-prompt recoveries now counted in `recovery_rate`/`retry_load`; SDK transient retries excluded), **per-source token in/out on console → `console.log`**, **gpt-5.4-mini temperature omitted** (GPT-5 reasoning rejects non-default temp; nullable per-model pool field), and **`gemma4-12b-qat-128k`** added (replaced placeholder). **✅ CHECKPOINT A — baseline-1 LOCKED (2026-06-07):** 4-model cohort scored. **gpt-5.4-mini #1 on QUALITY** (84.67; graph_score 0.95, 0 quarantine — once called right, it overtook deepseek, validating the #111 thesis) > deepseek 54.67 > gemini 21.33 (quarantines most → Phase-2 schema target; native path bought speed not quarantine-reduction) > qwen 9.33. **DECISION: deepseek-v4-flash stays the DEFAULT on cost** ($0.12/run vs gpt's $0.84 = 7.2×; deepseek = 2nd-best graph + 0 quarantine + clean) — gpt is the "premium quality" option if cost is no object. **✅ #111 CLOSED 2026-06-07 — Phase 2 (`json_schema`) SHELVED (data-mooted).** baseline-1 showed the default (deepseek) already at **0 quarantine** under `json_object` + the coerce ladder, so structured-output was solving a problem we don't have; the real win was the **per-provider thinking/reasoning-disable latency cut (~30–70%)** + the clean re-benchmark. Strict json_schema rejected: would grammar-mask our generative `body` (quality risk), can't express our semantic contracts (slug-format / body↔links — owned by the reconciler+coerce layer regardless), drops value-constraints across providers, and contradicts coerce-don't-reject. Un-shelve only if a future model genuinely needs schema-constrained decoding. [[project_111_optimal_model_calls]]. _Origin — filed 2026-06-07 from the #110 provider-API-call documentation pass_ (`docs/reference/model-provider-api-calls.md`). We send the WEAK `response_format={"type":"json_object"}` for ALL openai-compat providers; the strong **`json_schema` (strict, schema-constrained)** form is the real JSON-reliability lever — likely the fix for Gemini's #109 quarantines. **Verified:** openai (`gpt-4o-mini`+) and xai (`grok-4-1-fast-reasoning`) both support `response_format={"type":"json_schema",…,"strict":true}` **in the existing compat path** (no per-provider split needed — upgrade `response_format` + thread our schema in: `ModelRequest` gains an optional schema, callers pass `compile_result.schema.json`/Pass-1 schema; schemas must be made strict-compatible). **Gemini** uses the native SDK's `response_json_schema` → needs a native `_call_gemini` handler (sibling to `_call_anthropic`) UNLESS its compat endpoint accepts `json_schema` (⚠️ unverified — the one cell that forces a split). **Reasoning-disable (⚠️ no guessed params on paid calls):** xai `reasoning_effort:"none"` (verified grok-4.3, NOT our grok-4-1-fast-reasoning); openai gpt-5.5 `reasoning={"effort":"none"}` (`"low"` is OpenAI's recommended floor for extraction — A/B vs none; gpt-5.4-mini unconfirmed); gemini `thinking_config.thinking_budget=0` — all expressible via `_THINKING_DISABLE_EXTRA_BODY` once verified per-model. **Architecture insight (data-before-principle):** the structured-output win is mostly a `response_format` upgrade IN the shared path, NOT the wholesale per-provider split first floated — split only forced for Gemini. **Concrete-first sequencing:** build Gemini-native (2nd native concrete after anthropic) → prove quarantine reduction → then extract a per-provider adapter registry if warranted. Re-benchmark affected models (#109 scores move). All findings + per-provider examples/URLs in `docs/reference/model-provider-api-calls.md`. Relates to #109, #110. |
| 112 || Fix dead `read_only=True` flag in `GraphDB._open()`           | **CLOSED 2026-06-10 (`8d63019`), TDD 5 tests RED→GREEN, kdb_graph suite 260 + read_only-consumers 155 green.** Standalone correctness fix (Joseph-greenlit); prerequisite for the read-only MCP server (#113-family, design `docs/superpowers/archive/specs/2026-06-10-graph-access-package-design.md`). `GraphDB.__init__` stores `_read_only` but `_open()` ignores it — always `kuzu.Database(path)` (read-write) + always `_ensure_schema()` (DDL/migrations). Every current caller passing `read_only=True` (`tools/viewer/kdb_graph_viewer.py`, `orchestrator/emit_kpis.py`, `tools/viewer/bakeoff/export_graph.py`) silently gets a write handle. **"Read-only by construction" is currently false.** Fix: pass `read_only=self._read_only` to `kuzu.Database(...)` **and** skip `_ensure_schema()` on read-only open; on read-only, **verify** `SCHEMA_VERSION` and fail fast on mismatch (never migrate). Regression test: write methods (`apply_compile_result`/`apply_cleanup`/`detect_orphans`/`wire_links`) blocked/fail when `_read_only`. ~10 LOC + tests. Unanimous 5/5 panel blocker (P1+P2). |
| 113 || Graph-access package + read-only MCP server                   | **Parent. RATIFIED design `docs/superpowers/archive/specs/2026-06-10-graph-access-package-design.md` (v0.3, 5/5 panel GO-WITH-FIXES + 6-model killer-app challenge → Named Gate = Epistemic Load-Bearing Stress Test).** Extract `kdb_graph` as the standalone graph-access contract; compiler/orchestrator/tools/viewer become clients; read-only MCP server co-located. Prereq #112 (read_only fix) ✅ `8d63019`. **3 phased plans:** **Phase 1 ✅ DONE 2026-06-10** (`docs/superpowers/archive/plans/2026-06-10-graph-access-phase1-package-boundary.md`; 4 subagent-driven TDD tasks `2bb91fe`→`213d189`, each spec+quality reviewed, merged to main; published `kdb_graph.testing`, repointed cross-package importers off `kdb_graph.tests`, exported `GraphDBReadOnlyError`; 345 kdb_graph+tools + 8 boundary tests green); **Phase 2 ✅ DONE 2026-06-11** (`docs/superpowers/archive/plans/2026-06-11-get-body-content-tool.md`; subagent-driven TDD, per-task spec+quality review + final holistic review; shipped `common/wiki_io.py::get_body(slug, page_type, *, root)` + `ContentNotFoundError` — thin reader composing `paths.slug_to_abspath` + `source_io.parse_existing_frontmatter`, body-only/frontmatter-stripped; **reader lives in `common/` (placement A), NOT in `kdb_graph` — keeps the package zero-`common`; the Phase-3 MCP tool + viewer are thin consumers**; 9 unit tests + live smoke on real vault, full suite 1270 green); **Phase 3a ✅ DONE 2026-06-11** (`docs/superpowers/archive/plans/2026-06-11-phase3a-mcp-read-server.md` + SDK-verification `…/specs/2026-06-11-phase3-mcp-sdk-verification.md`; subagent-driven TDD, final holistic review; new in-repo sibling pkg **`kdb_mcp/`** = read-only MCP stdio server, 7 FastMCP tools — `get_entity`/`graph_neighborhood`/`find_path`/`sources_for_entity`/`entities_for_source`/`resolve_search_keys`/`get_body` — each per-query read-only reopen, stable Pydantic shapes, isError envelope; `mcp` 1.27.2 dep; in-memory `create_connected_server_and_client_session` integration test; `resolve_search_keys` slugifies inputs so human names resolve (advisor catch); **config default fixed to derive graph from `<vault>/KDB/graph`** (official `~/Obsidian/KDB`, sandbox `~/Obsidian/Vault-in-place-test-run`; the stray `~/Droidoes/GraphDB-KDB` 2.3 is dead); 19 kdb_mcp + full suite 1290 green; real-graph smoke validated end-to-end). **Phase 3b (`stress_test`, the Named Gate) ABANDONED 2026-07-07 — this abandons ONLY the metacognitive stress-test IDEA, not the server.** Precondition check on the live 248-entity sandbox graph showed the analytics are degenerate at personal scale: grounding has no variance (214/218 canonical entities have exactly 1 supporting source) and only 2 of 486 LINKS_TO edges cross a community boundary (4 bridge entities total). It needs grounding-variance + real bridges to yield non-trivial output — the ratified *scale hedge* ([[project_ontology_purpose_kernel_question]]) firing — and with no ground truth it would be correct-but-unvalidatable code against an untunable formula. Dropped with 3b: the 2 planned primitives (`indegree`/`entity_list`). **The `kdb_graph` package + 7-tool read-only MCP server (Phases 1/2/3a) are a RETAINED asset — the intended front door for querying the graph once it exists — deliberately parked, to be RE-PRIORITIZED after the vault-in-place ingestion (#88) produces a comprehensive graph.** Remaining MCP productionization (wire into Claude Code; `graph_neighborhood` missing-center asymmetry vs `get_entity`; `pyproject` workspace member; viewer as `get_body` consumer) is picked up then — file a fresh task when it is. **#113 closes at Phase 3a** (Phases 1/2/3a shipped; 3b abandoned). Still-parked (not abandoned): FTS, GraphRAG-answer tool, 2.0 Claim-layer tools (Worldview Reconciliation North Star). |
| 114 | Recovery-oriented Pass-2 parse stage (first principle made executable) | **Closed 2026-07-21 on `feat/114-recovery-parse-stage`** (9-commit chain `253af03` docs → `d205189` boundary-decode util → `b82fe41` loose unwrap → `88ec454` shared recovery → `0b643cb` telemetry fields → `76d6cd7` fixtures → `055f515` compile_one parse stage → `20bd8f7` KPI threading → `81e5690` replay parity; 1379 non-live tests green, 1 live-API skip). Shipped the recovery-oriented parse stage per spec v0.3.6: shared `recover_json_response` owning unwrap + strict-eval + the 5-step selection-first ladder (the parse stage may only *select* the complete document, never *edit* decoded content); root-preserving boundary-decode (`common/util/parse_document_prefix`); `recovered`-flag sentinel as the only success signal (JSON null ≠ failure); any-value recovery with the schema as the content judge; recovery-before-truncation behavior change; `boundary_recovered` + `prefix/tail_discarded_chars` telemetry threaded into `recovery_rate` / `repair_rung_rate`; `tools/replay.py` on the same shared function (replay ≡ live parse). **Evidence:** the 20 gemini-3.5 carrier-noise failures (2026-07-21 cohort) — 19 carried complete valid documents with trailing junk and quarantined/paid-retried purely on format; 1 genuinely incomplete; curated into 20 tracked fixtures in `compiler/tests/fixtures/` (19 recoverable + 1 incomplete negative). Spec `docs/superpowers/archive/specs/2026-07-21-recovery-oriented-parse-stage-design.md`. |
| 117 | Per-pass leaderboards — split Pass-1 / Pass-2 model boards | **Closed 2026-07-22 on `feat/117-per-pass-leaderboards`** (7 commits `d1848da` spec+North Star → `3437f44` plan v1.4 → `1ddbea9`/`425405d`/`635c066`/`1e40fff`/`687726e`/`86dba68`; 1436 non-live tests green, 1 live-API skip). `kdb-benchmark score` now writes three boards per invocation: the combined board (byte-identical, golden-pinned) plus `leaderboard-pass1.{json,md}` + `leaderboard-pass2.{json,md}` (North Star §7.7). Per-pass KPIs recomputed at score time from each row's `run_state/` (`load_run_measurements_with_stats` — production loader stays strict; type validation on both paths); `PassCallMeasurement.cost_usd` + 8 additive `compute_processing` diagnostics (per-pass recovery/retry/cost); §6 `score_models` reused with pass KPIs mapped onto canonical names (Pass-1 pro-rata 4:1:1 + penalty, graph inactive; Pass-2 + 4 graph KPIs). Cost = selection column, never a Borda axis (`≥$X (+N unknown)`); Pass-2 labeled downstream-outcome with eligibility/coverage/`p1_noise`/`p1_failed`; fail-closed completeness contract (Pass-1 sidecar reconciliation, required-KPI gate incl. graph four) → `unranked` with `missing_kpis` + `completeness_errors`; competition ranking (1, 1, 3); shared `updated_at`; pre-serialized individually-atomic writes. Live on the 2026-07-21 cohort: main board diff = stamps only; Pass-1 board surfaces the 6× cost spread (deepseek $0.050 vs gpt-5.4-mini $0.306) — the #118 split-model evidence base. Spec v0.3.1 + plan v1.4 (Codex 3+5 rounds, all findings code-verified): `docs/superpowers/{specs,plans}/2026-07-22-task117-per-pass-leaderboards*`; reviews archived alongside. |
| 115 | Pass-2 contract revision — derivation-first (was: prompt/schema contract audit) | **Closed 2026-07-23 on `feat/115-pass2-contract`** (5-commit chain `e9ca323` Gate 0 prompt-to-repo + stamps + cohort guard → `610ef77` Gate 2 contract revision → `15f16ff` Gate 3 confidence deprecation + snapshot v7 → `782120b` Gate 4 parity corpus + system tests → `f4233f7` Phase-5 guard report + sandbox-run Google Drive prompt; full suite green exit 0 at closure, 1 live-skip). Shipped: Pass-2 system prompt in repo (`compiler/prompts/`) with `pass2_prompt_version` + loaded-text SHA-256 run stamps; LLM contract shrunk to wiki-native `pages[]`(`slug`/`page_type`/`title`/`body`) + optional `compilation_notes` — six fields out (`concept_slugs`/`article_slugs`/top-level `summary_slug`/`outgoing_links`/`source_name`/`status`), no aggregate reconstruction; Python-owned `status` + `supports_page_existence` injection; graph owns wikilink→LINKS_TO derivation (legacy `outgoing_links` fallback inside the contract commit); body-authority canonical links; post-canon exact summary invariant; #65 Repair stage deleted whole; `confidence` logically deprecated end-to-end (Entity scope; Claim tier untouched); historical artifacts optional-deprecated read-only with dual-mode aggregate validation; Gate-2 F3 typed validate-stage telemetry (`failure_stage: validate` / `SemanticCheckError`). **Phase-5 (cohort validation):** both models fired from the Gate-4 anchor `782120b` (stamps verified 3.0.0/`c550a69a…` vs baseline 2.0.0/`dcfa3d1c…`); each quarantined exactly one source — the first Pass-2 quarantines on this corpus (summary-slug punctuation deviations `gemini31`/`whats`, proved pre-existing at prompt 2.0.0 via baseline `parsed_summary`). **The ratified KPI gate (quarantine/retry stable vs baseline) FAILED; Joseph explicitly accepted it as temporary production behavior (fail-closed — retry spend + per-run source absence; zero bad writes) and routed the root cause to #119 (normalization boundary), which inherits the identical gate + a cohort re-fire as its acceptance criterion.** Analysis v1.1 + Codex R1 review (all findings code-verified): `docs/superpowers/archive/specs/2026-07-22-task119-phase5-summary-slug-failure-analysis.md` + `…-review-codex.md`. Spec v1.6 + blueprint v1.10: `docs/superpowers/{specs,plans}/2026-07-21-task115-pass2-contract-*`. |
| 119 | Pass-2 normalization boundary — proposal contract vs canonical contract (reject ambiguity, not representation) | **Closed 2026-07-24 on `feat/119-normalization-boundary`** (11-commit chain: `4f356f0` design batch → `c1e6766` Phase-0 bridge corpus → `7978e06`+`40a2326` Phase-1 proposal schema + canonical re-role + CLI routing → `408dc42` Phase-2 bridge → `4942913` Phase-3 atomic switch (compile_one rewired proposal→bridge→canonical + prompt 4.0.0 + repair.py retired) → `c1f3f95` Codex exec-R1 fixes (type-faithful `_freeze`/`_json_equal`, slug-op occurrence pin) → `cdbc75f`+`b1a4202` Phase-4 replay era-dispatch + measurement diagnostics → `436a996` Codex exec-R2 fixes (prompt **4.0.1** + golden version→SHA guard per D-115-13) → `7c8174c` Phase-5 cohort scores → `2d84275` final-review minors; 1651 non-live tests green, 1 live-skip). Shipped per blueprint **v0.4** (17 Codex rounds total: 14 design + 3 execution; SDD subagent-driven, per-phase spec+quality reviews all clean): discriminated **proposal schema** (summary: no slug, stray any-type tolerated; concept/article: slug 1–512) → typed **PLAN-APPLY-VERIFY bridge** (`compiler/proposal_bridge.py` — lossless `NormalizationOp` plan, `_validate_plan`, sole-mutation apply, bijective conservation, bounded telemetry) → unchanged canonical validation shape; the summary slug's raw value **never rejects, never retries** (D-119) — Python stamps by role + `source_id` authority; alias-ledger stays exclusively in canonicalize; §3.5 truth table (stamping/ignoring never set `slug_coerced`, never change `final_status`); replay 3.x/4.x era dispatch (fail-closed otherwise); watched normalization diagnostics. **Phase-5 live cohort (anchor `0690e8b`, stamps verified 4.0.1/`afeff429…`): PASS on every acceptance criterion vs baseline `e9ca323`** — quarantine/recovery/retry_load Pass-2 = 0.0 on both models; both #115 failure sources dead on live traffic: gemini31 clean 1-attempt `summary_identity_derived` on **both** models, what's-react-and-tailwind clean on gpt (deepseek Pass-1 noise-excluded it — stochastic pre-#119 mechanism); semantic_pass_rate 1.0; zero bad writes. **Routed forward to #120:** the 4.0.1 proposal schema dropped the 3.0.0 `body`-field wikilink instruction → deepseek concept-page link emission collapsed (0.45→0.08/p) — a contract-surface regression, not variance (Joseph's catch; mechanism code-verified). Specs/plans + all review rounds: `docs/superpowers/{specs,plans}/2026-07-2{2,3,4}-task119-*`. |
| 120 | Pass-2 output-quality restoration — wikilink emission (proposal-schema contract surface) | **Closed 2026-07-24 on `feat/120-wikilink-emission`** (5-commit chain `8bc785e` design batch → `483cdc8` schema restore + prompt 4.0.2 + canary → `d71ebd6` D5 `summary-` namespace guard → `b597a21` D3 docstring → `9156033` gemini-3.6-flash pool entry; 1659 non-live tests green). Spec v1.4 RATIFIED (Codex R1–R5, all findings controller-verified: constrained B+ wording, resolved-edge gates, retrospective D5, executable audit contract). **D4 cohort (three models, 4.0.2):** deepseek canary MISSED the restoration band (0.883 overall / 0.257 concept vs ≥1.2/≥0.4 — a clean partial recovery: +33% emission, zero dangling, zero self-links; one stochastic retry) — a deepseek-specific ceiling, not a contract defect; gpt density guard PASSED (1.521) but FAILED safety (dangling 3.1%→9.9%, 4 self-links — amplification-sentence side effects, a gpt-specific trait); **gemini-3.6-flash trial PASSED everything** (1.621 overall / 0.685 concept / 0.7% dangling / 0 self-links, both named sources clean — best of the 4.x era; the 3.5-flash contract-slip class did not reproduce post-#114/#119). **Verdict (Joseph): 4.0.2 stands unchanged** — the schema link surface is restored and validated; model-fit routed to #118 with the three-model matrix as its seed. D5 reserved-prefix guard + the link-surface canary ship with it. Evaluations: `docs/superpowers/evaluations/2026-07-24-task120-*.md`; spec + Codex reviews: `docs/superpowers/archive/specs/2026-07-24-task120-*`. |

---

## Unused IDs

`#24` — historical gap (originally reserved on 2026-04-21 but was bypassed when #25 was assigned next). Per the no-backfill rule, this gap stays. Do not assign #24 to any new task.

---

## How to update this file

- **Add a task**: append to the `Open / In-Progress` table with the next free ID.
- **Close a task**: move the row to the `Closed` table, replace the `Status` column with the `Resolution` column content (commit SHA / doc path / memory note name).
- **Decompose a task**: flip parent to `in-progress`, add sub-tasks with new IDs, update parent's "Notes" column to list them.
- **Never** renumber or reuse IDs.
- **Always** commit changes to this file alongside the work that caused them, so git history shows the ledger evolving in lockstep with the project.
