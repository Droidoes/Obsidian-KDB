# Obsidian-KDB — Codebase Overview (North Star)

**Status:** v1 architecture locked; M0 → M4 landed (kdb_graph_compiler + validator + reconciler + benchmark engine + GraphDB-KDB layer + canonicalization all live). Post-`v0.5.7` (2026-07-17): #114 (recovery-oriented Pass-2 parse stage) shipped on `main` **untagged**. **#115 (Pass-2 contract revision) is ratified (spec v1.6 / blueprint v1.10, descoped) but not yet implemented** — per its pre-implementation gate, this doc describes the ratified post-#115 contract ahead of the code; until the Gate-2 contract commit lands, the running pipeline still matches the pre-#115 description.
**Last updated:** 2026-08-19
**Owners:** Joseph (human) + Claude Opus 4.7 (staff architect) + GPT 5.4 / Codex 5.3 (external review)

This is the **single source of truth** for the Obsidian-KDB project. All design rationale, decisions, and open questions live here. External AI consultation artifacts (Grok / Gemini Pro / GPT 5.4 / Codex 5.3) are referenced but not authoritative — they fed into the consensus captured below.

---

## Milestone Changelog

Dated architectural inflection points. Full retrospective and three-iteration history in [`docs/JOURNEY.md`](JOURNEY.md).

- **2026-08-19** — **🏁 Task #145 Phase 1 LANDED — the `kdb_fts` gate is live and calibrated; the corpus is clean.** Gate ran over the full corpus (`deepseek-v4-flash`, `gate_v1`): 2,511/2,511 gated, 0 failed, $6.42 observed (supersedes blueprint §11's estimate). Joseph labeled all 150 `calibration-p1` articles in the review web app (which gained back/next navigation, prior-selection display via `GET /labels`, and an unlabeled-jump mid-labeling); measured the topic-only rule at precision 0.370 / recall 0.625 and **ratified the hybrid accept rule: topic ∈ {investment, finance-econ} ∪ (signal ≥ 0.75)** (0.414/0.750 full batch; 0.550/0.917 post-cleanup). Then two corpus clean-up runs from the label evidence: **#151** — Joseph unsubscribed the 15-entry junk-author list; their 1,694 sources quarantined to `_blacklist/` (move-not-delete + movelog, `_promo/` precedent). **#152** — two new deterministic promo batteries (`link_dense_teaser`, zero false positives on the 150 labels; six title-pattern markers) ran over the corpus (342 more → `_promo/`) and were ported into the feeder so future junk never becomes an md file. **Corpus arc: 4,189 → 2,659 → 965 → 623 (607 ok), with investment now 34% of gated volume** (was 13%). Full record: blueprint §14 amendments. **Next:** Phase 2 — idea/lesson extraction over gate-accepted articles.

- **2026-08-19** — **🏁 Task #143 CLOSED — the first ingestion feeder shipped: Gmail/Substack → `KDB/raw/`, with the promo pre-filter yielding the 2,659-article corpus.** Downstream compile decision (Joseph, 2026-08-19): the corpus is information, not knowledge — it routes to #145 `kdb_fts` (search+rank), not the graph pipeline (no graphDB compile).

- **2026-08-16** — **🏁 Task #145 Phase 0 LANDED — `kdb_fts` exists: a parallel extraction/ranking system over the gmail-substack raw corpus, starting deterministic-only (no LLM).** Blueprint v0.1 ratified by Joseph (`docs/superpowers/specs/2026-08-16-task145-kdb-fts-blueprint-v0.1.md`, D1–D22; panel review deliberately skipped at his call). **The positioning:** gmail-substack's 2,659-article raw tree is a competing knowledge-extraction path alongside the graph pipeline — same repo, same vault, but its own package (`kdb_fts/`), its own state (`<vault>/KDB/fts/`, outside the graph), its own CLI (`kdb-fts`). **P0 shipped (subagent-driven execution, 10 commits `3e4e3f8..e29eb73`, 4 review-caught defects fixed):** deterministic intake (walk raw tree → parse frontmatter/body → content_kind classification article/video/podcast → cleanliness bucketing ok/short/digest-stub/media/bleed), a SQLite ledger (sources/paragraphs/authors, migration-versioned), FTS5 full-text index, alias-based author canonicalization (117 canonical authors from 117 raw strings at P0 — identity map), and a mechanical write-boundary guard test (AST scan: `kdb_fts` may write only under `KDB/fts/`). **Real-tree gate PASSED:** `seen=2659 upserted=2659 deleted=0`; cleanliness `{ok: 2511, media: 89, digest-stub: 34, short: 25, bleed: 0}` — the plan's "3 bleed" estimate was unreproducible under every detection signature and amended to 0 with Joseph's sign-off; 131,961 paragraphs indexed; re-run idempotent; zero writes outside `KDB/fts/` (D3 spot-check). Suite **3288 green**. **Ratified for later phases:** D21 — status labels (accepted/rejected/pending ideas; helpful/not-so-much lessons) override decay scoring; D22 — the review surface is a local stdlib web app (no framework), and the 150-label calibration is batch `calibration-p1` in that app. **Next:** Phase 1 — the LLM gate + labeling web app.

- **2026-08-15** — **🏁 Task #144 CLOSED — the graph viewer's layout is precomputed at build time; the browser renders static, and bookkeeping never renders.** Joseph's report: opening `KDB/graph-view.html` froze the desktop 10–15 min — a live `d3.forceSimulation` (~300 ticks, collide×2) over 8k nodes / 27k edges on the browser main thread, recomputed on every open, never persisted. **The change (Option A, Joseph's pick):** `tools/viewer/graph_layout.py` is a deterministic numpy/scipy port of the template's exact five d3 forces (300 ticks, fixed seed), baked into the HTML as per-node x/y + a `layout` provenance block; the template is static-mode (no `d3.forceSimulation`, pinned by test; drag = direct node move; filters/resize never re-layout). **Key debugging find:** d3's link force is SEQUENTIAL (Gauss-Seidel — semi-implicit, stable for stiff coherent springs); a vectorized Jacobi update is unconditionally unstable past per-node stiffness ~5 and exploded the first real-graph build to ±1.4e13 — the sequential link loop converges in ~60 s at build time. **Two follow-ups, both Joseph's visual catches:** (1) retiring the sim also retired forceLink's endpoint-mutation side effect, so the first static build drew nodes with no edges — the template now resolves edge endpoints at load via a `nodeById` map (pinned by test); (2) the #136 PendingLink drain-as-you-go ledger was being swept into the viewer by the generic `show_tables()` export — 722 ledger rows rendering as nodes, color-colliding with Source via the purple `--color-fallback`. Investigation: all 434 distinct pending targets exist **nowhere** in the vault (0/434 .md basenames) — not uncompiled notes, but link targets the compile LLM invented in direct violation of system-prompt §5 ("Do not link to slugs that do not exist anywhere"), with no code gate enforcing that rule (open thread, unfiled: canonicalize hard gate stripping unresolvable wikilinks + one-off repair of the 722 ledger rows). Fix: `SKIP_NODE_TABLES` gains `PendingLink` (bookkeeping ≠ knowledge — the ledger stays in the DB for drain-as-you-go, it just never renders) + fallback purple→magenta so future unknown types can't silently collide. Commit `1af3ff3`, suite green; rebuilt artifact 7,362 nodes / 26,790 edges opens instantly.

- **2026-08-07** — **🏁 Tasks #138 + #137 CLOSED — wipe decoupled from the run: `--cold`/`--yes` → `--wipe`; the run has no modes.** Blueprint v0.1 (`docs/superpowers/archive/specs/2026-08-07-task138-wipe-decoupling-blueprint-v0.1.md`, SHIPPED — deviations in its §9), suite **3207 green**. Joseph's call: cold/warm is a reflection of derived state, not a run option — warm-out-of-nothing IS cold, so `--cold` (wipe + run in one flag) was coupling, not convenience. **The change:** `kdb-orchestrate --wipe` is wipe-and-exit — archives `state/runs/` to `state/pre-wipe-runs/<ts>/` FIRST (**#137 option (a)**: replay/verify over the post-wipe replay root stays clean by location, no replayer changes), erases the wiki tree / graph / manifest / alias ledger, appends one line to the wipe ledger `state/wipes.jsonl` (a wipe has no run to journal into — the run-attached `cold_wipe` event is gone), and exits; it precedes pipeline listing and model resolution (needs neither). Confirmation is unbypassable — typed `yes` always, EOF refuses, `--yes` deleted; `--dry-run` is run-only (`--wipe --dry-run` rejected: the gate prints the identical preview). `run(cold=)` is gone — the run is always incremental against whatever state exists. **Verification:** 10-test block incl. the #137 integration regression (wipe between two runs ⇒ replay root holds only post-wipe journals, rebuild ≡ live); **live sandbox gate** — wipe (241 wiki files, 17 journal entries archived) → gpt-5.4-mini run `2026-08-07T13-25-12_EDT` (28 compiled) → `graphdb-kdb verify` over unscoped `state/runs/`: **zero divergences** (249 entities / 396 links / 266 supports / 10 domains; replayed = 1). The 561-false-`missing_in_live` class is dead.
- **2026-08-07** — **🏁 Task #136 CLOSED (supersedes #94) — LINKS_TO wiring is per-source: drain-as-you-go with a durable `PendingLink` ledger; the end-of-run finalize batch is erased.** Blueprint v0.1 (Option A over backward-only-wiring and checkpointed batch; `docs/superpowers/archive/specs/2026-08-06-task136-per-source-wiring-blueprint-v0.1.md`, 8 implementation deviations recorded in its §9), suite **3208 green**. **The change:** each per-source commit txn now wires links whose targets exist, upserts unresolved targets into a durable `PendingLink` node table (schema v2.5, same Kuzu txn ⇒ crash-safe) that later page upserts drain, and runs the SUPPORTS-diff deprecation flip (revive symmetric) — the standalone `wire_links`/`detect_deprecations` passes, their deferral flags, the `graphdb.py` wrappers, and the adapter's `finalize_progress`-gated derive are all deleted; `_finalize` is a slim convergence-fixpoint + journal-archive tail. **#94 deleted by construction:** committed sources are always fully wired; aborts lose nothing; run memory is O(outstanding pendings), not O(the run's edge set) — the 2M-source objection answered. A side benefit: yesterday's silently-skipped dangling links are now durable, queryable rows. **P3 sandbox verification (gpt-5.4-mini all passes):** (a) cold run `2026-08-07T10-11-31_EDT` (29 compiled / 7 noise / 0 failed — 374 wired in-commit + 36 durable pendings = 410 intents, zero drains missed) → isolated replay verify **live ≡ replay, zero divergences** (258 entities / 368 links / 264 supports / 11 domains). (b) abort-injection — SIGKILL at 17 commits: graph reopened clean (0 pendings with existing target, 0 duplicate edges); warm resume drained 1 pending across the crash boundary; all 120 frozen pre-kill pages present, **0 lost edges**. (c) no-op warm run: zero churn. **Surfaced:** full-history verify diverges after `--cold` (the #135 wipe preserves pre-wipe journals; replay has no wipe-boundary awareness) — filed as **#137**; post-`--cold` verification must scope replay to post-wipe journals until resolved.
- **2026-08-06** — **🏁 Task #126 CLOSED — the Pass-1 prompt now instructs for the semantic-selection contract, verified on live emissions.** `entity_search_keys` was specified for its retired consumer (deterministic PK/regex T2 lookup — "imperfect slugs simply miss, no harm"); under #123 the field became the selector's query expressions, where a wasted key burns one of ten slots. The fix shipped 2026-08-02 (`634d25a`, `PASS1_PROMPT_VERSION` 1.2.0 → 1.3.0): the identifier objective (*"canonical form a reader would search by"*) replaced with **"these are the search's query terms"**, the copy-`key_themes` rule inverted to do-NOT-repeat, all three worked examples rewritten (they had *taught* the duplication the rule merely licensed), and the `author` instruction now names the JSON literal `null` vs the four-character string. Slug format deliberately retained (the field's existence is still under #26's `P−k` ablation). **Live evidence (2026-08-05T22-55-58_EDT sandbox run, 36/36 sidecars on 1.3.0):** key_themes duplication **52.5% → 1.2%** (2/160 slots, both borderline straddles); sources ≥50% dup **30/39 → 0/36**; string `"null"` authors **9/39 → 0/36**. Zero-key sources **5/36, all legitimate** (2 image-only noise, 3 true abstentions) — the new prompt's "emit fewer — or none" license working as designed; §8.3 metric 6's degenerate case is now a live property, accepted on record. Probe fixture drift (39 probes captured under 1.2.0) accepted by owner ruling — no scored metric is keyed per-expression.
- **2026-08-06** — **🏁 Task #124 CLOSED — stop-reason normalization is a boundary primitive: gemini truncation is now visible to telemetry and the kdb_graph_compiler's truncation guard.** The pre-#124 predicates string-matched `("max_tokens", "length")`, but `_call_gemini` surfaces the enum value's UPPERCASE name — so a truncated gemini response fell through to a generic "invalid JSON" quarantine with `token_overrun=False`, on the pool's strongest 4.x-era model. **The fix (ratified, Joseph):** `common/call_model.py` carries the D9.4 route-aware table promoted from `kdb_graph_search.stage` (`normalize_stop_reason` → `output_cap`/`complete`/`unknown`, never guesses — cross-route spellings, `None`, and unknown routes are all `unknown`), and `ModelResponse` rides the stop reason **twice**: `stop_reason` raw/verbatim for archival and diagnosis, `stop_reason_normalized` computed in `call_model` — the one place `route.api_call_type` is always known. Both consumers (`llm_telemetry.token_overrun`, the post-recovery truncation guard) classify on the normalized value; the guard's message keeps the raw spelling, and the carrier-metadata-not-proof-of-absence ruling stands. Rejected: naive `.lower()` (silently un-matches kdb_graph_search's raw-spelling tables) and consumer-side tuple patches (provider spelling in consumers). TDD: 32 new pins, 9 fixtures migrated to the new response contract, suite **3195 green**. **KPI re-baseline on record:** gemini `token_overrun` was structurally 0 before today — future nonzero values are new visibility, not new behavior. Follow-up recorded: `kdb_graph_search.stage` keeps its own table copy for its artifacts; shared-copy adoption there is a later dedupe.
- **2026-08-06** — **🏁 Tasks #134 + #135 CLOSED — deprecated-GC measured and declined (trigger not fired); `--cold` makes a cold start one complete operation.** **#134 (measure-first):** deprecated-page accumulation is **zero everywhere** — sandbox graph 260/260 active, 0 deprecated files on disk; prod graph empty. Per-run deprecation rate under deepseek-v4-flash: 1 drop (#130 warm) / 0 drops (#131 warm) ≈ 0–3% of recompiled sources → ~100 pages/yr at vault scale, structurally invisible to every reader (Pass-1.5 space, Pass-2 context, KPI, MCP are active-only) — no quality-impact mechanism exists. **Verdict: build nothing;** re-measure after first production-scale runs or quarterly. **The measurement's real catch:** a different accumulation class the task didn't name — **172 cold-run-orphaned wiki files** (40% of the sandbox tree, 184 KB) with `status: active` frontmatter and **no graph node**, orphaned when run 7's cold rebuild re-planned 276 pages against run 6's warm ~432 and never re-emitted them. No in-loop mechanism can see node-less files — `detect_deprecations`/convergence operate on graph nodes, `page_writer` only writes. **#135 (the fix, ratified by Joseph):** `kdb-orchestrate --cold` erases derived state — `KDB/wiki/` tree, graph dir, `manifest.json`, `canonicalization/aliases.json` — *before* the manifest load/scan, so a cold start is one complete operation instead of the manual move-aside dance; `pipelines.json` (config) and `state/runs/` (#132 replay journals) are preserved. Two operator-safety additions (Joseph, same day): a **confirmation gate** — the CLI prints exactly what dies (paths + file counts) and requires typing `yes` (`--yes` bypasses for scripts; non-interactive stdin aborts with guidance) — and a **standing `deprecated_pages_total`** in the finalize stats (count of `status: deprecated` files in `KDB/wiki`, doubling as the #134 tripwire). The wipe is journaled as a `cold_wipe` event (severity `warning`; `--dry-run --cold` previews at `info`). TDD red→green (13 pins), suite **3163 green**. Runbooks (`test-run-procedure.md` §1, `benchmark-cohort-procedure.md` step 1) now lead with `--cold`; the manual `rm` remains for when journal history should go too.
- **2026-08-06** — **🏁 Task #133 CLOSED — orphan-reap-era dead code removed: nothing imports `tools` anymore.** With #132 having declared historical `cleanup` journals permanently replayable (through `kdb_graph.intake.apply_cleanup` — retained), the last consumers of the legacy helpers were gone, so they went: `reap_orphans` / `build_cleanup_artifacts` deleted from `tools/cleanup.py` (the `kdb-clean orphans` retirement notice stays), the fired one-shot `scripts/backfill_cleanup_journal.py` + its test file deleted, and the boundary guard's `kdb_graph_orchestrator→tools.cleanup` allowance removed — `test_nothing_depends_on_tools` now holds without exception. The slug-safe rebuild integration test in `kdb_graph/tests/test_rebuilder.py` keeps its graph-side coverage with the payload hardcoded (the journal records the computed set; the computation itself is retired). Suite **3150 green** (net −12: the dead-helper and backfill tests went with their subjects).
- **2026-08-06** — **🏁 Task #132 CLOSED — D39 restored for the kdb_graph_orchestrator era: every run now leaves a replayable 2.3 journal, and `graphdb-kdb verify` proves `live ≡ replay` on a cold sandbox run with zero divergences.** Blueprint v0.1 (Option A producer retrofit; `docs/superpowers/archive/specs/2026-08-06-task132-replay-journal-hardening-blueprint-v0.1.md`), suite **3162 green**. The kdb_graph_orchestrator had written no replay journals since the 0.5.1 realignment deleted `kdb-compile` — rebuild/verify were unfalsifiable on a modern state tree. **The design:** `kdb_graph_orchestrator/journal_writer.py` archives a slim schema-`"2.3"` journal + per-run sidecars (`compile_result.json` / `last_scan.json`) built from the exact intake inputs — including the cr whose graph commit succeeded but whose manifest write failed — and records `finalize_progress` (`committed`/`wired`/`deprecated`) so a crash mid-finalize is representable; archival never raises (`replayable_payload=false` ⇒ adapter skips). The adapter's 2.3 replay is **two-phase live order** — compile commits (`detect_deprecations=False, wire_links=False`), then applied reconcile ops, then the finalize derive gated on `finalize_progress` — because a single unioned `apply_compile_result` would erase-then-re-add a page whose sole supporter was deleted in the same run, killing inbound `LINKS_TO` that live preserved (pinned by test). Pre-2.3 journals replay unchanged (legacy monolith path). **Sandbox proof (deepseek-v4-flash all passes):** cold run `2026-08-05T22-55-58_EDT` (29 compiled / 7 noise / 0 failed, 410 links wired) → verify **fully clean** — 260 entities / 397 links / 276 supports / 10 domains / 260 belongs_to, preflight + replay diff + C1–C4 all zero-divergence. **One verifier fix folded in (blueprint deviation #4):** the gate exposed a pre-existing Layer-1 false positive — preflight expected EVERY manifest source in the graph, flagging the 7 noise sources `missing_in_live`; it now expects presence only for `run_state="in_graph_db"` (read path only, test-pinned). Ops note on record: `graphdb-kdb verify --vault-root` resolves journals/manifest from the vault but the GRAPH from `$KDB_GRAPH_PATH`/`--graph-dir` — a vault-scoped verify needs `--graph-dir <vault>/KDB/graph`. Unblocks #133 (dead-code removal).

- **2026-08-05** — **🏁 Task #131 CLOSED — T1 is must-see: the context `page_cap` no longer amputates a source's own pages from the model's view.** Blueprint v0.1 (`docs/superpowers/archive/specs/2026-08-05-task131-t1-cap-exemption-blueprint-v0.1.md`), TDD red→green (7 failing pins → suite **3147 green**). **The defect** (surfaced by #129's run-7 audit): `page_cap=50` applied across tiers, t1 included — a source owning >50 of its own pages (run 7's Pabrai source: 65 t1 candidates) had its tail silently cap-cut from the prompt and then retracted as "model drops." **The fix:** t1 is cap-EXEMPT, delivered in full; the cap governs the t2∪t3 tail only — flood control was always its purpose. Selection split at `kdb_graph_compiler/context_loader.py` (strict tier order / scoring / projection untouched); both context-record parsers (V1 replay-era + V2 live) loosened in lockstep (`sum(delivered) > page_cap` → `t2+t3 > page_cap`) — replay-safe, every historical record still parses, no schema-version bump. No prompt change (the prompt never mentions the cap), no pass-1.5 `max_results` change, no per-tier caps (R-129-5 stands). **Verification:** live-graph probe — a fabricated 55-t1 source delivers **55/55** under `page_cap=50` (old code: 50); warm sandbox run `2026-08-05T20-46-01_EDT` (deepseek-v4-flash all passes) — 29 sources, **every source `t1.delivered == t1.candidates`; zero deprecations, zero dropped-unshown**. Blueprint §8.3's revive-then-repro adapted on record (run-7's deprecated pages perished with #130's cold-run rebuild — the fabricated probe substitutes). KPI note: `context_t1_delivered_mean` may now exceed 50 (informational series; no re-baseline).

- **2026-08-05** — **🏁 Task #130 CLOSED — the deprecated page lifecycle: graph↔disk status moves in lockstep, zombies are structurally impossible, source deletion is total erasure.** Blueprint v0.1 (R-130-1..6; `docs/superpowers/archive/specs/2026-08-05-task130-deprecated-lifecycle-blueprint-v0.1.md`), four TDD stages, commit `a37d680`, suite 3111 green. **The lifecycle (replaces the orphan-reap era):** a page the model drops on recompile (SUPPORTS-loss) is marked **`deprecated`** at finalize on the graph node **and** the file frontmatter in one convergence pass — never reaped, never archived, revivable on re-emit via the pre-existing revive query + the compile path's wholesale rewrite (deprecated pages keep LINKS_TO/BELONGS_TO; they are invisible to all readers, the Pass-1.5 search space, Pass-2 context incl. T1, and MCP, by design). **Source deletion ⇒ ERASED at reconcile** — sole-supported canonical nodes DETACH DELETEd with their alias rows, files unlinked, **no `orphan-archive/`** (Joseph: "not in KDB/wiki, not archived" — supersedes D12's flag-don't-nuke); dual-supported pages survive active. `orphan_candidate` is gone from the live vocabulary: retraction.json / cleanup journal / `apply_cleanup` left the run loop (historical cleanup journals still replay), `kdb-clean orphans` prints a retirement notice, `graphdb-kdb orphans` → `deprecated`, KPI `orphan_rate` → `deprecation_rate`. **Two blueprint deviations on record:** (1) finalize re-flips ALL deprecated nodes every run, not just newly detected — idempotent convergence that heals dry-run/crash graph↔disk divergence; (2) KPI queries + context-loader PageRank needed active-only filtering restored (the blueprint's "no reader changes" was wrong for KPI). **§13 sandbox verification (Vault-in-place-test-run, deepseek-v4-flash all passes):** cold 288/288 clean · warm exactly **1 model-drop** deprecated on both sides (vs the 15-drop #129-era baseline; #131's t1-cap never triggered) · natural revival none (T1-invisible by design) + forced re-emit through the real intake/page_writer path ⇒ `active` both sides · erasure probe 7 sole-supported pages erased graph+disk / 5 dual-supported survive / no archive · final audit **316 files ↔ 316 nodes, all active, 0 problems**. Follow-ups filed: **#132** (replay/journal hardening), **#133** (legacy cleanup dead code), **#134** (deprecated-GC candidate).

- **2026-08-05** — **Task #129 CLOSED — Pass-2 tier-structured context shipped; the first cold/warm stewardship probe makes deepseek-v4-flash the single standing model for all three passes.** The EXISTING CONTEXT block no longer renders one flat `pages` list: `ContextSnapshot` carries `t1`/`t2`/`t3` (`common/types.py`; a projection-only split — selection, scoring, tier order, and the global `page_cap` byte-untouched), and system prompt **4.1.0** teaches per-tier contracts (t1 re-emit-or-extend, never silently drop; t2 link-don't-duplicate; t3 weak context) — `31aaf2d` → `2c7ed8e` → `78e193e`; era stays 4.x so replay dispatch and 4.x fixtures are untouched. The pass-1.5 selector seat is unpinned (`47981c2`): it follows `--model` by default with `--selector-model` as override — one model now runs all three passes (Joseph 2026-08-04). **The verification arc** (sandbox `Vault-in-place-test-run`, 36 unedited sources; a cold run, then a warm recompile over the run's own warm graph with the manifest cold): warm t1 churn went **49.4% → 23.3% → 0.3%** across the table below — the <20% ship bar cleared by two orders of magnitude under deepseek ("we have more than met the 20% threshold" — Joseph). Two findings outlive the task. **(1) Both models misreport `compilation_notes`** — qwen confabulates re-emission at scale (all 12 dropping sources claimed t1 re-emission while emitting none of those slugs); deepseek's note says "No t1 pages dropped" while dropping 1 of 25 — so notes are demoted to soft telemetry and the validator/keep-list enforcement lever is dropped as a standing task (owner call 2026-08-05); with a compliant model the blast radius is ~0.3%. **(2) The audit surfaced a system-side defect**: the global `page_cap=50` applies to t1 — a source owning >50 of its own pages has its tail silently amputated from the model's view and then retracted (14 of run-7's 15 zombies were cap-cut, never model drops). Filed as **#131** (exempt t1; the cap exists for t2/t3 flood control). #130 (dormant lifecycle, option C) proceeds as ratified — it neutralizes the residual rather than legitimizing it. **Model call (Joseph 2026-08-05): deepseek-v4-flash confirmed as the standing default** — already the `--model` default, now also the selector seat; context: the model moved preview → public beta on 2026-07-31 after drastic recent improvement. The cold/warm pair protocol is retained as the **standing model-evaluation harness** (~25 min per candidate model): cold-run pages minted, warm t1 shown, in-t1 model drops, cap-cut system drops, zombie count, note truthfulness.

  | warm run | prompt | model (all 3 passes) | cold pages minted | warm t1 shown | in-t1 model drops | cap-cut drops | zombies | t1 churn |
  |---|---|---|---|---|---|---|---|---|
  | `2026-08-04T13-10-43_EDT` (baseline) | 4.0.0 | qwen3.7-flash | n/a (pre-tiering) | 172 | 85 | 0 | 85 | **49.4%** |
  | `2026-08-04T23-45-24_EDT` | 4.1.0 | qwen3.7-flash | 167 | 163 | 38 | 0 | 38 | **23.3%** |
  | `2026-08-05T00-25-04_EDT` | 4.1.0 | deepseek-v4-flash | 305 | 299 | 1 | 14 | 15 | **0.3%** |

- **2026-08-04** — **Task #123 P3a COMPLETE — semantic graph search is live in the pipeline: pass-1.5 runs per source inside `compile_source` step 1, and the PK/regex T2 seeding is deleted (REPLACE, owner ruling 2026-08-03).** Six commits (`63dd795`→`128631b`) on `feat/123-semantic-graph-search`, each TDD-gated. **P3a.0** foundations — the 64K provider envelope, `entity_first_run_ids` on the graph, and the cost projection that sized the arc ($2–$40 vault-scale; the qwen3.7-flash selector seat). **P3a.1** `kdb_graph_compiler/search_adapter.py` — the thin boundary between the I/O-free `kdb_graph_search` core and the pipeline: gates pre-Pass-1 sources, runs `graph_search` with the run-level selector seat, persists one search envelope per source under `runs/<run_id>/search/` (one-line summary, warn-only write), projects the §4.5 positional key outcomes. **P3a.2a/2b** ContextRecordV2 (types + factory + parser + writer, then wired): `compile_source` step 1 is now ONE `run_pass15` whose products (`t2_selection` / `t1_slugs` / `search_summary` / `key_outcomes`) flow straight into `build_context_snapshot` — T2 is the selector's validated hits in selector order, T3 is always 1-hop, and the whole legacy family (`T2Mode`, the regex slug/title-in-text matchers, the resolver wrappers, `source_text`, the cold-start 2-hop widening) is deleted whole (§7 REPLACE). **P3a.3** the version-dispatching context-record loader (V1 history + V2 current through one strict reader) with the KPI consumers re-baselined onto V2 — all seven `search_key_*` series from #122 are cut (one rename, one per-key→per-hit re-baseline, three clean cuts); the §4.6 V2 rates (`search_expression_matched_rate`, hit-recency splits, `search_stage2_budget_bound_rate`) are the search KPI now, and **historical `search_key_*` values are NOT comparable across the re-baseline**. Also fixed here: the adapter now translates `kdb_graph_search`'s short stage vocabulary (`thin`/`fat`) to `StageName` (`thin_selection`/`fat_selection`) at the boundary — a defect the restored e2e test exposed. **P3a.4** the §4.7 measurement channel: one `SearchPassMeasurement` per search (persisted as an additive `measurement` sibling key inside the existing search envelope; calls/attempts/lower-bound tokens + cost/latency — unknowns counted, never zero-coerced), `searches_attempted`/`searches_written` header counters with emit-time envelope reconciliation (mismatch warns; a malformed measurement fails the emission safely), and the pass-1.5 diagnostic columns on BOTH pass boards — diagnostics only, no third ranked board; `effective_top_weights` gained an explicit `pass1_5` case. **Post-ratification amendment on record (TASKS.md #123):** `Pass15Outcome` carries `keys_emitted` + `key_outcomes` beyond blueprint §4.1, accepted in P3a.2b. Suite **3,021 → 3,102 passed** (exit 0 at the tip). **Outstanding (owner):** the sandbox full-chain run on `Vault-in-place-test-run` under the split gate — Pass-1 prompt-1.3.0 output inspected and accepted BEFORE pass-1.5 results are read; the production graph is EMPTY from the P3a.0 cost projection — the first real run populates it.
- **2026-07-28** — **Task #123 P2 COMPLETE — `kdb_graph_search` can now run a search: two-stage selector orchestration, every ratified terminal, and both non-live replay modes.** Eight sub-phases. **P2.0** the shared fake selector — order-scripted rather than stage-keyed (a stage-keyed fake would agree with whatever the controller believes the stage boundary is, which is the one thing the call-count assertions exist to check) and every canned document built FROM a space, since membership is the sole identity authority and a document whose slugs are not in the test's space classifies as `all_entries_dropped` while a test asserting "usable" still passes. **P2.1** the two prompt templates, single-pass slot substitution (sequential `str.replace` is a P10 injection vector — an excerpt containing `{{QUERY}}` would have the query substituted *into* it), and the version parsed from the filename so it cannot drift; **P2.1f** golden byte pins as hardcoded literals, with the folded constants pinned SEPARATELY because the overhead is not a pure function of the template (widening `MAX_RESULTS` moves the wrapper with every template byte identical). **P2.2** the spine and every zero-spend terminal, driven with a selector that raises on invocation so zero-work is pinned structurally rather than by status. **P2.3** `stage_call` — the attempt loop, one `StageRecord` per logical attempt including failures, and D9's post-call budget classification governing both stages from one site; its predicate is a **conjunction** (cap stop AND no complete usable document), tested from both sides, because a cap-stop test alone discards a good document and the repo had already ruled a cap stop to be carrier metadata. **P2.4** the two-stage flow — retain-all at N≤M enforced controller-side (a recall-oriented selector can omit an identity by judgment and validation cannot tell omission from judgment), stage 2 in MANIFEST order so fat stays unanchored to thin, the F1 path, the D3 terminal, concordance. **P2.5** blueprint §8's branch table as a parameterized oracle — P2's analog of P1.5's contract matrix, and the artifact that makes the orchestration *complete* rather than working on the happy path. **P2.6** the two replay modes, with record replay returning a narrower type than `GraphSearchResult` because the archive genuinely does not hold telemetry. **P2.7** the P10 adversarial fixtures, each asserting containment AND output-side fail-closed against a selector that obeys the injection. Suite **2,513 → 3,021**; every behaviour mutation-swept (12/18/28/27/14/8 across the phases). **Four findings worth the record.** (i) There was **no thin-side document classifier** — nothing did parse→structure→classification for the thin wire, and the fixture warning about it had existed since P2.0 with no production counterpart; its classification reads the RETURNED entry count, because `{"retained": []}` and an all-foreign document validate identically and take opposite branches. (ii) The **ratified contract matrix has a gap**: the F1 treatment reached the fat pre-flight and output terminals but not the fat input one, a reachable state that would have died fail-closed — a row was added with every cell copied from its neighbours and marked EXTENSION, NOT RATIFIED TEXT, pending owner ratification. (iii) The matrix's own budget guard was **wrong for a two-stage producer**, requiring every matching record to be a non-fit when a fat rejection legitimately carries thin's passing one. (iv) A mutation reported as caught was caught by a `NameError`; re-run honestly it survived, exposing that every test used the default `max_results` — the cap the prompt states and the cap the validator counts against could have silently disagreed. **Two items carried out of P2 for the owner, neither blocking:** the audit-**delivery** surface (the payload is built on every path and not returned; P3a's adapter needs it, and it changes a public signature) and the matrix extension above. **Next: D5 calibration — Joseph fires it.**
- **2026-07-26** — **Task #123 P1 COMPLETE — `kdb_graph_search/` lands as the repo's eighth package: everything semantic graph search does that does not call a model.** Consumer-neutral by ratified design (R2) — it imports `common` **only**, so the caller materializes the search space and no per-consumer contract exists anywhere in the core. Six sub-phases: **P1.0** packaging + boundary guard rows; **P1.1** projection — the §5 serializer grammar byte-exact including both G7 clauses (split on `"\n"` not `splitlines()`; blank lines indented), excerpt policy v2's 2,500 B ceiling with byte truncation taking precedence over the no-mid-sentence rule, and the query block's per-field allocations resolved as *option C* (the renderer lives in the core; the ratified `QueryPayload` is untouched; `RenderedQuery` is archived in the artifact); **P1.2** response salvage + accounting — four-way classification, per-entry drop, per-field coerce, `valid_entry_yield` `None` at zero returned entries (D9.6), truncation running *after* salvage (R1: take the content); **P1.3** the budget estimator, D9's four separately-addressable envelope quantities, `tokens_lte_bytes` as a validated `ModelSpec` premise checked at selector-route resolution, and the M=100 static guarantee — 283,168 < 320,000, asserted to **fail** at M=150 so the constant is provably load-bearing rather than merely satisfied; **P1.4** the audit payload + two hashes deliberately answering different questions (`search_snapshot_hash` = what was searched, excluding the result, so an identical snapshot means two searches faced the same world; `artifact_integrity_hash` = what happened, excluding latency and cost so it actually reproduces), built on **every** path including the zero-call ones — emptiness is the finding, not a reason to skip the record; **P1.5** the terminal contract matrix — 13 named terminals in `contracts.py` with `assert_result_contract` as P2's **fail-closed return-site guard**, which is what keeps the matrix from being a table that asserts it equals itself. Every ratified §7.0a byte figure is reproduced from the implementation rather than copied (12,314 / 8,251 / the whole break-even table). **Mutation testing became standing practice mid-phase**, after three consecutive instances of tests that passed without exercising their subject; it then caught `bool`-as-a-wire-index (`True` would have silently attributed a hit to expression 1 — a wrong attribution that looks correct), three P1.4 hash gaps, and one P1.5 check that no mutation could break, which was deleted rather than kept as an assertion that only echoes another. Suite **1963 → 2494**. Two gaps in the ratified text are recorded rather than papered over: blueprint §8 carries two *input*-side post-call terminals the plan's P1.5 bullet did not enumerate (now in the matrix), and three cells are genuinely unenumerated and carry an `UNENUMERATED` marker — inferring them would make an implementer's guess indistinguishable from a ratified decision. **Next: P2** — prompt templates, two-stage orchestration, retain-all, the F1 path, the D3 terminal, concordance, the 2-attempt loop, replay.
- **2026-07-26** — **Task #123 D7 truth artifact adjudicated and promoted.** Joseph completed all 39 probe labels and 14 special-outcome confirmations against the immutable SearchSnapshot v1. The canonical, fixture-valid truth artifact is `benchmark/truth/task123_search_probes_v1.json`; the review/export record remains in `docs/superpowers/archive/specs/task123_search_probes_v1.json`. D7 metric rules are now explicit: micro aggregation for final Class-A recall@5 and Stage-1 recall at each reduced-M point; semantic-abstention denominator = Class E + domain-gated A10; whole-graph M=5 watched. Joseph intentionally set semantic abstention, Class-A recall@5, and Stage-1 recall thresholds to 0%, and the selector-failure ceiling to 100%, making those measures diagnostic rather than automatic blockers. Escaped foreign identity remains the fixed zero-tolerance contract. The artifact is ready for the future truth-set harness; this promotion does not itself run a live selector experiment.
- **2026-07-26** — **Task #123 D7 owner-adjudication reviewer architecture confirmed.** Joseph selected a dependency-free static HTML decision aid served from the repository: it reads the frozen draft probes and SearchSnapshot v1, autosaves only to browser-local state, and downloads a new adjudicated v1 JSON artifact without a backend or repository writes. It is explicitly temporary review tooling, not a product search/CLI/MCP surface and not a runtime dependency. Design: `docs/superpowers/archive/specs/2026-07-26-task123-d7-adjudication-reviewer-design.md`.
- **2026-07-26** — **Task #123 BLUEPRINT v0.12 + SPEC v0.14 (D9 CONFIRMED: the provider envelope, not just the wire; panel review complete, no open design item) — the output-budget proof is completed on the quantity providers actually meter.** D8 had bounded the *visible* JSON response (exact max serialization under `tokens_lte_bytes`, no density step); codex's O1 established that `ModelRequest.max_tokens` caps the **whole completion** — `gpt-5.4-mini` receives `max_completion_tokens` and carries `reasoning_effort: low`, gemini runs `ThinkingConfig(thinking_level="minimal")` with full-off unsupported and bills `thoughts_token_count` as output, and `_THINKING_DISABLE_EXTRA_BODY` maps neither provider. **D9 splits the envelope:** `VISIBLE_OUTPUT_ALLOWANCE_THIN/FAT` 13,000/10,000 + `HIDDEN_OUTPUT_RESERVE` 16,000, the sum both sent as `max_tokens` (asserted ≤ each route's `max_output_tokens` at resolution) and reserved in the R2 pre-flight; the unbounded-hidden residual is typed `budget_exceeded`/`detected: post_call`/`budget_side: output` — never retried, excluded from the selector-failure gate, watched as a selector-admission signal. The M=100 static guarantee is recomputed on that envelope: **283k < 320k**, holding for any window ≥ ~355k (the whole configured pool). **`MAX_EXPRESSIONS` = 10 is now declared in the core `QueryPayload`** — the wire's index caps, index width and FAT allowance had all been inheriting pass-1's `maxItems: 10`, an adapter constraint standing in for a consumer-neutral invariant (the FAT exact max exceeds the 10,000 allowance from 22 expressions upward — 10,087 B; the 10,414-at-20 figure first recorded here is corrected below). **§8.6 removed** — its author retracted the premise: the cohort is not thinking-disabled, so the dropped prose field was redundant with an existing reasoning channel rather than the only one. Structural fix for the round: **§7.0 constants table + §7.0a exact serialized maxima and grammar** (12,314 B / 8,251 B once the serializer was pinned — the 12,315/8,404 pair recorded at v0.9 is corrected below; `test_budget.py` as executable authority), every other mention citing it — three consecutive confirmation rounds had each caught a stale-figure straggler. Panel process note: the drafting seat passed to opus5 for this round, so D9 was **drafted** on two calls rather than recorded directly as an owner ruling — **Joseph confirmed both (the option-2 split with its reserve value, and the §8.6 removal) on 2026-07-26**, making D1–D9 binding and leaving #123 with no open design item; implementation waits only on his explicit Proceed for P1. **v0.10 + spec v0.12 complete D9's executable contract** (codex P1–P4, who approves both decisions): the post-call output terminal gets three full outcomes — the fat-after-thin case keeping `evidence_status`/`body_coverage` because its evidence pool *was* presented — with F1 confined to retry-exhausted failures; provider stop reasons are normalized through one closed `api_call_type`-aware map, closing a **live** blind spot (gemini returns uppercase `MAX_TOKENS`, while `llm_telemetry.py:152` and `compiler.py:411` match only `length`/`max_tokens` — a truncation on the interim default selector would have been misgraded into the selector-failure hard gate); the classification predicate is *cap stop AND no complete usable document*, preserving R1's salvage rule on the repo's own precedent that a cap stop is carrier metadata; and the over-bound outcome is named `InvalidGraphSearchRequest(code="max_expressions_exceeded")`. The 16,000 reserve is relabelled an owner-selected *policy* reserve. **v0.11 + spec v0.13 close the panel: codex votes APPROVE / CONCUR-WITH-ITEMS and states no further concurrence round is needed before ratification and P1** — the only open gate on #123 is Joseph's confirmation of D9's two decisions. One correction landed, and it belongs to the drafting seat: the wire's "exact" maxima had been computed from a **one-based** index list under `separators=(",", ": ")` while the ratified contract is **zero-based** — the true figures are **8,251 B / 12,314 B** with a **22-expression** break-even, not 8,404/12,315 and ~15. Every decision, allowance and guarantee survives with a *wider* margin than was claimed, which is precisely why it is worth recording: the §7.0a table existed to end stale-figure drift and instead shipped a figure derived from an unnamed serializer, so the fix is structural — `separators=(",", ":")` and the zero-based index base are now **normative constants**, and the synthetic schema-maximum tests derive every integer from them rather than hard-coding bytes. The repaired §12 parameterization is the sharper lesson: `10 ⇒ 8,404 / 20 ⇒ 10,414` would have shipped a test whose two data points **both fit** the allowance and demonstrated nothing about the bound it existed to pin. Also settled: **D9.6** — a truncated attempt never enters `valid_entry_yield`'s denominator (entry-conformance ratios have no entry population without a usable document), with the obligation presentational rather than structural. The gemini stop-reason blind spot is filed as **#124** — a live pre-existing defect in pass-1/pass-2 telemetry, deliberately not fixed inside a feature diff that would have re-baselined a watched series.
- **2026-07-26** — **Task #123 BLUEPRINT v0.8 + SPEC v0.10 (D8 mechanics completed) — the budget saga closes with no density assumption left anywhere: allowances derive from the wire's exact max serialization under `tokens_lte_bytes`.** v0.7 confirmation (codex REVISE N1–N4; opus5 CONCUR N1–N3 — every checkable claim repo-verified) folded. codex N1: `Hit.evidence` removed from the public result contract (the field D8 dropped from the wire had survived in the public type). codex N2 (option 1): the wire now states **every** numeric maximum (`max_results` ≤ 50 globally; `matched` ≤ 10 unique indices; advisory `unresolved` indexed, bounded); **allowances = exact max serialization under the invariant — THIN 13,000 / FAT 10,000** (12.3k / 9.6k exact max bytes + buffer; `tokens_lte_bytes` ⇒ tokens ≤ bytes — the K1→L1→M1→N2 lineage closes with zero density steps); static guarantee 257 kB + 10k = 267k < 320k. codex N3 ≡ opus5 N1: slug bound = **`MAX_SLUG_LEN` (120 B) imported from `common.paths`** — the re-declared 96 B contradicted the canonical data model (97–120 B slugs are valid graph data; `kdb_graph_search` imports `common`, so no second constant; the render-time assertion now fires only on genuinely non-canonical input). codex N4 + opus5 N3: **`tokens_lte_bytes` ownership** — a validated `models.json` boolean carried by `ModelSpec`, a declared per-route premise with its byte-level-BPE theorem (every token maps to ≥ 1 byte), missing/false ⇒ typed config error at selector route resolution, all three D4 candidates declared + test-covered. opus5 N2: **§8.6 selector task variants** — the with-/without-`evidence` prompt axis rides the selector A/B (the cohort runs thinking-disabled, so the dropped field was the only in-band reasoning channel; the drop gets confirmed on evidence, not on the absence of a consumer). Next: v0.8 panel confirmation → ratification → P1.
- **2026-07-26** — **Task #123 BLUEPRINT v0.7 + SPEC v0.9 (D8 owner amendment) — the minimal wire: numbered expressions, index attribution, no free prose; allowances now derived from a bounded schema instead of measured hopes.** v0.6 confirmation (codex REVISE M1 + consistency sweep; opus5 CONCUR M1–M4 — every checkable claim repo-verified) folded. **Joseph's D8:** (i) the request's expressions are rendered numbered; hits carry `matched: [int]` indices and Python maps back (out-of-range = coerce-drop — R1's salvage posture intact); (ii) **`evidence` dropped from the fat response** — Joseph's call ("who's going to use it? nobody"): nothing downstream consumed it, it was the only unbounded free-prose field on the wire, and it was ~60% of the worst-case response (the fat-stage evidence *pool* — excerpts — is untouched); (iii) allowances **THIN 4,000 / FAT 4,000 derived from the bounded compact schema** (100 × ≤ 96 B slugs ≈ ≤ 3.3k tokens; 50 × ~170 B ≈ ≤ 2.8k at 3 B/token), asserted against synthetic schema-maximum documents (codex M1); slug wire bound ≤ 96 B with a render-time controller assertion. The M=100 static guarantee stands with more margin (257 kB + 4k = 261k < 320k); `tokens ≤ bytes` recorded as the named `tokens_lte_bytes` supported-route invariant. opus5 M1: `budget_estimation_miss` re-typed **`budget_exceeded`/`detected: post_call`, excluded from the §8.4 selector-failure gate** — our mechanics, not the model's, and it would otherwise penalize smaller-window models. opus5 M2: §8.5 retention-pressure confound note + the reduced-M curve gains M≈5/163 (~3%) to bracket production's vault-scale regime. opus5 M4: `keys_emitted` carries original expressions. Next: v0.7 panel confirmation → ratification → P1.
- **2026-07-26** — **Task #123 BLUEPRINT v0.6 + SPEC v0.8 (D7 owner amendment) — M=100 settles the context-fit dispute by sizing, not measurement: the fat request's absolute worst case fits the configured pool by construction.** v0.5 confirmation (codex REVISE L1–L3; opus5 CONCUR L1–L3 — every checkable claim repo-verified) folded. **Joseph's D7:** (i) **M=100** — fat evidence ≤ 250 kB + query ≤ 4,096 B + system/template ≈ ≤ ~257 kB bytes ⇒ ≤ ~257k tokens at the pathological 1-token/byte extreme + 4k output **< 320k, the pool's smallest budget** — codex's 381 kB counterexample dies with the constant; no estimation in fat's safety path for any window ≥ ~330k. Zero behavioral change at current scale (largest domain 51 < 100); M=100 is the production constant (the 163-entity whole-graph fixture space is now binding — recall@100 a real measurement; reduced-M gate protocol unchanged). (ii) **Thin stays estimate-guarded** by design (unbounded N — the space is searched whole); the residual density-underestimate is typed **`budget_estimation_miss`** (attempted-once, spend possible, never retried, watched per model — codex option 2; "never a provider error" withdrawn for thin, unnecessary for fat at pool windows). (iii) **Output allowances sized from response schemas** — THIN 3,000 / FAT 4,000 (opus5's measured truncation ⇒ unparseable ⇒ retry ⇒ identical-overflow ⇒ hard-gate chain; codex's 2,000 closed-list superseded on evidence). (iv) **Query ceiling per-field deterministic allocations** (author ≤ 256 B / keys ≤ 10 × 128 B / themes ≤ 1,024 B / summary ≤ remainder; archived original+rendered forms). (v) Fat-`budget_exceeded` terminal's complete contract + two-row matrix + named F1 interaction. Next: v0.6 panel confirmation → ratification → P1.
- **2026-07-26** — **Task #123 BLUEPRINT v0.5 + SPEC v0.7 (D6 owner amendment) — one budget calculation method for both stages: the fat call gets the same calibrated pre-flight guard as thin, and token-fit claims are honest about being estimates.** v0.4 confirmation (codex REVISE K1–K3 + minors; opus5 CONCUR J1–J4 — every checkable claim repo-verified) folded. **Joseph's D6 (codex K1 option 2 ≡ opus5 J1):** R2 amended — the calibrated `bytes ÷ 4` estimate over the **fully rendered request** (system + template + query + evidence + reserved output) guards **both** stages against 80% of the configured window; over budget ⇒ typed `budget_exceeded` with the stage recorded, zero spend, never retried. Supporting contract: rendered **query block hard-capped at 4,096 B** (`summary`/`key_themes` were schema-unbounded — verified; `query_truncated` telemetry); **`OUTPUT_ALLOWANCE_FAT` = 2,000** reserved exactly as thin's. Static byte accounting (evidence ≤ 375 kB + query ≤ 4,096 B + system/template ≈ ≤ ~381 kB) bounds the absolute worst case; the calibrated estimate + 0.8 headroom is the operative guard — "provably fits any window ≥ 128k" withdrawn, headroom explicitly carries density variance (D5 unchanged at 3 calls). Codex's tokenizer-preflight option rejected (no local tokenizer; gemini-only provider counters). Also: R3 decision row synced with D1/D5; the stage-1 gate named "recall at the predeclared reduced-M points" (M=150 production constant); fixture v1 stays immutable with a sibling policy-v2 compatibility artifact (proof: max rendered block 2,209 B < 2,500 B); expense record carries both 160M expected-case and 273M policy-max ($38/$205/$410); envelope-count completeness qualified as successful-write (+ `search_artifact_write_failures`). Next: v0.5 panel confirmation → ratification → P1.
- **2026-07-26** — **Task #123 BLUEPRINT v0.4 + SPEC v0.6 (confirmation round absorbed; D5 owner amendment) — the stage-2 payload bound is now a property of the code, not a corpus statistic, and the ratified spec body speaks one contract.** Panel confirmation (codex REVISE 1–4; opus5 H1–H9 incl. a removal audit of the v0.2→v0.3 compression — every checkable claim repo-verified) folded: **excerpt policy v2** hard-caps the rendered per-entity fat block at 2,500 UTF-8 B (projector-enforced, char-boundary truncation) — `150 × 2,500 B ≈ 94k < 102.4k` holds by construction (the word-cap premises bounded words, not bytes: 102/163 fixture entities already exceeded the implied byte density); binds on nothing in fixture v1 (max rendered block 2,209 B) — no regeneration, checksums unchanged. **Spec normative-body sync:** `execution` enum gains `fat_after_thin_failure` (§1.1/§5.1), R4 carries the D3/F1 branch contract (1–2 thin + 0–2 fat per terminal path), D1's re-scope inlined at §8.1, stale 55k/64k + "exactly two calls" + wait-for-labels language retired, sizing refreshed to fixture-v1 measurements (thin 88 B / fat 691 B mean per entity). **Joseph's D5:** estimator calibration = one non-comparative provider-usage call per candidate (3-call ceiling, fixed prompt, Joseph fires at end of P2) persisted as a sibling artifact — the checksummed fixture manifest is never mutated (narrow D1 amendment). opus5's H7–H9 restorations bring back the v0.2 commitments lost to compression (V2 kept-field list, #119 byte-pinning note, five-way never-surfaced enumeration + four minors). Next: v0.4 panel confirmation → ratification → P1.
- **2026-07-26** — **Task #123 BLUEPRINT v0.3 + SPEC v0.5 (D1–D4 owner amendments) — semantic graph search is implementation-ready: a consumer-neutral `kdb_graph_search/` sibling, a byte-frozen P10-safe serializer with measured sizing, ContextRecordV2 with hit-level provenance, and the D7 gate re-scoped so coding starts at ratification while experiments stay label-gated.** Package boundary per the JOURNEY §6 producer/consumer/durable-asset lens: `kdb_graph_search` imports `{common}` only — the caller materializes the search space (pass-1.5: domain subtree via `kdb_graph`; human: whole graph), the function owns text projection (250-word safety-bound excerpts), the selector calls, and the always-constructed audit payload (caller-owned persistence; payload/envelope split). **Frozen serializer grammar** (2-space delimiters, content at 4 spaces, exact-terminator rule + both G7 clauses) with sizing recomputed byte-exact by both reviewers: thin 88 B/entity, fat largest-150 = 107,885 B ≈ 27.0k tokens, stage-2 policy-max bound 94k < 102.4k (8% margin), vault-scale thin whole-graph ~211k tokens — the R2 pre-flight budget rule (`bytes÷4` + 0.8 headroom + reserved 2k output) stays non-binding for the configured pool. **R4 as amended by D3 (Joseph):** always thin→fat, but no fat call on thin-empty with N>M (`completed` + `thin_retained_zero` watched; codex dissent recorded); opus5's F1 makes thin failure non-binding under retain-all (`fat_after_thin_failure`). **ContextRecordV2** carries the search summary + hit-level provenance (`first_run_id` is an Entity property — no resolver anywhere; the KPI-time resolver recomputation dies with T2Mode in P3b); V1 reader retained, version-dispatching loader incl. emit_kpis. **Measurement contract:** pass-1.5 stays OUT of the scored union (baselines comparable by test) + cost-centre diagnostics + `searches_attempted` completeness + total-cost reconciliation invariant. **Joseph's D1–D4 (spec §0):** D1 coding proceeds at blueprint ratification — experiments/tuning/ingestion and the destructive P3b stay gated on labels/gates/experiments; D2 State C runs the search (`query_kind: state_c`; D-90-8 retired); D3 above; D4 screening cohort = gemini-3.6-flash / gpt-5.4-mini / deepseek-v4-flash. Substrate landed: SearchSnapshot fixture v1 (`3d271e2`, 163 identities, checksummed) + 39-probe truth draft (`a3c832a`) awaiting Joseph's adjudication (his schedule). Panel: codex REVISE→CONCUR-WITH-ITEMS ×2, opus5 F1–F8→G1–G8 — every checkable claim repo-verified before absorption. Next: v0.3 confirmation → ratification → P1.
- **2026-07-25** — **Task #123 SPEC v0.4 + VISION v1.5 RATIFIED (spec-design gate) — semantic graph search now has a contract: always-two-stage LLM selection (thin→fat), per-entry salvage, retry-not-fallback, and no deterministic machinery anywhere.** Ratified under the **R3 narrower gate** ("spec design approved; evaluation substrate open" — codex's own wording, adopted): the D7 sequence is recorded and ordered — fixture + restoration smoke test **first** (blueprint work item 1, `benchmark/truth/`) → ~40-probe truth artifact drafted → **Joseph adjudicates labels + numerical gates** → only then experiments/tuning/vault ingestion. **The contract:** `graph_search(query_text, search_space, k) → ordered [entities]`; caller materializes the space (pass-1.5: domain subtree; human: whole graph); the function owns text projection (250-word safety-bound leading excerpts, `get_body` authority), per-entry validation, and stage-aware artifacts (`state/runs/<run_id>/search/`; exact rendered prompt + raw response bytes per attempt). **R4 (Joseph): always two-stage** — thin call (recall-oriented, ≤M=150, ranked) then fat call over the retained set, every source, every run; no single-stage, no routing logic; for N≤M the controller enforces retain-all so today's results equal single-fat **by construction** (codex #3); SD-5's switch dissolves into **R2's uniform pre-flight budget rule** (thin estimate > 80% of the configured selector window ⇒ `budget_exceeded`, zero spend, never retried, no per-consumer distinction). **R1 (Joseph): per-entry validation & salvage** — a parseable response is never discarded (the 6-of-10 rule); foreign/malformed dropped, unknown-expression/duplicate/over-cap coerced, every deviation counted per class (`valid_entry_yield` + attempted-violation rates → per-model leaderboard); four-way response classes (`unparseable`/`structurally_unusable`/`all_entries_dropped` retryable; empty-`selections` = honest empty, never failure); on failure **retry once, then honest empty + typed telemetry — no deterministic substitution of any kind, ever**: the exact/alias resolver is declared **not a valid search method** (it cannot find `warren-buffett`; its 0–2 hits prove nothing) and is never surfaced (no fallback, no annotations, no exact_matchable delta, no foregone-hits telemetry — superseding opus5's A1/§2.1/§D.2); all three legacy T2Modes retire. **D10 measured, not just argued:** thin-only visibility over the frozen corpus caps person-key recall at 0–57% (`charlie-munger` 0%, `warren-buffett` 38%) — fat wiki evidence is load-bearing. **The SD-4 gate has a falsifiable form** (opus5 §2.3): stage-1 recall at reduced M (10/20 over value-investing, 20/40 over whole graph) predeclared before vault ingestion; M=150 stays the production constant. Panel: codex REVISE → REVISE → **CONCUR-WITH-ITEMS** (all 7 gate conditions closed or closed-by-R3-sequence; whole-response fail-closed kept as dissent-for-record); opus5 CONCUR-WITH-ITEMS ×3 rounds (A1 supersession confirmed). Full trail: `docs/superpowers/specs/2026-07-25-task123-*` (spec v0.1→v0.4, vision v1.3→v1.5, 4 review docs, 2 syntheses, concurrence prompt — commit `71e1951`). **Next: blueprint** (package boundary P9, selector model choice, fixture layout, pre-flight estimator, T2Mode retirement mechanics), then TDD plan + implementation; the 3-model live cohort with the #122 decomposition is the post-implementation judge.
- **2026-07-25** — **Task #123 VISION RATIFIED (Phase-1 gate) — semantic graph search: the project's single objective now has an architecture; the exact-slug lookup is formally diagnosed as a placeholder impersonating a capability that does not exist.** Filed from the #122 baseline (3 cold + 1 warm runs): pass-1 keys for well-known entities (`warren-buffett`, `charlie-munger`, `mohnish-pabrai`) get zero hits from a graph rich with knowledge of all three — the graph *knows*, the lookup is *literal* (T2 delivered ≈ 0.14–0.25 pages/source cold). Joseph's four-point problem statement: the fundamental issue is how to search the graphDB to identify relevant knowledge in terms of graphDB elements; the chicken/egg (the build loop already consumes the missing search) is resolved by making the stop-gap the end-state at this N. **Vision v1.2 ratified (D1–D10)** through the everyone-answers process (Round-1 packet → synthesis D1–D9 ruled → concurrence APPROVE ×2 → Joseph's read-notes v1.1 → panel vision reviews folded v1.2 → codex re-vote APPROVE): **`graph_search(query_text, search_space, k) → ordered [entities]`** — one consumer-neutral capability; the **graphDB-or-subtree is an explicit input**, always searched whole, scoping applied by the *caller* before the search; **pass-1.5** is the first adapter (pass-1 enrich → pass-1.5 graph search → pass-2 compile — pass numbers load-bearing, no renumbering), its prompt assembled wholesale from the pass-1 metadata package, **one batched LLM selection per source** over the whole domain subtree. **Identity = return type; relevance = selection criterion** (Joseph's upstream ruling). Domain gate always on for context-build by construction (**Joseph's override of a 3/3 panel finding**, on record; domain-empty abstention correct-by-design, never scored as selector failure, both denominators preserved). **Read-only guardrail** (no ontology mutation; write-side subject ontology = separate future task). **Determinism by persisted search artifacts** (query + ordered identities + exact excerpt bytes + policy/model stamps; replay reads the record; opt-in re-call uses the archived space). **D10 fat text ratified**: wiki page bodies are the evidence source (graph = sole identity authority; typed `complete|partial` evidence status), bound into one **SearchSnapshot**; **P10**: candidate text is untrusted evidence, never instructions (adversarial fixtures in the test plan). Exact/alias survives as deterministic degraded-mode fallback (`LEGACY`/`LAYERED` T2 modes retire). FTS adopted early as infrastructure (CLI/MCP first consumer; never the relevance mechanism; thin-text identity pre-filter only). **Evaluation before tuning (#75 pattern):** held-out truth set over a fixed SearchSnapshot — wrong-answer definition, space-recall vs selector-precision, hub-returner case, cross-domain A/B cohort — defined in the spec phase before any tuning; **vault-scale sizing** (1,706 notes → ~2,900 entities → largest domain subtree ~800–1,300 → 500-word excerpts past context limits) is a pre-implementation gate deciding excerpt bound + FTS-in-v1. Vision: `docs/superpowers/archive/specs/2026-07-25-task123-semantic-graph-search-vision.md` (v1.2; opus5 + codex APPROVE). **#118 resumes after #123.** Next: spec phase.
- **2026-07-24** — **🏁 Task #122 CLOSED — event-time context capture shipped: the search-key KPI now measures what Pass-1's keys did at the moment they did it.** 4-commit chain `fc60507`→`92108af` (ratified docs/P0 → blueprint v0.5 → P1 capture → P2 aggregation), suite **1768 → 1956 green** (+188 tests). Shipped per blueprint v0.5 (Codex options R1–R3 + blueprint R1–R4, every finding code-verified): per-source `state/runs/<run_id>/context/*.json` records written **at context-build time** (dedicated namespace — the RespStatsRecord loader never sees them); the **two-product split** keeps telemetry out of the prompt (golden pre-change fixtures pin byte + hash identity across all five context paths); the resolver now has **ONE precedence classifier** over neutral rows from both query shapes (structural parity; provenance twins carry each hit's `first_run_id`); **per-key dispositions** separate resolver success from pre-cap T2 seeding from prompt delivery (cap pressure visible); headline `context_t{1,2,3}_delivered_mean` + `search_key_*` rates over the all-emissions denominator; **late-vs-never** split computed over `unresolved`-at-load keys only (the final graph's two declared reads); identity-based completeness reconciliation (coverage ≠ substantive population; aggregates fail-closed to `None`). **No-finalize runs now emit auditable measurements** (context evidence retained, finalized graph fields `None`, stale `compile_result.json`/wiki never packaged — unfinalized ≠ unchanged: residual/partial graph states told truthfully) and are **skipped at the score boundary** (`RunMeasurementHeader.finalize_ran`; historical headers backfill `True`); Pass-1 boards carry the new fields on ranked/partial/fallback rows. Legacy `entity_search_key_resolution` retained unchanged; prompt bytes provably unchanged; production write paths untouched beyond the new sidecar namespace. **The harness foundation for #118's split-model experiment is now in place.**
- **2026-07-24** — **Task #122 ARCHITECTURE RATIFIED (Phase-1 gate) — event-time context capture: the search-key KPI stops re-resolving against the post-run graph and starts recording what the keys did at the moment they did it.** Joseph's diagnosis ended three rounds of post-run designs: *"you recalculate T2/T3 after the run and you always get the hit — it becomes meaningless"* — the emit-time metric is self-fulfilling because the run contaminates the graph it measures against. The ratified architecture (options v1.3, Codex R1–R3): a per-source record (`state/runs/<run_id>/context/`, dedicated namespace — `pass2/*.json` belongs to the RespStatsRecord loader) written **at context-build time**; the **two-product split** (`ContextSnapshot` prompt-facing byte-identical / `ContextTelemetry` persistence-facing) keeps telemetry out of the prompt by construction; **per-key dispositions** distinguish resolver success from T2 seeding from prompt delivery (`resolved_t2_seed` / `already_t1` / `out_of_scope` / `duplicate_seed` / `unresolved` — a seed can still be capped out by T1 pressure); headline **`context_t{1,2,3}_delivered_mean`** (post-cap prompt pages per tier — "count T1/T2/T3 like links"); key age partition via `first_run_id` **equality** (pre-run / cohort / honest `age_unknown` residual — never ordering); `late_resolution` vs `never_resolved` computed over `unresolved`-at-load keys only — the final graph is read for exactly two declared purposes (legacy continuity + miss explanation), never to claim a hit; **identity-based completeness reconciliation** (coverage population ≠ substantive population; `context_failed` auditable but never a zero-context observation; aggregates fail-closed to `None` on incomplete evidence). Legacy `entity_search_key_resolution` retained unchanged (leaderboard continuity). Carved from #118's harness scope (deferred from #120 per Codex R2/R3). Blueprint next; then P1 capture / P2 aggregation+surfacing.
- **2026-07-24** — **🏁 Task #121 CLOSED — config-driven provider wiring shipped end-to-end (D1–D8); the elif chain is dead, `Settings` is gone, and a new provider of an existing type is now pure JSON + `.env`.** 5-commit chain `13816c5`→`208cf6f` on `main` (flash-lite pool drop → ratified docs/P0 → P1 engine → P2 pool+threading → P3 timeouts+docs), suite **1659 → 1768 green** (+109 tests) at closure. Shipped per blueprint v0.4 (Codex options R1–R3 + blueprint R1–R3, every finding code-verified): active pool entries carry a Gate-1-validated `ModelRoute(api_call_type, endpoint, api_key_env)` (whole-pool validation at `load_pool` — a malformed unselected entry fails at first touch); dispatch on the declared `api_call_type` (the only closed set); `provider` is a free-form canonical identity (padded rejected) for thinking-translation/pricing/telemetry; secrets resolve late by env-var **name** for all three transports; the 9-row Class-B registry factory serves route-less calls only (late `OLLAMA_BASE_URL`, ollama-local no-auth caller-inexpressible); `Settings` deleted in favor of late-bound `common/config` helpers; D4 fail-hard throughout (ordered runtime-type validation, no `or DEFAULT`, secrets never in errors). **D7 live:** `httpx.Timeout(connect/write/pool=120, read=900)` on openai/anthropic, gemini scalar 900s — read-silence watchdog, explicitly not a wall-clock bound; **D8:** retry budgets unchanged + documented, `max_retries=2` explicit. `httpx` mirrored into both manifests; `.env.example` activated (60→900, true semantics; production `.env` hand-migrated 300/90→300/**900**); `AGENTS.md:81` drift corrected (local-only, gitignored); provider reference rewritten. Consumed by **#118** (per-pass routes thread the five-hop carrier). Live smoke not run (optional per blueprint §8 — remains Joseph's call; routing pinned byte-identical by test).
- **2026-07-24** — **Task #121 ARCHITECTURE RATIFIED (Phase-1 gate) — config-driven provider wiring: required per-entry `ModelRoute` + Class-B compatibility registry; `Settings` deleted; two honest timeout knobs.** Joseph's ask ("each model gets an endpoint field and an api-key-name field — less hardcoding, more commonality") resolved through Codex options R1–R3 + blueprint R1–R3 into a **layered architecture (D1–D8)**: active pool entries carry a required, whole-pool-validated `ModelRoute(api_call_type, endpoint, api_key_env)` — authoritative; absent fields are load errors; explicit `null` endpoint meaningful (SDK built-in URL). A 9-row provider-default **factory** in `call_model.py` serves only route-less calls (raw `--provider` escape hatch, direct `ModelRequest`) — never a rescue for broken pool entries. Dispatch on declared `api_call_type` (`openai_compat`/`anthropic`/`gemini` — the only closed set); `provider` becomes a free-form canonical identity (padded values rejected) retained for thinking-translation, pricing, telemetry. Secrets resolve **late by env-var name** for all three transports (native handlers included); the `Settings` class is **deleted** (`common/config` keeps `.env` loading + late-bound helpers). **D4 fail-hard contract codified:** two validation gates (whole-pool at `load_pool`; every effective route at the call boundary), ordered runtime-type checks, no `or DEFAULT`, route-less unknown provider fails pre-SDK, secrets never in errors/telemetry. **D7 timeouts:** `LLM_TIMEOUT_SECONDS` = connect/write/pool (120); `LLM_INACTIVITY_TIMEOUT_SECONDS` = read-silence watchdog (900 — kills silent peers, explicitly **not** a wall-clock bound; the previously-unread `.env.example` placeholder activated at 900). **D8:** retry budgets preserved + documented (corrected call-path × transport matrix: Pass-1's broad loop retries transport once for all providers; Pass-2 up to 9/18 attempts openai-anthropic / 1-2 gemini) with `max_retries=2` made explicit against SDK drift. Blueprint v0.4; implementation P1–P3 per blueprint §8. Consumed by **#118** (per-pass model routes thread through exactly these kwargs).
- **2026-07-24** — **🏁 Task #120 CLOSED — Pass-2 wikilink-emission restoration shipped (prompt 4.0.2) and validated by a three-model cohort; model-fit routed to revived #118.** #119's Phase-5 surfaced the regression: the 4.0.1 proposal schema had silently dropped the 3.0.0 schema's link surface (top-level "wiki-native … `[[wikilinks]]`" + the `body`-field wikilink instruction), and schema-literal deepseek's concept-page linking collapsed 0.45 → 0.08/page (Joseph's catch over a "variance" misread). Shipped per spec v1.4 (Codex R1–R5): the **constrained B+ restore** (both 3.0.0 surfaces + an amplification limited to authoritative targets with no-invent / no-self-link / no-current-summary guardrails), prompt **4.0.2** per D-115-13, a **link-surface canary** pinning the instruction through the injection loader, the **D5 reserved `summary-` namespace guard** at the bridge (post-coercion, retrospective across the 4.x era), and `entity_search_key_resolution` honestly re-documented (mixed downstream realization; true decomposition deferred to #118). **The three-model 4.0.2 cohort triangulated the contract:** deepseek recovered cleanly but partially (0.883/0.257, zero fabrication — a model ceiling); gpt held density (1.521) but the amplification tripled dangling (9.9%) and added self-links (a model trait); **gemini-3.6-flash (new pool entry) passed everything — 1.621/0.685, 0.7% dangling, both named sources clean, the 3.5-flash contract-slip class gone post-#114/#119**. Verdict (Joseph): **4.0.2 stands unchanged — the contract is validated; Pass-2 model-fit moves to revived #118** (split-model runs) with the matrix as its seed. Evaluations: `docs/superpowers/evaluations/2026-07-24-task120-*.md`.
- **2026-07-24** — **🏁 Task #119 CLOSED — the Pass-2 normalization boundary is live: proposal contract → typed bridge → canonical strictness; the summary slug's raw value never rejects, never retries.** 11-commit chain `4f356f0`→`2d84275` on `feat/119-normalization-boundary` (Phase-0 bridge corpus → Phase-1 discriminated proposal schema + canonical re-role + `kdb-validate-response` proposal/canonical routing → Phase-2 PLAN-APPLY-VERIFY bridge → Phase-3 atomic switch: `compile_one` rewired + prompt 4.0.0 + `repair.py` retired → Phase-4 replay era dispatch + measurement diagnostics), 1651 non-live tests green at closure. Shipped per blueprint v0.4 through **17 Codex rounds** (14 design + 3 execution — execution caught what design missed: `_freeze` type-faithfulness, slug-op occurrence pin, the D-115-13 prompt-version bump to **4.0.1** + golden version→SHA guard). The bridge stamps summary identity by role + `source_id` authority (stray slugs of any JSON type tolerated, dropped with bounded telemetry); concept/article slugs form-coerce with collision/uncoercible as the only model-correctable rejects; alias resolution stays exclusively in canonicalize; §3.5 truth table keeps stamping/ignoring out of `slug_coerced`/`final_status`. **Phase-5 live cohort (anchor `0690e8b`, stamps verified 4.0.1/`afeff429…`): PASS on every acceptance criterion vs the `e9ca323` baseline — zero quarantine/recovery/retry on both models; both #115 failure sources dead on live traffic (gemini31 clean 1-attempt stamped on both models; tailwind clean on gpt, noise-excluded on deepseek by stochastic Pass-1).** **Routed forward to #120:** Joseph caught the 4.0.1 proposal schema having dropped the 3.0.0 `body`-field wikilink instruction → deepseek concept-page link emission collapsed 0.45→0.08/p (contract-surface regression, not variance; `link_density` is a scored axis — 0.30 of the graph block). Restoring the instruction + a Pass-1 output-quality axis review (`entity_search_key_resolution`) is #120's scope.
- **2026-07-23** — **Task #119 ARCHITECTURE RATIFIED (Phase-1 gate) — Pass-2 normalization boundary: Option 2 + form (b) + no links-to-current-summary + fail-closed alias handling.** Joseph's design challenge ("why should a missing dash `-` in the summary slug fail the LLM output at all?") resolved as a **boundary move**, not a tolerance widening: the exact-match summary-slug check leaves the raw model-response boundary entirely — the raw slug value never rejects and never retries (the Phase-5 quarantine + wasted-retry class dies) — while strictness stays load-bearing on the **canonical side** (strict canonical schema + exact pre-persistence summary invariant + post-canonicalization invariant now guard Python's derivation and the alias ledger, not model punctuation). **The architecture:** a discriminated **proposal schema** (summary: `page_type`/`title`/`body`; concept/article: + `slug`) becomes the prompt-facing contract; a **typed bridge** stamps the Python-derived summary identity (role + `source_id` authority, never string similarity); output validates against the **unchanged canonical validation shape**; raw parsed proposal preserved separately for telemetry. **Governing rule ratified as a North-Star principle:** reject ambiguity, not harmless representational differences — normalize only by deterministic authority (role / provenance / registry / context), reject on no-interpretation / ambiguity / collision / loss. Reference policy is single: preserve without authority (dangling-tolerant, KPI-visible, nonfatal); rewrite only with unique authority; reject attempted-rewrite ambiguity. Alias-ledger stays **fail-closed** on system-owned summary identities; #106 slug coercion moves into the bridge; retries only for model-correctable failure classes. **Revises D-115's slug-ownership clause** (→ D-119): the summary slug leaves model authorship — Python-owned mechanical identity; "fully model-authored including its slug" is superseded; canonical-slug prompt injection remains a non-goal. Five Codex rounds (R1–R5, every finding code-verified before absorption — including two contradictions of Kimi's own in the options doc v1.1); R5 verdict **GO**. Artifacts: seed v1.2 `docs/superpowers/archive/specs/2026-07-22-task119-phase5-summary-slug-failure-analysis.md` + options v1.3 `…/2026-07-23-task119-normalization-boundary-architecture-options.md` (+ R1–R5 reviews beside them). **Blueprint v0.4 RATIFIED 2026-07-23** (`…/2026-07-23-task119-normalization-boundary-blueprint.md`; Codex R6–R9 absorbed — stray-slug tolerate-and-ignore, typed pure coercion plan, alias ledger untouched by the bridge, `NormalizationOp` PLAN→APPLY→VERIFY + conservation bijection, §3.5 telemetry truth table, replay 3.x/4.x-only). Implementation per blueprint §9.
- **2026-07-23** — **🏁 Task #115 CLOSED (Phase-5 waiver; root cause routed to #119) — derivation-first Pass-2 contract revision landed.** 5-commit chain `e9ca323`→`f4233f7` on `feat/115-pass2-contract` (Gates 0/2/3/4 + Phase-5 docs), full suite green at closure. Shipped per spec v1.6 + blueprint v1.10 (13 Codex rounds total): the Pass-2 system prompt moved into the repo (`kdb_graph_compiler/prompts/`) with `pass2_prompt_version` + loaded-text SHA-256 run stamps; the LLM contract shrunk to wiki-native `pages[]` + optional `compilation_notes` (six fields out, no aggregate reconstruction); Python-owned `status`/`supports_page_existence` injection; graph-owned wikilink→LINKS_TO derivation with legacy `outgoing_links` fallback inside the contract commit; body-authority canonical links; post-canon exact summary invariant; the #65 Repair stage deleted whole; `confidence` logically deprecated end-to-end (Entity scope) with snapshot v7; Gate-4 kdb_graph_compiler/graph wikilink-parser parity corpus; typed validate-stage telemetry. **Phase-5 comparison cohort (both models from anchor `782120b`, stamps verified 3.0.0/`c550a69a…` vs baseline 2.0.0/`dcfa3d1c…`): each model quarantined exactly one source — the first Pass-2 quarantines ever on this corpus** — summary-slug punctuation deviations (deepseek `gemini31` vs derived `gemini3-1`; gpt `whats` vs `what-s`) proved **pre-existing at prompt 2.0.0** via baseline `parsed_summary` records; the new exact-match gate made an old behavior visible and disposed of it exactly as designed (typed telemetry, zero bad writes). **The ratified Phase-5 KPI gate (quarantine/retry stable vs baseline) FAILED.** Joseph explicitly accepted the failure as **temporary production behavior** (fail-closed: retry spend + per-run source absence) and routed the root cause to **#119 — the Pass-2 normalization boundary**: the defect (Codex R1, code-verified) is that canonical representation is enforced at the raw model-response boundary before any deterministic normalization stage exists; the governing rule is **"reject ambiguity, not harmless representational differences"** (normalize when Python can resolve exactly one valid meaning by role/provenance/registry/context authority — never string similarity; reject on ambiguity, collision, or information loss). #119's scope: proposal contract vs canonical contract, contract-wide per-field ownership audit, summary-slug resolution by role+`source_id`, exact canonical invariants kept, and — as its acceptance gate, inherited from the gate #115 waived — a new clean anchor + re-fire of both cohorts with quarantine/retry/recovery KPIs stable. Until #119 lands, the exact-match summary-slug gate remains production behavior. Analysis v1.1 + Codex R1: `docs/superpowers/archive/specs/2026-07-22-task119-phase5-summary-slug-failure-analysis.md` (+`…-review-codex.md`); spec/blueprint `docs/superpowers/{specs,plans}/2026-07-21-task115-pass2-contract-*`.
- **2026-07-22** — **🏁 Task #117 CLOSED — per-pass leaderboards.** `kdb-benchmark score` now writes **three** boards per invocation: the combined board (unchanged — byte-identical output, golden-pinned) plus **`leaderboard-pass1.{json,md}` and `leaderboard-pass2.{json,md}`** (§7.7). Per-pass processing KPIs are **recomputed at score time** from each row's `run_state/` (new `load_run_measurements_with_stats` — the production loader stays strict; new `cost_usd` projection + 8 additive `compute_processing` diagnostics: per-pass recovery/retry/cost) and scored through the existing §6 `score_models` machinery (Pass-1: 3 processing axes, pro-rata 4:1:1 + penalty, graph inactive; Pass-2: 3 processing + 4 graph KPIs, 40/40/10/10). **Cost is a first-class selection column, never a Borda axis** (`≥$X (+N unknown)` — zero cost can mean unpriced/failed, not free); the Pass-2 board is honestly labeled a **downstream-outcome** board with eligibility/coverage/`p1_noise`/`p1_failed` columns; rows failing the per-board completeness contract (Pass-1 sidecar reconciliation, required-KPI gate incl. all four graph KPIs) render `unranked` with `missing_kpis` + `completeness_errors` — never pro-rated on missing evidence. Competition ranking (ties share ranks: 1, 1, 3); one shared `updated_at`; pre-serialized individually-atomic writes. Spec v0.3.1 + plan v1.4 (Codex 3+5 rounds, all findings code-verified): `docs/superpowers/{specs,plans}/2026-07-22-task117-per-pass-leaderboards*`. Live on the 2026-07-21 cohort: main board diff = stamps only; Pass-1 board surfaces the 6× cost spread (deepseek $0.050 vs gpt-5.4-mini $0.306) against GPT's reliability win — the #118 split-model evidence base. Commits `1ddbea9`→`86dba68` on `feat/117-per-pass-leaderboards`; 1436 non-live tests green.
- **2026-07-22** — **Task #115 DESIGN LOCKED — derivation-first Pass-2 contract revision; North Star updated pre-implementation (the ratified gate).** The Pass-2 LLM contract shrinks to **wiki-native data only** — `pages[]` (`slug`, `page_type`, `title`, `body`) + optional `compilation_notes` — with Python owning everything mechanical (D-115-1..15): six fields leave the contract (`concept_slugs`, `article_slugs`, top-level `summary_slug`, `outgoing_links`, `source_name`, `status`) with no aggregate reconstruction; **the contract speaks wiki, not graph** (pages, bodies, `[[wikilinks]]` — no edge-list projections); the summary page stays fully model-authored **including its slug**, gate-validated against the Python-derived `expected_summary_slug(source_id)` (exact-stem rule, pre-call underivable-stem rejection with zero API spend, post-canonicalization re-validation); **the system prompt moves into the repo** (`kdb_graph_compiler/prompts/`) with `pass2_prompt_version` + loaded-text SHA-256 run stamps; `warnings` → `compilation_notes` (kept, optional), `log_entries` dropped; **`confidence` logically deprecated end-to-end** (Entity scope only; Claim tier untouched — D-A2 updated); **canonical links become body-authority** (canonicalization rewrites body wikilinks only) and **the graph owns wikilink→LINKS_TO derivation** with a legacy `outgoing_links` fallback inside the contract commit; the #65 **Repair stage is deleted whole** (pairing defects are structurally impossible under the shrunk contract) and the pipeline renumbers. Historical artifacts stay read-compatible: removed fields become optional-deprecated read-only with dual-mode aggregate validation (D-115-14). **Carve:** cross-source summary-slug reservation / MOVED lifecycle / durability → **#116** (paired with #94 as a WS3 pre-production gate); accepted temporary behavior = today's wiki last-writer-wins / graph co-ownership on normalized derived-slug collisions (existing behavior, not a regression). Spec v1.6 `docs/superpowers/archive/specs/2026-07-21-task115-pass2-contract-audit-findings.md`; blueprint v1.10 `docs/superpowers/archive/plans/2026-07-21-task115-pass2-contract-revision-blueprint.md` (10 Codex rounds absorbed). §5/§6/§8/§9 updated to the ratified contract ahead of code — implementation proceeds Gates 0→4, each Joseph-approved, with baseline/comparison cohorts (gpt-5.4-mini + deepseek-v4-flash) bracketing the contract commit.
- **2026-07-21** — **🏁 Task #114 CLOSED — recovery-oriented Pass-2 parse stage.** The carrier/payload first principle made executable: the LLM response is a *carrier*, the JSON document is the *payload* — the parse stage recovers the payload with maximum tolerance for carrier noise; it may only **select** (locate the complete document), never **edit** decoded content, and declares failure only on (a) no complete decodable document or (b) recovered content failing the schema/semantic gates. A format deviation alone never fails a source. Shared `recover_json_response` owns the composition (loose `unwrap_response` → strict `json.loads` → 5-step selection-first ladder with root-preserving boundary-decode `parse_document_prefix`); `RecoveryResult.recovered` is the only success signal (JSON null ≠ failure); recovery runs **before** truncation (ratified behavior change). `boundary_recovered` + `prefix/tail_discarded_chars` telemetry threads into `recovery_rate` / `repair_rung_rate`; `tools/replay.py` consumes the same shared function (replay ≡ live parse). Motivated by the 20 gemini-3.5 carrier-noise failures (2026-07-21 cohort — 19 recoverable, 1 genuinely incomplete), curated into 20 tracked fixtures in `kdb_graph_compiler/tests/fixtures/`. 9-commit chain `253af03`→`81e5690` on `feat/114-recovery-parse-stage`; 1379 non-live tests green. Spec `docs/superpowers/archive/specs/2026-07-21-recovery-oriented-parse-stage-design.md`.

_Entries dated before 2026-07-15 were archived 2026-08-19 to [`docs/archive/milestone-changelog-archive.md`](archive/milestone-changelog-archive.md) — verbatim, original (newest-first) order._

---

## 1. Vision

> **Architectural history — required warm-up reading.** For the *why we walked this way* across three iterations (kdb_graph_compiler+wiki → GraphDB refoundation → loop closure + step-3 ops), see [`docs/JOURNEY.md`](JOURNEY.md). This Overview captures *what is true today*; JOURNEY captures *how we got here and what we learned*.

A Karpathy-style LLM-compiled knowledge base (KDB) that lives inside Joseph's Obsidian vault without disturbing its existing human-curated structure.

**Core insight (Karpathy):** The LLM is the kdb_graph_compiler. Raw sources (`raw/`) → compiled wiki (`wiki/`) via incremental LLM passes. Obsidian is the IDE/frontend; plain Markdown + wikilinks are the only storage format the end-user sees.

**What's novel in our build:** Most community implementations let the LLM directly write markdown files via agent tools (Claude Code / Cursor Write/Edit). That gives the LLM free authorial control and produces hallucinated paths, corrupt frontmatter, and inconsistent state. **Our architecture makes the LLM output structured JSON "page intents"; deterministic Python owns every file path, every byte of frontmatter, and every filesystem write.** This matches the discipline of a real kdb_graph_compiler (LLM = parser/semantic analyzer; Python = codegen/linker).

**Design philosophy — no complexity for imaginary risk.** This system has one user, one process, and infrequent operation (minutes to hours between compiles, not milliseconds). We do not add locking/retry/transaction ceremony designed for multi-tenant or high-contention systems. Any individual file's corruption is recoverable by re-compiling — the value is in the collective body of `raw/` + `wiki/` + connections, not any single file. Cheap insurance only (atomic temp+fsync+replace, journal file, 2-retry max on transient I/O). No lock files, no two-phase commits, no cross-file transactions.

---

## 2. Two-Sided Vault Architecture

The vault has two distinct sides with different ownership:

| Side | Path | Owner | Purpose | Mutability |
|---|---|---|---|---|
| **Human Side** | `~/Obsidian/{AIML, Daily Notes, Projects, ...}` (22 existing folders) | Joseph, manually | Daily capture, thematic organization, long-form notes | Pristine — no LLM writes |
| **Machine Side** | `~/Obsidian/KDB/` | LLM kdb_graph_compiler (via Python) | Raw sources + compiled wiki + state ledger | LLM owns `wiki/`; Python owns `state/`; Joseph owns `raw/` |

The two sides coexist as peers. Cross-linking between them is opt-in and asymmetric (see §8 Open Questions, Open-4).

---

## 3. Repo vs. Vault Separation

Two completely separate filesystems:

| Concern | Path | VCS / Backup |
|---|---|---|
| **Code** (this repo) | `~/Droidoes/Obsidian-KDB/` (WSL Linux) | git + GitHub |
| **Data** (vault) | `~/Obsidian/KDB/` (Windows local drive, Google-Drive-synced) | Google Drive (version history) |

The code reads from and writes to the vault via absolute paths. No nested git repos. No symlinks. Google Drive sync is the backup — we do **not** run a separate git repo inside the vault (earlier proposal dropped; cloud-sync version history is sufficient for v1). (The vault was OneDrive-synced until 2026-08; Google Drive since — the sync-pause runbook rule is provider-agnostic.)

---

## 4. Two Tracks

### Track 1 — KDB Compiler (this project, v1 focus)
`raw/` → LLM → `wiki/{summaries, concepts, articles}`

Karpathy-pattern incremental compile. Described in detail below.

### Track 2 — `llm-linker` (deferred to v1.5 / v2)
In-place enhancer for the Human Side. Scans user-authored notes and injects `[[wikilinks]]` to KDB concepts. Renamed from "enhancer" at Joseph's request. Separate implementation; not part of this v1 scope.

---

## 5. Track 1 Pipeline (V1a)

### Package structure (Phase B's six peers, since grown to eight)

```
common/          ← leaf: shared types, atomic I/O, call_model, paths, source_io
ingestion/       ← Pass-1 pipeline: kdb_scan, enrich/ (signal/noise + entity_search_keys), feeder/ (Gmail/Substack #143: gmail_client gws seam / gmail_extract / journal / gmail flow)
kdb_graph_compiler/        ← Pass-2 pipeline: kdb_graph_compiler, canonicalize, page_writer, context_loader, prompts/
kdb_graph/       ← producer-agnostic graph layer: schema, intake, queries, adapters
kdb_mcp/         ← read-only MCP stdio server over kdb_graph (#113 Phase 3a)
kdb_graph_search/      ← consumer-neutral semantic graph search: projection, budget, response, artifact (#123)
kdb_graph_orchestrator/    ← conductor: kdb_orchestrate, manifest_writer, event recorder
tools/           ← operational tools: replay, cleanup, benchmark, viewer, diagnostics
```

Pipeline registry: one file per pipeline under `<vault>/KDB/state/pipelines.d/<id>.json` (#143; legacy pipelines.json removed).

**Dependency contract (B.3):**
- `common` → nothing (true leaf; guard-tested)
- `kdb_graph` → nothing (**zero internal imports** — stricter than the "→ `common`" this line carried until 2026-07-26; the guard in `tools/tests/test_package_boundaries.py` has enforced `set()` for it all along, so the doc was the stale side)
- `ingestion` → `common`
- `kdb_graph_search` → `common` (consumer-neutral by ratified design — #123 R2: no per-consumer contracts, so no `kdb_graph` import; the **caller** materializes the search space)
- `kdb_mcp` → `common` + `kdb_graph` (a sibling, not a sub-package: staying outside `kdb_graph` preserves that package's zero-`common` invariant)
- `kdb_graph_compiler` → `common` + `kdb_graph` + `kdb_graph_search`
- `kdb_graph_orchestrator` → all packages (the conductor)
- `tools` → `common` / `kdb_graph` / `ingestion` / `kdb_graph_compiler` / `kdb_graph_search`

Nothing depends on `tools`. (Historical note: one documented exception — `kdb_graph_orchestrator` calling `tools.cleanup` for orphan cleanup inline — was retired by #130: the kdb_graph_orchestrator no longer imports `tools.cleanup`; the boundary-guard allowance row is filed for removal under #133.)

---

The live conductor is `kdb_graph_orchestrator/kdb_orchestrate.py`. Per-source flow:

```
┌────────────────┐   ┌──────────────────────────┐   ┌──────────────────┐
│   kdb_scan.py  │──▶│  enrich/ (Pass-1)         │──▶│  compiler.py     │
│ (deterministic │   │  (LLM: signal/noise,       │   │  (Pass-2 LLM:    │
│  scan, hashes) │   │   domain, entity_search_   │   │   wiki-native    │
└────────────────┘   │   keys; writes frontmatter)│   │   pages[] with   │
       │             └──────────────────────────┘   │  [[wikilinks]]   │
       ▼                                            │   in bodies)     │
 last_scan.json                                     └──────────────────┘
                                                              │
                                                              ▼
                                                   ┌──────────────────┐   ┌────────────────┐   ┌──────────────────┐
                                                   │ canonicalize.py  │──▶│  page_writer.py│──▶│ manifest_writer  │
                                                   │ (alias remap of  │   │ (Python writes │   │ (atomic ledger   │
                                                   │  body wikilinks) │   │  all .md files)│   │  update)         │
                                                   └──────────────────┘   └────────────────┘   └──────────────────┘
                                                                                                                │
                                                                                                                ▼
                                                                                                          manifest.json
                                                                                                          runs/<run_id>/
```

**Finalize pass** (post-#136 — a slim tail only): convergence flip — every `deprecated` node whose file frontmatter still reads `active` is marked `deprecated` on disk (`page_writer.mark_deprecated`, #130) → archive the combined `compile_result.json` replay payload (#132). **LINKS_TO wiring and deprecation are per-source, inside the commit txn** (#136, Option A drain-as-you-go): each source's commit wires links whose targets exist, upserts unresolved targets into the durable `PendingLink` node table (schema v2.5) that later commits drain on page arrival — the ledger survives aborts, so #94's stranded-links class is deleted by construction and memory stays O(per-source) instead of O(run's edge set); and the SUPPORTS-diff flips pages losing their last SUPPORTS to `deprecated` at the same commit (revive-on-re-support symmetric at the re-supporter's commit — #130 lifecycle preserved). The end-of-run `wire_links` / `detect_deprecations` batch passes, their intake flags, and the `graphdb.py` wrappers are deleted. (Graph intake itself runs **per source**, inside the commit sequence — see stages below and §8.3.)

**Derived-state wipe (#138):** `kdb-orchestrate --wipe` erases derived state — the `KDB/wiki/` tree, the graph (file or dir), `manifest.json`, and the `canonicalization/aliases.json` ledger — **and exits**: it is decoupled from the run, which has no modes (#138 — cold/warm is a reflection of derived state, not a run option; an empty world simply means every source is new). Before deleting, the wipe moves `state/runs/` to `state/pre-wipe-runs/<ts>/` (#137 — pre-wipe history is audit data, not live history; replay/verify over the post-wipe replay root matches live by construction) and appends one line to the wipe ledger `state/wipes.jsonl` (the wipe has no run to journal into). Preserved: `pipelines.d/` (config) and per-run outputs (overwritten by the next run). The gate is unbypassable: the CLI prints exactly what dies (paths + file counts + the archive plan) and requires typing `yes` — there is no `--yes`; non-interactive stdin refuses. This is the only removal path for cold-run-orphaned wiki files — files whose slug the new plan never re-emits have no graph node, so the per-source deprecation diff (#136)/convergence/`page_writer` can never see them (the #134 measurement: 172 such files = 40% of the sandbox tree). Related: finalize stats report **`deprecated_pages_total`** — the standing count of `status: deprecated` files in `KDB/wiki`, the operator-visible #134 tripwire.

**Strict separation of concerns:**
- **LLM is stateless compute.** It receives prompt + source content + graph-derived context snapshot; returns structured JSON (no paths, no timestamps, no frontmatter). Never writes files. Never reads filesystem state.
- **Python is deterministic state + I/O.** Scans, hashes, resolves paths, applies page intents (writes markdown), stamps frontmatter, updates ledger.
- **GraphDB (`kdb_graph`/Kuzu) is the substrate.** `context_loader.py` reads it via the graph query API. `manifest.json` is the source-state metadata ledger (hashes, run_state, timestamps — not an ontology index per D50).
- **Markdown vault is persistent state.** Everything the system knows is reconstructible from `raw/` + `wiki/` + `state/`.

### Page ownership split

| Page | Authored by | Strategy |
|---|---|---|
| `wiki/summaries/*.md` | LLM | Full-body replacement; Python adds frontmatter |
| `wiki/concepts/*.md` | LLM | Full-body replacement; Python adds frontmatter |
| `wiki/articles/*.md` | LLM | Full-body replacement; Python adds frontmatter |

No `index.md` (D23) and no `log.md` (D24) are generated. Obsidian's file explorer + `manifest.json` serve as the TOC; `state/runs/<run_id>/` is the authoritative per-run journal.

### Pipeline stages

`kdb_orchestrate.py` is the live conductor. Per-source flow (post-#115: the #65 Repair stage is deleted whole — its pairing-reconciliation classes are structurally impossible under the shrunk contract — and the stages renumber; Task #74's canonicalize sits between validate and page_writer):

1. **Scan** (`kdb_scan.py`) — walks `raw/`, computes SHA-256, compares to `manifest.json`, emits `last_scan.json` with `to_compile` + `to_reconcile` lists. Compile eligibility is the single honest comparison `current_hash != last_compiled_hash` (D46) — `run_state` plays no part, and there is no force-recompile flag. Handles symlinks (skip), binaries (flag `no_graph_db`), two-pass rename detection. Atomic write.
2. **Enrich** (`enrich/` — Pass-1 LLM) — for each source: LLM judges content (signal/noise), classifies domain and source_type, emits `entity_search_keys` (≤10 kebab-case slugs); writes enrichment frontmatter atomically to the source file. Signal sources advance to Pass-2; noise sources are recorded and skipped.
3. **Compile** (`compiler.py` — Pass-2 LLM) — for each signal source: runs **pass-1.5** first (`kdb_graph_compiler/search_adapter.run_pass15`, post-#123 P3a — exactly one semantic graph search per source against the live ontology; gates pre-Pass-1 sources; persists one search envelope under `runs/<run_id>/search/`), then builds the graph-derived context snapshot from its products (`t2_selection` / `t1_slugs` / `search_summary` / `key_outcomes`) via `context_loader.build_context_snapshot`, calls `call_model_with_retry` with source content + context + the repo-packaged `kdb_graph_compiler/prompts/KDB-Compiler-System-Prompt.md` (repo-owned post-#115, D-115-13; every run header stamps `pass2_prompt_version` + the loaded prompt's SHA-256); receives a **proposal** (post-#119, D-119): `pages[]` of wiki-native data only — `page_type`, `title`, `body`, plus `slug` for concept/article pages (the summary page carries none; a stray of any JSON type is tolerated and dropped with telemetry) — + optional `compilation_notes`. The contract speaks wiki, not graph: no `source_name`, `summary_slug`, `concept_slugs`/`article_slugs`, `outgoing_links`, `status`, `confidence`, or `log_entries` — Python owns everything mechanical. The **normalization bridge** (`kdb_graph_compiler/proposal_bridge.py`, PLAN-APPLY-VERIFY) coerces concept/article slug form, stamps the summary identity Python derives by role + `source_id` (`expected_summary_slug(source_id)` — slugify + length budget, never prompt-injected; the raw summary-slug value never rejects, never retries), then the canonical object passes the unchanged canonical schema gate + conservation self-check before reaching canonicalize. An underivable stem fails before the model call (`failure_stage="validate"`, zero API spend). Accumulates into `compile_result.json`.
4. **Validate compile_result** (`validate_compile_result.py`) — schema-gates `compile_result.json`; aborts per-source with quarantine if malformed (source-local failure does not kill the batch — D-96-1). Post-#115 **dual-mode** (D-115-14): new-shape payloads validate against the derived expected summary slug; historical payloads carrying the six removed fields still validate — those fields are optional, deprecated, read-only, and never copied forward.
5. **Canonicalize** (`canonicalize.py`, Task #74, see `docs/archive/tasks/task74-canonicalization-blueprint.md`) — loads `state/canonicalization/aliases.json` (missing ⇒ empty + warning, D-R5-8), resolves alias surface forms to canonical slugs (chain-flattened to root, D-R5-13), rewrites `pages[].body` wikilinks only (post-#115 body-authority, D-115-9: `outgoing_links` no longer exists in the contract — the old outgoing-links union and slug-list remap are deleted; links that exist only in a dropped body die with it), drops alias entries from `pages[]` (canonical-only, D-R5-12), emits `canonical_meta` (`aliases_emitted`, `outgoing_link_remaps` — historical field name retained for read-compat, now describing body-link remaps —, `algorithm`), atomically overwrites `state/compile_result.json` (D-R5-10). Algorithmic failures (circular aliases, malformed ledger, ambiguous v2, sha mismatch) are **fatal** — failure journal written, pipeline halts before page_writer (D-R5-9). **Post-canonicalization invariant (D-115-11):** for every source, exactly one `page_type == "summary"` page AND its slug still equals `expected_summary_slug(source_id)` — re-checked immediately after `canonicalize.run()`, before any write; summary/non-summary cross-type merges are hard-rejected via typed `CanonicalizationError` (quarantine, no writes). Wiki ≡ graph at the naming layer.
6. **Apply page intents** (`page_writer.py`) — resolves slugs to paths (`paths.py`), stamps frontmatter (`run_context.py`), writes markdown files atomically (`atomic_io.py`). `status` is a **Python-owned stamp** post-#115 — the model no longer emits it; a page without one defaults to `active`. No canonicalization-awareness required (D-R5-12): `pages[]` is already canonical and body wikilinks already remapped. Never writes state files. A throw here leaves the graph untouched (β case-a, clean).
7. **Graph intake** (`kdb_graph/intake.py::apply_compile_result`) — per-source Kuzu transaction on the run's single shared GraphDB connection (Task #91 shared-connection design; see §8.3), updating the live GraphDB ontology authority (D50/D51). **LINKS_TO derivation is graph-owned** (post-#115): the intake derives a page's edge target-set from its body wikilinks (mirrored extractor); historical payloads carrying `outgoing_links` prefer the stored list — the delete-then-recreate contract is unchanged, so recompiles under the new shape do not erase edges. **Wiring and deprecation are in-txn** (#136): links to existing targets wire immediately, unresolved targets upsert to the durable `PendingLink` ledger (drained by later commits on page arrival), and the SUPPORTS-diff flips losing pages `deprecated` (revive symmetric) — nothing is deferred to a finalize pass. A throw rolls the Kuzu txn back clean and the manifest is never written, so the source self-heals on re-run (β case-a). Fatal for non-dry-run compiles (D50; revokes D38).
8. **Persist state** (`manifest_writer`) — the **commit boundary** of the β ordering (D-91-15, graph-sync-first): the advanced manifest is computed pure (no I/O) up front, then `manifest.json` is written atomically **LAST** — only after the wiki pages and the graph txn both succeed. A throw here is the distinct, surfaced `manifest_post_graph` residual (graph committed, manifest absent), which self-heals via idempotency on re-run.

---

## 6. State Model

### Architectural layers (D51)

| Layer | Path | Role |
|---|---|---|
| **Source corpus** | `KDB/raw/` | Human-authored raw sources |
| **Live ontology authority** | `GraphDB-KDB/` (Kuzu) | Primary. Updated immediately on every compile (Stage 7 `graph_sync` post-#115 renumbering). Owns Entity, LINKS_TO (derived from body wikilinks post-#115), SUPPORTS, ALIAS_OF, canonical_id, entity lifecycle status (`active` | `deprecated` — #130) |
| **Reconstruction material** | `KDB/state/runs/<run_id>/` sidecars | Backup. Durable compile_result + scan snapshots. Enables `graphdb-kdb rebuild` if GraphDB is lost/corrupted |
| **Audit log** | `KDB/state/runs/<run_id>.json` journals | What happened, when, with which model, what failed |
| **Source-file lifecycle** | `KDB/state/manifest.json` | Source-state metadata ledger (hashes, run_state, timestamps). Not an ontology index (D50) |
| **Per-page provenance** | Frontmatter in each `.md` file | Human-readable (`raw_path`, `raw_hash`, `compiled_at`) |
| **Rendered view** | `KDB/wiki/` | Markdown output for Obsidian consumption |

**Primary data flow:** `raw/ → kdb-orchestrate → Stage 5 canonicalize → graph-sync (immediate GraphDB update)`. Runs are not in this hot path — they are written as audit records whose sidecars happen to also serve as reconstruction material.

**Rebuild/verify:** Proves the live authority matches what a clean reconstruction from sidecars would produce. Operational safety net, not the normal data flow.

**Rejected alternatives:** SQLite (too opaque, no diff, breaks OneDrive sync), vector DB (Karpathy explicitly rejects this), pure frontmatter-only (Grok's proposal — too lean at projected scale), `ontology_sources/*.json` per-source durable layer (redundant with sidecars, adds a third consistency surface — rejected in D51).

---

## 7. Benchmark Architecture

The benchmark is a **two-step** quality measurement layer: `kdb-orchestrate --emit-kpis` (the run vehicle) **emits** per-run measurement data from real pipeline runs; `kdb-benchmark score` (the scoring tool) **ranks** models via hierarchical weighted Borda. The two roles are permanently separated — the scorer never re-runs the passes, and the production pipeline never imports from `tools/benchmark`.

### 7.1 Package layout & boundary

```
tools/benchmark/
├── cli.py          # `kdb-benchmark score <run-id…>`
├── promotion.py    # watched-KPI promotion evaluator
├── paths.py        # benchmark dir conventions
└── tests/

kdb_graph_compiler/kpi/
├── processing.py   # processing-family KPI computation
├── graph.py        # graph-family KPI computation (reads via kdb_graph.queries)
├── score.py        # Borda engine + hierarchical composite scorer + penalty
└── report.py       # per-run report.md renderer

common/
└── measurement.py  # PassCallMeasurement + RunMeasurementHeader (logical view over telemetry — not a new store)

benchmark/
├── sources/        # canonical 36-source sandbox corpus (tracked in git)
├── runs/           # per-model measurements.json + report.md (gitignored)
└── scores/         # leaderboard.json + leaderboard.md (tracked)
```

**One-way import boundary** (D25, preserved): `tools/benchmark` and `kdb_graph_compiler/kpi` import from `kdb_graph_compiler` and `common`; production pipeline packages never import from `tools`. `kdb_graph_compiler/kpi` is used by both the kdb_graph_orchestrator (emit path) and the scorer (load-and-rank path).

### 7.2 How to run a cohort

1. **Reset the sandbox** (`~/Obsidian/Vault-in-place-test-run`) and run `kdb-orchestrate --emit-kpis --model <model-id>` once per candidate model. Each run writes `benchmark/runs/<run-id>/measurements.json` (reset-surviving — the sandbox graph is wiped before the next model; measurements must be emitted inside the run). See the cohort runbook: `docs/reference/benchmark-cohort-procedure.md`.
2. **Score** with `kdb-benchmark score <run-id> [<run-id>…]` (one run-id per model). Writes/updates `benchmark/scores/leaderboard.json` + `leaderboard.md`.

### 7.3 KPI families

Two families, kept structurally separate — never blended at scoring time.

**Processing family** (from per-run telemetry, per-1M-token normalized):

| KPI | Direction | What it measures |
|---|---|---|
| `quarantine_rate` | ↓ | fraction of sources that failed to produce a usable compile result |
| `recovery_rate` | ↓ | fraction of sources requiring compile re-prompts (SDK transient retries excluded) |
| `latency` | ↓ | median end-to-end latency per source (ms) |

**Graph family** (from the Kuzu knowledge graph after the run, combined into one `graph_score`):

| KPI | Direction | What it measures |
|---|---|---|
| `graph_connectivity` | ↑ | largest-connected-component fraction of canonical entities |
| `link_density` | ↑ | LINKS_TO edges per entity |
| `supports_density` | ↑ | SUPPORTS edges per entity |
| `entity_reuse` | ↑ | fraction of entities referenced by ≥2 sources |

**Watched diagnostics** (emitted each run, not scored; evaluated for promotion after each cohort via `tools/benchmark/promotion.py`):
`entity_search_key_resolution` · `deprecation_rate` (#130 — was `orphan_rate`) · `semantic_pass_rate` · `signal_noise_ratio` · `retry_load` · `token_overrun_rate` · `repair_rung_rate` · `domain_breadth` · `domain_null_rate` · `belongs_to_coverage` · per-pass breakdowns (`quarantine_rate_pass1/2`, `latency_pass1/2`; #117 adds `recovery_rate_pass1/2`, `retry_load_pass1/2`, `cost_usd_pass1/2`, `cost_unknown_calls_pass1/2`) · search-event rates (#123 P3a.3, V2-re-baselined: `search_expression_matched_rate`/`_unresolved_rate`, `search_hit_recency_{pre_run,cohort,age_unknown}_rate`, `search_stage2_budget_bound_rate` — the seven #122 `search_key_*` series are retired and historical values are NOT comparable across the re-baseline) · pass-1.5 diagnostic columns (#123 P3a.4: `calls/attempts/retries/cost_usd/cost_unknown_calls/input_tokens/input_token_unknown_attempts/latency`, all `_pass1_5`-suffixed, surfaced on both pass boards' `raw_values` — diagnostics only, never a scored axis, no third ranked board)

### 7.4 Weights & penalty (all PINNED)

Moved 2026-08-19 to [`docs/reference/benchmark-architecture.md`](reference/benchmark-architecture.md): top-level composite weights (40/40/10/10), within-graph weights (35/30/20/15), hierarchical weighting with pro-rata renormalization, weak-spot penalty (τ=0.5, λ=0.10 PINNED).

### 7.5 Data flow

Moved 2026-08-19 to [`docs/reference/benchmark-architecture.md`](reference/benchmark-architecture.md): sandbox → `kdb-orchestrate --emit-kpis` → `benchmark/runs/<run-id>/` → `kdb-benchmark score` → leaderboards flow diagram.

### 7.6 Promotion rule

Moved 2026-08-19 to [`docs/reference/benchmark-architecture.md`](reference/benchmark-architecture.md): watched-KPI promotion gates (CoV > 0.2; Q1 > ε; max |Spearman| vs scored < 0.7), advisory only.

### 7.7 Per-pass leaderboards (#117, spec v0.3.1)

Moved 2026-08-19 to [`docs/reference/benchmark-architecture.md`](reference/benchmark-architecture.md): three boards per invocation; recompute-don't-re-run; cost as selection column; fail-closed completeness contract; honest metadata; write boundary.

---

## 8. GraphDB-KDB Layer (Kuzu-backed knowledge graph)

The reframe locked on 2026-05-10 (paradigm doc: `docs/reference/New-GraphDB-Paradigm.md`): **KDB is a raw-text → knowledge-graph kdb_graph_compiler**, not a wiki-page kdb_graph_compiler with a graph as a byproduct. Wiki pages and `manifest.json` are *renderings* of the graph; the graph is the architectural primitive that downstream tooling (search, knowledge-hole detection, EXISTING CONTEXT seed selection, adaptive learning paths) consumes.

The bet is **explicit edges beat implicit similarity** — vector RAG flattens ontology into cosine distance; the graph preserves the explicit edges paid to construct. See memory note `feedback_graph_over_vector_for_kdb`.

### 8.1 Package layout & boundary

```
kdb_graph/                                  ← producer-agnostic ontology layer
├── schema.py                               # Kuzu DDL (Entity / Source / LINKS_TO / SUPPORTS)
├── types.py                                # Entity, Source dataclasses; SyncResult, RebuildResult
├── graphdb.py                              # GraphDB connection manager + idempotent schema bootstrap
├── intake.py                               # apply_compile_result — atomic per-run mutations
├── queries.py                              # neighbors, paths, provenance reads (single Kuzu door)
├── analytics.py                            # PageRank, Louvain, structural holes (hybrid via NetworkX)
├── verifier.py                             # verify_against_manifest — overlap audit
├── rebuilder.py                            # rebuild() — generic chronological replay (D-B1)
├── snapshot.py                             # snapshot() — JSONL+manifest+schema.cypher export (#63.9)
├── adapters/
│   ├── base.py                             # ProducerAdapter Protocol; RunDescriptor / EligibilityResult
│   └── obsidian_runs.py                    # Obsidian-KDB adapter (reference impl)
└── cli.py                                  # graphdb-kdb CLI dispatcher
```

**One-way import boundary** (D34 + D-B1, mirrors D25 for kdb_benchmark): `kdb_graph/` has **zero imports from the production kdb_graph_compiler packages**. Producer-specific knowledge lives inside `adapters/obsidian_runs.py` and is expressed as JSON parsing of producer artifacts — never as Python imports of producer types. A grep invariant on `from kdb_graph_compiler\|from ingestion\|from kdb_graph_orchestrator` inside `kdb_graph/` returns nothing.

**Physical separation** (D35): the Kuzu *data* directory lives at `~/Droidoes/GraphDB-KDB/` (sibling to `Obsidian-KDB/`, not OneDrive-synced — avoids binary-file corruption). (Superseded 2026-06-11, #113: the default now derives `<vault>/KDB/graph` from `OBSIDIAN_VAULT_PATH`; `KDB_GRAPH_PATH` overrides. The old `~/Droidoes/GraphDB-KDB/` dir is a retired stray.) The Python package code lives at `kdb_graph/` inside `Obsidian-KDB/`; the extraction arc to a standalone repo `~/Droidoes/GraphDB-KDB-package/` is documented in `docs/reference/graphdb-kdb-extraction-roadmap.md`.

### 8.2 Schema (Kuzu DDL)

Moved 2026-08-19 to [`docs/reference/graphdb-layer.md`](reference/graphdb-layer.md): Entity/Source node-table DDL, LINKS_TO/SUPPORTS/ALIAS_OF rel tables, canonicalization invariants C1–C4, alias deprecation exemption, naming history.

### 8.3 Pipeline integration — per-source graph intake (β commit model, D-91-15)

`kdb-orchestrate` runs graph intake **per source**, inside the commit sequence (post-#74 + Task #91; canonicalize precedes intake — Stage 5 after the post-#115 renumbering):

```
Per-source commit (β order):  scan → enrich (Pass-1) → compile (Pass-2) →
                               canonicalize → page_writer →
                               graph intake → manifest write (LAST = commit boundary)
  (post-#115: the Repair stage is deleted whole; stages renumber.)

  Graph intake (D50: fatal for non-dry-run; revokes D38 non-fatal):
    kdb_graph.intake.apply_compile_result(cr, single_scan, run_id, conn=conn)
    — one Kuzu transaction per source over the run's shared read-write
      GraphDB connection; a throw rolls back clean and the manifest is never
      written (β case-a self-heal). Post-#115 the LINKS_TO target-set is
      graph-owned: derived from body wikilinks via a mirrored extractor
      (`body_wikilink_slugs`); legacy payloads carrying `outgoing_links`
      prefer the stored list. Delete-then-recreate per source unchanged.
      Post-#136 the same txn wires resolvable links, pends unresolved
      targets in the durable `PendingLink` table (drained by later commits),
      and runs the per-source SUPPORTS-diff deprecation flip.

Finalize (post-#136 — slim tail, once): convergence flip marking any
  `deprecated` node's file frontmatter (#130) → archive the combined
  compile_result.json replay payload (#132).
```

Two architectural properties of the wiring:

1. **The kdb_graph_orchestrator holds a shared `GraphDB` connection and calls `kdb_graph.intake` entry points directly** (Task #91 shared-connection design — `kdb_orchestrate.py` imports `GraphDB` + `apply_compile_result`; post-#136 the standalone `wire_links`/`detect_deprecations` entry points no longer exist — wiring and deprecation ride the per-source commit txn). This supersedes the original D-S0 adapter-only wiring for the live path; the `ObsidianRunsAdapter` remains the single producer→graph entry point for **rebuild/replay** (`graphdb-kdb rebuild`, D39). (The D-S0 ledger row below is kept as the historical record.)
2. **Replay material survives a sync failure.** Per-source Kuzu failures roll back before the manifest commit boundary (β case-a), and the combined `compile_result.json` replay payload is written at finalize — so `graphdb-kdb rebuild` remains a real recovery path. (Per D50, graph intake failure is fatal for non-dry-run compiles since GraphDB is the live ontology authority; D38 non-fatal semantics were revoked for ontology writes.)

**Adapter's canonicalization responsibilities** (Task #74, Phase 3.5 in `kdb_graph/intake.py`): on top of canonical-entity upsert + LINKS_TO + SUPPORTS, the adapter reads `canonical_meta.aliases_emitted` from the canonicalized compile_result and writes one `Entity` row per alias (`canonical_id` = root canonical slug, `page_type` = `'alias'`) plus one `ALIAS_OF` edge alias→canonical with `algorithm` provenance. Promotion edge case: when a slug previously written as alias appears as canonical, `_upsert_entity` resets `canonical_id = NULL` and drops outgoing `ALIAS_OF` (preserves C1). Re-running the same `canonical_meta` is idempotent (drop-then-create on `ALIAS_OF` keeps the flat invariant — one edge per alias, run_id reflects most recent run; older provenance lives in the per-run sidecar).

### 8.4 Replay / rebuild path (D39 — the independence proof)

`graphdb-kdb rebuild --vault-root <P>` drops all Kuzu tables and replays the eligible subset of `state/runs/*.json` chronologically:

- **Eligibility filter** (D39): `success=true AND dry_run=false AND payload_present` (D50 amendment: `replayable_payload`, when present, alone decides the success leg). Payload = per-run sidecar at `state/runs/<run_id>/{compile_result,last_scan}.json`. Adapters declare which producer journal `schema_version` they support (D-S3) — version mismatches return structured skip reasons (`'unsupported_version'`), not silent corruption. The Obsidian adapter declares `supported_journal_versions = ["2.0", "2.1", "2.2", "2.3"]`: `2.0` = compile runs (pre-cleanup, pre-#74), `2.1` = `kdb-clean` cleanup runs, `2.2` = post-#74 runs carrying `canonical_meta` in the sidecar `compile_result.json`, `2.3` = **kdb_graph_orchestrator-era runs (#132)**.
- **Orchestrator-era journals (#132, D39 restored):** `kdb-orchestrate` archives a slim 2.3 journal + both sidecars every run (`kdb_graph_orchestrator/journal_writer.py`, called from `run()`'s `finally` — aborted/crashed runs archive their partial-but-faithful payload with `success=false, replayable_payload=true`). The sidecars are the union of the run's *actual intake inputs*, split by live phase: `files`/`to_compile` = committed sources' post-embed scan entries (noise/failed/unchanged excluded); `moved_files`/`to_reconcile` = *applied* reconcile ops only. The adapter replays 2.3 journals **two-phase, live-order** (commits then reconcile — a single unioned `apply_compile_result` would erase-then-re-add a page whose sole supporter was deleted in the same run, killing inbound `LINKS_TO` that live preserved; pinned by test). Post-#136 the replayed per-source calls run with intake defaults — wiring/draining/deprecation happen per commit in replay exactly as live; the journal's `finalize_progress` key still *selects* the 2.3 two-phase path but its *value* is ignored (no deferred finalize derive remains — `finalize_progress` now records `none`/`complete` for audit only). Legacy journals replay with monolith defaults, unchanged. (Gap history: no producer wrote replayable compile journals between the 0.5.1 realignment (2026-06-01) and #132 (2026-08-06) — that era's runs are replay-invisible, recoverable only via cold rerun.)
- **B-lite split** (D-B1): `rebuilder.py` is producer-agnostic (drop-all + chronological iterate + per-run try/except); the adapter (`adapters/obsidian_runs.py`) supplies discover_runs / is_eligible / load_payload / apply. No producer-specific code in the core.
- **Blast radius v1** (D-S2, L8): whole-DB drop only; producer-scoped rebuild deferred until producer #2 ships. CLI prints a warning before the drop unless `--yes`.
- **Canonicalization replay** (Task #74): rebuild reads `canonical_meta.aliases_emitted` from each post-#74 sidecar and reproduces the exact `Entity.canonical_id` + `ALIAS_OF` edges the original compile produced. No re-execution of the canonicalization algorithm during rebuild — output is replayed from the journal, preserving D-R5-4 purity under replay. Pre-#74 journals (no `canonical_meta`) leave `canonical_id IS NULL` for all entities (matches their original state).
- **Baton-backfill** (one-shot, opt-in via `--backfill-baton`): synthesizes a `RunDescriptor` pointing at `state/{compile_result,last_scan}.json` baton files using `manifest.runs.last_successful_run_id` as the synthetic run_id; sorts before all real runs (`sort_key="0000-pre-63-backfill"`); idempotent — silently skipped if a sidecar already exists at `state/runs/<run_id>/`. The one-time migration entry for the latest pre-#63 run, per #63.0 outcome (d) — the other 9 pre-#63 runs are unrecoverable.

Independence claim: **delete `manifest.json` → GraphDB still queryable; delete `~/Droidoes/GraphDB-KDB/` → manifest still works**. Both are derived from `compile_result`. `graphdb-kdb verify` audits overlap (Layer 1 source-state preflight + Layer 2 replay structural diff + Layer 3 C1–C4 canonicalization invariants); `graphdb-kdb rebuild` regenerates either store from the post-#63 run history. `graphdb-kdb snapshot` (#63.9) writes a JSONL+manifest+schema export under `state/graph-snapshots/<run_id>/` — Task #74 bumped `snapshot_format_version` to `2` to include per-Entity `canonical_id` and an `alias_of.jsonl` file with full ALIAS_OF provenance, so Tier-2 cloud-sync (Google Drive) recovery preserves alias state.

**Maintenance — `kdb-clean orphans` is RETIRED (#130):** the command prints a
retirement notice and exits 0 — the `orphan_candidate` status no longer exists
(model-dropped pages are marked `deprecated` in place — per-source at commit
post-#136, with a finalize convergence flip for any graph↔disk drift; pages of a
deleted source are erased at reconcile, no archive). Historical `cleanup`
journals remain replayable through `kdb_graph.intake.apply_cleanup`, so
`graphdb-kdb rebuild` over pre-#130 history still reproduces reaped retractions
(Task #68). The legacy `reap_orphans` / `build_cleanup_artifacts` helpers, the
fired one-shot backfill script (`scripts/backfill_cleanup_journal.py`), and the
boundary-guard's `kdb_graph_orchestrator→tools.cleanup` allowance were removed in #133 —
nothing imports `tools` anymore.

### 8.5 Pointers to companion docs

The blueprint + companion docs are the durable architecture record. This section is the navigation summary.

| Doc | Scope |
|---|---|
| [`docs/reference/task-graphdb-kdb-blueprint.md`](reference/task-graphdb-kdb-blueprint.md) | Locked decisions D32–D40 + D-A1/A2/B1/S0–S3; schema DDL; ingestion algorithm; rebuild semantics; #63.1–#63.9 sub-task ledger |
| [`docs/reference/New-GraphDB-Paradigm.md`](reference/New-GraphDB-Paradigm.md) | Conversational record of the 2026-05-10 reframe (graph-is-the-system); scope distinction GraphDB-KDB vs future `kdb-graph` |
| [`docs/reference/graphdb-kdb-producer-contract.md`](reference/graphdb-kdb-producer-contract.md) | Formal contract for what GraphDB-KDB expects from any producer (mutation payload + scan + run journal + sidecar archive); adapter interface |
| [`docs/reference/graphdb-kdb-extraction-roadmap.md`](reference/graphdb-kdb-extraction-roadmap.md) | 5-stage path from monorepo to standalone PyPI package; invariants PR1–PR10; anti-patterns |
| [`docs/reference/manifest-succession-arc.md`](reference/manifest-succession-arc.md) | M0–M4 transition arc: manifest.json from swiss-knife (source meta + ontology) to source-meta-only ledger; EXISTING CONTEXT switches to GraphDB at M1 |
| [`docs/archive/tasks/task63-phase3-implementation-blueprint.md`](archive/tasks/task63-phase3-implementation-blueprint.md) | Implementation plan for #63.5b + #63.6 + #63.7-pre (the three sub-tasks landed 2026-05-14) |
| [`docs/archive/tasks/task74-canonicalization-blueprint.md`](archive/tasks/task74-canonicalization-blueprint.md) | Canonicalize-stage blueprint (Stage [6] in pre-#115 numbering; Stage 5 today): locked decisions D-R5-1..D-R5-13; algorithm (string-norm → ledger → embedding v2 → llm-judge v2); schema delta (`Entity.canonical_id`, `ALIAS_OF`); C1–C4 verify invariants; rebuild semantics |
| [`docs/archive/tasks/task75-predeclared-eval-criteria-blueprint.md`](archive/tasks/task75-predeclared-eval-criteria-blueprint.md) | **Step-3 query-time eval contract (Task #75, blueprint v2 — Codex + Gemini review applied).** Predeclares the operations roster (PPR + community routing + subgraph extraction V1; typed traversal V0; shortest-path V0 / scored-multi-hop V2), per-op pass/fail/quantitative-gate criteria, hedge-watch rules HW-1..HW-7 (symptom → §8.3 hedge), step-3 preconditions (Task #76 `domain` field gates the community/domain-ratio acceptance only; Task #77 probe set), and OQ-1..OQ-9. Satisfies Round 5 §8.5/§8.6 path-forward precondition (Codex Q6 — avoid "implementation momentum disguised as empiricism"). Pattern mirrors Task #19 (compile-side KPI predeclaration → §7) extended to query-time. |

### 8.6 CLI surface (current)

Moved 2026-08-19 to [`docs/reference/graphdb-layer.md`](reference/graphdb-layer.md): the full `graphdb-kdb` subcommand list and `--graph-dir` path resolution.

### 8.7 Graph context loader — retrieval tiers (post-#123 P3a)

`context_loader.py` builds a source-specific `ContextSnapshot` from the graph. It reads the graph via the `kdb_graph.queries` API (no raw Kuzu), replacing the legacy manifest-based approach. Post-#123 P3a.2b the loader searches nothing itself: `kdb_graph_compiler/search_adapter.run_pass15` runs the pass-1.5 semantic graph search FIRST (exactly one per source, inside `compile_source` step 1), and the builder consumes its products (`t2_selection` / `t1_slugs`). The legacy T2 family — `T2Mode`, the regex slug/title-in-text matchers, the resolver wrappers, `source_text`, and the cold-start 2-hop widening — is **deleted whole** (blueprint §7 REPLACE; semantic selection is the sole T2 seeding path, R-P3a-2). The loader assigns entities to **retrieval tiers** — relevance-source categories, not graph-distance measures:

| Tier | Name | Score | Semantics |
|------|------|-------|-----------|
| T1 | Provenance tier | 3 | Entities directly supported by the current source via `(:Source)-[:SUPPORTS]->(:Entity)`. Strongest signal: "this source already owns/contributed to these entities." |
| T2 | Selector tier | 2 | The pass-1.5 selector's validated hits (`t2_selection`), in selector order — semantic, LLM-chosen matches against the eligible graph space. Never lexical evidence from source text (the regex/PK family is deleted). |
| T3 | Neighborhood-expansion tier | 1 | Entities connected to the seed set (T1 ∪ T2) through `[:LINKS_TO]`. Always 1-hop — the cold-start conditional 2-hop is gone. |

Key distinction: **T1/T2 are seed-selection tiers** (what we search for). **T3 is graph-expansion** (how far we walk from seeds). Only T3 maps to "degree of separation"; T1 and T2 are relevance-source categories, not distances.

**Same-domain gate (D3 override, #103):** T2/T3 pull only from the source's Pass-1 domain (entity anti-entropy). T1 stays on the full set — it is the source's own provenance.

Tie-break: within T2 the **selector's rank** wins (first rank on duplicates); within T1/T3, PageRank descending then slug ascending.

Cap (#131): `page_cap` (default 50) flood-caps the **t2∪t3 tail only** — T1 is must-see, delivered in full regardless of size (a source owning more pages than the cap keeps its whole tail in the model's view).

#### Cold-start (post-#123 P3a)

A source is still cold-start when `len(t1_slugs) == 0` — it has no `SUPPORTS` edges (never compiled before). Cold-start is now **telemetry only** (recorded on the ContextRecordV2): it triggers no widening. Tier scores (3/2/1), tie-breaks, the cap semantics above (#131), and the `ContextSnapshot` shape are invariant; empty T1/T2/T3 is valid — Pass-2 compiles cold (owner ruling 2026-08-03).

---

## 9. Decisions Ledger

| # | Date | Decision | Rationale |
|---|---|---|---|
| D1 | 2026-04-18 | Two-Sided Vault (Human Side / Machine Side) with `KDB/` at vault top-level | Zero disruption to existing human-curated folders; clean ownership boundary |
| D2 | 2026-04-18 | Code repo separate from vault data | Git on code (WSL), OneDrive on data (Windows); no nested repos |
| D3 | 2026-04-18 | Track 2 renamed "enhancer" → `llm-linker` | User preference; shorter |
| D4 | 2026-04-18 | v1 target use case = pull `docs/` from `~/Droidoes/*` repos as seed raw sources | Known high-value content, low risk |
| D5 | 2026-04-18 | OneNote stays on Human Side | User decision |
| D6 | 2026-04-18 | No git inside vault; OneDrive version history is sufficient backup | Avoids `.git/index.lock` races with OneDrive sync |
| D7 | 2026-04-18 | Adopt GPT 5.4 state model (manifest.json + frontmatter + log.md + runs/) | Middle ground: richer than Grok's pure-markdown, lighter than Gemini's DAG+Git+Intent Architecture, matches working community patterns |
| D8 | 2026-04-18 | **LLM outputs "page intents" (slug + title + body + logical links); Python owns paths, frontmatter, timestamps, versions, backlink reconciliation.** Revised from original "patch-ops" wording after Codex M0 review. | Predictability, auditability, dry-run capability, no hallucinated paths / corrupt frontmatter. Boundary purity: LLM = semantic analyzer, Python = codegen/linker. |
| D9 | 2026-04-18 | Controller pipeline over mega-prompt (chunk 10–20 sources/batch) | Prevents prompt drift at 100+ files (Codex guidance) |
| D10 | 2026-04-18 | Reject SQLite for v1 (possibly v2 if scale demands) | JSON is LLM-inspectable, diff-friendly, OneDrive-compatible |
| D11 | 2026-04-18 | Content-hash (SHA-256) as source-of-truth; mtime is advisory only | Survives renames, OneDrive timestamp rewrites, cross-machine sync |
| D12 | 2026-04-18 | Flag-don't-nuke on delete: `orphan_candidate` + `tombstones`, never auto-delete wiki pages | User reviews orphans manually; no data loss — **SUPERSEDED by #130 (2026-08-05):** model-drop ⇒ `deprecated` in place (graph node + file frontmatter, revivable on re-emit); source deletion ⇒ total erasure, no archive (Joseph: "not in KDB/wiki, not archived") |
| D13 | 2026-04-18 | Two-pass rename detection (hash-match before NEW/DELETED classification) | Prevents double-counting moved files (Codex fix) |
| D14 | 2026-04-18 | Minimal atomic write: temp + fsync + os.replace + ≤2-retry on transient I/O. **No lock files, no 6-retry ladder, no multi-phase commit.** | Single-user single-process workload; heavier machinery is imaginary-risk complexity. Revised from original Codex proposal after user philosophy note. |
| D15 | 2026-04-18 | Journal-then-pointer manifest writes (`runs/<run_id>.json` first, manifest.json last) | Crash-safe: failed runs don't corrupt ledger. Cheap; keep it. |
| D16 | 2026-04-18 | Provider abstraction ported from `~/Droidoes/Code-projects/youtube-comment-chat/src/llm.py` | Reuse: Anthropic / Gemini / OpenAI / Ollama already supported |
| D17 | 2026-04-18 | V1a: build full pipeline upfront (not evolve from mega-prompt) | Cleaner foundation, no rework later |
| D18 | 2026-04-18 | **Full-body replacement** over patch-ops for LLM-authored pages | Wiki pages are 100% LLM-owned; no human edits to preserve; no concurrent writers. Patch-op language, merge semantics, and per-op test surface = complexity for zero gain. Forward-compatible: the applier can accept `body` today and `ops[]` later without schema break. |
| D19 | 2026-04-18 (revised 2026-04-20) | Page-ownership split: LLM authors `summaries/`, `concepts/`, `articles/`. Originally included Python-authored `index.md` + `log.md`; both removed (D23, D24). | Python-authored files are pure functions of state; having the LLM emit them would waste tokens and risk drift. |
| D20 | 2026-04-18 | Shared seam modules: `paths.py`, `atomic_io.py`, `types.py`, `run_context.py` | Codex M0 review: three modules independently claim atomic-write discipline; centralize to prevent subtle divergence. |
| D21 | 2026-04-18 | Reserve `prompt_builder.py`, `context_loader.py`, `response_normalizer.py` as named stubs | Codex M0 review: `compiler.py` is trending toward god-module; reserve split points before M2 to avoid accretion. |
| D22 | 2026-04-18 | Design philosophy: **no complexity for imaginary risk** | Single-user, single-process, infrequent workload. Individual file corruption is recoverable by re-compile; the value is the collective body, not any single file. Drop machinery designed for multi-tenant/concurrent scenarios. See `~/.claude/projects/.../memory/feedback_no_imaginary_risk.md`. |
| D23 | 2026-04-20 | Drop `index.md`. Obsidian's file explorer + `manifest.json.pages{}` already serve as the TOC; the generated `index.md` was adding a misleading hub node to the graph view (every page had an inbound edge from it) without unique value. | Graph noise was real; the "single entry-point file" was redundant with Obsidian's native navigation and with `manifest.json` for programmatic consumers. Revises D19. |
| D24 | 2026-04-20 | Drop `log.md`. Same reasoning as D23: derived state, zero wikilinks = isolate node in graph view, and `state/runs/<run_id>.json` already holds the authoritative per-run journal with full detail. Warnings/info surfaced via stdout banners during the run; anyone needing post-hoc detail opens the JSON journal. | Eliminates a redundant human-facing mirror; no information is lost — just stops maintaining two views of the same data. Revises D19. |
| D25 | 2026-05-04 | Benchmark engine is a separate top-level package (`kdb_benchmark/`) with a strict one-way import boundary: `kdb_benchmark` may import from `kdb_compiler`; `kdb_compiler` never imports from `kdb_benchmark`. | Production pipeline stays unaware of measurement concerns. Forces clean dependency direction; Codex review confirmed this catches accidental coupling early. See §7.1. |
| D26 | 2026-05-04 | `RespStatsRecord` (capture-full mode) is the scorer's authoritative input — not `compile_result.compiled_sources[]`. | Resp-stats has one record per attempted compile (success or fail); compile_result only contains successful sources, so it can't ground stage-success rates. See §7.2. |
| D27 | 2026-05-04 | Locked benchmark weights: S0=20, M1=20, M2=5, M3=5, M4=15, M5=5, M6=15, M7=15 (sums to 100). Source-words denominator on M6/M7. | Round 3 closure after Codex hostile review surfaced page-spam exploit on per-page denominators. Per-1K-source-words is corpus-controlled, model-independent, tokenizer-independent — least-bad denominator without ground truth. See §7.3; §7.4 moved to `docs/reference/benchmark-architecture.md` (2026-08-19). |
| D28 | 2026-05-04 | Average-rank Borda for cross-model normalization on M6/M7 only; `final_score` comparable only within the same candidate set (rank-latest-pick-best workflow). | Cost/latency raw magnitudes differ 3× or more across models; direct summation would dominate. Other measures stay as raw rates (already on a [0,1] scale). User workflow does not need cross-version comparability — defuses Codex's biggest critique without adding scorecard_version ceremony. See `docs/reference/benchmark-architecture.md` (§7.5 moved there 2026-08-19). |
| D29 | 2026-05-10 | M5 retired body_link_jaccard (=1.000-by-construction post-#57) is replaced by `body_emit_set_coverage`: per-source `\|((⋃_p (body_wikilink_slugs(p.body) − {p.slug})) ∩ (concept_slugs ∪ article_slugs))\| / \|concept_slugs ∪ article_slugs\|`, micro-aggregated across the run. Computed in `kdb_benchmark/scorer.py` from captured `parsed_json` — no new RespStatsRecord fields (preserves one-way boundary D25). Self-links excluded to reward cross-page integration. Weight stays 5%. See `docs/archive/tasks/task59-m5-replacement-design.md` for full design (D29.1–D29.9 sub-decisions). |
| D30 | 2026-05-10 | M5 weight bumped 5% → 15%; M6 and M7 each bumped 15% → 10%. Total still 100%. Quality core (S0 + M1 + M4 + M5) becomes 70% of FINAL (was 55%); cost+latency become 20% (was 30%). | Post-#59/#60 regression scorecard showed M5=0.111 outlier (qwen-flash-us) couldn't be discriminated at the 5% weight level — model still topped FINAL despite barely integrating concepts via body wikilinks. Reweight reflects the user's "if other models can do it, why can't you?" stance: quality should dominate FINAL more than cost/latency. Cross-generation FINAL comparison invalidated again per D29.9 (same doctrine). See `docs/TASKS.md` → Task #61. |
| D31 | 2026-05-10 | Outlier penalty added to FINAL composition. For each model and each in-scope measure (S0, M1, M2, M3, M4, M5), units = floor(((norm − value)/norm × 100) / 10) when value < norm; total = Σ units across measures; FINAL_post = max(0, FINAL_pre − 0.05 × total). M6/M7 excluded (Borda-relative). Surfaces single-axis outliers that the weighted sum would average away (e.g. qwen-flash-us with M5=0.111 dethroned). Cross-generation FINAL comparison invalidated again per D29.9 doctrine. See `docs/archive/tasks/task62-outlier-penalty-design.md` (D31.1–D31.12 sub-decisions). |
| D32 | 2026-05-13 | **GraphDB-KDB is a multi-source raw-text → knowledge-graph kdb_graph_compiler at the storage layer**; the schema admits `Source.source_type` as a discriminator and is source-agnostic. The ingestion API (`apply_compile_result`) is Obsidian-flavored for v1 (D32-tempered per Codex Round 1 v2 review). Graph is the architectural primitive; manifest.json + wiki markdown + future visualizations are *renderings*. | Differentiating bet: explicit edges beat implicit similarity. Vector RAG flattens ontology into cosine distance; the graph preserves what we paid to build. Storage-layer multi-source readiness is cheap to bake in; ingestion-layer abstraction without a second producer would be speculative. |
| D33 | 2026-05-13 | Storage = Kuzu 0.11.3 (embedded graph DB, Cypher dialect, multi-language bindings, MIT). | Purpose-built for embedded graph; file-based (no daemon), portable, industry-standard Cypher. NetworkX+JSONL is Python-only; SQLite-with-graph-schema forces consumers to reimplement traversal. |
| D34 | 2026-05-13 | Independence-by-shared-upstream: `manifest_writer` and `graphdb_kdb.ingestor` each consume `compile_result + last_scan + run_id` independently. Neither reads or writes the other's store. | Ablation: delete `manifest.json` → GraphDB still queryable; delete `GraphDB-KDB/` → manifest still works. Both regenerable from `state/runs/<run_id>.json` history. Real independence by structural construction. |
| D35 | 2026-05-13 | Kuzu *data* directory location: `~/Droidoes/GraphDB-KDB/` (sibling to Obsidian-KDB; not OneDrive-synced). Override via `KDB_GRAPH_PATH` env var. (Superseded 2026-06-11, #113: default derives `<vault>/KDB/graph` from `OBSIDIAN_VAULT_PATH`; `KDB_GRAPH_PATH` overrides.) | Physical separation mirrors logical separation. Avoids OneDrive corruption on Kuzu binary catalog files. Backup = recovery-via-rebuild (D39); belt-and-suspenders via `graphdb-kdb snapshot` (#63.9). |
| D36 | 2026-05-13 | Naming triad: Python module `graphdb_kdb`, Kuzu directory `GraphDB-KDB/`, CLI command `graphdb-kdb`. `kdb-graph` is **reserved** for a future Obsidian-graph-view utility — out of #63 scope. | Avoid conflating the multi-source ontology layer with a (future) Obsidian-specific rendering tool. Memory: `project_graphdb_kdb_vs_kdb_graph_distinction`. |
| D37 | 2026-05-13 (renamed D-A1 2026-05-14) | Schema: `Entity` and `Source` node tables; `LINKS_TO` (Entity→Entity), `SUPPORTS` (Source→Entity) rel tables. Originally `Page` node-table; renamed to `Entity` per D-A1 to remove the Obsidian-isms from the storage-layer vocabulary. | Provenance is first-class graph data, not a sidecar. `Entity` reads as abstract identity (vs `Page` which presumed wiki-page rendering) — better positioning for multi-source future. |
| D38 | 2026-05-13 | Pipeline integration: Stage 9 `graph_sync` runs AFTER Stage 8 (manifest write); failure is **non-fatal** — emits warning + journal entry, but overall compile run still returns success. | Honors D34 independence: a failed graph write must not roll back a successful manifest write. Recovery via `graphdb-kdb rebuild`. |
| D39 | 2026-05-13 | Rebuild path: `graphdb-kdb rebuild` drops all Kuzu tables and replays the **eligible** subset of `state/runs/*.json` chronologically. **Eligibility:** `success=true AND dry_run=false AND payload_present` (payload = sidecar archive at `state/runs/<run_id>/{compile_result,last_scan}.json` per #63.0 outcome). Independence proof: Kuzu regenerable without ever reading `manifest.json`, prospectively from #63 forward. **Amended 2026-08-06 (#132):** kdb_graph_orchestrator-era producers archive slim `2.3` journals + sidecars per run (`kdb_graph_orchestrator/journal_writer.py`); replay of 2.3 journals is two-phase live-order (commits then reconcile). **Amended 2026-08-07 (#136):** the `finalize_progress` value no longer gates a deferred derive (none exists — wiring/deprecation are per-commit in replay as live); its presence still selects the 2.3 path. The 0.5.1 realignment had silently deleted the only compile-journal writer — D39 was unfalsifiable for kdb_graph_orchestrator-era runs until #132 restored it. | If GraphDB drifts from compile-history truth, regenerate from compile-history truth. Pre-#63 historical runs are unrecoverable except for the latest baton state — see §8.4 baton-backfill. |
| D40 | 2026-05-13 | Hybrid analytics: Kuzu Cypher fetches topology (edge lists, node attrs); NetworkX + python-louvain computes PageRank, Louvain communities, structural-holes. | Kuzu lacks native PageRank/Louvain; implementing iteratively in Cypher is awkward. At 10⁴-node ceiling the hybrid cost is sub-second per algorithm. |
| D41 | 2026-05-15 (Task #64) | **Recompile supersession.** A source's recompile removes that source's support from prior pages the new run no longer emits. The graph ingestor already implements this; D41 binds the manifest path to parity. See `docs/archive/tasks/task64-recompile-supersession-blueprint.md`. | Graph ingestor's `_replace_supports_for_source` is the reference design (Codex CRITICAL #2 fix in #63). Manifest-only union (`_ensure_page` always unions) diverges from graph truth after any recompile that emits fewer pages. D41 closes that gap without touching `graphdb_kdb`. |
| D42 | 2026-05-15 (Task #64) | **`source_refs` is current-state provenance, not an eternal log.** Stripped on supersession alongside `supports_page_existence`. History lives in run journals, `sources[].previous_versions`, and `orphans[].previous_supporting_sources`. See `docs/archive/tasks/task64-recompile-supersession-blueprint.md`. | Keeping stale `source_refs` after supersession creates false provenance claims (a source "supports" a page it no longer emits). Run journals and `previous_supporting_sources` preserve history without polluting current-state. |
| D43 | 2026-05-15 (Task #64) | **Status-aware `source_refs` invariant.** `active` page → `source_refs` non-empty. `orphan_candidate` page → may be empty (provenance preserved in `orphans[].previous_supporting_sources`). Also fixes the pre-existing DELETED-path invariant crash. See `docs/archive/tasks/task64-recompile-supersession-blueprint.md`. | Prior invariant rejected empty `source_refs` for all pages without a status filter — supersession legitimately empties them for orphan candidates. DELETED path had the same latent crash (never triggered because no source has been deleted in practice). Status-aware check makes the invariant correct for all reachable states. |
| D44 | 2026-05-15 (Task #64) | **D12 preserved.** Supersession flags pages `orphan_candidate`; never deletes page records or files. `delete_policy` stays `mark_orphan_candidate`. See `docs/archive/tasks/task64-recompile-supersession-blueprint.md`. | D12 is the non-destructive safety invariant: orphan candidacy is a flag, not a deletion. #64 adds a new trigger (recompile supersession) but the outcome is identical to the existing orphan path — page stays in manifest and on disk, human reviews. **Superseded with D12 by #130 (2026-08-05):** supersession-dropped pages become `deprecated` in place (graph + file, revivable); only source deletion erases. |
| D45 | 2026-05-15 (Task #65) | **`pairing_type_mismatch` is reconcilable; `pages[].page_type` is authoritative.** A new unconditional `reconcile_slug_lists()` rebuilds `concept_slugs`/`article_slugs` from `pages[]` in `compile_one` (mirroring Task #57's body-wins `reconcile_body_links`). The validator demotes `pairing_type_mismatch` `gate`→`measure` and drops it from `HARD_ZERO_FINDING_TYPES`. See `docs/archive/tasks/task65-pairing-reconcilable-blueprint.md`. | `concept_slugs`/`article_slugs` are denormalized indexes of `pages[].page_type`; the page object (title+body-bearing) is the deliberate classification. Hard-gating a slug-list mis-file discarded whole good compiles (EP1: 28 valid pages lost over 2 mis-filed slugs). Rebuilding from `pages[]` makes the pairing-inconsistency class structurally impossible. Removing `pairing_type_mismatch` from `HARD_ZERO_FINDING_TYPES` redefines the benchmark hard-zero pass-rate measure — a post-#65 re-fire sets the new baseline. |
| D46 | 2026-05-16 (Task #66) | **Compile eligibility is `current_hash != last_compiled_hash`.** A new source field `last_compiled_hash` records the hash last *successfully processed*; it advances **only on successful processing of the current content** — `apply_compile_result` for LLM-compiled text sources, `apply_scan_reconciliation` for metadata-only binaries (Q6) — never by the scan merely *seeing* a changed text source, never for an error-marked / missing source. The scan carries the prior value onto every `ScanEntry` as `compiled_hash` (required-but-nullable); `to_compile`/`to_skip` partition purely on the hash comparison, never on `action`. The `error_retry` side-channel is removed; lifecycle state is informational but no longer affects eligibility (`compile_state` historically, `run_state` after Task #96 schema v3.1). Force-recompile = a real source-content change; no manifest flag, no `--force`; content hash only, never mtime. See `docs/archive/tasks/task66-compile-trigger-model-blueprint.md`. | `manifest.hash` advances during the *scan* (it means "last hash seen"), so reading it back as "last hash compiled" conflated two facts: a failed compile left `hash` already advanced, so the file read UNCHANGED next scan despite never compiling. `error_retry` was a patch over that conflation — and it made force-recompile possible by hand-editing `compile_state: "error"` into the manifest. Splitting the two hashes makes the trigger one honest comparison and removes the manifest-editable force path; Task #96 later deprecated `compile_state` in favor of `run_state` without changing the trigger. |
| D47 | 2026-05-16 (Task #70) | **Superseded by D49.** Originally held manifest as default context source pending cold-start fix. Cold-start resolved (#71); D49 removed manifest-as-context entirely. | Historical: prevented premature default flip while graph context was weaker on cold-start. Now moot — graph is the only context authority. |
| D48 | 2026-05-17 (Task #71) | **Graph context loader must be self-sufficient — no manifest fallback path.** Cold-start is resolved by widening graph-native matching (title phrase + extended neighborhood hops), never by delegating to manifest. | Manifest is being phased out of the context-generation pipeline. Any fallback would be architectural regression. Rejected: (b) min-context fallback to manifest; (c) manifest-for-first-compile / graph-for-recompile split. |
| D49 | 2026-05-17 (Task #70 closure) | **GraphDB is the only supported EXISTING CONTEXT authority.** `manifest.json` must not be used for context generation. The `KDB_CONTEXT_SOURCE` env var has no effect; the kdb_graph_orchestrator calls `context_loader` directly. If GraphDB is missing/empty/corrupt, context snapshot build fails loud → operator runs `graphdb-kdb rebuild`. | Manifest is the wrong substrate for ontology/context — a flat index cannot encode graph relationships. Keeping it as rollback implies it is an acceptable competing source of truth; it is not. Graph outperforms manifest on both cold-start (17–23 vs 0–8 pages) and steady-state. Recovery path is rebuild from run journals (D39), not fallback to a weaker substrate. |
| D50 | 2026-05-17 (Task #73) | **`manifest.json` is no longer an ontology store.** GraphDB owns Entity, LINKS_TO, SUPPORTS, orphan status, graph topology. Manifest becomes source-file metadata ledger only (hashes, compile state, timestamps). Stage 9 `graph_sync` becomes fatal for non-dry-run compiles (revokes D38 non-fatal semantics for ontology writes). No piecemeal removal — pages, outgoing_links, source_refs, orphan status stripped together once consumers migrate. | Dual-write is architecturally confusing (two "sources of truth" invite drift) and blocks manifest slimming. Piecemeal removal (e.g., outgoing_links only) creates half-stale state — worse than either extreme. GraphDB is deterministically regenerable from run journals (D39); manifest cannot serve as fallback once it stops tracking ontology. See `docs/archive/tasks/task73-manifest-ontology-removal-blueprint.md`. |
| D51 | 2026-05-17 (Task #73 closure) | **GraphDB is the live ontology authority; `state/runs/` sidecars are reconstruction material, not the primary data flow.** Layer model: `raw/` = source corpus; `GraphDB-KDB/` = live ontology authority (primary); `state/runs/` = audit log + reconstruction material (backup); `manifest.json` = source-state metadata ledger (hashes, run_state, timestamps — not ontology); `wiki/` = markdown rendering. Primary path: `kdb-orchestrate → graph-sync (immediate GraphDB update)` — runs are not in this hot path. Rebuild/verify use sidecars as backup to prove or restore consistency. The source-state ledger must not carry replay-selection pointers — replay eligibility belongs to the adapter/rebuilder, not the source metadata ledger. | Rejected: (a) event-sourcing framing where runs/ IS the ontology authority and GraphDB is "just a projection" — technically correct but cognitively misleading (implies the normal path goes through runs/; it doesn't). (b) `ontology_sources/*.json` per-source durable layer — redundant with sidecars, adds a third consistency surface, re-introduces coupling removed by D50. The current architecture already matches the kdb_graph_compiler mental model (source → distill → update GraphDB); the naming just needed to make that explicit. |
| D52 | 2026-05-21 (Task #74 closure) | **Canonicalization is a top-level compile stage (new Stage [6]), not a side-effect of page_writer or graph_sync.** Both downstream renderings — `page_writer` (wiki .md files) and `graph_sync` (live GraphDB) — consume the canonicalized `compile_result`, so wiki and graph agree on entity names at the rendering layer. Algorithmic failure is fatal (D-R5-9; pipeline halts before page_writer writes anything). Run journal `schema_version` bumps `2.1 → 2.2` to carry `canonical_meta` for D39 replay (D-R5-7). GraphDB gains `Entity.canonical_id` + `ALIAS_OF` rel table; alias entities are `canonical_id IS NOT NULL` + chain-flattened to root (D-R5-13). C1–C4 invariants are checkable from the live graph alone (no sidecar reads). Full locked-decision register (D-R5-1..D-R5-13) and algorithm details live in `docs/archive/tasks/task74-canonicalization-blueprint.md`. | If canonicalization ran only inside `graph_sync`, the vault would show `[[AAPL.md]]` while the graph stored `apple-inc` — a divergence the human sees in Obsidian. Single source of canonical truth, consumed identically by both renderings, is the only way wiki ≡ graph at the naming layer. v1 implementation = string-norm + manual ledger (`state/canonicalization/aliases.json`); embedding-similarity + LLM-judge layers reserved for v2 (L9 in blueprint §14). |
| D-A1 | 2026-05-14 (Round 1 Codex) | Schema rename: `Page → Entity` node-table label. | `Node` would collide with Kuzu's NODE keyword + universal graph-theory term. `Entity` signals abstract identity. Free upgrade while schema is empty/small. |
| D-A2 | 2026-05-14 (Round 1 Codex; updated by Task #96 on 2026-05-30) | Graph-side source field renames: `compile_state → ingest_state`, `compile_count → ingest_count`, `last_compiled_at → last_ingested_at`. Producer source-state later moved active lifecycle status from deprecated `compile_state` to `run_state` (schema v3.1) while GraphDB retained `ingest_state` as its neutral projection name. Page enum values (page_type/status) retained — *values* are Obsidian-flavored; renaming names without revisiting values is cosmetic. **`confidence` value dropped 2026-07-21 (D-115-12):** logically deprecated end-to-end — Entity-scope writes/reads/queries/snapshots stop; the dead Kuzu column stays until the next destructive schema change. Claim-tier computed confidence is untouched. | Pipeline-specific graph field NAMES become pipeline-neutral now. Pipeline-specific VALUES wait for producer #2 to inform the right abstraction. Verifier carries an alias map bridging producer `run_state` to graph `ingest_state`; legacy `compile_state` is migration/replay fallback only. |
| D-B1 | 2026-05-14 (Round 1 Codex) | Rebuilder is **B-lite (adapter split)**: thin generic core in `graphdb_kdb/rebuilder.py` (drop+recreate, chronological iter, error reporting) + producer-specific logic in `graphdb_kdb/adapters/obsidian_runs.py`. Rule: `graphdb_kdb/` MUST NOT `import kdb_compiler.*`. Public function name `rebuild_from_obsidian_runs(...)`. | Pure-C (core imports producer types) would silently weaken D34 independence. B-lite preserves it by structure, not convention. Cost: ≤200 LOC adapter; verified by grep invariant. |
| D-S0 | 2026-05-14 (Round 2 Codex) | **Graph-sync routes through the Obsidian adapter**, not direct core call. `kdb_orchestrate.py` calls `graphdb_kdb.adapters.obsidian_runs.sync_current_run(cr, scan, run_id)`. Single producer→graph entry point for both live sync and replay. | Makes Doc C's "producer never calls core directly" rule literal, not aspirational. Single code path = one place to debug/test/evolve. Closes OQ-E9 in extraction roadmap. |
| D-S1 | 2026-05-14 (Round 2 Codex) | **Multi-producer entity-id namespacing**: Obsidian grandfathered as bare slugs (implicit `obsidian:` namespace); all future producers MUST use explicit `<source_type>:<entity_id>` prefix. Adapter declares `entity_id_namespace: ClassVar[str \| None]`. | Retroactive migration of existing entities is destructive without operational benefit; grandfathering is cheaper. Cross-producer queries filter via `Source.source_type`, not slug prefix parsing. |
| D-S2 | 2026-05-14 (Round 2 Codex) | **Rebuild blast radius v1**: `graphdb-kdb rebuild` always drops the whole DB regardless of `--producer` flag. Producer-scoped rebuild deferred until producer #2 ships AND the team agrees the scoped semantics (tracked as L8 + blueprint TR-3). CLI prints warning before drop. | At v1 single-producer the simple correct semantics. Deferring lets the right scoped-rebuild rules be informed by real co-tenancy needs. |
| D-S3 | 2026-05-14 (Round 2 Codex) | Adapter declares `supported_journal_versions: ClassVar[list[str]]`. Mismatched versions return structured skip reason `'unsupported_version'` rather than silent corruption. | Producer journals evolve (Obsidian is at `2.0` today). Versioning discipline must be in place before Stage 1 of package extraction, not Stage 4. |
| D-S4 | 2026-05-14 (#63.7-A1 finding) | Phase 1 source-refresh in `graphdb_kdb/ingestor.py` does NOT bump `last_run_id` — `last_run_id` is bumped only by Phase 3 (`_update_source_ingest_state`) on actual ingest. `ON CREATE` seeds it as `''`. | Manifest's `last_run_id` is bumped only on compile events, never on bare scan. Graph must mirror this to produce zero `attribute_mismatch` divergence for sources that aren't touched in a given run. Without this, every scan-only run causes spurious drift. Discovered live during A1 inspection. |
| D-S5 | 2026-05-14 (#63.7-A2 finding) | Test isolation via autouse `conftest.py` fixture at `kdb_compiler/tests/conftest.py`: `monkeypatch.setenv("KDB_GRAPH_PATH", str(tmp_path / "graph_isolated"))`. Every test in the package gets a per-test graph directory. | Graph-sync routes through `ObsidianRunsAdapter().sync_current_run(...)` which resolves `KDB_GRAPH_PATH` to the live `~/Droidoes/GraphDB-KDB`. Without isolation, tests that exercise the full `compile(...)` pipeline silently write synthetic fixtures into the production graph. Discovered live during A2 inspection. |
| D-S6 | 2026-05-14 (Task B from #63.7) | `kdb-orchestrate --model <id>` accepts an id from `kdb_benchmark/models.json` registry (default: `gemini-3.1-flash-lite`). Inline registry loader avoids `kdb_benchmark` import (would create a cycle: `kdb_benchmark.runner` already imports `kdb_compiler.compile_one`). Both tools read the same `models.json`. Fail-fast on unknown id (prints active-model list) or `dropped: true` entries (prints `dropped_reason`). `kdb_graph_compiler.run_compile()` extended to accept `use_completion_tokens` + `extra_body` provider knobs (previously only `compile_one` accepted them; `kdb_benchmark.runner` bypassed `run_compile`). | Same registry = same fail-fast behavior across both tools. Provider-specific knobs (e.g., gpt-5+ `use_completion_tokens`) reach the live LLM call from the production CLI path, not just the benchmark path. Validated live across 3 providers (anthropic/haiku-4.5, gemini/gemini-3.1-flash-lite, alibaba/deepseek-v4-flash) plus a graceful-failure scenario. |
| D-115 | 2026-07-21 (Task #115 ratification, spec v1.6) | **The Pass-2 contract is derivation-first: the LLM emits wiki-native data only** — `pages[]` (`slug`, `page_type`, `title`, `body`) + optional `compilation_notes`. Python owns identity (deterministic `expected_summary_slug(source_id)` + exact-stem semantic gate, model-authored slug), `status` stamping, and summary identification; the graph owns wikilink→LINKS_TO derivation (body-authority; legacy `outgoing_links` fallback). Six fields leave the contract (`concept_slugs`/`article_slugs`/top-level `summary_slug`/`outgoing_links`/`source_name`/`status`) with no aggregate reconstruction; `warnings` → `compilation_notes`; `log_entries` dropped; `confidence` logically deprecated (Entity scope — D-A2 updated); the system prompt moves to the repo (`kdb_graph_compiler/prompts/`) with `pass2_prompt_version` + loaded-text SHA-256 stamps; the #65 Repair stage is deleted whole. Historical artifacts read-compatible via optional-deprecated fields + dual-mode validation. Full register D-115-1..15: `docs/superpowers/archive/specs/2026-07-21-task115-pass2-contract-audit-findings.md`. Cross-source slug reservation / MOVED lifecycle / durability carved to #116 (paired with #94). | Model-emitted mechanical aggregates were unearned classifications — Python derives them deterministically, so the model's surface shrinks to what only it can author (titles, bodies, wikilinks, page_type). Rejected: prompt-injected summary slug (model authorship preserved — gate-validated instead); Python reconstruction of removed aggregates (fictitious precision); keeping Repair-stage reconcilers (their defect classes are structurally impossible under the shrunk contract). **Slug-ownership clause revised 2026-07-23 by D-119** (summary slug → Python-owned, bridge-stamped; the raw value never rejects/retries). |
| D-119 | 2026-07-23 (Task #119 Phase-1 ratification, options doc v1.3) | **Pass-2 gains a proposal→canonical normalization boundary (Option 2: dual schemas + typed bridge).** The prompt-facing contract becomes a discriminated **proposal schema** (summary: `page_type`/`title`/`body` — no `slug`; concept/article: + `slug`); a **typed bridge** stamps the Python-derived summary identity (role + provenance authority; never string similarity); the normalized object must pass the **unchanged canonical validation shape** + exact pre-persistence summary invariant + post-canonicalization invariant; the raw parsed proposal is preserved separately for telemetry. **Governing rule (ratified as a principle):** reject ambiguity, not harmless representational differences — normalize only by deterministic authority (role / provenance / registry / context); reject on no-interpretation / ambiguity / collision / information loss. Reference policy: preserve without authority (dangling-tolerant, KPI-visible); rewrite only with unique authority; reject attempted-rewrite ambiguity. The summary slug's raw value **never rejects, never retries**. Links-to-current-summary declared unnecessary (prompt injection of the canonical slug stays a non-goal); alias-ledger **fail-closed** on system-owned summary identities; #106 slug coercion absorbed into the bridge (parity corpus + collision refusal preserved); retries only for model-correctable failure classes. **Revises D-115:** the summary slug leaves model authorship (Python-owned mechanical identity). | A mechanical field gated at the raw boundary cost a retry + quarantine per offending source (Phase-5 evidence, both cohort models, proved pre-existing at prompt 2.0.0) while adding zero information — Python already knew the answer. Normalize-then-check at the raw boundary would merely fudge model output to satisfy a gate aimed at the wrong artifact (Joseph's challenge, Codex-endorsed); moving the boundary keeps strictness where it protects the canonical product. Rejected: similarity/tolerance gates; in-place normalizer without a versioned boundary artifact (Option 1 — accretion risk, reversibility illusory under form (b)); typed intent decoder (Option 3 — disproportionate to a 4-field surface); form (a) (an optional-but-present slug field exists only to manufacture telemetry). Acceptance: the #115-waived Phase-5 gate satisfied for real — one cohort vs the zero-quarantine Phase-0 baseline (`e9ca323`), the two named sources compiling without retry/quarantine. |

| D-121 | 2026-07-24 (Task #121 Phase-1 ratification, options v1.4 + blueprint v0.4) | **Provider wiring becomes config-driven via a layered `ModelRoute` architecture (D1–D8).** Every active models.json entry carries a required, whole-pool-validated `ModelRoute(api_call_type, endpoint, api_key_env)` — authoritative, no fallback: absent fields are load errors, explicit `null` endpoint means the SDK built-in URL. A 9-row provider-default **factory** in `call_model.py` serves only route-less calls (escape hatch / direct `ModelRequest`) — never a rescue for broken pool entries. `api_call_type` (`openai_compat`/`anthropic`/`gemini`) is the sole, closed dispatch selector; `provider` becomes a free-form **canonical** identity (padded/blank rejected) for thinking-translation, pricing, telemetry. Secrets resolve late by env-var **name** for all transports; the `Settings` class is deleted (`common/config` keeps `.env` loading + late-bound helpers). **D4 fail-hard codified:** two validation gates, ordered runtime-type checks, no `or DEFAULT`, route-less unknown provider fails pre-SDK, secrets never in errors/telemetry. **D7:** `LLM_TIMEOUT_SECONDS` = connect/write/pool (120); `LLM_INACTIVITY_TIMEOUT_SECONDS` = read-silence watchdog (900 — kills silent peers, not a wall-clock bound). **D8:** retry budgets preserved + documented (corrected call-path × transport matrix); `max_retries=2` explicit. | Joseph's goal — per-model routing freedom with less hardcoding; a new provider of an existing type becomes pure JSON + `.env`, zero code edits. Precedent audit found **no** same-provider endpoint variance in repo history (qwen-us/-non-us differ only in deployment id), so the policy rests on ownership/flexibility, not history. The layered shape beats fallback/rescue designs because silent defaults are exactly the failure mode being engineered out (D4). `Settings` deletion keeps one source of truth for key resolution; renaming `LLM_TIMEOUT_SECONDS` was rejected because it would silently orphan the var in seeded `.env` files. |

| D-122 | 2026-07-24 (Task #122 Phase-1 ratification, options v1.3) | **Pass-1 context KPIs are measured at event time (context-load), never reconstructed from the post-run graph.** Per-source `state/runs/<run_id>/context/*.json` records capture per-key dispositions (resolver hit vs pre-cap T2 seed vs capped-out) and T1/T2/T3 candidate/delivered counts at snapshot build; emit-time aggregation reads the recordings; the final graph is read only for legacy continuity + miss explanation. Two-product `ContextBuildResult` keeps telemetry out of the prompt (byte/hash pinned). Completeness is identity-reconciled (coverage vs substantive populations); aggregates fail closed to `None`. Legacy `entity_search_key_resolution` retained unchanged. | Post-run re-resolution is self-fulfilling — the run materializes the very nodes/aliases that make its own keys resolve (Joseph: "you recalculate T2/T3 after the run and you always get the hit"). The v1.0 age-split patch still couldn't separate retrieval from late aliasing (Codex R1 F2). Event-time capture measures the only moment that matters — whether the key fired the retrieval channel when Pass-2 needed it. |

---

## 10. Open Questions

| # | Question | Status | Plan |
|---|---|---|---|
| Open-1 | Statefulness implementation | ✅ **Closed (D7–D15)** | GPT 5.4 approach + Codex hardening |
| Open-2 | Output format / schema | 🟡 Partial | Frontmatter keys locked (D7); patch-ops JSON schema pending M2 |
| Open-3 | Safety model for Human Side edits | 🔴 Deferred | Only relevant to Track 2 (`llm-linker`), not v1 |
| Open-4 | Link direction between Sides | 🔴 Deferred | Track 2 concern; recommended **Option C (asymmetric + opt-in bidirectional)** but not confirmed |
| Open-5 | Binary file handling (PDF, images) in `raw/` | 🟡 Partial | v1 marks as `compile_mode: metadata_only`; actual PDF/image parsers = v2 |
| Open-6 | Page-intents JSON schema design (shape, not mechanism) | 🟡 Skeleton in M0.1; full design in M2 | Skeleton committed at `kdb_graph_compiler/schemas/compile_result.schema.json` |
| Open-7 | Chunk size tuning (10–20 default) | 🟡 Heuristic | Validate empirically during M2 first compile |
| Open-8 | Slug → path policy (how `[[slug]]` resolves to a file) | 🟡 Partial | `paths.py` stub declared; rules locked in M1 (see module docstring) |

---

## 11. Roadmap

Completed milestones M0 → M4 — all ✅; full text archived 2026-08-19 to [`docs/archive/roadmap-completed-milestones.md`](archive/roadmap-completed-milestones.md):

- **M0 — Scaffolding** ✅ — vault + repo scaffold, system prompt, manifest, initial commit (`796848b`)
- **M0.1 — Codex review remediation** ✅ — system prompt rewritten, shared seams, schema skeleton + fixtures
- **M1 — Deterministic layer (no LLM)** ✅ — scanner, manifest updater, validators, call_model + retry, e2e dry run
- **M1.7 — Validator + reconciler on real vault** ✅ — page_writer, manifest_writer, 8-stage pipeline, gate/measure split
- **M2 — LLM layer + benchmark** ✅ — live compile, response capture/replay, benchmark engine, baseline scorecard
- **M3 — GraphDB-KDB Layer (#63)** ✅ — Kuzu refoundation, sub-tasks #63.0–#63.9 (see §8)
- **M4 — Canonicalization layer (#74)** ✅ — canonicalize compile stage, D-R5-1..D-R5-13 + D52 (see §8.3)

### M3+ (deferred)
- [ ] Track 2 (`llm-linker`) — separate sub-project
- [ ] News Clippings ingestion channel (from Google Sheet)
- [ ] Books sub-project (Track 3)
- [ ] Binary parsers (PDF, image OCR)
- [ ] Scale validation at 1,000+ sources
- [ ] Add 3rd model to benchmark to restore Borda gradient on M6/M7
- [ ] Ground truth dataset for benchmark (Task #20)

---

## 12. External References

Consulted AI artifacts (stored in `~/Obsidian/Projects/Obsidian-KDB/`):
- `Karpathy LLM Knowledge Base in Obsidian - Grok.md` — original design + lean v1 statefulness
- `Statefulness Implementation -Grok.md` — community impl survey
- `Karpathy's Obsidian LLM Knowledge Base -Gemini Pro.md` — academic treatise (not adopted)
- `Karpathy Obsidian LLM Complier Implementation - GPT 5.4.md` — state model baseline (adopted)
- `Codex 5.3 Reviews of GPT 5.4 Implementation of Kaparthy Obsidian LLM KDB.md` — hardening + working code (adopted)
- `docs/archive/early/code-review-M0-codex.md` — Codex 5.3 review of M0 scaffold (drove M0.1 remediation; 5 findings all actioned)

Reference implementations surveyed:
- Reddit #1 (fabswill) — mtime-based scan, manual orphan handling
- Ustaad (Sohardh/ustaad) — SHA-256 in frontmatter, log.md flagging
- ussumant/llm-wiki-kdb_graph_compiler — standalone engine + MCP server
- Ar9av/obsidian-wiki — manifest-based state (closest to our v1)

Karpathy source:
- X post: https://x.com/karpathy/status/2039805659525644595

---

## 13. kdb_fts — the parallel extraction system (#145, architecture ratified 2026-08-16)

The repo hosts **two competing extraction architectures** over vault sources.
The incumbent is the classify-and-connect knowledge pipeline (§5/§8: scan →
pass-1 → pass-1.5 → pass-2 → graph). The challenger is **kdb_fts**: a
rank-and-learn extraction ledger over the gmail-substack corpus (2,625 rankable
articles + weekly inflow), built for **information, not knowledge** — it ranks
investment ideas and authors and distills lessons, calibrated to Joseph's
judgment; it never builds graph structure. The experiment is deliberate: a
head-to-head against the GraphDB approach is planned, via versioned exports as
the only coupling.

**Decisions on record (Joseph, 2026-08-16; full record in `docs/TASKS.md` #145):**

- **Family** (round-1 panel 5/5): extraction ledger — extract once into
  structured SQLite records, rank deterministically many times; retrieval is a
  read surface, never the core. Ranking objective = expected value of Joseph's
  next research hour, never predicted return.
- **Coverage**: gate-then-extract — a cheap relevance/topic gate over every
  file, deep idea/lesson extraction only for relevant buckets; **per-topic
  decay** (geopolitics decays faster than finance/econ/investment).
- **Placement**: top-level `kdb_fts/` package in this repo (monorepo), imports
  `common` **only** (AST boundary guard extended); a new mechanical
  write-boundary guard pins its writes to its own state root.
- **State**: `<vault>/KDB/fts/` (SQLite ledger + journals + exports) — parallel
  to `<vault>/KDB/graph`; clean to delete if the experiment dies.
- **Boundaries**: raw sources are read-only to both extraction systems (feeders
  are the sole writers); kdb_fts never writes the wiki, manifest, graph, or
  pipeline configs; no cross-imports either direction; versioned JSONL/CSV
  exports are the only sanctioned coupling.
- **NOT** part of 10x-Learning-Engine (equity research only; it may consume
  kdb_fts exports later).

Briefs: `docs/superpowers/specs/2026-08-16-gmail-info-search-rank-problem-statement.md`
and `…-repo-placement-problem-statement.md` (both options-free, panel-reviewed).
Extraction path stays open per the `graphdb-kdb-extraction-roadmap.md` Stage-0
precedent: the `common/` infra/domain seam is documented; a split triggers only
when a real second repo needs it.
