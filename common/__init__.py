# Bumped manually at each release tag (kept in sync with the tag, e.g. v0.5.7).
# Runtime code needing the precise build should use common.version.release_version()
# (git describe) instead — this string is stamped into run journals.
__version__ = "0.5.7"
